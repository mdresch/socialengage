# SocialEngage — Tenant Admin Manual

**Audience:** a `tenant_admin` identity — manages your own organization's SocialEngage tenant (users, connectors, watchlists).

**Learning & Development Writer addendum, 2026-09-15 (scheduled queue pass, continuing oldest-first through the `docs/pending-learning-development-reviews.md` backlog from commit `4eac552`):** confirmed and documented a substantial batch of real, contract-verified Epic 11/12/13 capabilities that had shipped but weren't yet reflected here: a boolean watchlist's own visual query builder (Story 12.4), a Topic Evolution timeline with semantic drift analysis (Stories 11.5/11.6/13.11/13.12), pushing a post or author to your CRM plus a CRM connector settings screen (Stories 11.1/11.2), a bulk "Push to CRM" for an entire Prospecting list (Story 13.14), a Daily Digest email preference screen (Stories 11.3/11.4), a Webhook subscriptions screen (**Story 10.12**, not 12.12 — see the note below), a Plan & seats screen (Story 13.6), and a separate "Outbound Publishing & Scheduling" screen that schedules posts and gives LinkedIn a real publish trigger for the first time, plus live @-mention suggestions (Stories 11.7/11.8/11.12/12.1), and a Unified Social Inbox triage screen (Stories 11.9/11.10). **Also found and corrected a stale claim in this manual's own prior addenda:** Story 6.41 (composer Deep Research panel) is genuinely built and wired into the real "Compose" screen's "🔬 Deep Research" button — this manual's own 2026-09-09 note above said Story 3.17's Deep Research endpoint had "no UI trigger," which was true when written but is no longer true; the root `CLAUDE.md`'s own status line still says Story 6.41 is "Ready but not yet built," which is now stale too — flagged for the Documentation Steward. Also corrected a real, previously-undocumented redesign of the ordinary Tenant settings screen itself (Story 6.40/ADR-0074, shipped 2026-08-26 but never queued for this role's review by the post-commit hook) — it now has real Data Export, CRM Connectors, and Notification Preferences cards, and a Tenant-Admin-only offboarding link straight to tenant deletion, resolving this manual's own former "no link to tenant deletion" and "export has no UI trigger" gaps. **A real story-numbering collision was found and is flagged for the Documentation Steward:** both Story 10.12 and Story 12.12 are titled "webhook management UI," each with its own separate contract test — only Story 10.12's component (`WebhookSubscriptionsView`) is actually mounted in a real page route (`/tenant/settings/webhooks`); Story 12.12's component (`WebhooksView`) exists and passes its own contract but isn't wired into any reachable route. The same is true of several other Epic 12 "frontend" stories checked this pass — Story 12.2 (connector capability badges), 12.8 (topic curation UI), 12.10 (dashboard widget renderer), and 12.16 (influencer discovery UI) each built a real component with a passing contract test, but none is imported by any page under `social-listening-admin/src/app` — so none is a real, reachable screen today, and none is documented here per this role's hard rule against documenting unreachable UI; flagged for the Documentation Steward as a pattern worth checking (a contract passing does not by itself mean a route exists). Also extended the already-documented Platform Operations dashboard (system-admin-manual) with Story 16.4's quota burn-rate forecast and guided connector remediation drawer. This pass did not reach the full 189-entry pending backlog in one sitting — see that file's own queue for what remains.

**Learning & Development Writer addendum, 2026-09-10 (scheduled queue pass, continuing oldest-first from `4ae7f92`):** added three new tenant screens confirmed built and contract-verified from Epic 10 — Prospecting lists (Story 10.2), ad-hoc analytics query (Story 10.5), and real-time alerts (Story 10.10) — plus YouTube Data API as an eleventh connector (Story 10.13; note this project's own epic-10 story document mislabels 10.13 as "Compliance audit pack" — the real, contract-verified capability, per both repos' own contract filenames, is the YouTube connector/admin-UI integration; flagged for the Documentation Steward, not resolved here). This pass also confirmed Stories 10.11, 10.12, and 10.14 (as titled in `docs/user-stories/epic-10-adr-0086-to-0094.md` — "Author-initiated takedown," "DSR self-service portal," "Trust and rights admin UI") have **no real UI route or matching contract test anywhere in `social-listening-admin`** despite each carrying a `**Status:** Ready` / `**Built:** 2026-08-28` header — the contract test files that exist under those same story numbers are for entirely unrelated features (webhook notifications, the YouTube connector, an AI insights digest). None of the three is documented in this manual, per this role's own hard rule; this is a real story/contract-traceability gap outside this role's own file scope, flagged for the Documentation Steward rather than fixed here.

**Current coverage, as of 2026-09-09 (Learning & Development Writer, scheduled queue pass):** sign-in, plus every tenant-facing screen this pass has actually traced through real source and confirmed built, processing the `docs/pending-learning-development-reviews.md` backlog oldest-first from commit `aa2488b` through `4ae7f92` (2026-08-25 through 2026-08-27 by commit date). Three genuinely new, previously-undocumented capabilities were confirmed and added this pass: a getting-started checklist on the Workspace Overview page (Story 9.6), a "Crisis Threshold Wizard" on the Manage watchlists screen for activating a standardized risk-monitoring template (Stories 9.3/9.4), and a semantic-search-and-AI-assistant "Discovery" screen over your tenant's own posts (Stories 9.7–9.11) — all three documented in their own sections below. (Two of these three — Story 9.6's real live component, and all of Stories 9.7–9.11 — were only found by reading real source, since the commit that actually shipped them, `social-listening-admin@c4021b3`, was never itself queued for this role's review by the post-commit hook; flagged here for awareness, not something this role's own file can fix.) Also corrected this pass: your own connected Facebook Page is now confirmed to be auto-polled every 30 minutes (Story 1.15) — before that story shipped (2026-08-18), a connected Facebook Page was never actually polled by the running scheduler at all, despite showing as connected; and Facebook's own engagement counts (reactions/comments/shares, Story 2.18) are now captured by the connector but still not shown in the post feed, named as a backend-only gap below. **This pass did not reach every pending queue entry** — the tenant sidebar now also lists Inbox, Prospecting, Alerts, and Plan & Seats screens (shipped via Epics 10–11, and beyond) that this pass's oldest-first review had not yet reached and that are therefore deliberately not described anywhere in this manual yet; they'll be added once a future scheduled pass reaches their own queue entries and verifies them the same way. Before this pass, as of 2026-08-26: sign-in, plus every tenant-facing screen then built. Since that note, one genuinely new capability had shipped: replying to a Facebook post directly from its detail panel, with a real (not simulated) send and its own audit trail (Stories 2.26, 2.27, 3.14, 6.38) — documented in its own section below. A large amount of outbound-publishing backend work also landed in the same window (Stories 2.28–2.30, ADR-0075: connector publish framework, Facebook Page post publishing, LinkedIn post publishing; Story 3.15's outbound-post audit table; Story 3.16's tenant/posts export; Story 3.17's Deep Research composer endpoint; Story 9.1's watchlist preview-volume endpoint) — all of it is real, contract-verified `social-listening-core` backend, but none of it has a UI trigger in `social-listening-admin` yet, so none of it is documented as a usable capability here; each is named as a backend-only gap in "What's not built yet" instead, per this role's rule against documenting a capability with no shipped screen. The connector roster has grown substantially since this note was last written (2026-08-19): connecting/disconnecting/activating platforms now spans **ten** real platforms — GNews, Newswire, Azure AI Language, Azure OpenAI, Wikipedia, Facebook, Brave Search, Bing Search (Azure), Instagram Business, and LinkedIn (Stories 6.3, 6.15, 6.21, 6.23, 6.27, 6.30, 6.32, 6.34, 6.35) — managing your own watchlists, now with Brave Search and Bing Search as additional selectable sources alongside GNews/Newswire/Wikipedia (Stories 6.4, 6.22, 6.30, 6.32), checking connector status, now split into a "Connectors" section and a separate "AI Providers" section with correct labeling for AI providers that are invoked on demand rather than polled, plus real ingestion status badges (including a new "stalled" state), a dismissible stalled/failing/reconnect-required alerts banner, and an on-demand "Re-sync now" action (Stories 6.5, 6.24, 6.29), managing your tenant's users on a redesigned Team & access screen with a real seat-utilization meter and a per-user access-history view (Stories 6.8, 6.14), a read-only view of your tenant's own settings (Story 6.9), the Same-Domain Invite Assist view (Story 6.10), browsing your tenant's ingested posts — newest-first, with search and filtering that now covers every ingested post rather than just the current page, a dynamic Provider filter, Markdown-rendered article bodies, entity/key-phrase chips, per-post Facebook Page and matched-watchlist attribution, and a manual "run enrichment now" button plus a full manual-override editing drawer for a post's AI-assigned sentiment, key phrases, language, country, and summary (Stories 6.11, 6.16, 6.18, 6.19, 6.25, 6.26, 6.31, 6.33, 6.37), setting up and activating your own company domain's content feed, now a real multi-feed admin screen with an optional friendly name per feed (Stories 6.12, 6.17, 6.20, 6.28), a full Analytics dashboard whose Overview tab was substantially rebuilt into an eight-widget grid — including a new Location & geospatial insights widget with a real country map, and a Watchlist Coverage widget with a topic/watchlist selector — alongside the existing Sentiment, Conversations, and Sources tabs (Stories 8.1–8.7, 8.9, 8.10), a cross-platform post composer with live preview rails for seven networks and an AI rewrite assistant, where drafting and previewing are real, and publishing to your own connected Facebook Pages is now genuinely real too (Story 6.39, extended by Story 13.10) — other networks remain simulated, and requesting deletion of your entire tenant (Story 6.13). **All of these screens are real, backed by your tenant's actual data, with working actions**, except where a section below says otherwise. A handful of specific, narrower gaps remain — each is called out in its own section below, and summarized together in "What's not built yet" at the end. The app also gained its first real stylesheet on 2026-08-12, the main content area was widened from 900px to 1280px on 2026-08-18, and the sidebar navigation gained icons and became collapsible on 2026-08-20 — all purely visual/navigational changes, nothing about how any screen below works changed because of any of them. **Merge-conflict resolution correction, 2026-09-09:** this note's own body previously said Facebook publishing was still simulated — Story 6.39 (`social-listening-admin@e0abfdd`, 2026-08-26), later extended by Story 13.10, made it genuinely real; corrected here since `e0abfdd` was never queued by the post-commit hook and so fell outside every oldest-first pass's own queue range, this one included.

---

## Signing in (Story 6.1)

1. Go to `social-listening-admin`. If you're not already signed in, you're redirected to a sign-in page automatically.
2. Sign in with your organization's Microsoft Entra identity — the same real sign-in flow every SocialEngage identity uses.
3. Once signed in, SocialEngage recognizes your Tenant-Admin role and routes you to your own tenant's screens, never a System Admin's or another tenant's.
4. Your session stays signed in for up to 8 hours, then you'll be asked to sign in again automatically, even if you were actively using the app right up to that point — a deliberate security limit, not a bug.
5. To sign out, use the sign-out action — this ends your session immediately on this device.

## How you get your own tenant today

A self-service sign-up screen now exists and works end to end (Story 6.7) — entering your organization's name and signing in with Microsoft creates a brand-new tenant with you as its first Tenant-Admin, a real, persisted tenant, not a preview. That said, per this project's own current stage (`docs/project docs/Project Management Plans/Go-Live-Readiness-Definition.md` — Stage 0; no persistent environment or public onboarding is open yet), this path isn't open to the general public today. In practice, a tenant and its first Tenant-Admin are still typically set up by a Platform Admin directly, rather than through public self-service sign-up.

## Your tenant's own screens (Story 6.2)

Once you're signed in, SocialEngage shows you your own tenant's shell — screens for connecting platforms and turning them on or off, managing your own watchlists, checking connector status, managing your tenant's users, viewing your tenant's own settings, browsing your tenant's ingested posts, replying to a Facebook post (Story 6.38), drafting cross-platform posts (Story 6.36), searching your posts semantically or asking an AI assistant about them (Stories 9.7–9.11), and the Analytics dashboard. As a Tenant-Admin only, you also get same-domain sign-up review, your own company domain's content feed (Tenant-Admin only as of Story 6.20), per-user access history, and requesting deletion of the entire tenant. You'll never see another tenant's data or the System Admin area from your own sign-in.

## Your tenant's workspace overview (Story 6.2, visually refreshed 2026-08-20)

Signing in takes you to a "Workspace Overview" landing page — this section names it directly since it was never previously described in this manual, even though it's real and has existed since Story 6.2.

1. Four real metric cards — Active Watchlists, Ingestion Connectors, Ingested Posts, and Seat Utilization — each a live count from your tenant's own data, and each links straight to the fuller screen it summarizes.
2. If any connector is currently degraded or failing, a warning banner names it and links straight to the connector status screen.
3. A "Recent Ingestion Stream" shows your tenant's most recently ingested posts, newest first, and an "Active Connectors" list on the side shows which platforms are currently active.
4. As a Tenant-Admin, you also get quick-action buttons here ("+ New Watchlist," "Tenant-wide connect").

## Getting-started checklist (Story 9.6)

The Workspace Overview page (see above) also shows a "Get Started with SocialEngage" checklist card whenever your tenant has one — both a Tenant-Admin and a Tenant User see it.

1. Four core steps — Connect an Ingestion Source, Create a Brand Watchlist, Invite a Team Member, Verify Ingested Posts — each shown with a checkmark once genuinely completed, a plain description, and a link straight to the screen where you'd do it. A "Show Advanced Setup Steps" toggle reveals two more: Enable AI Enrichment and Configure Crisis Monitoring. A progress percentage and a filled bar at the top of the card reflect your tenant's real, computed completion state, not a guess.
2. As a Tenant-Admin, clicking "Dismiss ✕" hides the card and this is remembered for your tenant going forward (a "Show Setup Checklist" button reappears in its place if you want it back, and reopening is remembered the same way).
3. **Current limitation:** if a Tenant User (not a Tenant-Admin) dismisses the checklist, that dismissal is only remembered for their own current browser session — it isn't saved to your tenant's record, so it reappears for that Tenant User the next time they load the page. Only a Tenant-Admin's dismissal persists.

## Semantic search and an AI assistant over your posts — Discovery (Stories 9.7–9.11)

Open "Discovery" from your tenant's screens to search your tenant's ingested posts by meaning rather than exact keywords, or ask a natural-language question and get a generated answer grounded in your own posts. Both Tenant-Admins and Tenant Users can use this screen; there's no role difference on it.

1. A status indicator at the top shows how many post chunks are currently indexed for semantic search, and a green or amber dot for whether indexing is healthy.
2. Two modes, switched with a tab: **Semantic Search** (returns a ranked list of matching post snippets, each with a percentage match badge and a link back to that post in your Posts feed) and **Ask AI Assistant** (returns a written answer with numbered citation markers, each backed by a real indexed post snippet shown in a citations rail alongside the answer, plus a plain-language confidence label). Press `Cmd/Ctrl+K` at any time to jump straight to the search box.
3. You can narrow either mode by Platform, Watchlist, or Sentiment before searching or asking.
4. If your tenant's own indexed posts don't actually contain enough to answer a question, the Ask assistant tells you so directly in an amber callout rather than inventing an answer — it never fabricates a citation.

**Current limitation, real and not yet resolved:** a post is only searchable or citable here once it has been indexed into this feature's own separate vector index — indexing is not yet wired into live ingestion itself (Stories 9.8/2.32's Deep Research feature and this feature use separate pipelines), so a newly ingested post won't show up in Discovery until an operator has run the backend indexing/backfill process. In practice this means Discovery's coverage of "your posts" can lag behind what the Posts feed already shows.

## Topic evolution and semantic drift (Stories 11.5, 11.6, 13.11, 13.12)

Open "Topic Evolution" from your tenant's Analytics screens to see how a topic's volume, sentiment, and driving voices have moved over time.

1. Pick a topic from a fixed list (Artificial Intelligence, Customer Support, Product Feedback, Pricing, Security — this list isn't yet generated from your own tenant's real topics), a granularity (day/week/month), and optionally "Compare Previous." A stacked bar chart shows mention volume split into positive/neutral/negative sentiment for each period, with a 🔥/📉 marker on any period trending sharply up or down. Hovering or clicking a bar shows that period's mention count, unique-author count, top keywords, and top contributing authors in three cards below the chart.
2. **Semantic drift analysis (Story 13.11 backend, 13.12 UI).** Pick a "then" and "now" date range and click "Analyze Drift" to see a real, computed drift score between the two windows, the top semantic clusters and sample posts from each window side by side, and a warning badge if the drift is significant. A "Generate AI Summary" button asks the AI assistant (the same one behind Discovery's "Ask AI Assistant," Stories 9.7–9.11) to explain the shift in plain language, grounded in the real clusters and sample posts shown.

## Connecting a platform (Story 6.3)

1. From your tenant's screens, open "Connect a platform."
2. You'll see every platform SocialEngage currently supports as its own card — GNews, Newswire, Azure AI Language, Azure OpenAI Service, Wikipedia (Story 6.21), Brave Search (Story 6.30), Bing Search/Azure (Story 6.32), and YouTube Data API (Story 10.13) — each shown with its real connection state (Connected, along with the credential's own status such as valid, expiring soon, expired, or revoked, or Not connected) and whether it's currently Active or Inactive. Facebook, Instagram, and LinkedIn are also shown here, but work differently — see "Connecting your own social accounts" below.
3. Before you submit any credential, the screen states plainly that you're creating your own account or API key directly with that provider, under that provider's own terms — SocialEngage is not signing you up with the provider and is not a billing intermediary for it.
4. To connect GNews, Azure AI Language, Azure OpenAI Service, Brave Search, Bing Search, or YouTube Data API, enter the credential fields that platform needs (a single API key for GNews, Brave Search, Bing Search, or YouTube Data API; an endpoint, key, and region for Azure AI Language; an endpoint, deployment name, and key for Azure OpenAI Service) and submit. Newswire and Wikipedia need no credential at all — there's nothing to submit, and both always show as connected.
5. **Scope varies by platform, not just by whether you're a Tenant-Admin.** GNews is the only platform that offers you a choice: as a Tenant-Admin connecting GNews, you can choose whether the new connection is "Tenant-wide" (shared by everyone in your tenant) or "Just for me" (a personal connection only you can use); as a Tenant User, connecting GNews always creates a personal connection just for you. Azure AI Language, Azure OpenAI Service, Brave Search, Bing Search, and YouTube Data API are always tenant-wide only — Azure, Brave/Bing Search, and YouTube API keys authenticate your organization's own subscription, not an individual's personal account, so no personal option is offered for any of the five, even to a Tenant-Admin; a Tenant User who isn't a Tenant-Admin and finds one of these not yet connected sees a plain note asking them to have their Tenant-Admin connect it, rather than a button that would just fail. Newswire and Wikipedia need no credential and so have no scope choice either. Facebook, Instagram, and LinkedIn are the opposite — always personal, never tenant-wide, for anyone; see below.
6. Disconnecting asks you to confirm before it actually removes the connection — it's a two-step "Disconnect," then "Confirm: disconnect," never a single click, and it's immediate.
7. Alongside connect/disconnect, each platform also has its own Activate/Deactivate control (Story 6.15) — a separate switch from the credential itself. Deactivating a platform keeps its stored credential in place (so you can reactivate it later without reconnecting) but stops it from being used for ingestion. A newly connected platform, and Newswire in particular, is not automatically Active — a Tenant-Admin has to explicitly turn it on.

**Current limitation:** the personal ("Just for me") Activate/Deactivate control always starts by showing Inactive, even if you'd already activated it before — there's no way yet for this screen to read back your own real personal on/off state; only the tenant-wide state can be read today. If it's actually already on, clicking Activate again is harmless.

## Connecting your own social accounts — Facebook, Instagram, LinkedIn (Stories 6.23, 6.27, 6.34, 6.35)

Facebook, Instagram, and LinkedIn work differently from every other platform on the Connect a platform screen — each uses a real sign-in redirect (OAuth) instead of an API key, and all three are personal to you, and only you, with no "Tenant-wide" option, for anyone, including a Tenant-Admin.

1. Click that platform's own "Connect" button. You're taken to a real sign-in and permission screen on that provider's own site, not a form inside SocialEngage.
2. **Facebook (Story 6.23, extended by Story 6.27 to support more than one Page):** after you approve it, you're brought back and shown checkboxes for every Facebook Page you're an admin of — you can select and connect more than one Page at once, not just a single one as this connector originally allowed. Each connected Page gets its own row with its own real health, and its own "Reconnect" or "Disconnect" action — disconnecting one Page doesn't touch any others you've connected. If SocialEngage's access to a Page is later lost (for example, you remove SocialEngage's access on Facebook's own side, or lose admin rights to that Page), that row is marked "Access lost — reconnect to restore this Page" rather than showing a generic failure. "Connect another Page" is always available to add more later. Facebook posts are never ingested from public content, other people's posts, or comments — only your own connected Page's own published posts and engagement.
3. **Instagram Business (Story 6.34):** connects an Instagram Business or Creator account the same way, via Meta's Graph API. Only your own connected account's own published photos, videos, carousels, and reels are ingested.
4. **LinkedIn (Story 6.35):** connects your own LinkedIn member account. If LinkedIn hasn't yet approved this app for organization-level (company page) access, a plain callout banner says so ("Organization features unavailable — partner scope approval pending") without blocking ingestion of your own member-level posts.
5. Turning any of the three on or off uses the same Activate/Deactivate control every other platform has.
6. If a provider ever revokes your connection (for example, you remove SocialEngage's access on that provider's own side), the affected card shows a distinct "Reconnect required" status rather than a generic failure — click "Reconnect" to go through the sign-in flow again.

## Managing watchlists (Story 6.4)

1. From your tenant's screens, open "Manage watchlists."
2. You'll see your own watchlists — name, match type (keyword, hashtag, account, or boolean), whether each is active, and which connected platforms it covers. **Watchlists are private to you** — even as a Tenant-Admin, you can't see or manage another user's watchlists from this screen; there is no tenant-wide oversight view of everyone's watchlists.
3. Below the list, a form lets you create a new watchlist: a name, a match type, either search terms (one per line) or — for a boolean watchlist — a boolean query, and which of your connected platforms it should cover. Only platforms you've actually connected (GNews, Newswire, Wikipedia, Brave Search, and/or Bing Search, since Stories 6.22, 6.30, and 6.32) are offered here; the two AI enrichment providers never appear as watchlist platforms, since a watchlist matches ingested posts by the source platform they came from, not by which AI provider might later analyze them. Your own company domain's content feed and your connected Facebook/Instagram/LinkedIn accounts aren't offered here yet either — a real, still-open gap, not a deliberate exclusion.
4. **A boolean watchlist's query has its own visual builder now (Story 12.4, ADR-0102).** A "🧭 Guided Builder" / "⚡ Advanced Text" toggle lets you either build the query clause-by-clause (pick a clause type — keyword, phrase, hashtag, mention, author, source, sentiment, or date — enter its value, combine clauses with AND/OR/NOT, and nest a group inside another group) or type the raw boolean syntax directly; the two stay in sync, and a "View AST JSON" toggle shows the exact structured query that will be saved. As you build it, the screen checks your query against the real query limits of whichever platforms the watchlist covers and shows an inline warning for anything a platform can't natively evaluate, or a blocking error (with saving disabled) if the query exceeds a platform's hard limits.
4. Each watchlist can be edited the same way it was created, switched active/inactive with its own dedicated toggle, and deleted. Deleting asks for a separate confirm step ("Delete," then "Confirm delete") before anything is actually removed, and it's permanent.
5. If a watchlist has changed elsewhere since you loaded the page (for example, edited in a second browser tab), saving your own change won't silently overwrite the other one — you'll see a message telling you it changed elsewhere and asking you to reload the page before retrying.

### Crisis Threshold Wizard (Story 9.4)

An "⚡ Crisis Wizard" button on the Manage watchlists screen (also offered from the empty-watchlists state) opens a 3-step wizard for activating a standardized reputation-risk monitoring template rather than building a watchlist by hand.

1. **Step 1 — Select a template.** Choose from a small set of pre-built scenarios (for example, Brand Crisis, Product Recall, Executive Attack, Competitor Surge, Data Breach), each with a plain description and a severity badge.
2. **Step 2 — Variables.** Fill in the specific names or keywords the template needs (for example, your own brand name or product name) — a live preview shows the exact search query your values will produce. Required fields are marked, and the wizard won't let you continue past a missing one.
3. **Step 3 — Thresholds & playbook.** Adjust the volume-spike and negative-sentiment percentages that would flag this as a crisis (sensible defaults are pre-filled), and review the template's own advisory response playbook — a numbered list of response steps, each with a named owner role and an SLA in minutes, shown for reference only.
4. Clicking "⚡ Activate Crisis Watchlist" creates a real, active watchlist immediately from your selections — this is a real, persisted watchlist just like one you'd build by hand, private to you like every other watchlist.

**Current limitation, disclosed directly in the wizard itself:** activating a template creates a real monitoring watchlist right away, but automated alert dispatch (email, Slack, or webhook) for a crisis threshold being crossed is not built yet — the wizard's own banner says this plainly ("will be enabled in an upcoming release").

## Checking connector status (Story 6.5, restructured by Stories 6.24 and 6.29)

1. From your tenant's screens, open "View connector status."
2. **As of Story 6.24, this screen is split into two sections: "Connectors" and "AI Providers."** GNews, Newswire, Wikipedia, Facebook, Brave Search, Bing Search, Instagram, and LinkedIn — every platform that's actually polled for content — appear under "Connectors," each with whether it's Active or Inactive, and if Active, its real health, the time of its last successful fetch (now shown together with how many posts that run ingested, Story 6.5 telemetry enhancement), the time of its last attempt, its consecutive-failure count, and its own real polling interval. Azure AI Language and Azure OpenAI Service appear separately, under "AI Providers," with a plain note explaining they're invoked on demand while a post is being processed, not polled on a schedule — this corrects an earlier, real display bug where an AI provider that was actually connected and working could show as "Paused" next to a Deactivate button, because the connector-status logic never distinguished the two kinds of platform.
3. **A connector can now show a "Stalled" status (Story 1.16, Story 6.29)** — distinct from "Failing" — when it hasn't successfully polled in a while even though it isn't actively erroring, for example because a scheduled run never completed. A dismissible banner at the top of this screen (and on the Analytics dashboard) lists every connector currently Stalled, Failing, or needing reconnection, with a direct action for each.
4. **As a Tenant-Admin, an active, unhealthy connector's card also has a "Re-sync now" button** (Story 6.29) that asks SocialEngage to attempt an ingestion run for that connector right now, rather than waiting for its next scheduled poll. If a run is already in progress, you'll see a plain "already in progress" message rather than a duplicate run being started.
5. As a Tenant-Admin, you also get the same Activate/Deactivate control here that's available from the Connect a platform screen.
6. Facebook, Instagram, and LinkedIn each show a distinct "Reconnect required" status here too, with its own "Reconnect" action, whenever that provider itself has revoked your connection — the same signal the Connect a platform screen shows.
7. This screen shows status only — never your posts or any other tenant content.

**Current limitations:**
- Watchlist compatibility warnings (which boolean-query features a given connector can't natively evaluate) are not shown here yet — the screen says so directly rather than showing fake data. Surfacing this needs a new backend endpoint that doesn't exist yet.
- The polling-interval and consecutive-failure-ceiling figures shown per connector are drawn from a small, hand-maintained list in the screen's own code, not read live from the backend — accurate as of the connectors that exist today, but not automatically guaranteed to stay in sync if a connector's own cadence ever changes.

## Managing your tenant's users — Team & access (Story 6.8, redesigned 2026-08-17)

1. From your tenant's screens, open "Team & access." Every user in your tenant — Tenant-Admin or Tenant User — can see this list: email, role, status, and when their access ends (shown as "active indefinitely" if no end date is set). This screen was visually redesigned on 2026-08-17 (a real seat meter and restyled table, described below) — the underlying actions themselves are unchanged from Story 6.8's original build.
2. At the top of the screen, a real seat-utilization card shows how many of your licensed seats are currently active out of your total, with a filled progress meter — real numbers pulled from your tenant's own record, not an estimate.
3. As a Tenant-Admin, you additionally get an "Invite a user" form (opened in a pop-up dialog) — enter an email address, choose a role (`tenant_user` or `tenant_admin`), and send the invite.
4. If your tenant is already at its licensed seat limit, inviting one more person is rejected with a plain explanation — free up a seat, or ask your Platform Admin to raise the limit, before inviting anyone else.
5. As a Tenant-Admin, each user row also has access controls: end that user's access immediately, schedule an end date for later, or — once someone's access has an end date, whether already passed or still scheduled — reactivate them. Each of these asks you to confirm before it takes effect, and the confirmation text itself makes clear whether the change is immediate or scheduled for later, so you can't mistake one for the other.

## Reviewing a user's access history (Story 6.14)

1. As a Tenant-Admin, each row on the Team & access screen also has a "View access history" button.
2. Clicking it opens a panel listing every real, recorded change to that user's own access — who made the change (shown by their real email where SocialEngage can still resolve it, or the raw internal identifier if that person is no longer in your tenant), when it happened, what kind of change it was, and the access-end-date value before and after (shown as "active indefinitely" when there was or is no end date).
3. If a user's access has never been changed, you'll see a plain "no access changes recorded" message rather than an empty-looking list.
4. This history loads only the first time you open it for a given user, not automatically for every row on the page.

## Viewing your tenant's settings (Story 6.9, restyled and extended by Story 6.40/ADR-0074)

1. From your tenant's screens, open "Tenant settings." A "Workspace Configuration" card shows your tenant's real name, status, domain, how many of your licensed seats are currently used out of your total (shown as "N of M active"), and when the tenant was created — still read-only, with no form to change any of it here.
2. **A "Data Export" card offers two real, working downloads.** "Export Full Workspace (JSON)" is Tenant-Admin only — a Tenant User sees the button disabled with a "Requires Tenant-Admin role" note next to it — and downloads a full export of your tenant's own data (Story 3.16). "Export Matched Posts (CSV)" is available to any tenant member and downloads your tenant's ingested posts.
3. **A "CRM Connectors" card** links straight to "Manage CRM Connectors" (see "Configuring your CRM connector" above).
4. **A "Notification Preferences" card** links straight to "Manage Daily Digest Email" (see "Daily digest email" below).
5. **As a Tenant-Admin only, an "Offboarding and Decommission" card** also appears here, with a "Request Tenant Deletion" link straight into the tenant deletion flow (Story 6.13, below) — this corrects an earlier, real gap where that flow had no link from this screen at all.

## Reviewing same-domain sign-up attempts (Story 6.10)

1. As a Tenant-Admin, open "Same-Domain Invite Assist" from your tenant's screens. This screen is Tenant-Admin only — a Tenant User is redirected away from it, not shown a stripped-down version.
2. You'll see a list of email domains that already match your tenant's own domain, where someone tried to sign themselves up rather than being invited, grouped one entry per domain. A domain with repeated attempts is flagged "⚠ Escalated."
3. Expand a domain to see the individual email addresses that attempted sign-up under it.
4. Next to each email, an "Invite this person" link takes you straight to the Team & access screen with that email already filled into the invite form — it doesn't send the invite for you; you still review it and click "Send invite" yourself.

## Browsing your tenant's posts (Story 6.11, updated by Stories 6.18, 6.19, 6.25, 6.26, 6.33, 6.37)

Once you've connected and activated a platform, SocialEngage now polls it automatically in the background — GNews, Newswire, and Wikipedia every 15–30 minutes, your own domain's content feed every 30 minutes (Story 1.13), and your own connected Facebook Page every 30 minutes (Story 1.15) — so posts appear here on their own, with nothing you need to click to make ingestion happen. (Before this scheduler shipped, on 2026-08-13, there was no automatic ingestion in this app's real running instance at all; and before Story 1.15 specifically, on 2026-08-18, a connected Facebook Page was never actually polled at all despite showing as connected — only what's described here reflects real, live behavior.)

1. From your tenant's screens, open "Posts" to see what SocialEngage has ingested for your tenant — **newest-ingested first** (Story 6.25), each with the platform it came from, the author where known, a title and body preview, and its published date. If a post has already been analyzed by an AI provider, you'll also see its sentiment, key phrases, entities, and the detected language right in the list, along with up to three entity and three key-phrase chips.
2. A search box and a set of filters — Provider, Sentiment, and Watchlist — sit above the list. **As of Story 6.18, search and every filter operate over your tenant's entire ingested history, not just the current page** — before that story shipped, they only ever searched the current 20-post page, which was a real, confirmed gap. The Provider filter (Story 6.26) automatically lists every platform your tenant actually has posts from, including every connector that's shipped since, without needing to be updated by hand as new connectors ship.
3. Below the list, a "Show more" button reveals more of your already-filtered result set in batches — there's no separate page-number picker or server round trip for paging; everything you've searched or filtered has already been fetched.
4. Click any post to open a detail panel on the same screen (a slide-over, not a separate page) showing the same information plus the full article body — rendered as real, formatted Markdown, not raw HTML or a plain-text snippet (Story 6.19) — the author (currently shown as a raw internal identifier — there's no lookup yet to turn it into a friendlier name) and which ingestion run brought it in (also a raw identifier, for the same reason). A direct link to a single post's own page (`/tenant/posts/:id`) still exists and shows the same detail, but nothing in the Posts screen itself links to it — opening a post always uses the slide-over panel.
5. **A post from Facebook shows which of your connected Pages it came from** (Story 6.33), and, separately from that, its own real author when Facebook reports one distinct from the Page itself — a "By: <author>" line only appears when the two differ, since most Page posts are published as the Page. **Any post matched by one of your watchlists shows that watchlist's name** in the card and detail view too (Stories 6.37, 3.11–3.12), reflecting the real, persisted link between a post and the watchlist(s) that matched it, not a guess made after the fact. An Instagram post shows its own media type and a thumbnail, with a gallery for a multi-image carousel; a LinkedIn post shows its own reaction/comment/share counts.

## Replying to a post (Stories 2.26, 2.27, 3.14, 6.38)

1. On a post's detail panel, next to the "Details" tab, there's a "Replies" tab and, alongside it, a "Reply" button. Today the "Reply" button is only ever enabled for a post from Facebook, and only when you personally have a connected, active Facebook Page credential — for any other platform, or if you haven't connected your own Facebook Page, the button is disabled with a tooltip explaining why.
2. Clicking "Reply" opens a composer drawer next to the post detail panel. Write your reply (up to Facebook's own character limit, shown live as you type) and click "Send Reply."
3. **Sending a reply is real, not simulated** — unlike the cross-platform post composer's "Publish" action, this actually posts a live comment to that post on Facebook through Facebook's own API, immediately, with no draft, review, or scheduling step.
4. Whether it succeeded or failed, you'll see a toast, and the attempt is added to that post's own "Replies" tab, showing its status (sent or failed), when it happened, and — for a sent reply — a "View live reply" link straight to the real comment on Facebook.
5. Every reply you send is recorded permanently against that post, whether it succeeded or failed, so the Replies tab is a genuine audit trail, not just a live status indicator.

**Current limitations:** replying only works for Facebook today — no other platform's connector supports it yet, so the button stays disabled everywhere else. If you have more than one of your own Facebook Pages connected (Story 6.27), replying currently uses whichever Page you connected most recently, not necessarily the Page the post actually came from — if those two differ, Facebook will reject the reply with a permission error. The reply composer also has no media attachment or AI-rewrite option, unlike the full cross-platform post composer.

## Escalating a post or author to your CRM (Stories 11.1, 11.2)

A "Push to CRM" (💼 CRM) button — on a post's detail panel, and on each author row inside a Prospecting list — lets you escalate that post or author into your organization's own CRM system as a support case, lead, opportunity, account, or contact record. Any tenant member can use this, not just a Tenant-Admin.

1. Before you can push anything, your tenant's CRM connector needs its own credentials configured — see "Configuring your CRM connector" below.
2. Clicking "Push to CRM" opens a dialog: pick which CRM connector to send to (if more than one is configured), the record type (Support Case/Incident, Lead/Prospect, Opportunity/Deal, Account, or Contact), and optionally an assignee/queue ID and internal notes.
3. Clicking "Escalate to CRM" sends it immediately — this is a real, live write to your organization's own Dynamics 365, Salesforce, or HubSpot environment, not a simulation. On success you get a direct link to the new CRM record; on failure you see a plain error.
4. **If that exact item has already been escalated to that CRM before, you're warned rather than silently creating a duplicate** — you can open the existing record instead, or explicitly choose "Create duplicate anyway."

## Configuring your CRM connector (Story 11.2, ADR-0095)

1. From your tenant's settings, open "Manage CRM Connectors" to see Microsoft Dynamics 365, Salesforce, and HubSpot each as their own card, showing whether it's configured yet.
2. Open a connector to enter its credential fields (for example, a Dataverse environment URL and client credentials for Dynamics 365), save them, and use "Test Connection" to verify SocialEngage can actually reach your CRM environment with what you entered. "Remove Credentials" clears a configured connector.
3. This screen has no role gate of its own beyond being a member of your tenant — any tenant member who reaches it can configure or remove your tenant's shared CRM credentials, not just a Tenant-Admin.

## Daily digest email (Stories 11.3, 11.4, ADR-0096)

Open "Manage Daily Digest Email" from Tenant settings (or "Notification Preferences" there) to set up a personal morning summary email. This is a per-user preference — a Tenant-Admin and a Tenant User each configure their own, independently.

1. Turn the subscription on or off, and choose a delivery time and your own timezone from a list of common timezones.
2. Choose what's included: an AI-generated executive summary, the top 5 highest-impact conversations, and/or a trending-topics breakdown — and optionally scope the digest to specific watchlists rather than all of your active ones.
3. "Preview Daily Digest" renders a real, live preview of what your next digest would look like, right in the browser, using your tenant's current data and your current settings, before you save anything.
4. "Save Preferences" persists your choices for the real scheduled send.

## Webhook subscriptions (Story 10.12, ADR-0092)

Open "Webhooks" from Tenant settings (there's no direct link to it yet from the settings screen itself — go to `/tenant/settings/webhooks` directly) to receive real-time HTTP notifications when certain events happen in your tenant (currently: an alert firing, or a crisis-wizard threshold being exceeded).

1. "+ Add Webhook" lets you register your own HTTPS endpoint URL. SocialEngage will POST a signed JSON payload to it in real time when a subscribed event occurs, with an HMAC-SHA256 signature you can verify on your own end using the shown secret.
2. "⚡ Test Ping" sends a real, signed test payload to that endpoint immediately, so you can confirm your receiving system is wired up correctly before relying on it.
3. Remove an endpoint at any time with its ✕ button.

## Plan & seats (Story 13.6, ADR-0112)

Open "Plan & seats" from your tenant's screens to see your tenant's current plan name, a real seat-usage meter (used out of your licensed total), and which features your plan currently makes available. A feature your plan doesn't include (for example, CRM push or data export, gating the Prospecting screen's own actions) is shown as disabled here with a note to contact your Platform Admin to upgrade, rather than just failing silently elsewhere in the app. Both Tenant-Admins and Tenant Users see the identical screen.

## Running enrichment manually, and correcting it by hand (Story 6.16, extended by Stories 3.13 and 6.31)

1. On a post's detail screen, if that post has never been analyzed by an AI provider, you'll see a "Run enrichment now" button — this typically applies to a post that was ingested before your tenant had connected and activated any AI provider.
2. Clicking it asks SocialEngage to analyze that one post right now, using whichever AI provider your tenant currently has connected and active. On success, the screen reloads and shows the real sentiment, key phrases, entities, and which provider produced them.
3. If no AI provider is currently connected and active for your tenant, you'll see a plain message saying so rather than an error — connect and activate one first (see "Connecting a platform" above) if you want to enrich posts this way.
4. **As of Story 6.31, a post that has been analyzed also has an "Edit" button** on its AI analysis card. It opens a second panel, alongside the post detail panel rather than replacing it, letting you correct the AI's own sentiment (a Positive/Neutral/Negative choice), key phrases (add or remove, up to 50), detected language, country, and summary by hand. Saving records your correction immediately and marks the post with an amber "Edited by user" badge, which shows who made the change and when if you hover over it — your correction is never silently blended back into what the AI originally produced.
5. Once a post has been manually corrected this way, clicking "Run enrichment now" again asks you to confirm first, since re-running would otherwise overwrite your own correction with a fresh AI result — confirming replaces it (keeping the prior AI and human values on record for that post), while backing out leaves your correction exactly as you left it.
6. This "Run enrichment now" button only appears on a post with no enrichment yet, or after you've explicitly confirmed you want to re-run it on a corrected one — it's never available to silently overwrite a correction.

## Monitoring your own company domain's content feed — now a multi-feed admin screen (Stories 6.12, 6.17, 6.20, 6.28)

If you want SocialEngage to monitor your own company blog or newsroom rather than a third-party platform, this is a separate flow from "Connect a platform," with no credential to submit — instead, you prove you control the domain.

**Corrected 2026-08-17 (Story 6.20): this entire screen is now Tenant-Admin only.** Earlier (Stories 6.12/6.17), a Tenant User could set up and verify a feed, with only activation reserved for a Tenant-Admin. Story 6.20 tightened this — connecting, editing, removing, and viewing your tenant's configured feeds now all require a Tenant-Admin session on the backend itself, not just on the button that starts the flow. A Tenant User can still open this screen (they're not redirected away), but sees an empty feed list and no working actions, since the list itself is now Tenant-Admin-only data.

1. As a Tenant-Admin, from "Connect a platform," follow "Configure Custom Domain Feed" (or go directly to "Monitor your own domain's content feed"). You'll see every feed your tenant has already configured, each with its domain, feed URL, and status (pending verification, verified, or inactive).
2. To add another one, click "Connect another feed," enter the domain and the feed URL (your blog or newsroom's RSS/Atom feed), optionally give it a friendly name (Story 6.28 — for example, "Company Blog" instead of the raw domain), and submit. A feed with a name shows that name as its main label, with the domain shown underneath it; a feed with no name just shows its domain, as before. A name can be added or changed later without re-verifying the feed. If your tenant has already verified that exact domain for a different feed, the new one is verified immediately — you don't need to repeat the DNS proof for a domain you've already proven you control.
3. Otherwise, SocialEngage gives you a DNS TXT record (a host, a value, and an expiry) to publish at your own domain registrar. Publishing it can take anywhere from a few minutes up to 72 hours to take effect — this is normal DNS propagation, not a stuck or broken state. **As of Story 6.20, these DNS details stay visible on this screen for as long as the feed is still pending** — leaving and returning, or reloading the page, no longer loses them.
4. Once you've published the record, click "Verify now" on that feed's own row. If it's not detected yet, you'll see a plain message to try again shortly — this isn't an error, and you can click "Verify now" as many times as you need.
5. A verified feed can be edited (its feed URL only — its domain can't be changed) or removed at any time. Removing a feed stops it from being polled but keeps everything it already ingested.
6. A verified feed still isn't monitored on its own — a single Activate/Deactivate control at the bottom of the screen turns ingestion on or off for every feed your tenant has configured together (there's no per-feed on/off switch). If you have verified feeds but this switch is off, the screen tells you plainly that nothing is currently being polled.

**Current limitation:** this connector still has no personal, "just for me" option at all — every feed is tenant-wide, and, as of Story 6.20, only a Tenant-Admin can see or manage any of it.

## Analytics dashboard (Stories 8.1–8.7, 8.9, 8.10)

Open "Analytics" from your tenant's screens to see aggregated, dashboard-style views over your tenant's own ingested posts — every number here is computed live from your real posts, never a fabricated or placeholder figure. Both Tenant-Admins and Tenant Users can open this screen; there's no role difference on it. The dashboard still has four tabs — Overview, Sentiment, Conversations, and Sources — but the Overview tab was substantially rebuilt on 2026-08-19/20 (Stories 8.7, 8.9, 8.10); the description below reflects that rebuilt version, not the original three-card layout this section used to describe.

1. **Date range.** A date-range picker at the top controls every tab at once — pick a standard range (Today, Last 7/14/30 Days, This Month, Last Month, All Available History) or a custom start/end date. A "Compare to previous period" checkbox is still present and still fetches comparison data, but as of the Overview tab's Story 8.7 rebuild, that comparison is no longer visibly shown anywhere on the Overview tab — a real, known gap (see "Current limitations" below), not something removed on purpose from the UI you actually see.
2. **Overview tab.** Now an eight-widget grid rather than three summary cards: a Sentiment gauge (click a segment to filter), an Authors-by-source donut, a Watchlist Coverage donut with its own topic/watchlist selector (Story 8.9) that narrows every widget on the tab to just one watchlist's matched posts, a Volume & projections chart with an optional statistical forecast toggle and a "Crisis Alert Radar" note that only appears when negative-sentiment volume is genuinely trending up, a Location & geospatial insights widget (Story 8.10, described below), a Sources breakdown, a Top authors list, a Key phrases cloud, and a Languages breakdown — plus one slot reserved for a future widget that isn't built yet. Clicking almost any row, segment, or chart point filters every other widget on the tab to match (every active filter combines with AND, not OR), with a dismissible chip per active filter and a "Clear all" to reset everything at once. A "N matching posts" button opens the exact filtered list in a slide-over panel, and your current filter selection round-trips into the page's own URL, so a link to this tab can be shared with your filters already applied.
   - **Location & geospatial insights (Story 8.10).** An interactive country map and a ranked "Top Countries" list, built from a real per-post country field that GNews, Newswire, and your own domain feed's articles, and Instagram's own location tag, can now carry (Story 2.20, shipped 2026-08-20 — this is genuinely new data that didn't exist before that date). A post with no country information is counted honestly in its own "Unknown / Unmapped" bucket rather than excluded or guessed at, and a country with fewer than 3 matching posts in the current range shows no sentiment score, to avoid a misleadingly precise figure from too few posts. Clicking a country row or a map region filters the rest of the tab to that country.
3. **Sentiment tab.** A sentiment donut and a sentiment-over-time chart, plus your tenant's "Top fans" and "Top critics" — the authors whose posts skew most positive or most negative — and positive/negative key-phrase clouds. Clicking an author or a phrase filters every widget on this tab down to just their matching posts, and a "View matching posts" link opens the exact filtered list.
4. **Conversations tab.** A key-phrase cloud (sized by real frequency) and a top-phrases-over-time chart, plus a Languages breakdown (Story 8.5) — the real, detected language of each enriched post, ranked by count — and an Entity mentions breakdown by entity type (person, organization, location, and so on), drawn from the same AI-extracted entities shown as chips in the Posts feed. Clicking a phrase or a language filters the tab the same way the Sentiment tab does.
5. **Sources tab.** Post volume by source, a detail list per source with its own post count, sentiment counts, and a real 0–10 weighted sentiment score (Story 8.6) — honestly blank when a source has no enriched posts yet, never a fabricated default — and a volume-over-time chart broken out per source.
6. A widget only ever shows a real, computed value or an honest "no data yet" message — nothing on this dashboard is a hardcoded or sample figure.

**Current limitations:** the "Compare to previous period" checkbox on the date-range picker still fetches real comparison data, but nothing on the current Overview tab actually displays it since that tab's Story 8.7 rebuild — a real, confirmed gap, not a deliberate removal you're meant to notice going away. Location coverage only reflects posts whose source can actually supply a country today (GNews, Newswire, and tenant-owned-feed articles with a dateline location, plus Instagram's own location tag) — Facebook, LinkedIn, Wikipedia, Brave Search, and Bing Search posts don't carry this field yet, so they always land in "Unknown / Unmapped" on the map. Per-widget export doesn't exist yet. Period-over-period comparison, where it is shown, only appears on the Overview tab's own totals, never on Sentiment, Conversations, or Sources.

## Drafting a cross-platform post (Story 6.36, publishing made real by Story 6.39, research made real by Story 6.41)

Open "Compose" from your tenant's screens (or the "Compose Post" button on the Posts screen) to draft a single message and preview how it would look on up to seven different networks (Facebook, Instagram, LinkedIn, X/Twitter, Bluesky, Mastodon, and Threads) side by side.

1. Write your message once; each network's own live preview card updates as you type, matching that network's own real character limits and layout.
2. You can attach images (with accessibility alt-text that carries through to every preview card), import a Markdown or plain-text document as a starting draft, and ask an AI assistant to rewrite your copy in a different tone.
3. Drafts are saved automatically to your own browser as you work (up to your 50 most recent), not to any shared tenant storage — a draft made on one device or browser isn't visible from another.
4. **"🔬 Deep Research" (Story 6.41) is real, not simulated.** Once you've written at least 10 characters, clicking it asks an AI assistant to research the public conversation around your draft using your tenant's connected search provider (Brave or Bing) and returns key phrases and related topics, a written context summary, how your draft compares to what's actually being said, and the real source links it drew from. This needs your tenant to have an active Azure OpenAI connection and an active Brave or Bing Search connection — if either is missing, you're told plainly which one to connect rather than shown a generic failure. Research results are ephemeral (not saved with your draft).
4. **As of Story 6.39, publishing to Facebook is genuinely real — not simulated.** The "Publish" button opens a dialog listing your active, connected Facebook Pages; select which ones to send to and confirm. SocialEngage actually posts your message to each selected Page through Facebook's own API. On success you'll see a direct link to the real published post for each Page; on failure you'll see a plain per-Page error message instead, so a failure on one Page never gets mistaken for success on another.
5. **Every other network in the preview rail — Instagram, LinkedIn, X/Twitter, Bluesky, Mastodon, Threads — still can't actually be published to.** The same dialog shows each of them as "not yet available for publishing," with a plain note that they'll be enabled once their own publishing is built. You can still draft and preview a message for them; only sending it is not yet possible for anything but Facebook.

## Outbound Publishing & Scheduling (Stories 11.7, 11.8, extended by 11.12, 12.1)

A separate "Outbound Publishing & Scheduling" screen — distinct from the "Compose" screen described above — lets you schedule a post for later, not just publish immediately, and gives LinkedIn a real publishing UI trigger for the first time. Any tenant member can use it.

1. The screen lists every outbound post your tenant has queued, sent, or attempted, filterable by status (All, Scheduled, Published, Failed, Cancelled), each with its content, target platform, and — for a scheduled post — "Reschedule" (pick a new future date/time) and "Cancel" actions.
2. "✍️ Compose Post" opens a composer offering **LinkedIn, Facebook, Bluesky, and X/Twitter** as target networks, with per-platform Page/account targeting where the platform needs it (LinkedIn and Facebook). Write your text, choose "Publish now" or "Schedule for later" with a future date and time, and submit.
3. **As you type, an "@" mention gets live suggestions** (Story 11.12) drawn from real accounts relevant to your selected target platforms — click one to insert it.
4. **LinkedIn and Facebook publish for real** through this screen, immediately or at your scheduled time — the same real connectors already described under "Connecting your own social accounts" and the Compose screen above.

**Current limitations, real and not yet resolved:** this composer also lets you select **Bluesky** or **X/Twitter** as a target, but neither has a real connector behind it yet (unlike the roadmap document for these platforms, ADR-0118, no working Bluesky or X/Twitter connector actually exists in `social-listening-core` today) — selecting either and submitting will fail rather than silently succeed, since there's nothing to actually publish to. A backend endpoint for editing or deleting an already-published outbound post exists (Story 14.2, ADR-0119), but this screen only offers "Reschedule" and "Cancel" for a post that's still scheduled — there's no way yet to edit or delete one that's already published.

## Unified Social Inbox (Stories 11.9, 11.10)

Open "Inbox" from your tenant's screens for a dedicated triage view — a queue of individual mentions on the left (filterable by All/Urgent/High Priority/Snoozed/Resolved, plus a text filter) and a detail-and-reply pane on the right. Any tenant member can use it.

1. Selecting an item shows its content and priority, with actions to **Snooze** it for a chosen number of hours, **Resolve** it, or **Reply** to it directly from the pane.
2. **Sending a reply from here is real, not simulated, for any platform whose connector supports replying** — today that's Facebook only, the same real send described under "Replying to a post" above; replying also automatically marks the item Resolved.

**Current limitation, real and not yet resolved:** nothing in SocialEngage's shipped screens actually adds an item to this inbox queue yet — there's no "send to inbox" action anywhere in the Posts feed or elsewhere. In practice this means the Inbox will show "No conversations in this view" for a real tenant today, even though triage, snoozing, resolving, and replying all genuinely work once an item exists in the queue.

## Deleting your tenant (Story 6.13)

This is the one truly irreversible action available anywhere in SocialEngage — read this section in full before starting. **This screen and everything in it is Tenant-Admin only** — a Tenant User has no entry point to it anywhere, not even a hidden or greyed-out one; going to `/tenant/settings/delete` directly redirects them back to the ordinary tenant settings screen. There is also no link to this screen from the ordinary Tenant settings screen (Story 6.9) today — you currently have to navigate to it directly.

1. Open the tenant deletion screen and choose "Request deletion." This starts a 30-day grace period — nothing is deleted yet, and your tenant keeps working normally.
2. During the grace period, you'll see the date your tenant will actually be deleted unless you cancel before then. You can export your tenant's data as JSON or CSV any number of times before that date.
3. To change your mind, choose "Cancel deletion request," then confirm — ingestion resumes immediately and your tenant carries on as if nothing happened.
4. Once the 30-day grace period has actually elapsed, a "Confirm deletion" button becomes available (it's disabled, with a plain explanation, before then). Confirming asks you to confirm a second time, with an explicit warning that this is final and cannot be undone, and that all of your tenant's data — posts, watchlists, and credentials — will be permanently deleted.
5. After you confirm, deletion runs in the background. The screen shows a plain "in progress" message and does not update further — your own session will simply stop working once deletion actually completes.

**Current limitation:** if a deletion request is already active when you open this screen, there's no way for the screen itself to tell you exactly when the grace period ends (only the moment you first request deletion shows that date) — the "Confirm deletion" button still correctly stays disabled until the backend itself confirms the grace period has elapsed, so you can't accidentally jump ahead, but the countdown display itself may be missing on a return visit.

## Prospecting lists (Story 10.2)

Open "Prospecting" from your tenant's screens to manage lists of authors you're tracking as potential leads — the same screen and capabilities available to any tenant member (see the User Manual's own "Prospecting lists" section for the full walkthrough): create a list, set a per-author relationship stage and note, and export or push a list you own to a connected CRM system. As of Story 13.14, "Push to CRM" pushes the whole list at once (with a per-entry checkbox to leave some out) and reports how many entries were pushed versus skipped as already-pushed duplicates — both actions are also gated by your tenant's plan features (Story 13.6, see "Plan & seats" below), not just by list ownership.

## Ad-hoc analytics query (Story 10.5)

Open "Query" from the Analytics section to build your own custom data slice — pick dimensions and metrics, add filters, run, and export as CSV. Same screen available to any tenant member; see the User Manual's own section for the full walkthrough.

## Real-time alerts (Story 10.10)

Open "Alerts" from your tenant's screens to create alert rules (Volume Spike, Negative Sentiment Outcry, Influential/VIP Author Post, Connector Error/Ingestion Failure, or Keyword Burst, each with its own cooldown) and to triage the resulting alert feed (Active / Acknowledged / Resolved). Same screen available to any tenant member; see the User Manual's own section for the full walkthrough.

## What's not built yet

- **Public self-service sign-up is not open.** The sign-up screen itself (Story 6.7) is real and works, but per this project's own current stage (Stage 0, no persistent environment or public onboarding), it isn't offered to the general public today — see "How you get your own tenant today" above.
- **A friendlier author or ingestion-run display on a post's detail screen** — both currently show as raw internal identifiers, since no lookup exists yet to resolve either one into something more readable.
- **A concise AI-generated summary field** — Azure OpenAI (only, not Azure AI Language) now computes one per post (Story 2.17), but no screen in `social-listening-admin` displays it yet, except as one of the fields you can hand-edit via the manual enrichment-override drawer (Story 6.31); it's otherwise only in the raw ingestion payload.
- **Watchlist compatibility warnings on the connector status screen** — which boolean-query features a connector can't natively evaluate — are named on that screen as a real, deliberate gap, not shown.
- **Reading back your own personal (as opposed to tenant-wide) activation state for a connector** — the Activate/Deactivate control for "just for me" connections always starts assuming it's off, even if it's actually already on, since there's no way yet to read that back.
- **A real, live "list this tenant's connectors" view** — the Connect a platform and connector status screens both show a fixed list of the platforms SocialEngage currently supports rather than a dynamically discovered one. In practice this makes no difference today, since that fixed list already matches every platform that actually exists.
- **Your own company domain's content feed and your connected Facebook/Instagram/LinkedIn accounts as watchlist sources** — a watchlist can only target GNews, Newswire, Wikipedia, Brave Search, or Bing Search today.
- **Seeing the exact grace-period end date on a return visit to the tenant deletion screen** — it's only shown at the moment you first request deletion.
- **Period-over-period comparison on the Analytics dashboard** — the "Compare to previous period" checkbox still fetches real data, but nothing on the current Overview tab displays it since that tab's Story 8.7 rebuild; it's also still absent from the Sentiment, Conversations, and Sources tabs. Per-widget export doesn't exist yet either.
- **Location coverage on the Analytics dashboard is source-limited** — only GNews, Newswire, tenant-owned-feed, and Instagram posts can carry a country today; every other platform's posts land in "Unknown / Unmapped" on the map until their own connectors gain the same geospatial extraction.
- **Publishing from the cross-platform "Compose" post composer (Stories 6.36, 6.39) to anything but Facebook** — drafting and previewing work for all seven networks, and publishing to your own connected Facebook Pages is now genuinely real (Story 6.39), extended to include media/asset uploads (Story 13.10); Instagram, X/Twitter, Bluesky, Mastodon, and Threads are still shown as "not yet available for publishing" in that screen's own Publish dialog. **LinkedIn now publishes for real, but only from the separate "Outbound Publishing & Scheduling" screen** (Stories 11.7/11.8, ADR-0075), not from "Compose."
- **Replying to a post works for Facebook only** (Stories 2.26/2.27/3.14/6.38, 11.9/11.10) — no other connector supports outbound replies yet, so the Reply button (and the Unified Social Inbox's own reply action) stays disabled or fails for every other platform. If you manage more than one connected Facebook Page, replying uses your most-recently-connected Page's credential rather than necessarily the Page the post came from.
- **Bluesky and X/Twitter are offered as publish targets in the Outbound Publishing & Scheduling composer** (Stories 11.7/11.8), but neither has a real connector built yet — selecting either will fail rather than publish.
- **Facebook post engagement counts (reactions, comments, shares) are now captured by the connector** (Story 2.18) but aren't shown anywhere in the post feed or detail panel — unlike LinkedIn's own reaction/comment/share counts (Story 6.35) or Instagram's like/comment counts (Story 6.34), which are displayed.
- **A Tenant User's dismissal of the getting-started checklist (Story 9.6) doesn't persist** — only a Tenant-Admin's dismissal is saved to the tenant's own record; a Tenant User's dismissal only lasts for their current browser session.
- **A backend endpoint for a plain-language, confidence-graded explanation of any dashboard metric now exists** (Story 9.2, `POST /v1/explain`), but no screen anywhere in `social-listening-admin` calls it yet — this is a separate capability from the real Discovery "Ask AI Assistant" feature described above.
- **Automated alert dispatch (email, Slack, or webhook) for a Crisis Threshold Wizard template is not built yet** (Story 9.4) — activating a template creates a real watchlist immediately, but nothing is sent yet when a threshold is actually crossed, exactly as the wizard's own banner discloses.
- **Discovery's semantic search/Ask index (Stories 9.7–9.11) is not kept current with live ingestion** — a post only becomes searchable or citable there once an operator has run the backend indexing/backfill process, which can lag behind what's already visible in the ordinary Posts feed.
- **Nothing in the app currently adds an item to the Unified Social Inbox queue** (Stories 11.9/11.10) — triage, snoozing, resolving, and replying all genuinely work, but there's no "send to inbox" action anywhere else yet, so the queue stays empty in practice.
- **The Topic Evolution screen's topic picker is a fixed list** (Stories 11.5/11.6), not yet generated from your tenant's own real topics.
- **No in-app link to the Webhook subscriptions screen** (Story 10.12) from Tenant settings yet — it's real and works, but you have to go to `/tenant/settings/webhooks` directly.
- **Editing or deleting an already-published outbound post** — a backend endpoint for this exists (Story 14.2, ADR-0119), but the Outbound Publishing & Scheduling screen only offers Reschedule/Cancel for a still-scheduled post.
