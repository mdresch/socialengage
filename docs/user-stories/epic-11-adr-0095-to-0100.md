# Epic 11: Engagement, workflow, and composer (ADRs 0095–0100)

Stories derived from the ADRs drafted in `docs/adr/`. Backend stories are sequenced before their frontend counterparts where both repos are touched.

---

## Story 11.1 — CRM connector and case handoff (backend)

**Source:** ADR-0095 · **Status:** Ready

**As a** backend engineer,
**I want** a `CRMConnector` interface, HubSpot and Salesforce implementations, and `POST /v1/inbox/:id/case`,
**so that** a `Tenant-Social-Care-Agent` can escalate an inbox item to a CRM as a lead, opportunity, or support case.

**Acceptance Criteria**
- `CRMConnector` interface with `pushCase()`, `validateCredentials()`, and `status()`.
- CRM credentials stored in `platform_credentials` with `credential_type='crm'`.
- At least one CRM provider (HubSpot or Salesforce) is implemented.
- `POST /v1/inbox/:id/case` creates an `outbound_activities` row with `activity_type='crm_handoff'`.
- Default field mapping is provided; `Tenant-Admin` can override via `crm_field_mappings`.
- Batched pushes are idempotent where the CRM allows.

---

## Story 11.2 — Case handoff to CRM UI (frontend)

**Source:** ADR-0095 · **Status:** Ready (depends on Story 11.1)

**As a** `Tenant-Social-Care-Agent`,
**I want** a "Create case in CRM" action on the post detail and inbox item,
**so that** I can push a relevant post into our CRM without leaving the platform.

**Acceptance Criteria**
- "Create case" button on `PostDetail` and `InboxItem`.
- A modal lets the user pick the CRM connector, `caseType`, assignee, and optional notes.
- On success, the UI shows the CRM URL and a link to the `outbound_activities` record.
- Errors (missing connector, failed push) are surfaced inline.

---

## Story 11.3 — Daily digest email (backend)

**Source:** ADR-0096 · **Status:** Ready

**As a** backend engineer,
**I want** `user_digest_preferences`, an hourly digest scheduler, and a rendered email,
**so that** `Tenant-Reader` receives a daily summary of watchlist activity.

**Acceptance Criteria**
- `user_digest_preferences` table with `send_at_utc`, `timezone`, `watchlist_ids`, and `include_*` flags.
- Hourly worker checks for users whose local send time is due.
- Digest content uses `*DailyCount` tables (ADR-0087) for fast aggregation.
- Optional AI summary is bounded to one call per user per day.
- Email sent via Azure Communication Services or tenant-configured SMTP.
- `POST /v1/users/me/digest-previews` returns a one-off preview.

---

## Story 11.4 — Daily digest email UI (frontend)

**Source:** ADR-0096 · **Status:** Ready (depends on Story 11.3)

**As a** `Tenant-Reader`,
**I want** a digest preferences page and a preview of my daily email,
**so that** I can configure what time and what content I receive.

**Acceptance Criteria**
- `DigestPreferencesView` with time, timezone, watchlist, and content toggles.
- A preview shows how the email will look for yesterday's data.
- Send time uses the user's timezone and converts to UTC.
- Users can enable/disable the digest entirely.

---

## Story 11.5 — Topic evolution timeline (backend)

**Source:** ADR-0097 · **Status:** Ready

**As a** backend engineer,
**I want** `GET /v1/topics/evolution` to return time-series data for a topic's volume, sentiment, top authors, and keywords,
**so that** `Topic-Center-Analyst` can see how a conversation developed.

**Acceptance Criteria**
- Endpoint accepts `topicId`, `start`, `end`, and `granularity` (`day`, `week`, `month`).
- Data sources: `TopicDailyCount`, `SentimentDailyCount`, `AuthorTopicSignal`, and `post_topics`.
- Response includes `points` with `mentionCount`, `uniqueAuthors`, `sentiment`, `topAuthors`, `topKeywords`, and `trend`.
- `trend` is computed from the 7-day slope of `mentionCount`.
- Supports `compareToPrevious` for overlaying the previous period.

---

## Story 11.6 — Topic evolution timeline UI (frontend)

**Source:** ADR-0097 · **Status:** Ready (depends on Story 11.5)

**As a** `Topic-Center-Analyst`,
**I want** a topic evolution timeline with volume, sentiment, author, and keyword charts,
**so that** I can understand how a topic is changing over time.

**Acceptance Criteria**
- `TopicEvolutionTimeline` component with a multi-series chart.
- `TrendAnnotation` marks `rising`/`falling`/`stable` periods.
- `AuthorSparkline` and `KeywordHeatmap` widgets are present.
- Users can pick date range and granularity.
- Deep links include the selected `topicId`, `start`, `end`, and `granularity`.

---

## Story 11.7 — Publishing and scheduling (backend)

**Source:** ADR-0098 · **Status:** Ready

**As a** backend engineer,
**I want** `SocialConnector.publish?()`, scheduled `outbound_activities`, and `POST /v1/outbound/posts`,
**so that** the composer can publish real posts to connected platforms.

**Acceptance Criteria**
- `SocialConnector` exposes optional `publish?()` with `OutboundPost` and returns `platformPostId` and `url`.
- `outbound_activities` supports `activity_type='post'` with `status` and `scheduled_for`.
- `POST /v1/outbound/posts` creates one activity per target platform and supports `scheduledFor`.
- A scheduler worker dispatches scheduled posts at `scheduled_for`.
- `PATCH .../cancel` and `PATCH .../reschedule` endpoints are available.
- At least one platform (e.g. LinkedIn or Bluesky) implements `publish?()`.

---

## Story 11.8 — Publishing and scheduling UI (frontend)

**Source:** ADR-0098 · **Status:** Ready (depends on Story 11.7)

**As a** `Tenant-User`,
**I want** a composer that lets me publish immediately or schedule for later and pick which pages/accounts to target,
**so that** I can manage outbound posts in one place.

**Acceptance Criteria**
- `PolypostComposer` supports `Publish now` and `Schedule for later`.
- Users can pick `targetPlatforms` and `assetTargets` (pages, accounts).
- Scheduled posts appear in an `OutboundPostsView` with status and cancel/reschedule actions.
- Errors from `POST /v1/outbound/posts` are surfaced with platform-specific detail.

---

## Story 11.9 — Unified social inbox and reply (backend)

**Source:** ADR-0099 · **Status:** Ready

**As a** backend engineer,
**I want** `inbox_items`, triage state, and `POST /v1/inbox/:id/reply`,
**so that** `Tenant-Social-Care-Agent` can manage and respond to matched posts.

**Acceptance Criteria**
- `inbox_items` table with `priority`, `status`, `assigned_to`, `snoozed_until`, `notes`, and `tags`.
- `GET /v1/inbox` supports filters for status, priority, assigned user, watchlist, and platform.
- `PATCH` and assignment endpoints are RLS-scoped.
- `POST /v1/inbox/:id/reply` proxies to the reply endpoint (ADR-0073) and marks the item `resolved` on success.
- Inbox items are created from `IngestionPostMatchedEvent`.

---

## Story 11.10 — Unified social inbox and reply UI (frontend)

**Source:** ADR-0099 · **Status:** Ready (depends on Story 11.9)

**As a** `Tenant-Social-Care-Agent`,
**I want** an inbox with filters, priority, assignment, and a reply composer,
**so that** I can triage and respond to mentions efficiently.

**Acceptance Criteria**
- `InboxView` with filter chips, sort by priority, and bulk actions.
- `InboxItemDetail` shows the post, notes, tags, and a reply composer.
- Users can assign, snooze, resolve, and reply from the detail pane.
- Reply uses the same composer as `ADR-0073`.
- Mobile view stacks filters and detail.

---

## Story 11.11 — Composed post author mention suggestions (backend)

**Source:** ADR-0100 · **Status:** Ready (depends on Stories 9.10 and 11.10)

**As a** backend engineer,
**I want** `POST /v1/composer/mention-suggestions` to combine `AuthorTopicSignal`, `RAG` search, and keyword signals,
**so that** the composer can suggest relevant authors to mention.

**Acceptance Criteria**
- Endpoint accepts `text`, `targetPlatforms`, `watchlistId`, and `maxSuggestions`.
- Suggestions include `authorId`, `authorName`, `platformId`, `handle`, `reason`, `matchSource`, and `confidence`.
- `AuthorTopicSignal`, `RAG` search, and keyword match are combined with weights.
- Authors already mentioned in the draft are excluded.
- Only authors active on the selected `targetPlatforms` are returned.

---

## Story 11.12 — Composed post mention suggestions UI (frontend)

**Source:** ADR-0100 · **Status:** Ready (depends on Story 11.11)

**As a** `Tenant-User`,
**I want** mention suggestions to appear as I type in the composer,
**so that** I can tag relevant people without searching manually.

**Acceptance Criteria**
- Suggestions appear below the composer with a 300 ms debounce.
- Each suggestion shows the platform icon, author name, and a short reason.
- Clicking a suggestion inserts the correct mention syntax at the cursor.
- Suggestions are marked by source (`topic`, `rag`, `keyword`) and confidence.
