# Epic 15: Research-driven ADR revisions — Alerts and Export (ADRs 0123–0124)

## Story 15.1 — Real-time alert rule exclusions, caps, and pre-save volume preview (backend)

**Source:** ADR-0123 (Accepted 2026-08-28) · **Status:** Ready  
**Built:** not yet  
*(Relates to Story 10.9 — see epic-10 for the currently-shipped version this would revise)*

**Documentation Steward correction, 2026-09-15.** Source line cited ADR-0123 as "Proposed 2026-08-28" — `docs/adr/0123-real-time-alert-rules-and-delivery-refinements.md`'s own Status line and `docs/adr/README.md`'s master table both show it Accepted 2026-08-28 (same day it was drafted). Corrected directly; this story's own `**Status:** Ready` line was already correct and is unchanged.

**As a** Tenant-Admin,  
**I want** to configure noise exclusions, daily caps, and preview alert firing volume before saving rules,  
**so that** our team is not overwhelmed by alert fatigue.

**Acceptance Criteria**
- lert_rules schema supports excluded_watchlist_ids, excluded_topic_ids, and max_alerts_per_day.
- AlertEvaluationWorker respects exclusion lists and suppresses firing when daily cap is reached.
- POST /v1/alert-rules/preview returns estimated alert counts over the specified lookback window.

---

## Story 15.2 — Data export lookback bounding and representative sampling (backend)

**Source:** ADR-0124 (Accepted 2026-08-28) · **Status:** Ready  
**Built:** 2026-09-07 — social-listening-core@2f905ff  
*(Relates to Story 10.8 — see epic-10 for the currently-shipped version this would revise)*

**Documentation Steward correction, 2026-09-15.** Two fixes: (1) Same drift class as Story 15.1 above — Source line cited ADR-0124 as "Proposed 2026-08-28"; `docs/adr/0124-data-export-posts-csv-sampling-and-bounded-lookback.md`'s own Status line and `docs/adr/README.md`'s master table both show it Accepted 2026-08-28. (2) The `**Built:**` field read "social-listening-core (2026-09-07)" — missing the `<repo>@<short-hash>` this doc's own "Built convention" requires. `docs/implementation-log.md`'s matching entry ("2026-09-07 — Story 15.2...") names no hash either, but `git log` confirms the real shipping commit is `2f905ff` ("feat(epic-15): implement Story 15.2 data export lookback bounding and representative sampling"). Both corrected directly.

**As a** Tenant-User,  
**I want** to request representative sampled CSV exports and have date ranges safely validated,  
**so that** I can instantly inspect high-volume trends without triggering server timeouts.

**Acceptance Criteria**
- GET /v1/posts/export.csv rejects ranges exceeding 24 months with 400 EXPORT_RANGE_TOO_LARGE.
- sample=true query returns deterministic representative row sample and sets X-SocialEngage-Sampled: true.