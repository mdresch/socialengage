# ADR-0041: Platform Admin is a distinct identity *kind*, never a value within the tenant-user role enum — structural distinctness required at every layer, not only the database

**Status:** Accepted (2026-08-06) — drafted by the AI Business & Requirements Analyst persona, at Menno's direct request, following a real healing pass the same day; accepted by Menno as Sponsor later the same day. Story: none — a no-story ADR, category 1 (see "A note on this ADR's own place in the series' conventions" below).
**Acceptance note (2026-08-06):** Accepted by Menno, verbatim: *"ive reviewed and confirm that its the intented clarification to avoid confusions regarding admins and the type of admins we will support."* Accepted as drafted, no revisions. Per Decision's own §37 instruction, ADR-0030, ADR-0032, ADR-0035, and ADR-0036 each now receive a dated "Note on relation to ADR-0041" appended to their own files.
**Source:** Menno's own words, requesting this ADR directly after the healing pass below: *"this could have been an additional story and a Platform Admin and Tenant Admin confusion was made already prior to this now passed as healing session to rectify this gap. I would not mind a ADR that would name the Platform Admin and separate this from the Tenant Admin to avoid confusion."* Also named as a direct follow-up in the healing pass's own Implementation Log entry: *"an ADR to name Platform Admin as its own concept, explicitly separated from Tenant Admin at the design level, to prevent this exact class of confusion from recurring — tracked as a named next step, not built here"* (`docs/implementation-log.md`, "2026-08-06 — Healing pass — Story 6.2 AC2").

## Context

**The concrete failure this ADR responds to, already found and fixed:** `docs/implementation-log.md`'s 2026-08-06 entry (`social-listening-admin@1f8960e`) records that `getRoleShell()` in `social-listening-admin/src/lib/role-routing.ts` switched on a resolved identity's `.role` field, but `social-listening-core/src/identity/identityResolution.ts`'s real `ResolvedIdentity` type is a discriminated union:

```ts
export type ResolvedIdentity =
  | { type: 'tenant_user'; tenantId: string; userId: string; role: string }
  | { type: 'platform_admin'; adminId: string };
```

A real `platform_admin` identity carries **no `role` field at all**. The original UI code blind-cast the session's untyped identity value to `{role?: string|null}` and branched on it directly — a real Platform Admin's `role` was therefore always `undefined`, and the code silently routed them into the tenant-facing shell rather than the Platform-Admin-facing one. This was not a cosmetic bug: it is exactly the class of confusion Menno's own request names — a Platform Admin session rendering as, and being treated as, an ordinary tenant identity. The fix is already built, logged, and verified (`social-listening-admin/.claude/skills/role-routing-shell/SKILL.md`'s "Load-bearing constraints"; `social-listening-admin/src/lib/role-routing.ts`'s `isResolvedIdentity()`/`isShellAllowed()`), and is not redone here — this ADR is about closing the *design-level* gap that let it happen at all, per Menno's own request, not about re-fixing already-fixed code.

**What this project has already decided, and where it already gets this right:**

- **ADR-0030** (Accepted) decided Platform Admin needs a structurally separate database mechanism from Tenant-Admin — a dedicated `platform_admin_role` Postgres role, granted `BYPASSRLS`, locked to the `tenants` table and its own identity table only, "never granted, and must never be used to query... any... tenant-content table" (§2). Tenant-Admin, by contrast, "requires no database-level RLS exception at all" — an ordinary tenant-scoped session with an application-layer role check (§1). Two tiers, two different mechanisms, stated explicitly.
- **ADR-0032 §3** (Accepted) decided Platform Admin "is not a row in this table" (`users`) — modeled in its own, separate `platform_admins` table, with "no `tenant_id` column and no `tenant_isolation` policy," specifically because "forcing Platform Admin into a nullable-`tenant_id` row on `users` would force every RLS predicate... to special-case `tenant_id IS NULL` — for a role that conceptually does not belong to any tenant at all."
- **ADR-0035** (Accepted) decided the admin UI is one role-gated app, gating on the fact that "a Platform Admin session structurally has no `tenant_id` context at all (it is not a `users` row, per ADR-0032 §3), which naturally separates what it can render from what a Tenant-Admin/Tenant User session can."
- **ADR-0036 §4** (Accepted) decided UI role-gating is a layered UX convenience, never the real security boundary — the real boundary stays in `social-listening-core`'s own RLS and application-layer checks.
- **Story 5.9's shipped code** (`resolveIdentity()`, `social-listening-core/src/identity/identityResolution.ts`) already implements exactly this distinctness as a TypeScript discriminated union, not a flat role field — this predates the bug and was never itself wrong.

**The real gap, stated precisely:** every one of the four ADRs above already treats Platform Admin as structurally distinct — but each states that distinctness *locally*, scoped to its own layer (ADR-0030/0032: the Postgres schema and role grants; ADR-0035/0036: the route-tree split). **None of them states, as a general, project-wide, cross-layer principle, that this distinctness is a property of the identity's own *type* that every consumer — in either repository, at any layer — must preserve by construction.** Nothing previously written down told a future implementer (human or AI) that a resolved identity is a discriminated union that must be narrowed on its `type` discriminant before any type-specific field is read, or that assuming a bare `.role` field exists on "whatever the caller's identity turns out to be" is exactly the kind of assumption this project's own data model forbids. The bug is direct proof this gap was real, not hypothetical: it happened in the one repository (`social-listening-admin`) whose story (6.2) cites ADR-0035 and ADR-0036 §4 as its governing ADRs — neither of which said anything about how identity-consuming *code* must be shaped, only about which routes render for which identity.

**A second, live, present-tense piece of evidence for the same gap, found while drafting this ADR, not fixed here:** `social-listening-admin/src/lib/role-routing.ts` line 1 still defines and exports `export type AdminRole = 'tenant_admin' | 'tenant_user' | 'platform_admin';` — a flat, three-value role enum that models Platform Admin as if it were a value alongside the two tenant roles, the exact shape this ADR argues against. Checked directly (`grep -r AdminRole social-listening-admin`): this type is referenced nowhere else in the repository, in source or contracts — it is dead code, but it is dead code that re-encodes the healed bug's own wrong mental model, sitting in the very file whose `SKILL.md` now warns against that model. It is named here as a concrete illustration that the gap this ADR closes is not abstract, and as a named follow-up for whoever next touches this file (the AI Delivery Agent or Menno) to remove — **not fixed by this ADR**, which is documentation-only per its own drafting persona's scope boundary.

### Why a new ADR, not an amendment to ADR-0030 or ADR-0032 — a real judgment call, argued explicitly

`docs/adr/README.md`'s "Conventions for changing an existing ADR" table was checked against directly, not assumed. Two rows are candidates:

- **Row 4 ("Clarification")** — "Implementation surfaces a constraint that was already logically required by the decision, just not stated." This does **not** fit cleanly: ADR-0030's decision is a Postgres role/grant design; ADR-0032's decision is a table schema. Neither one's own Decision text logically required anything about how `social-listening-admin` — a different repository, not governed by either ADR — shapes its own TypeScript identity-handling code. A Clarification is for making an already-*implicit* requirement of the same decision explicit; the requirement this ADR states (exhaustive, type-narrowed handling of a discriminated union, in any consuming code, in either repo) is broader than what ADR-0030 or ADR-0032's own text was ever about.
- **Row 6 ("A new ADR formalizes... something a prior Accepted ADR's Decision text already assumed but never stated as project-wide policy")** — this is the fit. The precedent is exact: ADR-0027 formalized, as a new, durable, project-wide principle, something ADR-0024's and ADR-0026's own Decision text already practiced locally but never stated generally (`docs/adr/README.md` footnote 9); ADR-0028's first tier did the same for ADR-0027 itself. This ADR does the same thing for ADR-0030/0032/0035/0036: the *practice* (Platform Admin is structurally separate) already exists in four places, locally stated; this ADR is the first to state it as a general, cross-layer rule with a durable consequence for how future code must be written, in either repository — new content, not previously written anywhere, even though the underlying facts it rests on are all already Accepted decisions.

Per that row's own prescribed response, this ADR does **not** edit ADR-0030's, ADR-0032's, ADR-0035's, or ADR-0036's own Decision or Consequences text. Each instead receives a dated "Note on relation to ADR-0041" (below, appended to each of those four ADRs' own files at this ADR's acceptance, not before) naming exactly what it already satisfies and confirming no change follows.

## Decision

### 1. Platform Admin is formally, explicitly decided to be a distinct identity *kind* — never a value within the tenant-user role enum

Restated here as explicit, durable, project-wide policy — not a new fact, but the first time this project states it as a rule rather than only practicing it in four separate, locally-scoped ADRs: **Platform Admin and Tenant-Admin are two different *kinds* of identity, not two values of the same `role` field.** `role` (`'tenant_admin' | 'tenant_user'`, per ADR-0032 §1) is a property that exists only on a `tenant_user`-kind identity; it is meaningless to ask "what is a Platform Admin's role" the same way it is meaningless to ask what a `tenants` row's `role` is — the question itself is a category error, and any code, schema, or document that treats it as answerable (e.g., by defaulting a missing `role` to `undefined` and falling through, as the healed bug did) is wrong by construction, not merely incomplete.

This governs, and is already satisfied by, exactly the four ADRs named in Context above (ADR-0030's separate Postgres role/grant; ADR-0032 §3's separate table; ADR-0035's route-tree split; ADR-0036 §4's UX-layer gating) — this Decision does not change any of their mechanics. What it adds is the general rule those four each independently, locally assumed.

### 2. Every layer, in either repository, must preserve this distinctness — a durable rule for future code, not only a restatement of past design

Any code — in `social-listening-core` or `social-listening-admin`, present or future — that consumes a resolved identity (`ResolvedIdentity` as shipped, or any future equivalent) must:

- **Narrow on the identity's own discriminant (`type`) before reading any type-specific field.** A `tenant_user`-only field (`role`, `tenantId`, `userId`) must never be read, defaulted, or optionally-chained against a value whose `type` has not first been checked to be `'tenant_user'`.
- **Handle both variants exhaustively, with no silent, privilege-assuming fallthrough.** Code that branches on identity kind must have an explicit case for `platform_admin` and an explicit case for `tenant_user` — never a default/else branch that treats an unrecognized or unnarrowed identity as the lower-privilege tenant case by omission, which is precisely the shape of the healed bug (a missing `role` silently evaluated as "not `tenant_admin`," never as "not a tenant identity at all"). Where the implementation language supports it (TypeScript, both repositories today), an exhaustiveness check (e.g., a `never`-typed default branch) is the concrete mechanism recommended, though not mandated by this Decision — see Open Questions.
- **Never re-flatten a discriminated identity into a single-shape type for convenience** (e.g., a type like `{ role: 'tenant_admin' | 'tenant_user' | 'platform_admin' }`) anywhere in the codebase, including in code that is not currently exercised. `social-listening-admin/src/lib/role-routing.ts`'s own unused `AdminRole` type (Context, above) is named here as a concrete, present-tense violation of this rule, flagged as a follow-up for removal — not itself an approval to leave it in place.

### 3. Naming stays as this project already, informally, gets right — checked, not assumed

`Stakeholder-Register.md`, every ADR in this series, and the codebase itself already consistently use "Platform Admin" and "Tenant-Admin" as two distinctly-named concepts in prose — checked directly against ADR-0030 through ADR-0036 and `Stakeholder-Register.md`'s own persona entries; no instance of collapsing both to a bare "Admin" in a context where the distinction matters was found. This Decision does not change any naming — it is named here only to confirm explicitly that the gap this ADR closes was never a naming/prose problem, only a structural-representation-in-code problem. No action follows from this section.

## Consequences

**Positive**
- States, for the first time as a general rule rather than four separately-scoped local facts, the exact principle whose absence let the healed bug happen — closes the gap at the design level Menno asked for, not just at the one call site the healing pass already fixed.
- Gives whoever next writes identity-consuming code in either repository (the AI Delivery Agent, Menno, or a future contributor) a citable, explicit rule to check new code against, rather than requiring each author to re-derive "Platform Admin isn't a role value" independently from four separately-scoped ADRs each time.
- Costs nothing to accept — restates and generalizes decisions already Accepted (ADR-0030, ADR-0032, ADR-0035, ADR-0036) and a shipped type (`ResolvedIdentity`), rather than introducing new mechanism, schema, or endpoint.

**Negative**
- **Names, but does not itself close, a real residual risk:** this Decision is a documentation-level rule, not an enforced one. Nothing currently in either repository's CI, lint configuration, or `enforce-contract-first.cjs` hook would catch a future violation mechanically — the healed bug itself passed whatever review existed until the Ideal Manager's own Decision Evaluator caught it (`docs/implementation-log.md`'s own healing-pass entry). Until a mechanical check exists (Open Questions, below), this ADR's Decision §2 depends on the same human/AI-review discipline that already once let the violation through once.
- `social-listening-admin/src/lib/role-routing.ts`'s own unused `AdminRole` type (Context, above) is a live, present-tense counter-example to this Decision that this ADR names but does not fix — a real, if minor, gap between this ADR's Decision and the current state of the codebase at the moment of acceptance.
- Adds a fourth and fifth "Note on relation" annotation to two already-Accepted ADRs each carrying several prior amendments (ADR-0030, ADR-0032) plus two more (ADR-0035, ADR-0036) — a small, cumulative documentation-maintenance cost this project's own governance convention already accepts as the price of not editing Accepted Decision text in place.

## Alternatives Considered

- **Fold this directly into ADR-0030's or ADR-0032's own Decision text** — rejected per `docs/adr/README.md`'s own governance table: both are Accepted, and neither's Decision text is a historical record to be edited in place. A new ADR plus "Note on relation" annotations is the established mechanism (ADR-0027 → ADR-0024/0026's own precedent), not a novel choice.
- **Treat this as a Clarification on ADR-0030 or ADR-0032** — rejected; see "Why a new ADR" above. Neither ADR's own Decision text logically required anything about `social-listening-admin`'s own code shape; a Clarification is for an already-implicit requirement of the *same* decision, not a new cross-repository rule.
- **Do nothing further — treat the healing pass's fix and its `SKILL.md` update as sufficient** — rejected, this is Menno's own explicit request: a component-level `SKILL.md` warns only whoever next touches that one component; it does not state a project-wide rule a different future component (e.g., Story 6.6's Platform Admin console, or any future `social-listening-core` endpoint) would be checked against before a similar mistake is made a second time in a different file.
- **Mandate a mechanical lint rule or type-level exhaustiveness check now, as part of this Decision** — considered, not mandated (Decision §2 recommends but does not require it); implementing tooling is out of this drafting persona's own scope (documentation, not code), and the exact mechanism (a custom ESLint rule, a `no-explicit-any`-adjacent check, or reliance on TypeScript's own exhaustiveness inference via `never`) was not evaluated here. Named as a recommended follow-up, below.

## Open Questions

- **Whether a mechanical enforcement mechanism (lint rule, type-level exhaustiveness check, or a contract-test convention) should be built to catch a future violation of Decision §2 automatically, rather than relying on review alone** — not designed or mandated here; a concrete, real candidate follow-up story for the AI Delivery Agent or Menno, not built by this ADR.
- **Whether `social-listening-admin/src/lib/role-routing.ts`'s own unused `AdminRole` type (Context, above) should simply be removed** — named as a live counter-example this ADR does not fix; a small, concrete code-cleanup follow-up, not a design question, left to whoever next touches that file.
- **Whether this same rule should be stated at the code level itself** (e.g., a doc comment on `ResolvedIdentity`'s own definition in `social-listening-core/src/identity/identityResolution.ts` pointing back to this ADR, mirroring what `role-routing.ts`'s own comment already does informally) — a reasonable, low-cost addition; not made here since it is a code/comment change, out of this drafting persona's scope boundary (documentation, not implementation).

## A note on this ADR's own place in the series' conventions

**This is a no-story ADR, category 1 per `docs/user-stories/README.md`'s own "No-story ADR convention"** ("already satisfied by shipped code... no further implementation work follows from accepting it") — the same category ADR-0027 used. The structural distinctness this Decision states is already fully practiced in shipped code: `ResolvedIdentity`'s discriminated union (`social-listening-core`, Story 5.9), and `isResolvedIdentity()`/`isShellAllowed()`'s type-narrowed handling of it (`social-listening-admin`, the 2026-08-06 healing pass under Story 6.2). Accepting this ADR requires no new interface, stored field, or endpoint — only the two named, small follow-ups above (removing `AdminRole`; deciding whether to add mechanical enforcement), neither of which is this ADR's own build work. Following ADR-0027's own precedent, no story is proposed for this ADR itself; a future mechanical-enforcement follow-up, if pursued, would be its own separately-scoped, separately-storied piece of work.

## Amendment Log

- 2026-08-06 — Initial proposal, drafted by the AI Business & Requirements Analyst persona at Menno's direct request, following the same-day healing pass (`social-listening-admin@1f8960e`) that fixed the concrete bug this ADR is named to prevent from recurring. Grounded in a direct read of `social-listening-core/src/identity/identityResolution.ts`'s real `ResolvedIdentity` type, `docs/implementation-log.md`'s own healing-pass entry, `social-listening-admin/.claude/skills/role-routing-shell/SKILL.md`, and ADR-0030/0031/0032/0035/0036's own already-Accepted text — including a direct repository check (`grep -r AdminRole social-listening-admin`) confirming `role-routing.ts`'s own unused, flat-role-shaped `AdminRole` type is a live, present-tense counter-example, not a hypothetical one. Left **Proposed**, not accepted, per this persona's own charter boundary — see Status line.
- 2026-08-06 — **Accepted by Menno**, verbatim: *"ive reviewed and confirm that its the intented clarification to avoid confusions regarding admins and the type of admins we will support."* As drafted, no revisions. No story moves to Ready — this remains a no-story, category-1 ADR. ADR-0030, ADR-0032, ADR-0035, and ADR-0036 each receive a dated "Note on relation to ADR-0041," per Decision §37.
