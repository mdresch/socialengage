# Architecture Decision Records — Social Listening / Insights Subsystem

ADRs derived from [`2026-07-28-social-listening-ingestion-design.md`](../project%20docs/2026-07-28-social-listening-ingestion-design.md) (Status: Approved for implementation). Each record captures one architecturally significant decision from that spec, with the context and alternatives that motivated it.

Each ADR feeds a [user story](../user-stories/README.md); each story is built per [`docs/implementation-methodology.md`](../implementation-methodology.md) (contract-first, component `SKILL.md`s, permanent regression suite) — so an ADR's decision stays enforced in code, not just recorded in prose.

## Conventions for changing an existing ADR

Four situations, four different responses — don't default to editing the original Decision/Consequences text in place:

| Situation | Response |
|---|---|
| The underlying decision itself changes | New ADR, or a superseding ADR that says so explicitly |
| The decision stands, but *why* it was made needs more explanation | Add a "Note on provenance" or similar, without altering the original text (e.g. ADR-0002, ADR-0016) |
| An adjustable parameter changes (a window length, a threshold) | Log it in that ADR's "Amendment Log" (e.g. ADR-0017–0019) — doesn't need superseding |
| Implementation surfaces a constraint that was already logically required by the decision, just not stated | Add a dated "Clarification" section (e.g. ADR-0013) — not a new decision, just making an implicit requirement explicit |
| A still-Proposed ADR would change part of this (Accepted) ADR's decision, if accepted | Add a dated "Pending supersession note" pointing to it, naming exactly which part would change (e.g. ADR-0009/0010 → ADR-0023) — not an edit to the original text, and not a full supersession unless the whole decision is affected |
| A "Pending supersession note" above becomes real because the other ADR gets accepted | Add a dated "Supersession update" note confirming it, without editing the original Pending supersession note or Decision text (e.g. ADR-0009/0010, once ADR-0023 was accepted) — and don't assume already-shipped code changes automatically; it only changes when the superseding ADR's own story is actually built |
| A new ADR formalizes, as an explicit and durable principle, something a prior Accepted ADR's Decision text already assumed but never stated as project-wide policy — without changing that prior ADR's own Decision at all | Add a dated "Note on relation to ADR-XXXX" section on the prior ADR, naming what it already satisfies and why no scope change follows — not a supersession (nothing changes), not a provenance note (this isn't explaining *why* the prior decision was made), not a parameter change, not a clarification of an implicit requirement (e.g. ADR-0024/0026 → ADR-0027) |

The common thread: the original Decision and Consequences text is a historical record and stays put. Everything learned later is appended, dated, and labeled by which of the five categories it is.

| # | Title | Spec Section |
|---|-------|--------------|
| [0001](0001-two-repository-split.md) | Split into `social-listening-core` and `social-listening-admin` repositories | §2 |
| [0002](0002-unified-provider-connector-pattern.md)² | Unified `ProviderConnector` contract for social platforms and AI providers | §3.1 |
| [0003](0003-per-tenant-per-provider-rate-limiting.md) | Rate limiting enforced per `(tenantId, providerId)` via a shared `RequestGate` | §3.2 |
| [0004](0004-author-normalized-separately-from-post.md) | Normalize `Author` once per platform account, not embedded per post | §4.1 |
| [0005](0005-ingestion-run-as-audit-anchor.md) | `IngestionRun` as the immutable acquisition/audit anchor for every post | §4.3 |
| [0006](0006-watchlist-matching-connector-side-with-fallback.md) | Prefer connector-side native filtering for watchlist matching, with post-fetch fallback | §4.4 |
| [0007](0007-author-topic-signal-minimal-v1.md) | `AuthorTopicSignal` ships with raw signals only, no computed expertise score | §4.5 |
| [0008](0008-defer-topic-time-series-and-charting.md) | Defer `TopicDailyCount` aggregation and all charting to a future subsystem | §4.6, §9 |
| [0009](0009-connector-health-derived-not-stored.md) | `ConnectorHealth` is fully derived from `IngestionRun` history, not stored mutable state | §5 |
| [0010](0010-error-handling-and-auto-disable-policy.md) | Retryable-vs-non-retryable error policy with per-tenant auto-disable | §5 |
| [0011](0011-cursor-based-pagination-for-posts-api.md) | Cursor-based pagination for `GET /posts` | §6 |
| [0012](0012-thin-events-with-rest-fetch-on-demand.md) | Service Bus events carry IDs and minimal fields only; full data fetched via REST on demand | §7 |
| [0013](0013-per-tenant-event-filtering-via-subscription-rules.md) | Per-tenant event filtering via Service Bus subscription SQL filters | §7 |
| [0014](0014-credential-storage-envelope-encryption-oauth-first.md) | Envelope-encrypted credential storage via Azure Key Vault, OAuth preferred with API-key fallback | §8 |
| [0015](0015-tenant-isolation-via-postgres-row-level-security.md) | Enforce tenant isolation at the database layer with Postgres Row-Level Security | §8 |
| [0016](0016-postgres-as-database-engine.md)¹ | Postgres as the database engine | §2, §4.2, §8 |
| [0017](0017-api-versioning-and-compatibility-policy.md)³ | API versioning and compatibility policy | — (originated, see below) |
| [0018](0018-data-retention-and-archival-policy.md)⁴ | Data retention and archival policy | ADR-0005 Negative consequences |
| [0019](0019-event-schema-versioning-policy.md)³ | Event schema versioning policy | — (originated, see below) |
| [0020](0020-rate-limit-queue-bounds-and-distributed-gate-state.md)⁵ | Rate-limit queue bounds, dead-letter handling, and distributed gate state | ADR-0003 Negative consequences; third-party review |
| [0021](0021-watchlist-boolean-query-ast-and-capability-matrix.md)⁵ | Unified boolean-query AST for watchlist matching, with per-connector capability matrix | ADR-0006 Negative consequences; third-party review |
| [0022](0022-derived-data-caching-and-refresh-strategy.md)⁵ | Derived-data caching and refresh strategy (`ConnectorHealth` cache, `AuthorTopicSignal` refresh cadence) | ADR-0007, ADR-0009 Negative consequences; third-party review |
| [0023](0023-proportional-connector-failure-threshold.md)⁵ | Proportional (rate-relative) connector failure threshold for auto-disable | Spec §5/§10 placeholder; third-party review |
| [0024](0024-newswire-connector-direct-wire-rss-issuer-as-author.md)⁶ | Newswire connector — direct wire-service RSS feeds (GlobeNewswire, PR Newswire), with issuer-as-Author modeling | §10 (platform roster, order not yet decided) |
| [0025](0025-persistent-local-dev-database-separate-from-test-database.md)⁷ | Persistent local dev Postgres database, kept separate from Jest's ephemeral test database | — (originated, see below) |
| [0026](0026-rss-news-connector-gnews-api-publication-as-author.md)⁸ | RSS/News connector — GNews API (general news), with publication-as-Author modeling | §10 (RSS/News chosen as a category 2026-07-29; no concrete provider named until now) |
| [0027](0027-connector-is-technical-intermediary-not-contracting-party.md)⁹ | Connector architecture is a technical intermediary only — SocialEngage is never a party to, reseller of, or intermediary in the connecting party's relationship with a data source | — (originated, see below) |
| [0028](0028-credential-creation-authority-scoped-by-ownership-tier.md)¹⁰ | Credential creation authority is scoped by ownership tier — no system-wide credentials, tenant-wide credentials created only by Tenant-Admin, user-bound credentials self-activated by the user | — (originated, see below) |
| [0029](0029-authentication-mechanism-entra-external-id.md)¹¹ | Authentication mechanism — Microsoft Entra External ID, integrated as a thin, pluggable OIDC token issuer (single external tenant; Postgres owns tenant/role/license data) | — (originated, see below) |
| [0030](0030-admin-tier-design-platform-admin-rls-exception.md)¹¹ | Admin-tier design — Platform Admin via a narrowly-scoped, audited `BYPASSRLS` role; Tenant-Admin via ordinary tenant-scoped RLS + an application-layer role check | — (originated, see below) |
| [0031](0031-tenants-table-shape.md)¹¹ | `tenants` table shape — RLS scoped by `id`; app-layer seat/license enforcement; Platform Admin's bypass locked to this table only | — (originated, see below) |
| [0032](0032-users-table-shape-and-rls.md)¹¹ | `users` table shape + RLS — tenant-scoped like every other table; Platform Admin modeled as a separate, non-tenant table; request-time identity resolution via a fourth, narrower bypass role | — (originated, see below) |
| [0033](0033-retire-x-tenant-id-header-placeholder.md)¹¹ | Retire `X-Tenant-Id` as the tenant-identity trust mechanism, replaced by `Authorization: Bearer` + server-side resolution | — (originated, see below) |
| [0034](0034-connector-connect-disconnect-crud-ownership-tier-aware.md)¹¹ | Connector connect/disconnect CRUD reworked to be ownership-tier-aware (ADR-0028), closing a real gap in Story 1.6's already-shipped, placeholder-auth code | — (originated, see below) |
| [0035](0035-admin-ui-shape-one-app-role-gated.md)¹¹ | Admin UI shape — one role-gated Next.js app, not two separate deployables; a second "Platform Console" deployable named and deliberately deferred | — (originated, see below) |
| [0036](0036-admin-ui-authentication-session-and-role-gating-mechanism.md)¹² | Admin UI's own authentication/session mechanism — server-side (BFF) session, no bearer token in browser JS, no third-party auth framework adopted at v1, role-gating layered and UX-only | — (originated, see below) |
| [0037](0037-self-service-tenant-signup-and-first-tenant-admin-provisioning.md)¹³ | Self-service tenant sign-up — a new, narrower `tenant_signup_role` (never a `platform_admin_role` widening); domain-match sign-ups rejected toward the existing invite flow, not auto-joined; a static public-email-provider denylist; a named, narrow exception to ADR-0029 §4 for one endpoint only | — (originated, see below) |
| [0038](0038-ai-enrichment-provider-selection-azure-ai-language-first-llm-structured-extraction-named-second-candidate.md)¹⁴ | AI enrichment provider selection — Azure AI Language as the first concrete `AIProviderConnector`, an Azure-hosted general-purpose LLM (small/cheap tier) named as the deliberate second-provider swappability candidate; explicit reconfirmation that SocialEngage is never a billing intermediary for either | — (originated, see below) |
| [0039](0039-tenant-offboarding-data-lifecycle-export-and-deletion.md)¹⁴ | Tenant offboarding data lifecycle — export before deletion; asynchronous, partition-aware deletion across primary, archival, and Key Vault storage; a scoped, named exception to ADR-0018's own `IngestionRun` archival-not-deletion rule. Decision §1 ("Platform-Admin-initiated only") superseded in full 2026-08-07 — see ADR-0043 | — (originated, see below) |
| [0040](0040-self-service-signup-rate-limiting-and-abuse-prevention-mechanism.md)¹⁴ | Self-service sign-up rate limiting — a new, dedicated per-IP and per-verified-email-domain counter, independent of `RequestGate`, closing ADR-0037 §7's own named precondition | — (originated, see below) |
| [0041](0041-platform-admin-is-a-distinct-identity-kind-not-a-role-value.md)¹⁵ | Platform Admin formalized as a distinct identity *kind*, never a value within the tenant-user role enum — structural distinctness (already practiced in ADR-0030/0032's schema and ADR-0035/0036's routing) restated as an explicit, durable, cross-layer, project-wide rule | — (originated, see below) |
| [0042](0042-wikipedia-connector-mediawiki-api-article-as-author.md)¹⁶ | Wikipedia connector — MediaWiki Action API, `authMode: 'none'`, revision re-poll cadence via `recentchanges`, article-as-Author modeling (a structurally narrower third instance of ADR-0024/0026's issuer-as-Author departure); attribution satisfied via `SocialPost.url` (revision permalink), not `Author` | — (originated, see below) |
| [0043](0043-self-service-tenant-initiated-deletion.md)¹⁷ | Self-service, Tenant-Admin-initiated tenant deletion — the sole tenant-deletion mechanism, full supersession of ADR-0039 Decision §1 (revised 2026-08-07, before acceptance); request halts ingestion, an export window (CSV/JSON, re-triggerable) runs alongside a 30-day cancellable grace period, and confirmation executes the same asynchronous, bounded, cross-tier hard-delete; `platform_admin_role` has zero access, no exception, matching Story 5.7's own boundary; Platform Admin sees only the audit trail, no approval step | — (originated, see below) |
| [0044](0044-watchlist-api-design-and-database-schema-standardization.md)¹⁸ | Watchlist CRUD contract — JSON Merge Patch (RFC 7396) as the project-wide PATCH semantics; explicit 404-vs-403 split (cross-tenant / not-found → 404; Tenant-Admin role-check failure → 403, per ADR-0030 §1); optimistic-locking version column on /v1/watchlists (and per-resource opt-in elsewhere); before update trigger-maintained updated_at for mutable tenant-scoped tables, with an explicit exempt list (IngestionRun per ADR-0005, platform_admin_audit_log, anything else append-only); watchlists row shape explicitly reconciled with ADR-0021's boolean-query AST (disjoint columns for non-boolean vs boolean matchers); §5c — watchlists are personal, per-user resources (`user_id` column), creatable by both `tenant_admin` and `tenant_user`, fully private to their creator with **no** Tenant-Admin oversight override, RLS-enforced via a new `app.user_id` session predicate alongside `app.tenant_id` (so another user's watchlist, same tenant or not, is 404, never 403); no re-decision of ADR-0015/0017/0030/0033 or silent conflict with ADR-0021 | — (originated, see below) |
| [0047](0047-standard-pattern-for-cross-story-references-and-supersession-language.md)¹⁹ | Standard pattern for cross-story references and supersession language — codifies the working pattern for superseded-AC markers (in place with explicit obsolete marker and pointer), exact cross-dependency references (identifier + superseder by number + acceptance date + implementation-status note when distinct), known-conflict resolution to one of four named-owner forms (resolved ADR/story / deferred to named story / deferred to named ADR / intentionally retained obsolete), and "the last dated appendix is the live contract"; explicitly defers to `docs/adr/README.md`'s seven-row governance tabce table as the authority for which category applies, not as a parallel definition | — (originated, see below) |
| [0048](0048-no-core-pipeline-change-verification-for-new-connector-registration.md)²⁰ | Explicit policy for no-core-pipeline-change verification when registering new connectors — mandatory evidence, CI guardrail, and registration traceability for social and AI connector additions | — (originated, see below) |
| [0049](0049-point-in-time-author-follower-count-on-social-post.md)²⁰ | Retain point-in-time author follower count on `SocialPost` as a scoped exception to normalized `Author` storage | ADR-0004 named trade-off; flagged in README "Still outstanding" section |
| [0050](0050-tenant-owned-domain-rss-content-feed-connector.md)²⁰ | Tenant-owned-domain RSS/content-feed connector with DNS TXT domain ownership verification | `docs/adr/README.md` "Still outstanding" connector-candidate bullet (added 2026-08-04) |
| [0051](0051-connector-activation-decoupled-from-credential.md)²¹ | Connector activation decoupled from credential storage — two new, ownership-scoped tables (`connector_activations` tenant-wide, `connector_user_activations` per-user), mirroring ADR-0028/ADR-0034's own credential-ownership split, orthogonal to `platform_credentials`; resolves ADR-0034's own left-open "does activation need its own table" question | ADR-0034 own Open Question; a real UX bug (Newswire always-on) and a real implementation gap (`connectorHealth.ts` ignoring `retryable`) found while building Story 6.5's connector status screen |
| [0052](0052-live-ingestion-polling-scheduler.md)²² | Live ingestion-polling scheduler — an in-process interval loop in `social-listening-core` (not `pg_cron`, argued explicitly against ADR-0022's precedent generalizing), a required connector-registry bootstrap at server startup (found missing, not assumed), a new generic, ADR-0048-compliant `SocialConnector.poll()` method (no hardcoded per-`providerId` switch), and cadence derived from `ingestion_runs` history rather than any new stored field | ADR-0043 §3 and ADR-0051 Open Question 1, both of which name this exact gap and explicitly decline to design it; a real, live gap found this session (a DNS-verified tenant-owned-feed connector and an active GNews connector both ingesting zero posts) |
| [0053](0053-canonical-markdown-post-body-normalization-at-ingestion.md)²³ | A canonical Markdown post-body representation — `social_posts` gains `body_markdown`/`body_markdown_version`, computed once at ingestion by a shared, version-pinned `sanitize-html`→`turndown`(+`turndown-plugin-gfm`) pipeline, decoupling N ingestion sources from M future consumers; Newswire's and tenant-owned-feed's own parsers widened to actually capture body content (previously title-only); GNews's `content` field added to enrichment input | Found live this session: Newswire/tenant-owned-feed connectors enrich on title text alone because their own parsers never captured body content, and no `social_posts` column could hold it even if they did |
| [0054](0054-tenant-facing-analytics-dashboard-scope-and-data-source-strategy.md)²⁴ | Tenant-facing Analytics Dashboard — v1 scope (Overview/Sentiment/Conversations/Sources tabs; Location deferred entirely), 100% client-side aggregation from existing `GET /v1/posts`/`enrichment` (no new backend aggregation), and a narrow, scoped supersession of ADR-0008's "any charting UI" clause (its "no `TopicDailyCount` table/endpoint" clause stays fully reaffirmed) | `docs/design/frontend-design-specification.md` §10, citing ADR-0008, previously deferred this entirely; requested into formal scope directly by Menno, 2026-08-17 |
| [0055](0055-analytics-language-and-location-enrichment-feasibility.md)²⁵ | Language and location enrichment feasibility for the Analytics Dashboard — Language: buildable now, zero `social-listening-core` change (both real `AIProviderConnector`s already compute and persist `detectedLanguage`, never surfaced by `social-listening-admin`); Location: reaffirms ADR-0054 Decision §4's non-buildability, with a new GNews-`source.country` finding named and explicitly declined as a substitute | ADR-0054 Decision §4/Open Question 1 (Location); a gap ADR-0054 itself never researched (Language); requested directly by Menno, 2026-08-17, same day as ADR-0054 |
| [0056](0056-ai-inferred-origin-location-newswire-dateline-extraction.md)²⁶ | AI-provider-inferred origin location from a Newswire post's own dateline text — a new, optional `AIProviderConnector.inferOriginLocation?()` method (Azure OpenAI only, Azure AI Language structurally cannot), invoked conditionally only from the Newswire ingest loop, gated by a structural dateline pre-check rather than a hardcoded wire allowlist; confirms zero migration/`SocialPostSummary` change required (unlike `post_geo_location`); **recommends naming, not building** — no committed Location-tab consumer exists yet | ADR-0055's own 2026-08-17 Amendment Log ("tracked as a new candidate ADR, not yet drafted"); ADR-0054 Decision §4/Open Question 1; requested directly by Menno, 2026-08-17, later the same day as ADR-0055 |
| [0057](0057-tenant-owned-feed-multi-feed-administration.md)²⁷ | Multi-feed administration for the tenant-owned-feed connector — three new `tenant_admin`-only REST endpoints (list, edit `feedUrl` only, soft-remove via a new `removed` status), correcting a found pre-existing role-gating gap on `connect`/`verify-domain`, and a real multi-feed list UI replacing today's single-activation screen | ADR-0050's own Open Question 2 ("multiple-domain and multiple-feed support... needs explicit design at Story time"), left open since that ADR's 2026-08-11 acceptance; requested directly by Menno, 2026-08-17 |
| [0058](0058-wire-ingestion-events-into-real-connector-pipeline.md)²⁸ | Wire `SocialPostIngestedEvent`/`ConnectorHealthChangedEvent` publishing into the real ingestion pipeline — per-connector watchlist-match event publishing (new tenant-wide `listActiveWatchlistsForTenant()`), health-change publishing centralized inside `runIngestionAttempt()`, both best-effort and never blocking real ingestion | `ingestion-events/SKILL.md`'s own "Known gaps" section, naming `publishEvent()` as never wired into real ingestion; found live while scoping Story 2.13, flagged directly by Menno |
| [0059](0059-facebook-connector-tenant-owned-page-scope-organization-as-author.md)²⁹ | Facebook connector — narrowed to a tenant's own connected Page (posts only, v1), `authMode: 'oauth'`/Tier 3 credential (revised from an initially-assumed Tier 2 during review — see footnote 29), organization-as-Author (a fourth instance of ADR-0004's generalized clause); genuine public-content social listening verified as not viable on Meta's current API for a project this size (no keyword-search/discovery surface exists; Page Public Content Access requires App Review + Business Verification + possible additional contracts); comment/mention ingestion explicitly deferred over an unresolved third-party author-rights question | `docs/open-decisions.md`'s 2026-08-06 Meta bullet, left as unequally-verified groundwork ("Standard Access... confirmed easy"; genuine listening "not yet confirmed either way") |
| [0060](0060-facebook-connector-multiple-pages-per-user.md)³⁰ | Facebook connector — one user may connect more than one Page: a new per-user child table (`facebook_connected_pages`) referencing `platform_credentials` by id rather than a discriminator column or rework of `getLatestCredentialId()`/`deleteCredential()`; `pollFacebook()` fans out one `IngestionRun` per connected Page; `deriveConnectorHealth()` gains an optional, backward-compatible `pageId` parameter so per-Page health is real, not blended; two new REST endpoints authorized on the caller's own `userId` (not `tenant_admin`, unlike ADR-0057's precedent, argued explicitly as the one place that precedent doesn't transfer); ADR-0059 Decision §4's Tier-3-only credential-tier finding is explicitly unaffected — see its own "Note on relation to ADR-0059" | `docs/user-stories/epic-6-tenant-admin-ui.md` Story 6.27, a deliberately light stub sourced from Menno's own live-test finding (many real Facebook Pages he administers, a 5-seat license ceiling making one-Page-per-team-member impractical) |
| [0061](0061-tier-3-poll-scheduler-per-user-enumeration.md)³¹ | Tier-3 (user-bound) poll scheduling — a new nullable `user_id` column on `ingestion_runs`; new `listActiveUserActivations()`/`getMostRecentRunStatusForUser()` store functions; `deriveConnectorHealth()` gains a fourth `userId?` parameter, fixing a confirmed cross-user blending bug in already-shipped `shouldAttemptIngestion()` (Story 1.11) as a direct consequence; a new, distinct `SocialConnector.pollUser?(tenantId, userId)` method (not an overload of `poll?`), retiring Facebook's throwing-placeholder `poll`; a third, per-user enumeration level in `runSchedulerTick()`, composing with — never duplicating — ADR-0060's own per-Page fan-out; `jitterFraction()` extended with an optional `userId` segment | ADR-0052 Decision §6/Open Question 2, which named the future Tier-3 shape without designing it, explicitly pending a real Tier-3 poll connector existing; ADR-0060 gave Facebook one, but confirmed via repo-wide grep that `pollFacebook()` has zero real call sites in the running server; Menno confirmed (structured `AskUserQuestion` choice) this gets its own ADR/story, kept separate from ADR-0060/Story 6.27 |
| [0062](0062-analytics-dashboard-overview-tab-enhancement.md)³² | Analytics Dashboard — Overview Tab Enhancement: 3-column grid, 7-dimension filter model, active filter chips, deep-link share, statistical volume forecast, AI Spike Storyteller, and widget scope | Frontend Specification (§4–§10) provided by Menno, 2026-08-19; partially supersedes ADR-0054 Decision §2/§3 |
| [0063](0063-post-watchlist-matches-junction-table-and-server-side-watchlist-filter.md)³³ | Post-watchlist match persistence — `post_watchlist_matches` junction table, ingestion write (reusing ADR-0058's existing match-call path), and `GET /v1/posts?watchlistId` server-side filter; enables `activeWatchlistFilter` server-side upgrade and Watchlist Coverage widget | ADR-0062 Open Question 1; Menno's direct request 2026-08-19 to align the Topics filter with the existing Watchlist system |
| [0064](0064-location-and-geospatial-insights-from-posts-and-authors.md)³⁴ | Location and geospatial insights from posts and authors — country-level ISO 3166-1 alpha-2 as the v1 spatial unit, zero schema migration (`social_posts.enrichment` JSONB), `computeCountryBreakdown()` pure aggregation with < 3 post small-sample sentiment suppression, explicit unmapped volume bucket, and SVG Choropleth Map | Follow-up to ADR-0054/0055/0056/0062; resolves the Location Insights widget gap on the Overview tab |
| [0065](0065-active-watchlist-sourcing-via-brave-search-api.md) | Active watchlist sourcing via Brave Search API — polling connector iterating tenant active watchlists, dual discovery query + in-process AST validation filter, Domain/Publication as Author (ADR-0004 generalization), quota-safe 1–4h cadence with 1.2s pacing loop, post grounding and LLM enrichment | Menno's direct request 2026-08-19 to transform watchlists into active search discovery agents; follow-up to ADR-0062/0063 |
| [0066](0066-active-watchlist-sourcing-via-bing-search-api.md) | Active watchlist sourcing via Bing Search API (Azure) — enterprise-aligned polling connector iterating tenant active watchlists, dual discovery + AST validation filter with 25–50 candidate cap, deterministic auto endpoint fallback (`news` $\rightarrow$ `web`), multi-step URL canonicalisation, tenant-scoped cost telemetry, and lookback freshness mapping | Follow-up to ADR-0065; provides Azure-aligned active discovery companion with direct Azure EA billing |
| [0067](0067-reconfirm-facebook-connector.md) | Facebook connector reconfirmation — Managed Pages vs. Personal Profiles, hosting Page post dependency, two-tier author resolution (`from.name` falling back to `pageName`), Tier-3 OAuth credential harmonization, and ADR-0064 geospatial country normalization (amended 2026-08-22 for Story 6.37 — admin post-feed Page and matched watchlist attribution UI) | Follow-up to ADR-0059/0060/0061/0064 |
| [0068](0068-instagram-connector.md) | Instagram connector architecture — Business and Creator account model, linked Facebook Page discovery, Tier-3 OAuth credential harmonization, single-row carousel model with ordered gallery children, bounded 30-day/100-item pagination precedence, Graph API error code reclassification (`190`/`10`/`100` $\rightarrow$ `reconnect_required`), and ADR-0064 geospatial normalization | Accepted 2026-08-20; follow-up to ADR-0059/0060/0064/0067 |
| [0069](0069-linkedin-connector.md) | LinkedIn connector architecture — OAuth 2.0 confidential client flow, 60-day access token refresh with persisted `refreshTokenExpiresAt` (1-year conditional retention), tenant-isolated state caching (10m TTL), best-effort refresh-token revocation, Rest.li gateway rate limit parsing (epoch seconds), scheduler-enforced 1-hour polling guardrail, and graceful scope degradation | Accepted 2026-08-20; follow-up to ADR-0002/0003/0010/0014/0018 |
| [0070](0070-connector-ingestion-status-hanging-run-reconciliation-and-alerts.md)³⁵ | Connector Ingestion Health Status, Hanging Run Watchdog Reconciliation, and Inactivity Alerting — watchdog timeout reconciliation (`MAX_RUN_DURATION_MS = 15m`) preventing in-flight polling deadlocks, extended `stalled` health state, Service Bus `ConnectorIngestionAlertEvent` publishing, and Admin UI status badges/alert banners with on-demand "Force Retry / Re-sync" | Follow-up to ADR-0005/0009/0010/0023/0052/0058/0061; resolves orphaned running run deadlocks and silent ingestion inactivity |
| [0071](0071-human-in-the-loop-post-enrichment-overrides-and-cascading-drawer-ui.md) | Human-in-the-Loop Post Enrichment Overrides and Cascading Drawer UI — in-place `enrichment` (`JSONB`) updates with audit tracking (`enrichment.override`), `PATCH /v1/posts/:id/enrichment` REST endpoint, and dual-drawer cascading edit interaction on Posts Feed (`/tenant/posts`) | Requested by Menno 2026-08-20 to allow analysts to correct AI sentiment and enrichment attributes side-by-side with post details |
| [0072](0072-cross-platform-polypost-composer-and-multi-network-preview-engine.md) | Cross-Platform Polypost Composer and Multi-Network Preview Engine — unified multi-platform authoring tool, 7 network preview rails (LinkedIn, Instagram, Facebook, Bluesky, Mastodon, Threads, X/Twitter), OpenGraph link card scraping, local image uploads with Alt-Text, Markdown/.docx file importers, multi-draft autosave, and Azure OpenAI copy assistance | Requested by Menno 2026-08-21 to provide a cross-platform composition, preview, and AI-enhancement engine |
| [0073](0073-outbound-reply-to-ingested-posts.md) | Outbound Reply to Ingested Posts via Platform APIs — optional `SocialConnector.reply?()` contract, tenant-scoped `outbound_activities` audit table, `POST /v1/posts/:id/replies` endpoint, and Post Detail reply composer / replies tab; v1 limited to replies on the tenant's own connected-platform posts | Accepted 2026-08-22; requested by Menno 2026-08-22 to close the engagement loop on ingested posts |
| [0074](0074-tenant-facing-workspace-and-posts-export.md) | Tenant-facing workspace and matched-posts export — `GET` endpoints for full workspace JSON and posts CSV, powering the `/tenant/settings` export buttons; separates on-demand tenant export from ADR-0043's deletion export | Accepted 2026-08-23; sources the real-data constraint for implementing `docs/design/Google AI Studio/src/views/TenantSettingsView.tsx` |
| [0075](0075-outbound-social-post-publishing.md) | Outbound Social Post Publishing via Platform APIs — real `SocialConnector.publish?()` method, `POST /v1/outbound/posts`, per-asset targeting, and `outbound_activities` `activity_type='post'`; extends ADR-0072's simulated Polypost Composer dispatch into real posts and ADR-0073's audit trail | Accepted 2026-08-23; requested by Menno 2026-08-22 to enable real outbound publishing from ADR-0072's Polypost Composer |
| [0076](0076-composer-deep-research-agent.md) | Composer Deep Research Agent — context summary from post text, key phrases, and one-off Brave/Bing search results in the Polypost Composer | Accepted 2026-08-23; requested by Menno 2026-08-22 to add on-demand research/background context to the composer |
| [0077](0077-watchlist-connector-count-method.md) | Watchlist connector count and preview endpoint — optional `SocialConnector.count?()` method and `POST /v1/watchlists/preview-volume` for per-connector volume estimates | Accepted 2026-08-23; review adjustments incorporated (preview dry-run flag, partial-failure handling, sample extrapolation formula, and answered open questions) |
|| [0078](0078-metric-explainability-endpoint.md) | Metric explainability endpoint — `POST /v1/explain` returns plain-language metric explanations with confidence, caching, and UI conventions | Accepted 2026-08-23; review refinements incorporated (input sanitization, rate limiting, deterministic confidence, tenant kill switch) |
|| [0079](0079-crisis-template-bundle-and-activation.md) | Crisis template bundle and activation — `crisis_templates`, `tenant_crisis_templates`, and `POST /v1/crisis-templates/:id/activate` for one-click reputation-crisis monitoring | Accepted 2026-08-23; review refinements incorporated (template parameterization, notification channels, transactional activation, FK lifecycle, playbook linking) |
|| [0080](0080-onboarding-checklist-state.md) | Onboarding checklist state — `onboarding_checklist` JSONB on `tenants` and `GET/PATCH /v1/tenants/:id/onboarding-checklist` for self-service setup progress | Accepted 2026-08-24; review refinements incorporated (milestone locking, bundled query reconciliation, and TypeScript contracts) |
|| [0081](0081-rag-connector-provider-abstraction.md) | RAGConnector provider abstraction — vector-store interface (`upsert`, `search`, `deleteTenant`, `deletePost`, `status`) for Pinecone, Azure AI Search, or pgvector | Proposed 2026-08-23 to keep semantic search provider-agnostic |
|| [0082](0082-rag-post-chunking-and-embedding.md) | RAG post chunking and embedding pipeline — `RAGChunkingService`, `AIProviderConnector.embed()`, async `RAGIndexRequestedEvent`, and `rag_chunks_sync` bookkeeping | Proposed 2026-08-23 to keep posts retrievable as small, embedded chunks |
|| [0083](0083-rag-vector-store-rls-and-metadata.md) | RAG vector-store RLS and metadata — mandatory `tenant_id` metadata filter, chunk metadata schema, and deletion/offboarding sync | Proposed 2026-08-23 to enforce multi-tenant isolation in the vector store |
|| [0084](0084-rag-search-and-ask-endpoint.md) | RAG search and ask endpoint — `POST /v1/rag/search`, `POST /v1/rag/ask` with citations, `GET /v1/rag/status`, and rate/cost caps | Proposed 2026-08-23 to expose semantic search and Q&A |
|| [0085](0085-rag-ui-ux-and-loading-patterns.md) | RAG UI/UX and loading patterns — `RAGSearchBox`, `RAGResultCard`, `RAGAsk` answer/citation components, and loading conventions | Proposed 2026-08-23 to make semantic search usable and trustworthy |
|| [0086](0086-prospecting-list-model-and-sharing.md) | Prospecting list model and sharing — `prospecting_lists`/`prospecting_list_entries` schema, sharing rules, export contract, and relationship-stage label | Proposed 2026-08-23 to support social-selling lead lists |
|| [0087](0087-preconfigured-analytics-views.md) | Preconfigured analytics views — `TopicDailyCount`, `SourceDailyCount`, `AuthorDailyCount`, `SentimentDailyCount`, `WatchlistDailyCount` tables and refresh/routing rules for fast dashboards | Proposed 2026-08-23 to make analytics queries fast without scanning raw posts |
|| [0088](0088-ad-hoc-query-allowlist.md) | Ad-hoc query allowlist — structured `POST /v1/analytics/query` with allowlisted dimensions, metrics, and filters, plus resource guards | Proposed 2026-08-23 to give analysts safe, flexible aggregation |
|| [0089](0089-platform-operations-dashboard.md) | Platform operations dashboard — cross-tenant health, connector status, cost, and queue metrics for `Platform-Admin`/`Sole-Operator` with a strict tenant-content-free boundary | Proposed 2026-08-23 to give platform operators a single operational view |
|| [0090](0090-data-export-posts-csv.md) | Data export — posts CSV — `GET /v1/posts/export.csv` with streaming, bounding, and async large-export support | Proposed 2026-08-23 to give tenants portable, analysis-ready post exports |
|| [0091](0091-real-time-alert-rules-and-delivery.md) | Real-time alert rules and delivery — `alert_rules` model, threshold evaluation worker, and in-app/email/webhook delivery with cooldown | Proposed 2026-08-23 to turn dashboards into actionable notifications |
|| [0092](0092-author-initiated-takedown.md) | Author-initiated takedown — public takedown form, `data_subject_requests`, soft-redaction flow, and magic-link verification for content removal requests | Proposed 2026-08-23 to give authors a path to request removal of their public posts |
|| [0093](0093-dsr-self-service-portal.md) | DSR self-service portal — access/correction/erasure request portal, fulfillment worker, and tenant admin review UI | Proposed 2026-08-23 to support GDPR/CCPR data-subject rights |
|| [0094](0094-compliance-audit-pack.md) | Compliance audit pack — `compliance_audit_packs` model, tamper-evident signed export, and async generation for audit/defensibility | Proposed 2026-08-23 to produce verifiable audit records |
|| [0095](0095-case-handoff-to-crm.md) | Case handoff to CRM — `CRMConnector` abstraction, `POST /v1/inbox/items/:id/case`, field mapping, and `outbound_activities` audit for one-click CRM escalation | Proposed 2026-08-23 to close the loop from social listening to CRM |
|| [0096](0096-daily-digest-email.md) | Daily digest email — timezone-aware `user_digest_preferences`, hourly scheduler, and AI-summarized digest using precomputed analytics views | Proposed 2026-08-23 to keep users informed without real-time noise |
|| [0097](0097-topic-evolution-timeline.md) | Topic evolution timeline — `GET /v1/topics/evolution` using `TopicDailyCount`/`AuthorTopicSignal` for longitudinal topic/sentiment/author trends | Proposed 2026-08-23 to show how topics change over time |
|| [0098](0098-publishing-and-scheduling.md) | Publishing and scheduling — `SocialConnector.publish?()`, `outbound_activities` `activity_type='post'`, and `POST /v1/outbound/posts` with optional `scheduled_for` | Proposed 2026-08-23 to make the composer publish real posts |
|| [0099](0099-unified-social-inbox-and-reply.md) | Unified social inbox and reply — `inbox_items` workflow model, triage/assignment, and `POST /v1/inbox/:id/reply` | Proposed 2026-08-23 to turn the post feed into a care workflow |
|| [0100](0100-composed-post-author-mention-suggestions.md) | Composed post author mention suggestions — `POST /v1/composer/mention-suggestions` combining `AuthorTopicSignal`, RAG, and keyword signals | Proposed 2026-08-23 to help users mention relevant authors |
|| [0101](0101-multi-source-connector-capability-matrix.md) | Multi-source connector capability matrix — `SocialConnector.getCapabilities()` and `GET /v1/connectors/capabilities` for poll, count, publish, reply, and backfill features | Proposed 2026-08-23 to make connector capabilities discoverable and UI-driven |
|| [0102](0102-boolean-query-ast-and-visual-builder.md) | Boolean query AST and visual builder — canonical `WatchlistAST`, visual `BooleanQueryBuilder`, and per-connector query validation | Proposed 2026-08-23 to make watchlist queries visual and platform-aware |
|| [0103](0103-ai-sentiment-analysis-aspect-schema.md) | AI sentiment analysis aspect schema — `enrichment.sentiment` with overall, aspect-based, language, and override support | Proposed 2026-08-23 to enrich sentiment with aspects and multi-language signals |
|| [0104](0104-ai-topic-clustering-post-topics-schema.md) | AI topic clustering post-topics schema — `topics`, `post_topics`, `TopicClusteringService` refresh, and `selectedTopic` filter | Proposed 2026-08-23 to make topics queryable, curatable, and dashboard-filterable |
|| [0105](0105-dashboards-and-analytics-widget-contracts.md) | Dashboards and analytics widget contracts — `GET /v1/analytics/dashboard`, widget data shapes, `selectedTopic` filter, and query routing | Proposed 2026-08-23 to standardize dashboard widget data |
|| [0106](0106-api-and-integrations-versioning-and-webhooks.md) | API and integrations — versioning and webhooks — public API rate limits, `/v1/webhooks/subscriptions`, and HMAC-signed event delivery | Proposed 2026-08-23 to expose a public API and real-time webhooks |
|| [0107](0107-multi-user-workspaces-and-rbac-permissions.md) | Multi-user workspaces and RBAC permissions — per-connector and per-watchlist permissions, feature gates, and role matrix | Proposed 2026-08-23 to make workspaces and permissions granular |
|| [0108](0108-influencer-discovery-and-scoring.md) | Influencer discovery and scoring — `Author` scoring columns (`reach`, `engagement`, `authenticity`, `influence`), `GET /v1/influencers`, and explainability | Proposed 2026-08-23 to discover and rank relevant authors |
|| [0109](0109-connector-health-auto-disable-and-recovery.md) | Connector health auto-disable and recovery — `failing`/`disabled` transitions, `retryable` flag, and manual/auto re-enable rules | Proposed 2026-08-23 to protect quota from failing connectors |
|| [0110](0110-per-connector-query-translation-and-validation.md) | Per-connector query translation and validation — `ConnectorQueryTranslator`, `GET /v1/connectors/:platformId/query-capabilities`, and save-time AST validation | Proposed 2026-08-23 to make watchlist queries safe per platform |
|| [0111](0111-export-bounding-streaming-and-size-caps.md) | Export bounding, streaming, and size caps — sync/async thresholds, `export_jobs`, Blob lifecycle, and rate limits for post exports | Proposed 2026-08-23 to keep exports bounded and predictable |
|| [0112](0112-feature-gating-and-seat-limit-enforcement.md) | Feature gating and seat-limit enforcement — `tenant_settings.feature_gates`, per-plan allowlist, and `active_seat_count` enforcement | Proposed 2026-08-23 to support plan tiers and workspace limits |
|| [0113](0113-metric-explainability-prompt-and-caching.md) | Metric explainability prompt and caching — prompt template, JSON output, cache key, determinism, and confidence rules for `POST /v1/explain` | Proposed 2026-08-23 to make metric explanations consistent and cheap |
|| [0114](0114-platform-metrics-table-and-azure-metrics.md) | Platform metrics table and Azure Metrics integration — `platform_metrics`, hourly Azure Monitor ingestion, and dashboard aggregation | Proposed 2026-08-23 to feed the platform operations dashboard |
|| [0115](0115-publishing-media-upload-and-asset-targeting.md) | Publishing media upload and asset targeting — `POST /v1/outbound/media`, `media_assets`, per-asset targeting, and connector asset translation | Proposed 2026-08-23 to enable rich posts and multi-page publishing |
|| [0116](0116-semantic-drift-detection.md) | Semantic drift detection — `SemanticDriftService`, centroid cosine drift, and `GET /v1/topics/:id/drift` for v2 topic evolution | Proposed 2026-08-23 to detect when a topic's meaning shifts over time |
|| [0117](0117-prospecting-list-export-and-crm-push.md) | Prospecting list export and CRM push — `GET /v1/prospecting-lists/:id/export.csv` and batch `POST .../crm-handoff` via `CRMConnector` | Proposed 2026-08-23 to close the social-selling lead-to-CRM loop |
||| [0118](0118-additional-social-platform-publishing.md) | Additional social platform publishing — build order and shared constraints for Instagram, Bluesky, Mastodon, Threads, and X `publish?()` connectors | Proposed 2026-08-23 to roadmap the remaining Polypost Composer preview rails |
||| [0119](0119-editing-and-deleting-published-outbound-posts.md) | Editing and deleting published outbound posts — `outbound_activity_revisions` child table, `PATCH / DELETE` endpoints, and optional `SocialConnector.edit?()` / `delete?()` methods | Proposed 2026-08-23 to extend ADR-0075/0073 with safe mutation history |
|||| [0120](0120-search-provider-connector.md) | SearchProviderConnector — shared one-off search abstraction for Brave, Bing, and future search providers, optional `search?()` method, tenant credential and `RequestGate` model | Proposed 2026-08-23 to generalize ADR-0076's one-off search helpers |
|||| [0121](0121-composer-deep-research-caching-retrigger-cost.md) | Composer Deep Research caching, re-trigger, and cost justification — `research_cache` and `research_runs` tables, `?refresh=true`, tenant caps, and cost telemetry | Proposed 2026-08-23 to make ADR-0076 cheaper and auditable | |

¹ Unlike 0001–0015, the spec states this decision as a given rather than arguing it — the spec doesn't carry the reasoning. The reasoning is instead sourced from the chat conversation that produced the spec (linked in the ADR), not from the spec document itself. See its "Note on provenance."

² Mostly spec-sourced (§3.1/§3.3), with one exception: the `deliveryMode` rationale in its Consequences section is sourced from the same chat conversation as ADR-0016, not from the spec. See its "Note on provenance."

³ Originated as Proposed ADRs (gaps neither the spec nor the design conversation addressed), then accepted on 2026-07-29 — deliberately ahead of the implementation phase that would otherwise touch them (Phase 0 of `docs/implementation-plan.md`), because both are cheap to build in from day one and expensive to retrofit later. See each ADR's "Acceptance note." Unlike ADR-0016, these don't have spec/chat-sourced reasoning to cite — the reasoning is the ADR's own, same as when they were Proposed; only the Status changed.

⁴ Also originated as a Proposed ADR, accepted on 2026-07-29 — unlike ADR-0017/0019, this one wasn't accepted early for a schedule reason; it's a straightforward acceptance of the tiered-retention policy with `rawPayload` at 90 days and `IngestionRun` at 18 months, both configurable. See its "Acceptance note" and Amendment Log for the full numeric history.

⁵ ADR-0020–0023, all also originated as Proposed ADRs and all accepted on 2026-07-29, closing out this series' last four open decisions. Each ADR's own "Acceptance note" and Amendment Log carry the specifics, but the shared thread: numeric implementation defaults were kept as originally proposed in every case (no real traffic data existed to justify changing them), and two decisions were deliberately sequenced rather than built immediately — ADR-0020's Redis-backed distributed `RequestGate` state waits for an actual second concurrent instance to be deployed, and ADR-0023's rate-relative failure threshold doesn't retroactively change Stories 2.3/4.3's already-shipped flat-rule code until Story 2.5 is actually built (see ADR-0009's and ADR-0010's "Supersession update" notes). ADR-0023's `deliveryMode`-based variation question was deferred, not resolved — logged as a known gap, revisit once a push-mode connector exists.

⁶ ADR-0024 is a genuine 24th ADR, outside the original "23 ADRs, one per story" scope this series started with (see `CLAUDE.md`) — it originates a connector-specific provider selection and a scoped `Author`-modeling exception, not a decision the original design spec or its 23-ADR derivation anticipated. Drafted ahead of implementation like ADR-0017/0019, revised twice during review (RTPR's real pricing, then dropped entirely for direct wire-service RSS — see its own Amendment Log), then accepted 2026-07-30 the same day, with Story 2.6 added to Epic 2. Its own Acceptance note records that two follow-up deep-research passes (~25 additional candidates) found nothing that beat the accepted GlobeNewswire+PR Newswire scope.

⁷ ADR-0025 is a genuine 25th ADR, and — unlike every other ADR in this series — its own subject matter is dev tooling, not production architecture: `docs/implementation-plan.md`'s Phase 0 explicitly pre-classifies "local dev environment" as "also build, not storied," the same category this README's own "Not captured as ADRs" section uses for testing-strategy/CI details generally. It's here anyway as an explicit, user-directed exception — see its own Acceptance note — because the friction it fixes (a shared ephemeral test container getting torn down out from under a running dev server) was hit and demonstrated in practice, and fixing it surfaced a genuine `execSync`-vs-`spawn` correctness bug worth a durable record. Story 1.4 added to Epic 1, accepted the same day it was built and verified.

⁸ ADR-0026 is a genuine 26th ADR, drafted 2026-07-31 by the AI Business & Requirements Analyst persona to close Phase 1's own longest-standing gap — the RSS/News connector's concrete provider was never named, only the category ("poll-only, API-key auth, no paid tier," decided 2026-07-29). Like ADR-0024, it originates both a provider selection (GNews API, verified directly against `gnews.io`/`docs.gnews.io`) and a scoped `Author`-modeling exception (publication-as-Author — the second connector to need ADR-0024's exact pattern, not a new one). **Unlike ADR-0024/0025, it was not self-accepted the same day it was drafted** — the persona that drafted it does not hold ADR-acceptance authority — but it was reviewed and accepted by Menno later the same day (2026-07-31), see its own Acceptance note. Story 2.7 is now Ready. See its own Amendment Log for the full research trail, including two candidates (NewsAPI.org, Currents API) whose free tiers were verified to explicitly forbid the real/production use this connector needs, and one (NewsData.io) deferred after its own canonical terms pages proved unreachable for direct verification.

⁹ ADR-0027 is a genuine 27th ADR, drafted 2026-08-01 by the AI Business & Requirements Analyst persona from a principle Menno stated directly this session (in the context of Story 2.7/ADR-0026's GNews API key signup), not from the design spec. Unlike every prior ADR, it originates no interface, stored field, endpoint, or provider selection — it formalizes, as an explicit and durable project-wide rule, a practice already present but only stated as a connector-local default in ADR-0014, ADR-0024, and ADR-0026: SocialEngage's connector architecture is a technical connection mechanism only, never a party to, reseller of, or intermediary in the connecting party's own commercial/legal relationship with a data source. Deliberately framed as orthogonal to, not blocked by, this file's own 2026-07-30 multi-tenant governance note — it constrains whoever the future Authentication/Users ADRs (candidate ADRs #1–#4, below) determine "the connecting party" to be, rather than depending on that determination. Revised twice before acceptance the same day: a paid-tier-disqualifies-the-source eligibility rule was added, then reversed after Menno clarified the actual rule is narrower — paid tiers are fine, the connecting party pays the source directly, SocialEngage just never intermediates billing or pricing (see the ADR's own Amendment Log for the full, honestly-labeled reversal). **Accepted by Menno the same day** ("Approved as important foundation") — unlike ADR-0026, which took until later the same day it was drafted to be reviewed, this one was drafted, revised twice, and accepted all in one day. **Has no user story of its own** — the first ADR in this series without one; left as a documented, named exception rather than prompting a new "non-story ADR" convention (one instance isn't enough to generalize from). ADR-0024 and ADR-0026 each received a short, dated "Note on relation to ADR-0027" pointing to it, per this series' own governance-table convention (now a fifth row, added at this ADR's acceptance) for a new ADR formalizing an existing-but-unstated practice — neither ADR's own Decision/Consequences text was edited.

¹⁰ ADR-0028 is a genuine 28th ADR, drafted 2026-08-03 by the AI Business & Requirements Analyst persona from a principle Menno (Sponsor) stated directly this session, independent of (though related to) the connecting-party question ADR-0027 already governs: credential-creation authority is scoped by ownership tier — no system-wide credential is ever created, a tenant-wide credential may only be created by Tenant-Admin, and a user-bound credential is made available by the system but created only by the user's own act of activation. Its first tier substantially formalizes something already implicit in ADR-0027's Accepted "No pooling or sharing" bullet; its second and third tiers are new content with no prior shipped code or ADR language behind them, sourced only from Menno's statement this session — named as such in the ADR's own Context and Consequences rather than presented as already-practiced. Sharpened the same day with concrete social-platform account-type examples from a Menno follow-up (a Company Page/multi-admin Group for tier 2; an individually-issued account and personal, private profile for tier 3), grounding the tier 2/tier 3 distinction in each platform's own account/asset model rather than an abstract policy label. **Deliberately scoped, following ADR-0027's own technique, to state a governing principle without deciding candidate ADR #2's (Admin-tier design) or #4's (`users` table shape) exact mechanics** — Tenant-Admin's identity, authentication, and RLS shape remain those ADRs' job. **Raised, then resolved the same day, a real tension with ADR-0027's own already-Accepted Decision text**, which already lists "an AI enrichment provider" among the data sources its no-pooling rule covers: Menno confirmed directly that `AIProviderConnector` credentials (Azure AI Language) are tenant-owned tier 2 credentials, cost settled directly between the tenant and the vendor, no SocialEngage-operated shared subscription — a real, previously-uncosted consequence for `Project Management Plans/Cost-Management-Plan.md`'s existing project-level enrichment cost line, flagged there with a dated note rather than re-derived; see the ADR's own Context and Amendment Log. **Also corrected the same day: its own first-drafted Reddit worked example.** Verified directly against Reddit's own OAuth2 developer documentation (`github.com/reddit-archive/reddit/wiki/OAuth2`), Reddit's API supports both an app-only/"userless" grant (tenant-wide, tier 2 — public-content monitoring) and a personal user-context grant (tier 3 — needed only for capabilities that act as a specific account, e.g. replying to a post); the ADR's initial claim that "any Reddit connection is squarely tier 3, never tier 2" was wrong and corrected in place with a dated note, not silently rewritten. The tier 3 respond/reply case was named as a concrete example and parked as a requirement against the future Social Care subsystem in `docs/future-subsystems.md`, explicitly not new scope for this subsystem. **Has no user story at drafting time, but not for ADR-0027's reason** — tier 1 is already satisfied by shipped code the same way ADR-0027's principle is, but tiers 2/3 cannot be built against anything yet (no `tenants`/`users` table, no authentication, no Admin-tier role exist for a contract test to target); recommended as deferred to whichever of candidate ADR #4's or #6's own stories eventually implements the authorization/activation mechanics this ADR constrains, rather than treated as a second instance of ADR-0027's specific "permanently no story" exception. See the ADR's own "A note on this ADR's own place in the series' conventions" section. **Accepted by Menno the same day it was drafted** ("that will allow me to approve the ADR and confirm the ADR's intentions and described concepts are approved") — the fastest drafted-to-accepted turnaround in this series alongside ADR-0027, and unlike ADR-0027, revised in place five separate times before that acceptance, all in the same session. See the ADR's own Status line, Acceptance note, and Amendment Log. **Clarification added 2026-08-17, well after acceptance, triggered by a live production bug** (a tenant's personal-scope activation of Azure AI Language silently had no effect): the ADR's own already-established "which credential/grant type actually authenticates" test was applied explicitly, for the first time, to Azure AI Language/Azure OpenAI specifically, confirming neither has a Tier 3/personal variant — not a new decision, a mechanical consequence made explicit. Closed the same day in code: `connectorsRouter.ts`'s `forbidsUserScope()` (`social-listening-core@5d0fb49`) and `personalScopeAllowed` (`social-listening-admin@4082a8a`). See the ADR's own Clarification section and the Implementation Log's matching 2026-08-17 healing-pass entry.

¹¹ ADR-0029 through ADR-0035 are a genuine seven-ADR batch, drafted 2026-08-03 by the AI Business & Requirements Analyst persona, closing out this file's own 2026-07-30 "Candidate future ADRs, roughly in dependency order, none drafted" list in full — see that section, now updated below, for the running record of what each one decided and what it deliberately left open. Drafted strictly in dependency order (ADR-0029 first, since every later one assumes its shape; ADR-0035 last), per this batch's own task framing, not in parallel. **Menno's one fixed input this session — authentication mechanism is Microsoft Entra External ID, Azure-native, chosen for consistency with ADR-0014's own "stay Azure-native, avoid bespoke" precedent and its free tier — is treated as a given throughout, not re-litigated by ADR-0029.** Two mid-drafting course corrections from Menno were folded into ADR-0029 before this batch moved on to ADR-0030: an explicit "Entra as a thin, pluggable OIDC issuer only" Decision-level requirement (bounding a future provider-migration's blast radius), and a "single source of truth per field" data-ownership principle (Postgres owns profile data by default; two named, narrow exceptions). All seven ADRs were left **Proposed** — this persona does not hold ADR-acceptance authority (the same rule ADR-0026/0027/0028 followed) — Menno reviews and accepts each separately. **ADR-0029 accepted 2026-08-03**, verbatim: "reviewed ADR 0029 and approved." **ADR-0030 accepted 2026-08-03**, revised at review to add a narrow Tenant-Admin-credential-reset break-glass mechanism per Menno's own direct instruction — see its own Status line, Acceptance note, and Amendment Log. **ADR-0031 accepted 2026-08-03**, revised at review to add sign-up domain capture — see its own Status line, Acceptance note, and Amendment Log. **ADR-0032 accepted 2026-08-03**, revised at review to add `access_ends_at` after two clarifying exchanges that changed nothing — see its own Acceptance note and Amendment Log. **ADR-0033 accepted 2026-08-03**, as drafted, no revisions — see its own Status line and Acceptance note. **ADR-0034 accepted 2026-08-03**, its own flagged Tenant-Admin-revocation reading confirmed as drafted. **ADR-0035 accepted 2026-08-03**, its own rule-of-three recommendation decided: yes, formalize the no-story convention now (`docs/user-stories/README.md`). All 35 ADRs are now Accepted. Six of the seven generate a user story (ADR-0029–0034); the seventh (ADR-0035, admin UI shape) does not, following ADR-0027's/ADR-0028's own precedent for a reasoned "no story" case rather than forcing one for symmetry — see ADR-0035's own "A note on this ADR's own place in the series' conventions," which also flags that this is now the **third** such case and recommends Menno decide, at acceptance, whether that finally warrants a formal convention rather than a third one-off exception. ADR-0034 additionally surfaced a real, standing gap unrelated to any of the seven decisions themselves: `docs/implementation-log.md` has no entry for the already-shipped "Story 1.6" (connector connect/disconnect) despite its code and an 8-assertion contract test genuinely existing on disk, confirmed directly this session — flagged for correction, not fixed by this batch (see ADR-0034's own Context and Consequences).

¹² ADR-0036 is a genuine 36th ADR, drafted 2026-08-04 by the AI Business & Requirements Analyst persona while drafting the admin UI's required user stories (Menno's own request), after confirming directly that `social-listening-admin` remains an empty shell (no Next.js scaffold; only `src/lib/core-client.ts` and Story 1.1's own contract exist). Unlike Stories 1.5/1.6/1.7's ordinary CRUD precedent (no ADR needed), this ADR decides the admin UI's own authentication/session mechanism — where a bearer token physically lives (server-side session cookie only, never browser-reachable storage), whether to adopt a third-party auth framework (NextAuth.js/Auth.js, rejected for v1 pending verification of its Entra External ID/CIAM support), and how role-gating is enforced in Next.js routing — reasoned to be architecturally significant by the same bar ADR-0027/0028/0035 already established (hard-to-reverse, real security consequence, not just a new field/endpoint shape). It also surfaces a real, previously unnamed cross-repo dependency: `social-listening-core` has no endpoint exposing `resolveIdentity()`'s result over HTTP, confirmed directly against `src/identity/identityResolution.ts` and every `versions/v1/*Router.ts` file — the admin UI cannot determine a caller's role without one, since ADR-0029 §2 already forbids inferring role from any Entra-side claim. Drafted Proposed, then revised in place four times the same day (see its own Amendment Log) before **accepted by Menno 2026-08-04**, verbatim: "ADR 0036 is approved." Story 6.1 (`docs/user-stories/epic-6-admin-ui.md`) moves to Ready. See `docs/user-stories/epic-6-admin-ui.md` (Story 6.1) for the story this ADR sources, and its own "A note on this ADR's own place in the series' conventions"-equivalent reasoning inline in its Context section for why it departs from the "ordinary UI/CRUD surface doesn't need an ADR" default. **2026-08-05: ADR-0036 §5 now also sources a second story, Story 5.11** (`docs/user-stories/epic-5-security-isolation-and-messaging.md`) — the `GET /v1/me` endpoint itself, resolving §5's own named Open Question (exact path/response shape) directly rather than leaving it undesigned indefinitely. Placed in Epic 5 (backend, `social-listening-core`), not Epic 6 (`social-listening-admin`), since that is where the actual implementation belongs — see Story 5.11's own entry for the reasoning. This does not change ADR-0036's own Decision/Consequences text.

¹³ ADR-0037 is a genuine 37th ADR, drafted 2026-08-04 by the AI Business & Requirements Analyst persona at Menno's direct request ("a user sign up screen for tenant admins" — the self-service flow where a brand-new, not-yet-tenant-linked user signs up and becomes the first Tenant-Admin of a newly-created tenant), after confirming directly that no ADR, story, or shipped code currently designs this flow: ADR-0031 §5 *names* self-service sign-up but explicitly declines to design its rerouting UX or public-email-provider exclusion mechanism; ADR-0032 §6 designs only the *invite* flow (an existing Tenant-Admin inviting a colleague into their own already-existing tenant), not first-tenant creation; and `tenantStore.ts`'s `createTenant()` is confirmed reachable only through `platform_admin_role` today (`migrations/0017_create_tenants.sql`), which an unauthenticated-until-that-instant sign-up caller is nowhere near. Reasoned to be architecturally significant by the same bar ADR-0027/0028/0035/0036 already established: it resolves (a) how such a caller gets tenant-creation authority via a new, narrower `tenant_signup_role` — deliberately never a widening of `platform_admin_role`'s own ADR-0030 §2-locked boundary; (b) the domain-match "rerouting" UX ADR-0031 §5 explicitly deferred (decided here: reject toward the existing invite flow, never auto-join); (c) the public/free-email-provider exclusion mechanism ADR-0031 §5 also deferred (decided here: a maintained static denylist, named as permanently incomplete by construction); and (d) a narrow, explicitly-scoped exception to ADR-0029 §4's own "reject any unmatched caller" rule, for exactly one endpoint. Also names, but does not design or build, a new `social-listening-core` endpoint (`POST /v1/tenants/self-service-signup`-shaped) this decision depends on, and surfaces a real, unresolved cross-ADR tension with ADR-0029's own still-open "restrict Entra self-service sign-up at the IdP level" Open Question — resolving that question toward "yes, restrict" would directly disable this feature, flagged for Menno to reconcile explicitly rather than pre-decided here. Drafted Proposed, revised in place for a Security & Architecture Reviewer finding (domain-match rejection message must not name the matched organization), then **accepted by Menno 2026-08-04**, verbatim: "ADR 0037 is approved as well." Accepted together with three direct instructions folded into new Decision §8 — email-OTP-verification as a domain-matching precondition; a new tenant-scoped `domain_signup_attempts` record surfaced to the Tenant-Admin as a one-click "Same-Domain Invite Assist" proposal, with visibility escalating on repeated attempts; the same escalation also logged to `platform_admin_audit_log` for Platform-Admin review; and reassuring, still non-org-naming copy for the rejected caller — see the ADR's own §8, Acceptance note, and Amendment Log for the full record. Story 6.7 (`docs/user-stories/epic-6-admin-ui.md`) moves to Ready.

¹⁴ ADR-0038, ADR-0039, and ADR-0040 are a genuine three-ADR batch, all drafted 2026-08-05 by the AI Business & Requirements Analyst persona, as part of a 16-item (14 actually specified — see `docs/user-stories/README.md`'s own dated note on this batch) story-drafting pass Menno requested after cross-referencing all 6 epics against all 37 (then-existing) ADRs. Unlike ADR-0029–0035's batch, these three are not sequentially dependent on one another — each closes a separately-named gap in a different Accepted ADR (ADR-0028's own already-resolved AI-enrichment-billing question, ADR-0018's/ADR-0031's own twice-named tenant-offboarding gap, ADR-0037 §7's own named-but-undesigned rate-limiting precondition) — but are numbered as a genuine next-three batch rather than interleaved with the eleven no-ADR-needed stories this same pass also drafted, following this series' own "genuine Nth ADR, not a decimal insertion" convention (footnote 11's own precedent). All three drafted **Proposed**, per this persona's own charter boundary — awaiting Menno's own separate review and acceptance of each. **ADR-0038** was revised in place four times during drafting, all in direct response to Menno's own mid-drafting course corrections (documented in its own Amendment Log): correcting an initial overstatement of Azure AI Language's own call shape; giving a general-purpose LLM a genuinely fair, primary-source-verified comparison rather than defaulting to the design spec's own illustrative pick; weighing the real single-call-vs-per-capability-call efficiency argument explicitly; and adding an explicit, standalone Decision section (not merely inherited by cross-reference) reconfirming that SocialEngage is never a billing intermediary for either candidate provider, plus a further explicit section naming why the recommended LLM tier is deliberately the small/cheap one, not a flagship model, on both capability and cost-proportionality grounds together. **ADR-0039** and **ADR-0040** were each drafted once, without revision, directly against their respective already-Accepted source ADRs' own explicitly-named gaps. A forward-pointing "Pending supersession note" was added to ADR-0018's own Open Questions section (not its Decision/Consequences text) for the one narrow, scoped exception ADR-0039 would introduce to ADR-0018's own `IngestionRun` archival-not-deletion rule, per this file's own governance-table convention — it takes effect only once ADR-0039 is actually accepted, not before.

¹⁵ ADR-0041 is a genuine 41st ADR, drafted 2026-08-06 by the AI Business & Requirements Analyst persona at Menno's direct request, immediately after a real healing pass the same day (`social-listening-admin@1f8960e`, `docs/implementation-log.md`'s "2026-08-06 — Healing pass — Story 6.2 AC2") fixed a live bug: `role-routing.ts`'s `getRoleShell()` switched on a resolved identity's `.role` field, but `social-listening-core`'s real `ResolvedIdentity` type is a discriminated union in which a `platform_admin` identity carries no `role` field at all — a real Platform Admin session was silently routed into the tenant-facing UI shell. Menno's own words, requesting this ADR directly: *"I would not mind a ADR that would name the Platform Admin and separate this from the Tenant Admin to avoid confusion."* Unlike ADR-0038–0040, this one does not close a gap in a single already-Accepted ADR's own explicitly-named Open Question — it formalizes, as a new, general, cross-layer, project-wide rule, something four separately-scoped Accepted ADRs (ADR-0030's Postgres role/grant design, ADR-0032 §3's separate `platform_admins` table, ADR-0035's route-tree split, ADR-0036 §4's UX-layer gating) each already practice locally but never stated as a general principle any future identity-consuming code — in either repository — must itself preserve. Drafted **Proposed**, per this persona's own charter boundary, with an explicit "why a new ADR, not an amendment" judgment call argued in its own Context section against `docs/adr/README.md`'s own governance-table convention (this file's Row 6, the same "formalizes an already-assumed-but-unstated principle" pattern ADR-0027 used against ADR-0024/0026), rather than defaulting to Menno's own request without checking it against that convention first. Also names, but does not fix (documentation-only, per this persona's own scope boundary), a live, present-tense counter-example found while drafting: `social-listening-admin/src/lib/role-routing.ts` still defines an unused, flat, three-value `AdminRole` type re-encoding the exact wrong shape this ADR argues against. A no-story ADR, category 1 ("already satisfied by shipped code") per `docs/user-stories/README.md`'s own convention — the same category ADR-0027 used — since the structural distinctness it states is already fully practiced in shipped code (`ResolvedIdentity`, Story 5.9; the healing pass's own `isResolvedIdentity()`/`isShellAllowed()`, Story 6.2). **Accepted by Menno the same day, verbatim:** *"ive reviewed and confirm that its the intented clarification to avoid confusions regarding admins and the type of admins we will support."* As drafted, no revisions. ADR-0030, ADR-0032, ADR-0035, and ADR-0036 each now carry a dated "Note on relation to ADR-0041" on their own files, per Decision §37 — none of their own Decision/Consequences text changed.

¹⁶ ADR-0042 is a genuine 42nd ADR, drafted 2026-08-06 by the AI Business & Requirements Analyst persona, the same day as ADR-0041, from `docs/open-decisions.md`'s own 2026-08-06 connector-comparison entry (Reddit, X, YouTube, Meta, Wikipedia, prompted by Reddit's own real access friction found the same day) and the Knowledge-Graph & Semantic Data Modeling Reviewer's same-day finding on `docs/architecture/knowledge-graph-register.md`. Unlike ADR-0024/0026, which each closed a gap the design spec's own §10 roster already named a category for (Newswire, RSS/News), this ADR proposes a connector not named anywhere in the spec at all — its own justification rests entirely on this session's fresh research, not a prior category commitment. Every Wikimedia-sourced factual claim (Terms of Use §7/§12, User-Agent Policy, API Usage Guidelines' self-service and no-sublicense/no-white-label terms, CirrusSearch operators, RecentChanges API behavior) and the CC BY-SA 4.0 ShareAlike-on-modification-only trigger were verified directly against `foundation.wikimedia.org`, `meta.wikimedia.org`, `mediawiki.org`, and `creativecommons.org` in this same drafting pass — the same primary-source discipline ADR-0024/0026 held themselves to after RTPR's and Currents API's secondhand claims failed direct verification. Resolves, rather than defers a third time, the ingestion-cadence question the Knowledge-Graph reviewer explicitly declined to answer: the connector re-polls an already-tracked article's revision history (via `recentchanges`), producing one `SocialPost` per qualifying revision, all reused against one `Author` row keyed to the article itself — argued as a real, if structurally narrower, third instance of ADR-0024's/ADR-0026's issuer-as-Author departure (the reused entity is a persistent platform-native document identity, not an independently-existing real-world organization), not a mechanical repeat of the same pattern. A dated Pending supersession note was added to ADR-0004 accordingly, recommending Menno decide the exact generalized wording at this ADR's own acceptance rather than assume the three instances are identical in shape. Attribution (Wikimedia's own stated hyperlink-to-history-page mechanism) is deliberately kept out of `Author` entirely and routed through the already-existing `SocialPost.url` field instead, set to the ingested revision's own permalink — per this ADR's own drafting task, which required the entity-modeling question and the legal-attribution question not be forced into the same field. Also names, but does not design, a real anticipated future direction (RAG/embedding-oriented chunked retrieval, Menno's own words: "its more the question when then if") as an Open Question this ADR's whole-article `SocialPost.text` storage choice deliberately keeps open rather than forecloses — the Knowledge-Graph reviewer's own finding that chunk-granularity is a source-agnostic, not Wikipedia-specific, indexing-strategy decision. Drafted **Proposed**, per this persona's own charter boundary — awaiting Menno's own separate review and acceptance. **Accepted by Menno 2026-08-08**, verbatim: `"ADR 0042 Approved"` — see this ADR's own Amendment Log. **Correction, 2026-08-12:** this ADR's own acceptance note states a story would be added to Epic 2 at acceptance, the same precedent ADR-0024/0026/0050 followed — that never actually happened, confirmed directly by grepping every `docs/user-stories/epic-*.md` file for `ADR-0042` and finding zero matches, four days after acceptance. Found and closed at Menno's own direct request ("im also missing the stories to build the wikipedia ingestion?"), which also prompted a full sweep of every other Accepted ADR against every epic file's own citations — only ADR-0047 (a deliberate, already-documented no-story meta-ADR) was similarly absent; ADR-0042 was the sole genuine gap. **Story 2.13** (`docs/user-stories/epic-2-ingestion-connectors-and-rate-limits.md`) is now drafted, Ready, not yet built.

¹⁷ ADR-0043 is a genuine 43rd ADR, drafted 2026-08-06 by the AI Business & Requirements Analyst persona, hours after ADR-0039's own acceptance the same day, from Menno's own direct instruction relayed this session: a Tenant-Admin should be able to request deletion of their own tenant — halting ingestion immediately, exporting all available data as CSV or JSON during a soft-delete period, then confirming a final, irreversible deletion. Asked to clarify Platform Admin's role, Menno confirmed via a structured choice: **"Fully self-service, Platform Admin only sees the audit trail"** — no Platform Admin approval step, more aggressive than the reviewed break-glass-style routing ADR-0039 Decision §1's own aside had anticipated for a future self-service path. This is a real reversal of ADR-0039 Decision §1's own "cannot self-delete" prohibition, not a parameter tweak or an implicit-requirement clarification — `docs/adr/README.md`'s own governance table, row 1 ("the underlying decision itself changes → new ADR, or a superseding ADR that says so explicitly"), governs. **Explicitly weighed and rejected: an in-place, dated revision to ADR-0039 itself**, the pattern ADR-0030/ADR-0032 used for same-session revisions — found not actually the same situation on inspection, since those revisions happened *before* acceptance, while still Proposed, whereas ADR-0039's Decision §1 text was already Accepted, verbatim, "no revisions," hours earlier; see ADR-0043's own Context section for the full argued reasoning. Closing the gap for real surfaced a genuine additional architectural boundary this ADR also had to decide, not merely "who may trigger deletion": `tenants.status` is database-GRANT-restricted to `platform_admin_role` (`migrations/0017`), so the new flow instead grants `app_user` two new, narrowly-scoped columns (`deletion_requested_at`, `deletion_confirmed_at`) mirroring `active_seat_count`'s existing precedent, and a new `INSERT` grant on `platform_admin_audit_log` mirroring `tenant_signup_role`'s own existing precedent (`migrations/0021`, Story 5.15) — the final, irreversible execution step still runs under `platform_admin_role`'s existing, already-locked bypass, reusing Story 3.7's own deletion-execution function directly rather than building a second pipeline. Also verified directly, not assumed: no tenant-iterating scheduler exists yet anywhere in `social-listening-core` for the ingestion-halt requirement to hook into — the ingestion-halt guard is placed at `runIngestionAttempt()` instead, the one real, shared per-attempt function every connector already funnels through. Names a real, explicit 30-day (configurable) grace-period default, with cancellation available throughout, per this project's own "name a real number, mark it revisable" convention (ADR-0018/ADR-0037's own precedent) — argued explicitly against leaving cancellation undesigned ("a grace period with no way to change your mind is a confusing half-feature"). Drafted **Proposed**, per this persona's own charter boundary — Menno's instruction was relayed through this session's orchestrating agent, not received as Menno's own direct message to this persona, so it is treated as the request to draft this decision, not its acceptance. A dated Pending supersession note was added to ADR-0039's own Open Questions section accordingly. **Story 3.8** (`docs/user-stories/epic-3-data-model-storage-and-archival.md`) is **Blocked — pending this ADR's acceptance**.

**Correction and acceptance, 2026-08-07.** Story 3.7 was built the same night exactly as ADR-0039 Decision §1 originally specified (`platform_admin_role`-gated). Running the resulting contract against the full accumulated suite surfaced that this collides directly with Story 5.7's own already-accepted "`platform_admin_role` has zero access to any tenant-content table" boundary. Menno reviewed this and corrected the decision: there is no separate Platform-Admin-initiated path at all, not merely a self-service one added alongside it. This ADR was revised in place (still Proposed at the time, so an in-place revision, not a supersession, per this file's own governance table) to supersede ADR-0039 Decision §1 **in full** rather than narrowly on its "cannot self-delete" sentence — the final execution step now runs under a new, dedicated, narrowly-scoped `tenant_deletion_role` (deliberately not `BYPASSRLS`, unlike every other specialized role in this project, for defense-in-depth) rather than `platform_admin_role`'s bypass, a real, deliberate design refinement decided during the build, not merely a re-gating of the same mechanism. Then **accepted by Menno**, verbatim: *"Tenant Admin requests deletion → export window → grace period → Tenant Admin confirms → system deletes → Platform Admin only sees audit logs."* Confirmed separately, verbatim: *"yes approved."* Story 3.7's implementation was reverted before ever being committed; Story 3.8 was built for real against this corrected design (`social-listening-core`, full suite 41/43 suites, 255/260 tests — the only two failures are the pre-existing, external, real Azure Entra break-glass flake, unrelated). Story 3.8 moves to **Done**.

18 ADR-0044 is a genuine 44th ADR, drafted 2026-08-08 by the AI Business & Requirements Analyst persona, in response to a review finding that the prior draft re-decided ADR-0015 (RLS), ADR-0017 (API versioning), ADR-0030 (Platform Admin bypass boundary), and ADR-0033 (X-Tenant-Id retirement) and silently conflicted with ADR-0021 (boolean-query AST) by introducing a JSONB ilters column as an alternative matching model. This revision scopes the durable decisions to the four actually unanswered questions for Story 1.5: (a) project-wide PATCH semantics (JSON Merge Patch, RFC 7396); (b) an explicit 404-vs-403 split that does not collapse the cross-tenant case (404, per ADR-0015 §Consequences) with the Tenant-Admin role-check case (403, per ADR-0030 §1); (c) optimistic locking via a ersion integer column on /v1/watchlists and per-resource opt-in elsewhere; (d) a efore update trigger policy for updated_at on mutable tenant-scoped tables, with an explicit exempt list (IngestionRun per ADR-0005, platform_admin_audit_log, anything else append-only). It also explicitly reconciles the watchlists row shape with ADR-0021 by separating non-boolean matchers (JSONB 	erms, indexed where appropriate) from boolean matchers (oolean_query text, parsed into the AST ADR-0021 owns) — the two apply to disjoint match_type values, not overlapping ones. Deliberately does not re-decide anything ADR-0015/0017/0030/0033 already decided, and does not introduce a new audit table name (platform_admin_audit_log per ADR-0030 §5/ADR-0031 §27/ADR-0037 §125 remains this project's only audit table). Drafted **Proposed**, per this persona's own charter boundary — awaiting Menno's separate review and acceptance. Source ADRs explicitly closed: none (this ADR opens new durable decisions rather than closing gaps already named in other Accepted ADRs' Open Questions); supersedes nothing. Story 1.5's existing implementation continues to be **Blocked** pending this ADR's acceptance, the same convention every other ADR-sourced story in this series follows.

**Revised in place twice before acceptance, same in-place-revision-before-acceptance convention as ADR-0030/0031/0032/0036/0037/0043 (footnotes 11–13, 17).** **2026-08-08, same day as drafting:** `terms` corrected to `text[]` (matching migration `0014_create_watchlists.sql` and `WatchlistRow.terms: string[]`), not `jsonb` as the immediately-prior version of this same draft proposed; the `updated_at` trigger function name corrected to `update_updated_at_column()` (matching what migration 0014 already ships), not the illustratively-invented `set_updated_at()`. Status remained Proposed. **2026-08-11:** revised again to resolve the "watchlist creation authorization" Open Question left by the 2026-08-08 draft — added §5c: watchlists are personal, per-user resources, not tenant-wide shared ones; both `tenant_admin` and `tenant_user` may create one; ownership is fully private to the creator with no Tenant-Admin oversight override (extending ADR-0030 §2's "Platform Admin never touches `watchlists`" precedent one level further); enforced via a new `user_id` column and a second RLS predicate on a new `app.user_id` session variable, propagated the same way `app.tenant_id` already is (verified against `social-listening-core/src/db/withTenant.ts`); consequence for §2 — a request for another user's watchlist is 404, never 403, since ownership is RLS-enforced, not an application-layer role check. Also added Appendix A (PATCH contract examples) and Appendix B (PR review checklist) from an external AI-assisted review pass, judged genuinely cheap and additive. Status remained Proposed. See the ADR's own Amendment Log for the full record of both revisions, including what was considered and rejected.

**Accepted by Menno 2026-08-11**, verbatim: *"Approve the ADR 0044 Watchlist Crud contract with Personal Watchlist for Admin and Users."* Accepted as revised (both the 2026-08-08 and 2026-08-11 in-place revisions in effect, including the full §5c ownership decision and Appendices A/B) — see the ADR's own Status line and Acceptance note. **Story 1.5 moves from Blocked to Ready** — see `docs/user-stories/epic-1-repository-and-api-foundation.md`, rewritten against ADR-0044's actual accepted contract (PATCH semantics, error-code mapping, optimistic locking, `updated_at` trigger policy, and §5c ownership model), superseding its own pre-ADR-0044 text, which predated this ADR entirely and still required the since-retired `X-Tenant-Id` header (ADR-0033/Story 5.10).

19 ADR-0047 is a genuine 47th ADR, drafted 2026-08-08 by the AI Business & Requirements Analyst persona, from a backlog entry on Epic 2 cross-story dependency clarity (Stories 2.3/2.4/2.5; ADRs 0009/0010/0020/0023), where the 2026-07-30 supersession of Story 2.3/4.3's flat-failure threshold by Story 2.5's rate-relative rule exposed that the working pattern for superseded-AC markers and cross-dependency notes was being re-derived per-document rather than codified. Drafted **Proposed**, then revised in place the same day in response to a review finding that the initial version risked duplicating `docs/adr/README.md`'s seven-row governance table rather than codifying an existing pattern - the revision adds an explicit "Relation to `docs/adr/README.md`'s governance table" section stating that the README's table is the authority for *which* category applies (this ADR is the working pattern for *how* to write the resulting entry), splits §1 into three cases (Decision changes / parameter tweaks / implicit-requirement clarifications) consistent with the README's seven rows, cites `docs/user-stories/README.md`'s "Known cross-story conflict - resolved, 2026-07-30" entry as the canonical worked example for §2, enumerates the four valid "named owner artifact" forms in §3 and forbids unowned TBDs, and rephrases §4 as "the last dated appendix is the live contract" to avoid the dual-source-of-truth risk a separate top-of-file "current contract" sentence would have created. **Has no user story of its own**, the same "no-story ADR" category as ADR-0027 (connector-intermediary principle) and ADR-0041 (Platform Admin distinct identity kind) - meta-decisions about authoring practice rather than new features, with no story a contract test could target. Per `docs/user-stories/README.md`'s "No-story ADR convention" section (formalized at ADR-0035's acceptance), this is now an explicit, expected category, not a one-off exception.

**Accepted by Menno 2026-08-11**, verbatim: *"ADR-0047 is approved."* Accepted as drafted, no revisions at acceptance — see the ADR's own Status line and Acceptance note. Takes effect immediately as the working cross-reference pattern; no story, no migration, no code change follows from acceptance. Discussed and reaffirmed at acceptance: a proposal to track documentation TODOs via an external issue tracker (Jira) was raised and set aside in favor of the mechanism this ADR already provides for free — §3's four named-owner forms (resolved by ADR/story, deferred to a named future story, deferred to a named future ADR, or intentionally retained obsolete) already forbid unowned "TBD" text within the docs themselves, matching this project's existing git-native pending-work convention (`docs/pending-*.md`, `docs/management/pending-manager-reviews.md`) rather than introducing a second, external tracking system. The ADR's own lint/consistency-script Open Question (deferred as premature until the series exceeds ~50 ADRs — a threshold now reached) remains undecided by this acceptance; revisiting it, if ever, is a separate future decision.

²⁰ ADR-0048, ADR-0049, and ADR-0050, drafted at different times and by different means, closing out this file's own last three named gaps. **ADR-0048** (drafted 2026-08-08, from backlog input, by the AI Business & Requirements Analyst persona) codifies an explicit no-core-pipeline-change verification policy for every connector registration — mandatory PR-level evidence, a CI guardrail, and a registration-traceability documentation requirement, applying equally to `SocialConnector` and `AIProviderConnector` additions. It sources **Story 2.10**, which already existed in `docs/user-stories/epic-2-ingestion-connectors-and-rate-limits.md`, Blocked pending this ADR — a real gap in this file's own master-index table, which had never listed ADR-0048 or Story 2.10 in Epic 2's row despite both already being real; corrected in the same pass as this acceptance, not a separate fix. **ADR-0049** and **ADR-0050** (drafted 2026-08-10, per `docs/handoff-cursor-adr-brainstorm-2026-08-10.md`, following this series' own persona-drafts/Menno-accepts authority boundary even though the drafting tool differed) close the two items this file's own "Still outstanding, not yet drafted" section had carried since 2026-08-04/2026-08-06: ADR-0049 decides the point-in-time `author_follower_count_at_publish` exception to ADR-0004's normalized `Author` model (a single, nullable, ingest-time-only field, never backfilled); ADR-0050 decides the tenant-owned-domain RSS/content-feed connector (`authMode: 'none'`, `deliveryMode: 'poll'`, DNS TXT domain-ownership verification as a durable gate, organization-as-Author modeling keyed on the verified domain, v1 explicit-feed-URL-only). All three left **Proposed**, per this persona's own charter boundary — acceptance authority rests with Menno (Sponsor). **Accepted together by Menno 2026-08-11**, verbatim: *"ADR 0048, ADR 0049 and ADR 0050 approved."* Accepted as drafted, no revisions, all three. **Story 2.10 moves to Ready.** Two new stories are added, per the ADR-0024/0026 "no story until acceptance" precedent, both **Ready**: **Story 3.9** (`docs/user-stories/epic-3-data-model-storage-and-archival.md`, ADR-0049) and **Story 2.11** (`docs/user-stories/epic-2-ingestion-connectors-and-rate-limits.md`, ADR-0050). ADR-0050's own Decision §4/Open Question 6 is resolved at this same acceptance: `docs/adr/0004-author-normalized-separately-from-post.md` now carries a permanent, generalized "organization-as-Author" clause, citing all three established instances (ADR-0024, ADR-0026, ADR-0050) — see that ADR's own new "Supersession update — 2026-08-11 (ADR-0050, organization-as-Author generalization)" section; ADR-0049's own matching "Supersession update — 2026-08-11 (ADR-0049)" section was added the same way. Neither edits ADR-0004's own original Decision/Consequences text. This file's own Epic 2/Epic 3 ADR-and-story columns (`docs/user-stories/README.md`) are updated to match: ADR-0048/0050 and Stories 2.10/2.11 added to Epic 2; ADR-0049 and Story 3.9 added to Epic 3.

²¹ ADR-0051 is a genuine 51st ADR, drafted 2026-08-12 by the AI Business & Requirements Analyst persona, at Menno's own direct instruction that connector activation needed an ADR drafted before any code — surfaced while building this session's Story 6.5 connector-status-indicator enhancement, which found Newswire (`authMode: 'none'`, ADR-0024) hardcoded as permanently "connected" for every tenant with no opt-in mechanism at all. Widening the investigation (Menno's own framing: connectors shouldn't be active automatically) and grounded in Menno's own directly-recalled failure mode from the discontinued Microsoft Social Engagement product — a connector getting disconnected merely for exhausting its rate-limit quota — direct code inspection found the modern codebase reproduces the identical anti-pattern via a real implementation gap: `connectorHealth.ts`'s `deriveConnectorHealth()` never reads the `retryable` column `runIngestionAttempt.ts` already writes per `ingestion_runs` row, so a connector that's merely rate-limited can trip the same `failing`/ingestion-halt path (`shouldAttemptIngestion()`) as one with a genuinely revoked credential — a gap against ADR-0010's own already-stated Decision text ("retryable errors → automatic retry" vs. "non-retryable → immediate failing"), not a new decision. ADR-0051 **resolves ADR-0034's own explicitly-left-open Open Question** ("whether connector activation needs its own table separate from `platform_credentials`") by deciding **two new, credential-independent tables** — `connector_activations` (tenant-wide scope) and `connector_user_activations` (user-specific scope), mirroring ADR-0028/ADR-0034's own already-decided tenant/user credential-ownership split exactly, revised in place from an initial single-table design after Menno flagged that a tenant-wide credential's activation and an individual user's own personal credential's activation need to stay independently controllable, the same way the credentials themselves already are (see the ADR's own Amendment Log for both pre-acceptance revisions). Every connector defaults to inactive on tenant creation, controlled only by a human action in this pass (a Tenant-Admin for the tenant-wide table, the user themselves for their own row) — system-driven auto-deactivation is named as a deliberately unresolved Open Question, not designed, since no quota-consumption-tracking mechanism exists yet to trigger one. Live credential validation ("test the key") was explicitly scoped out at Menno's own direction (confirmed via direct code research: none of the four real connectors has any existing lightweight validation call; building one means new, real, cost-incurring provider API calls) — named as a deferred future gap, not designed here. **Platform-wide connector availability (staged/canary rollout)** was also raised and confirmed, directly with Menno, not to be a current need — this project has no live tenants under real load yet — and named with a concrete future trigger (a new connector needing genuine gradual rollout to validate real-world performance once real tenants exist) rather than designed now, per the same "don't build ahead of a demonstrated need" discipline ADR-0020 already established. The retryable-conflation fix itself is **not** part of this ADR's own Decision — it's addressed via dated Clarification notes appended directly to ADR-0010 and ADR-0023 (its rate-relative math needs the same fix), since it corrects an implementation gap against already-Accepted policy rather than deciding anything new. Four more short, dated, additive notes were also appended, none editing any Accepted ADR's own Decision/Consequences text: ADR-0009 (activation is a separate concept from `ConnectorHealth`'s derivation), ADR-0022 (the 60s health cache will need to combine with, not include, activation state), and ADR-0024 (an Amendment Log entry, using that ADR's own "adjustable implementation default" carve-out, clarifying `authMode: 'none'` governs credential requirements only, never automatic activation). Left **Proposed**, per this persona's own charter boundary — Menno (Sponsor) reviews and accepts separately, then revised in place seven times during live review before acceptance (see the ADR's own Amendment Log for the full record — the two-table redesign, the platform-wide-availability axis, lazy row-creation semantics, pre-activation impact estimation, the scope-independence invariant, activation-timing semantics, and finally Open Question 8 elaborated with the concrete watchlist/post-volume shape Menno named at review). **Accepted by Menno 2026-08-12**, verbatim: *"This scoped ADR is approved and perfectly alligns with the inteded connector related activation. Approved. Great piece of wor."* Accepted as revised, all seven pre-acceptance revisions in effect — see the ADR's own Status line and Acceptance note. **Story 1.11** (`docs/user-stories/epic-1-repository-and-api-foundation.md`) is added at this acceptance, per this series' own "no story until acceptance" precedent (ADR-0024/0026) — covers the backend schema (Decision §1) and REST surface (Decision §3/§4/§5) only; the admin-UI wiring onto the existing Story 6.3/6.5 screens is deliberately left as separate future work, per the ADR's own Negative Consequences bullet naming that UI change, not bundled into Story 1.11 speculatively.

²² ADR-0052 is a genuine 52nd ADR, drafted 2026-08-13 by the AI Business & Requirements Analyst persona, at Menno's direct request for either an existing-ADR citation approving a real, live ingestion-polling scheduler or a new ADR if none actually approves it. Investigated and confirmed independently, not taken on trust: `social-listening-core/src/http/server.ts` was read in full and contains no scheduling loop of any kind; a repo-wide grep of every call site of `pollGNewsSearch`/`pollNewswireFeeds`/`pollTenantOwnedFeed` found each invoked only by its own Jest contract test. This matches, and is the third independent restatement of, a gap ADR-0043 §3 and ADR-0051 Open Question 1 each already named and explicitly declined to design, plus a third dated note in `docs/implementation-plan.md`'s own 2026-08-12 Story 1.11/1.12 batch — no existing ADR approves or designs this; ADR-0022's `pg_cron` decision is scoped to a categorically different, in-database-only job (`AuthorTopicSignal` refresh) and is argued explicitly, not assumed, not to generalize to a job requiring outbound HTTP, Key Vault credential decryption, and in-process `RequestGate` state. Two further, previously-unnamed preconditions were found while drafting and folded into this same ADR's own Decision rather than left implicit: the shared connector registry (`registry.ts`) is populated only by contract tests, never by the real running server (confirmed via a repo-wide call-site grep, with a named live side effect — `connectorsRouter.ts`'s `authModeForbidsUserScope()` silently never matches in production today as a result); and `SocialConnector` has no generic, registry-visible poll-invocation method, meaning a naively-hardcoded scheduler would violate ADR-0048's already-Accepted "no core pipeline change for new connector registration" policy. Decides: an in-process `setInterval` loop in `social-listening-core` (mechanism argued against ADR-0022's `pg_cron` precedent and against a separate scheduling service, both explicitly); a required connector-registry bootstrap at server startup; a new optional `SocialConnector.poll()` method so future connectors need no scheduler edit; enumeration via already-existing `listTenants()` (Story 5.12) × `listSocialConnectors()` × the already-built-for-this-purpose `shouldAttemptIngestion()` (`connectorHealth.ts`, whose own doc comment already names a future scheduler as its intended caller); cadence derived from `ingestion_runs` history rather than any new stored field, extending ADR-0009's/ADR-0022's own "derived, not stored" discipline; and — mirroring ADR-0020's own precedent exactly — no distributed/multi-instance coordination built now, deferred to an actual second concurrent instance the same way ADR-0020's Redis-backed `RequestGate` state already is. Scoped deliberately to tenant-wide (`ownerType: 'tenant'`) poll connectors only; Tier-3/user-bound poll scheduling is named, not designed, since no real Tier-3 poll connector exists yet. Also names, but does not close, two further real gaps found while drafting: no cost/quota budget ceiling exists for continuous polling (flagged for Menno directly, not fabricated a number), and `pollGNewsSearch()`'s hardcoded `'technology'` query default (not yet wired to actual tenant watchlist terms) will become newly visible once polling actually runs, but is out of this "when/how to trigger polling" ADR's own scope. Left **Proposed**, per this persona's own charter boundary — Menno (Sponsor) reviews and accepts separately — then revised in place four times during live review before acceptance (see the ADR's own Amendment Log for the full record): Decision §5's eligibility comparison stated explicitly, with a matching `pollCadenceMs?: number` member added to Decision §4's `SocialConnector` extension; Decision §8 gained the explicit inverse of the scheduler's own catch obligation — a `poll()` implementation must let `ClassifiableError` propagate uncaught, never swallow it locally; Decision §5 gained a deterministic ±5% per-`(tenantId, platformId)` `jitterFraction`, closing a real thundering-herd risk on server restart / across tenants with similar poll history, with the stale "no jitter is designed here" Negative Consequences bullet corrected in place to describe only the smaller residual risk left open; and Decision §6 now names, without designing, the expected future Tier-3 shape (`poll(tenantId, userId)`, `RequestGate` key `(tenantId, userId, providerId)`, verified directly against `requestGate.ts` before being written). **Accepted by Menno 2026-08-13**, verbatim: *"Excellent ADR very strong seems to get better each time we create a new ADR. The ADR 0052 is hereby Approved!!"* Accepted as revised, all four pre-acceptance revisions in effect — see the ADR's own Status line and Acceptance note. **Story 1.13** (`docs/user-stories/epic-1-repository-and-api-foundation.md`) is added at this acceptance, per this series' own "no story until acceptance" precedent (ADR-0024/0026) — registry bootstrap, the generic `poll()`/`pollCadenceMs` extension, the in-process interval loop itself, and the derived-cadence-plus-jitter eligibility formula, all traced to Decision §1–9.

²³ ADR-0053 is a genuine 53rd ADR, originated by Menno directly this session while debugging degraded AI enrichment quality. Verified directly, not assumed: `rssFeedParser.ts`'s and `feedItemParser.ts`'s own parser types never captured a body/description field at all (only `guid`/`link`/`title`/`pubDate`/`issuer` and the Atom equivalent), so `pollNewswireFeeds.ts`/`pollTenantOwnedFeed.ts` enriched on `item.title` alone — a real, live root cause, not assumed from the complaint. A second, previously-unnamed finding surfaced while investigating: because `raw_payload` for these two connectors spreads the already-parsed object (`{ providerId, externalId, ...item }`), the missing fields were never archived either, in tension with ADR-0018's own "full original platform JSON, never discarded" framing. Decides: a new `body_markdown` column, `enrichment`-shaped (computed once at ingestion, persisted) rather than `ConnectorHealth`-shaped (derived fresh at read time) — argued explicitly against ADR-0009's own precedent, and independently required by ADR-0018's own 90-day `raw_payload` archival tiering, which would silently break a read-time-derivation design for any post older than the hot-storage window; a shared `htmlToMarkdown()` utility (`sanitize-html`→`turndown`, later extended with `turndown-plugin-gfm` for GFM table support), version-pinned exactly rather than left to implicit library defaults; both RSS/Atom parsers widened to capture `description`/`content:encoded`/`summary`/`content` and (added during review) each item's entire raw XML block (`rawXml`), closing the `raw_payload` gap completely rather than only narrowing it; GNews's own `content` field added to enrichment input, with its free-tier truncation caveat named plainly (`docs.gnews.io`, fetched live); a uniform `enrichmentText` composition rule (`[title, body_markdown].filter(Boolean).join('. ')`) across all three connectors; and a `body_markdown_version` column (added during review) recording which pipeline configuration produced a given row. Microsoft's own `MarkItDown` was raised directly by Menno and researched live — real, official, but Python and whole-document-scoped; rejected for this narrow HTML-fragment use in favor of `turndown`, the same "smallest mechanism for the actual requirement" standard ADR-0052 §1 already established. Revised in place **twelve times** during live review before acceptance — among them: a required pre-conversion `sanitize-html` pass, argued directly from `turndown`'s own `SECURITY.md`; the exact `allowedTags`/`nonTextTags` values enumerated literally rather than inherited from library defaults; a `MAX_BODY_SOURCE_LENGTH` pre-conversion length guard (parser-exposure and enrichment-cost reasons, distinct from ADR-0052's own still-open aggregate cost-ceiling question); an `exclusiveFilter` fix for tracking-parameter stripping that would otherwise leave dead, bare-domain links (the Mailchimp `mc_cid`/`mc_eid` case); a documented (not fixed, per direct instruction) `enrichPost()`/`insertSocialPost()` write-ordering exposure, pre-existing and not worsened by this ADR; `turndown-plugin-gfm` for GFM tables, closing a real data-loss gap the `allowedTags` decision had itself newly exposed, with an honest correction that the plugin is real and under the right GitHub org but not actively maintained since 2018/2023; a GNews truncation-marker regex trim, with its exact literal format honestly left unconfirmed (Open Question 11); and, following a critical review of an independent Copilot review of this same ADR (six of its eight findings rejected or downgraded, with reasoning recorded in the Amendment Log), `body_markdown_version` and `rawXml` accepted as the two findings that held up. **Accepted by Menno 2026-08-13**, verbatim: *"The last finding where sufficient I would like to approve ADR 0053."* Accepted as revised, all twelve pre-acceptance revisions in effect — see the ADR's own Status line, Acceptance note, and Amendment Log. **Story 3.10** (`docs/user-stories/epic-3-data-model-storage-and-archival.md`) is added at this acceptance, per this series' own "no story until acceptance" precedent (ADR-0024/0026) — Epic 3 chosen since this ADR's primary deliverable is a new `social_posts` schema addition, the same shape as Story 3.9's own precedent.

²⁴ ADR-0054 is a genuine 54th ADR, drafted 2026-08-17 by the AI Business & Requirements Analyst persona, at Menno's own direct request to bring a Tenant-facing Analytics Dashboard into formal scope for `social-listening-admin` — previously named as explicitly out of scope by `docs/design/frontend-design-specification.md` §10 ("Rich Analytics & Charting Dashboards: deferred to dedicated analytics/reporting subsystems (Design Spec §9, ADR-0008)"). Verified directly before drafting, not assumed from the request: five untracked, unwired prototype components already existed at `social-listening-admin/src/app/tenant/analytics/`, none of which compile as-is (they import a `FlatPost` type from a `./types` module that doesn't exist) and several of which mix genuinely-computed real-data derivations with fabricated static fallback data presented with no visual distinction — most notably a silent fallback to a hardcoded name list when a real "Top Fans/Critics" computation yields too few results, and two entire widgets (Intentions, Tags) with no corresponding field anywhere in this project's real `enrichment` schema. Also verified directly: `social_posts.post_geo_location` exists as a real column but is populated by none of this project's three real connectors, and — independently and more decisively — is not even included in `SocialPostSummary`, the shape `GET /v1/posts` actually returns, meaning the reference design's Location tab is not shippable in v1 by two separate, both-disqualifying findings, not merely one. Decides: a four-tab v1 (Overview, Sentiment, Conversations, Sources — no Location), computed entirely client-side from data `GET /v1/posts`/`enrichment` already return with zero new `social-listening-core` work; a narrow, scoped supersession of ADR-0008's "any charting UI" clause specifically bounded to that client-side scope, while ADR-0008's "no `TopicDailyCount` table/endpoint" clause is fully reaffirmed and untouched; and explicit exclusion of five speculative brainstorm ideas (`docs/design/frontend-design-future-devs.md` — AI "explain the spike" narrative panels, predictive forecasting, a D3 topic-cluster graph, a real-time pulse-map, and 1-click PDF export/automated workflows) from any decided or implied scope. A dated "Pending supersession note" was added to ADR-0008's own Open Questions section accordingly (not editing its Decision/Consequences text), taking effect only if and when this ADR is accepted. Drafted **Proposed**, per this persona's own charter boundary — Menno (Sponsor) reviews and accepts separately. Unlike this series' usual "no story until acceptance" precedent (ADR-0024/0026), a new **Epic 8** (`docs/user-stories/epic-8-analytics-dashboard.md`, Stories 8.1–8.3) was drafted alongside this ADR at Menno's own explicit instruction for this specific task, each story originally **Blocked — pending ADR-0054 acceptance**, the same convention every other ADR-sourced Blocked story in this series follows. **Accepted by Menno 2026-08-17**, via a structured approval decision in the orchestrating session, not a freeform verbatim message — the orchestrating assistant summarized this ADR's scope, the ADR-0008 relationship, and the Location-tab finding, and Menno selected "Approve as summarized"; named honestly here per this persona's own house discipline against fabricating a quote in his own words. Accepted as drafted, no revisions — see the ADR's own Status line and Acceptance note. `docs/adr/0008-defer-topic-time-series-and-charting.md`'s own 2026-08-17 "Pending supersession note" gains its matching "Supersession update" note at this same acceptance, per this file's own governance-table convention — ADR-0008's Decision/Consequences text is untouched. **Story 8.1, Story 8.2, and Story 8.3 all move from Blocked to Ready** — see `docs/user-stories/README.md`'s own dated note and each story's updated Source/Status line in `epic-8-analytics-dashboard.md`. **Story 8.4 added 2026-08-17, later still the same day**, after a retrospective comparison against the Google AI Studio design reference found Overview enrichment (a real volume chart, a real sentiment donut) and the date picker's already-wired-but-ignored `compareWithPrevious` toggle both closeable inside this ADR's own already-Accepted Decision §2/§3 scope — no new ADR needed. See `epic-8-analytics-dashboard.md`'s own dated note and Story 8.4's entry. **Story 8.6 added the same day still**, closing the same review's largest remaining named gap — a per-source sentiment score and volume-over-time chart on the Sources tab, again fully inside this ADR's own already-Accepted Decision §2 scope.

²⁵ ADR-0055 is a genuine 55th ADR, drafted 2026-08-17 by the AI Business & Requirements Analyst persona, the same day as ADR-0054, at Menno's own direct request after a structured comparison between the shipped Analytics Dashboard and the earlier Google AI Studio design reference found two categories of widget "NOT BUILDABLE" for lack of a real field: a Languages breakdown and everything Location-related. Menno's own words, requesting this ADR directly: *"could we add an ADR to build the missing fields in the backend the language and the revisit of the location as it brings many graphical datapoints in the dashboards?"* Investigated both independently rather than forcing one verdict. **Language: a genuine, previously-unresearched gap, found to already be closed at the data layer.** Verified directly: both real `AIProviderConnector`s (`azureAiLanguageConnector.ts`, Story 2.8; `azureOpenAiConnector.ts`, Story 2.9) already compute `detectedLanguage` on every `analyze()` call — Azure AI Language via a fourth, already-being-made `LanguageDetection` capability call; Azure OpenAI via its own structured-output schema — and `AnalyzeResult` already declares the field as part of the shared contract both connectors implement. That value already reaches `social_posts.enrichment` (an unfiltered JSONB blob) on every real ingest path and is already returned unfiltered by `SocialPostSummary`/`GET /v1/posts`, the exact data source Epic 8's client-side aggregation already reads — confirmed the only reason it isn't visible today is that `social-listening-admin/src/app/tenant/posts/postDisplay.ts`'s `PostEnrichmentSummary`/`extractEnrichmentSummary()` never reads it, a client-side surfacing gap, not a missing backend capability. Decides: widen `PostEnrichmentSummary` to include `language`, and add a Languages breakdown widget to Epic 8, with **zero `social-listening-core` change of any kind** — no migration, no connector change, no endpoint. **Location: re-investigated independently rather than assumed still correct, reaching the same non-buildable verdict ADR-0054 already reached hours earlier the same day**, with one genuine new finding ADR-0054 didn't have: `GNewsArticle.source.country` is a real, already-captured field (spread unmodified into `rawPayload`, already wire-visible), named and explicitly declined as a Location substitute — it covers only one of three real connectors and reports the publisher's declared country, not per-post or conversation geography; adopting it under a "Location" label would risk exactly the real-but-mislabeled failure mode ADR-0054's own Alternatives Considered table already rejected for fabricated data. A dated Clarification note (not a supersession — nothing about ADR-0054's Decision §4 changes) was added to ADR-0054's own Open Question 1 accordingly, per `docs/adr/README.md`'s own governance-table row 4. Drafted **Proposed**, per this persona's own charter boundary — awaiting Menno's own separate review and acceptance. **Story 8.5** (`docs/user-stories/epic-8-analytics-dashboard.md`) is drafted alongside this ADR, **Blocked — pending ADR-0055 acceptance**, matching ADR-0054/Epic 8's own precedent for this specific kind of task. No story is drafted for Location — ADR-0054's own "Not storied in this epic: Location tab" precedent stands unchanged. **Accepted by Menno 2026-08-17, later the same day**, via a structured approval decision in the orchestrating session — Menno replied "yes please extend the language field," approving the Language half directly; **Story 8.5 moves from Blocked to Ready** and is built the same day (`social-listening-admin@8b8bb14`). In the same reply, Menno raised a new, related feasibility question the ADR's own Decision text doesn't cover — see the ADR's own new Amendment Log entry and `docs/implementation-log.md`'s matching dated entry for the live PRNewswire/GlobeNewswire verification this prompted.

²⁶ ADR-0056 is a genuine 56th ADR, drafted 2026-08-17 by the AI Business & Requirements Analyst persona, later still the same day as ADR-0055, per ADR-0055's own same-day Amendment Log entry naming this exact question as "tracked as a new candidate ADR, not yet drafted." Investigates whether Azure OpenAI's generative completion model (`azureOpenAiConnector.ts`, explicitly not Azure AI Language's structured NER capability) can infer a Newswire post's likely origin location from its own already-ingested wire-service "dateline" text. Independently re-verified (not merely re-cited) the orchestrating session's own prior live-`curl` finding: `WebFetch` against PR Newswire's real, current RSS feed confirmed the dateline premise, surfacing a genuine refinement the original check didn't have — the dateline is not always the literal first line of body content, so any extraction must search the text rather than anchor at position zero; three further `WebFetch` attempts against GlobeNewswire's real feed all timed out, honestly reported as an incomplete independent re-verification rather than silently dropped. Confirmed directly against `enrichPost.ts` that this codebase's enrichment pipeline has zero connector-awareness today — all four real call sites invoke it identically — so any connector-gated design is a genuinely new pattern, named as such rather than understated. Decides, if ever built: a new, optional `AIProviderConnector.inferOriginLocation?()` method (Azure OpenAI only), invoked conditionally only from the Newswire ingest loop, gated by a structural dateline pre-check rather than a hardcoded per-wire allowlist — never added to the shared, every-call `ENRICHMENT_SCHEMA`, to avoid the wasted-cost outcome of requesting it on every GNews/tenant-owned-feed post. Confirms directly against `socialPostStore.ts` that storing the result requires **zero migration and zero `SocialPostSummary` widening** — materially easier than ADR-0054's own `post_geo_location` blocker, since `enrichment` is already an unfiltered JSONB blob already fully included in `SocialPostSummary`. **Recommends naming this design, not building it now** — no committed Location-tab consumer exists yet, applying the identical "don't build ahead of a demonstrated need" discipline ADR-0054/ADR-0055 already applied to their own declined, structurally-easier options. Dated Clarification notes (not supersessions) added to ADR-0054's own Open Question 1 and ADR-0055's own Amendment Log, per `docs/adr/README.md`'s own governance-table row 4 — neither ADR's Decision/Consequences text changes. Drafted **Proposed**, per this persona's own charter boundary — Menno (Sponsor) reviews and accepts separately. **No story is drafted** — matching ADR-0054's own precedent for its Location tab (a "name it, don't build it" verdict has nothing yet for a contract test to target). **Accepted by Menno 2026-08-17, later still the same day**, verbatim: *"ADR 0056 is approved as well."* Accepted as drafted, no revisions — Decision §5's own "name it, don't build it now" recommendation stands as accepted policy. Still no story drafted.

²⁷ ADR-0057 is a genuine 57th ADR, drafted 2026-08-17, requested directly by Menno ("what needs to change to enable the feeds to be administered?", then "let's build the new ADR") after a direct investigation of the tenant-owned-feed connector's current admin surface. Resolves ADR-0050's own Open Question 2, left open since that ADR's 2026-08-11 acceptance. Confirmed directly, not assumed: `migrations/0026_create_tenant_owned_feed_activations.sql`'s own header comment already states the storage model supports multiple feeds per tenant (no uniqueness constraint on domain/feedUrl), and both `getVerifiedActivations()` and `pollTenantOwnedFeed.ts` already iterate every verified row for a tenant, not one — the poller has supported multiple feeds since Story 1.13's scheduler first called it. Nothing above the storage layer exposes this: `tenantOwnedFeedRouter.ts` has exactly two routes (`connect`, `verify-domain`), no list/edit/delete; `TenantOwnedFeedSetup.tsx` is a single-activation client state machine with no path to a second feed once one is verified — both gaps this component's own `SKILL.md` already names under "Known gaps." A further, previously-unflagged finding surfaced while reading the router for this ADR's own purposes: `connect`/`verify-domain` authorize via bare `requireTenantUser()` with no role check at all, unlike every other tenant-wide connector action in this codebase (`connectorsRouter.ts`'s connect/disconnect/activate/deactivate, all `tenant_admin`-only) — named and corrected in this same ADR's Decision, not treated as a separate issue. Decides: three new `tenant_admin`-only REST endpoints — `GET .../activations` (list, closing the SKILL.md-named "TXT instructions lost on reload" gap as a side effect), `PATCH /:id` (`feedUrl` only — `domain` is never editable in place, since that is the exact claim DNS TXT verification proves, and an in-place domain change would silently invalidate a completed verification), and `DELETE /:id` (soft-removal via a new `removed` status value, not a hard SQL `DELETE` — the table has no `DELETE` grant today, and a hard delete would erase the domain-verification audit trail this project consistently prefers to keep, the same "never discarded"/reversible-in-spirit framing already established for `raw_payload`, `accessEndsAt`, and tenant offboarding). No cap on feed count per tenant — left unbounded, since no real, demonstrated constraint backs a specific number (unlike the license-seat ceiling, a real ADR-0032 business rule) — named as an Open Question, not solved. No token-regenerate/retry endpoint for an expired `pending` activation — remove-and-reconnect is the smallest mechanism that closes the loop. Explicitly out of scope: Newswire's own hardcoded, non-tenant-configurable feed set; Platform-Admin cross-tenant feed visibility (confirmed a real, separate gap — zero references anywhere in the Platform-Admin console today — named as a candidate future ADR, not folded in here). Drafted **Proposed**, per this persona's own charter boundary — Menno (Sponsor) reviews and accepts separately. **No story is drafted** — will be added at acceptance, per the ADR-0024/0026 "no story until acceptance" precedent. **Revised in place the same day against a four-point external review** (see the ADR's own Amendment Log) — most substantively, a new Decision §1a auto-verifying a second feed registered on an already-verified domain, skipping redundant DNS re-proof of ownership already established. **Accepted by Menno 2026-08-17, later still the same day**, via a structured approval decision disambiguating it from ADR-0056, also under review at the same time. Accepted as revised, all four review-pass amendments in effect. **Story 6.20** added to `docs/user-stories/epic-6-tenant-admin-ui.md`. **Revised in place the same day** against a four-point external review Menno relayed, each independently checked against the codebase rather than accepted on trust: exact `DROP`/`ADD CONSTRAINT` syntax now spelled out (PostgreSQL can't alter a `CHECK` constraint in place); a new Decision §1a (registering a second feed on an already-`verified` domain now auto-verifies server-side, skipping redundant DNS re-proof — a genuine friction fix, not just a clarification); a `listActivations()`-must-stay-unfiltered guard-rail note (checked directly — no other consumer of this table exists yet to actually miscount a `removed` row); and explicit Admin UI copy distinguishing per-feed verification status from the separate tenant-wide activation switch. See the ADR's own Amendment Log for the full per-point disposition.

²⁸ ADR-0058 is a genuine 58th ADR, drafted 2026-08-17, found live while scoping Story 2.13 (Wikipedia connector) — Menno flagged directly that Service Bus needed to be "hooked up... for the functionality to work appropriately." Investigated directly, not assumed: a repo-wide grep of `src/connectors/`, `src/posts/`, and `src/ingestion/` found zero real callers of `publishEvent()` — the mechanism itself (`serviceBusPublisher.ts`) is real and contract-tested (Stories 5.1/5.2/5.5), but no real connector calls it, matching `ingestion-events/SKILL.md`'s own already-honest "Known gaps" admission. Investigation surfaced the gap is deeper than "add one function call": `SocialPostIngestedEvent.watchlistId` is required/non-nullable but no real ingestion path has ever run watchlist matching before publishing (Story 5.1's own contract only ever fakes it via a fixture literal); `listWatchlists()` is owner-scoped (`tenantId, userId`), with no existing tenant-wide equivalent ingestion-time matching needs; and `ConnectorHealthChangedEvent` has no stored "previous status" anywhere to diff against, since `deriveConnectorHealth()` (ADR-0009) is fully derived at read time. Decides: `SocialPostIngestedEvent` published per-connector, from each connector's own ingest loop, one event per matching watchlist (reusing `resolveWatchlistAstDispatch()`/`matchPostsForWatchlistAst()`, `dispatch.ts`, unmodified); a new tenant-wide `listActiveWatchlistsForTenant(tenantId)` store function, deliberately kept separate from the existing owner-scoped `listWatchlists()` rather than overloading it; `ConnectorHealthChangedEvent` published from inside the shared `runIngestionAttempt()` itself (before/after health snapshot, diff, publish if changed) — zero per-connector wiring, automatic for every connector present and future; both publish call sites best-effort, catch-and-log, matching `enrichPost()`'s own "never blocks ingestion" precedent (ADR-0038); scope covers all three existing connectors (GNews, Newswire, tenant-owned-feed) in the same pass, with Wikipedia (Story 2.13, paused pending this ADR) joining once it resumes. Drafted **Proposed**, per this persona's own charter boundary — Menno (Sponsor) reviews and accepts separately. **Revised in place the same day, before acceptance:** Menno reviewed and added three concrete implementation safeguards as a new Decision §6 — watchlists fetched once per poll batch, not per post (closing a real N+1 hazard the original draft's Consequences had only named); `publishEvent()` fires only after the post-insertion transaction has actually committed, never from inside it (preventing a read-your-own-write race against ADR-0012's own "fetch on demand" assumption); and Service Bus message-throughput from multi-watchlist-match fan-out must be watched during this story's own staging validation, even though no throttling is built for v1. **Accepted by Menno 2026-08-17**, verbatim final verdict: *"Approve. This ADR is a model of thorough architectural investigation and decisively closes a major lingering gap in the event-driven architecture. Proceed to Story creation."* Accepted as revised (Decision §6 in effect). **Story 5.19** (`docs/user-stories/epic-5-security-isolation-and-messaging.md`) is added at this acceptance, per this series' own "no story until acceptance" precedent (ADR-0024/0026) — Epic 5 chosen since ADR-0012/0013/0019 (the event-shape/transport ADRs this one wires up for real) already live there, the same epic-placement logic Story 5.11 used for `GET /v1/me`.

²⁹ ADR-0059 is a genuine 59th ADR, drafted 2026-08-18 by the AI Business & Requirements Analyst persona to resolve `docs/open-decisions.md`'s 2026-08-06 Meta bullet, which had left two propositions unequally verified: Standard Access for a tenant's own Page ("confirmed easy, free, no-App-Review") and genuine public-content social listening ("not yet confirmed either way"). Verified directly against `developers.facebook.com` (Page Public Content Access / Page Public Metadata Access feature pages, Permissions Reference, Business Verification requirements, Webhooks for Pages, Access Token Guide, Graph API Rate Limiting overview) and `developers.facebook.com/terms` (Meta Platform Terms), not taken on secondhand summary, per this project's own established connector-ADR bar (ADR-0024, ADR-0026, ADR-0042). Two real findings resulted: (1) genuine social listening — discovering public posts from accounts/Pages a tenant doesn't own — is not buildable today; no keyword-search/discovery surface exists on the current Graph API at all, independent of approval odds, corroborated (secondary-sourced) by Meta's own 2024 shutdown of CrowdTangle and its pointer toward established paid vendors rather than ordinary self-service; (2) even the narrower, verified-viable alternative (a tenant's own connected Page's own posts) requires a real, one-time, project-level Meta App Review + Business Verification gate for SocialEngage's own registered App before any real (non-tester) tenant can use it — correcting `docs/open-decisions.md`'s own prior "no-App-Review" framing, which held only for the degenerate case of a Page administered by the app's own developers/testers. Recommends the narrower scope (tenant's own Page, posts only at v1; comment/mention ingestion explicitly deferred over a real, unresolved third-party personal-data/author-rights question, per this role's own standing author-rights check) rather than forcing an unqualified "Facebook connector" through — the same "real access friction found, scope down rather than force it through" discipline as ADR-0024's RTPR reversal and ADR-0042's Reddit de-prioritization. Confirms the Facebook Page as a real, fourth instance of ADR-0004's already-generalized organization-as-Author clause (added at ADR-0050's 2026-08-11 acceptance) — no new Pending-supersession note needed. Includes explicit "Note on relation to ADR-0027" and "Note on relation to ADR-0028" sections: the OAuth model does not make SocialEngage a party to any tenant's own relationship with Meta (each Page token stays the connecting individual's own credential), but SocialEngage's own App-level Business Verification/App Review requirement is named as a real, first-of-its-kind degree of project-level commitment no prior connector required — not a violation of ADR-0027, but not identical to GNews's/Newswire's/Wikipedia's zero-SocialEngage-involvement shape either. `docs/open-decisions.md`'s Meta bullet updated in place with a dated note pointing here. **Revised twice in place during review, before acceptance:** first, Menno directly instructed the credential tier be restricted to Tier 3 (user-bound) only, no Tier 2 tenant-wide path — verbatim, "only the user accounts please" — checked against Meta's own OAuth grant mechanics and found to hold independently (Meta provides no app-only/organizational grant for an ordinary Page-connect flow); second, three further review-round additions (App Review discretionary-rejection risk for a solo-developer project; a hard "Facebook Page (Owned Feed)" UI-naming constraint, not just a soft recommendation; a distinctly-labeled "reconnect required" connector-health state for credential-invalidation triggers). The connector's content scope (the Page's own posts, not an individual's personal profile) is explicitly unchanged by either revision. See the ADR's own Amendment Log for the full record. **Accepted by Menno the same day, verbatim: "Approved ADR 0059 Facebook Connector is approved."** Accepted as revised — both review rounds above are in effect. **Story 2.15** (`docs/user-stories/epic-2-ingestion-connectors-and-rate-limits.md`) added at this acceptance, per this series' own "no story until acceptance" precedent (ADR-0024/0026/0042) — the Business Verification/App Review preconditions named in Decision §3 remain live, unresolved implementation-time risks, carried into that story rather than dropped.

³⁰ ADR-0060 is a genuine 60th ADR, drafted 2026-08-18, later the same day as ADR-0059's own acceptance, by the AI Business & Requirements Analyst persona, requested directly by Menno ("could you please draft the ADR for multiple Facebook Pages") after live-testing Story 6.23's real Page picker against the live Meta Graph API and finding it impractical at his own scale: the `/me/accounts` picker correctly returned every real Facebook Page he personally administers (a long list spanning several organizations and cities), but the picker is single-select and the tenant's real license seat count (max 5) makes "have a different team member connect each Page" — the only path Story 6.23 supports — impractical when one person alone administers far more Pages than there are seats to spread them across. Sourced from Story 6.27 (`docs/user-stories/epic-6-tenant-admin-ui.md`), a deliberately light stub added the same day naming the real open questions without answering them. Investigated directly, not assumed from the stub's own text: `credentialStore.ts` (`storeCredential()` has no uniqueness constraint blocking more than one row per `(tenantId, platformId, ownerType, userId)`, but `getLatestCredentialId()`'s `ORDER BY created_at DESC LIMIT 1` and `deleteCredential()`'s tuple-wide `DELETE` both structurally assume at most one live row — the latter confirmed as a real, latent, not-yet-live gap: disconnecting one Page today would delete every credential row for that user+platform, though no UI path currently reaches this call for Facebook), `pollFacebook.ts` (resolves and polls exactly one credential per call, no loop over Pages), `connectorHealth.ts` and `ingestion_runs`' real schema (`deriveConnectorHealth()` derives health purely from `WHERE platform_id = $1`, with no `user_id` or Page-level column anywhere — a pre-existing cross-user health-blending gap this ADR's own multi-Page ask makes materially worse, not one it originates), and `ConnectorsClient.tsx`'s real `FacebookPagePickerModal` (confirmed single-select, one radio group, one `selectedId`). Explicitly evaluated whether ADR-0057's own tenant-owned-feed multi-feed precedent transfers: its list/soft-remove endpoint and per-row-list UI shape does, but its `tenant_admin`-only authorization gate does not — argued explicitly, since this connector's Pages are Tier-3, per-user resources under ADR-0028 §3's own rule that no one may act on another user's user-bound credential on their behalf, so the new endpoints authorize on the caller's own `userId` instead. Decides: a new child table (`facebook_connected_pages`, referencing `platform_credentials` by id) over a `platform_credentials` discriminator column or a rework of its existing exported functions, reasoned explicitly (keeps a Facebook-specific concept off a shared, provider-agnostic table; avoids decrypting every credential row just to list Page names); one new, narrowly additive `deleteCredentialById()` function; `pollFacebook()` becomes a per-Page fan-out with one independent `IngestionRun` per connected Page (one Page's failure must not block another's attempt — the direct, load-bearing consequence of the actual business need); a new nullable `page_id` column on `ingestion_runs`, following the exact additive-column precedent `is_credential_failure` (migration `0032`) already established; `deriveConnectorHealth()` gains a new, optional, backward-compatible `pageId` parameter, recommended as genuinely Page-aware rather than left platform-level — the platform-level-only alternative is named explicitly as defeating the feature's own purpose (a tenant can't tell which of several Pages needs reconnecting) rather than dismissed as a mere trade-off; two new REST endpoints (`GET`/`DELETE .../facebook/pages[/:id]`), both authorized on the caller's own resolved `userId`; a revised, breaking (no external caller, so acceptable) plural `/select-page` request shape; and Admin UI follow-on scope (multi-select picker, a real per-Page connected list mirroring ADR-0057 Decision §6's own shape, a redesigned card-level reconnect action) named as necessary but explicitly left for Story 6.27's own Acceptance Criteria, not designed here. Explicitly confirms ADR-0059 Decision §4's Tier-3-only, no-Tier-2 credential-tier finding is unaffected — a cardinality question, not a credential-ownership one — via a dedicated "Note on relation to ADR-0059" section, the same category ADR-0059 itself used for its own "Note on relation to ADR-0027"/"Note on relation to ADR-0028" sections. Applies this role's own standing author-rights check explicitly and finds nothing new: organization-as-Author stays per-Page, never merged or reassigned across a user's several connected Pages, unaffected by cardinality. Real, named, unresolved rather than solved: `RequestGate`'s `(tenantId, providerId)` keying (ADR-0003) doesn't account for the new per-Page/per-user call-volume fan-out; the real per-Page Engaged-Users rate ceiling (ADR-0059's own already-named gap) is sharpened, not closed, by this ADR; a two-different-users-connecting-the-same-underlying-Page health-blending edge case is named, not resolved. Drafted **Proposed**, per this persona's own charter boundary — Menno (Sponsor) reviews and accepts separately. **Revised in place the same day, before acceptance, against a four-point external review Menno relayed** (see the ADR's own Amendment Log for full dispositions): accepted — a re-consent call that no longer returns a previously-connected Page now auto-transitions that row to a new `orphaned` status; a structured `{ connected, errors }` partial-failure response for the plural `/select-page` endpoint; the per-Page poll loop now runs explicitly sequentially, reusing `RequestGate`'s already-confirmed existing per-key serialization (`requestGate.ts`) as a concrete, cost-free rate-limiting mitigation; `GET .../pages` now returns a server-computed `parentConnectionActive` boolean. Rejected — a single Page's credential failure cascading to flag every one of a user's Pages, checked against ADR-0059's own Access Token Guide finding and against this ADR's own per-Page-independent design and found unsupported. Flagged, not adopted as fact — the review's specific "Meta Error Code 4" claim, not primary-source-verified this session. **Accepted by Menno 2026-08-18, later still the same day**, verbatim: *"yes and i can now Approve the ADR 00060"*. Accepted as revised — the four-point amendment round above is in effect. **Story 6.27** (`docs/user-stories/epic-6-tenant-admin-ui.md`) is expanded from its prior light stub into a real, buildable story at this acceptance, per this series' own "no story until acceptance" precedent (ADR-0024/0026/0057) — moves to **Ready**. The Open Questions named above (`RequestGate` re-keying, the two-users-same-Page health-blending edge case, `GET .../pages` pagination, the unverified "Meta Error Code 4" claim) remain live, unresolved risks, carried into Story 6.27's own out-of-scope note rather than silently dropped.

³¹ ADR-0061 is a genuine 61st ADR, drafted 2026-08-18, later still the same day, by the AI Business & Requirements Analyst persona, per Menno's own confirmed direction (a structured `AskUserQuestion` choice, not a freeform quote) that the Tier-3 poll scheduler gap gets its own ADR and story, kept separate from ADR-0060/Story 6.27's own scope. Resolves ADR-0052 Decision §6/Open Question 2, which explicitly named a future `poll(tenantId, userId)`/`RequestGate` `(tenantId, userId, providerId)` shape without designing it, deferred pending a real Tier-3 poll connector existing — ADR-0060 gave Facebook one, but a repo-wide grep confirmed `pollFacebook()` has zero real call sites anywhere in the running server (only its own definition and a doc comment in `bootstrapConnectors.ts` explaining why the scheduler's own wrapper `poll()` throws rather than ever reaching it). Investigated directly, not re-derived from ADR-0052's own naming alone: `pollScheduler.ts`'s real `runSchedulerTick()` (confirmed its in-flight guard and due-check are keyed by `(tenantId, platformId)` only), `getMostRecentRunStatus()`'s real SQL (`ingestionRunStore.ts`, confirmed no `user_id` filter or column exists), `connectorActivationStore.ts` (confirmed no function enumerates every user with an active Tier-3 activation for a platform), `types.ts`'s real `SocialConnector` interface, `requestGate.ts`'s real `socialConnectorKey`, and `bootstrapConnectors.ts`'s real Facebook registration. **Confirms Menno's own suspected in-flight-guard/multi-user bug directly against the actual query** — real, not hypothetical: two different users' own independent Facebook polls would incorrectly block/unblock each other if the tenant-wide scheduler machinery were naively extended, since nothing in `ingestion_runs` distinguishes one user's runs from another's. Two further, previously-unnamed, adjacent bugs found in the same investigation: `deriveConnectorHealth()`'s own `credentialStatus` sub-query is also unscoped by user, and `shouldAttemptIngestion()` (Story 1.11, already shipped) already accepts `ownerType`/`userId` parameters but silently ignores them in its own internal `deriveConnectorHealth()` call — a real, cross-user auto-disable false-negative bug in already-shipped code, fixed here as a direct consequence of this ADR's own design work. Decides: a new nullable `user_id` column on `ingestion_runs` (populated directly per Tier-3 connector, not via a connector-specific join, to keep the scheduler generic per ADR-0048); new `listActiveUserActivations()`/`getMostRecentRunStatusForUser()` store functions; `deriveConnectorHealth()` gains a fourth `userId?` parameter (explicitly named as a build-order coordination point against ADR-0060/Story 6.27's own already-specified third `pageId?` parameter — Story 6.27's own Acceptance Criteria are not edited, per Menno's own instruction); a new, distinct `SocialConnector.pollUser?(tenantId, userId)` method, since TypeScript cannot express two differently-shaped optional methods under one name; Facebook's throwing-placeholder `poll` retired outright, now genuinely unreachable rather than merely unlikely; a third, independent per-user enumeration level in `runSchedulerTick()`, explicitly composing with — never duplicating — ADR-0060 Decision §3's own already-Accepted per-Page fan-out; `jitterFraction()` extended with an optional `userId` segment to prevent a narrower, per-user thundering-herd risk this design would otherwise introduce. Includes a "Note on relation to ADR-0052" (clarifying that `RequestGate`'s own re-keying belongs to each Tier-3 connector's own poll-function implementation, not the scheduler, since the scheduler itself never calls `RequestGate` directly) and a "Note on relation to ADR-0060" (the scheduler triggers `pollFacebook()`; it does not redesign or duplicate its own per-Page fan-out). Drafted **Proposed**, per this persona's own charter boundary — Menno (Sponsor) reviews and accepts separately. **Story 1.15** (`docs/user-stories/epic-1-repository-and-api-foundation.md`) is drafted alongside this ADR, **Blocked — pending this ADR's acceptance**, matching Story 8.1/8.2/8.3's own original pending-ADR-0054 pattern. **Accepted by Menno the same day, verbatim: "ADR-0061 is complete, coherent, and ready for my acceptance. It cleanly bridges the gap between ADR-0060's data model and the background scheduling engine, resolving all implicit cross-user race conditions before they ever manifest in production. Excellent work and I approve ADR 0061"** — accepted as drafted, no review-round amendments (unlike ADR-0060, whose own review requested four changes, this one's review confirmed the design rather than changing it). **Story 1.15** moves from Blocked to **Ready** at this acceptance. The real build-order coordination point named in Decision §2 (`deriveConnectorHealth()`'s eventual four-parameter shape — `pageId?` reserved by ADR-0060/Story 6.27, `userId?` added by this ADR/Story 1.15) is not resolved by this acceptance, carried forward as a live coordination fact for whichever of the two stories is implemented second.

³³ ADR-0063 is a genuine 63rd ADR, drafted 2026-08-19 by the orchestrating session at Menno's direct request ("could you please write an additional ADR for the endpoint to query watchlist/topics items?"), closing ADR-0062 Open Question 1. Provides the server-side foundation that ADR-0062 Decision §3's `selectedTopic` (watchlist selector) explicitly blocked pending this ADR: a new `post_watchlist_matches` junction table (`post_id`, `watchlist_id`, `tenant_id`, `matched_at`; `UNIQUE (post_id, watchlist_id)`; RLS on `app.tenant_id`; cascade-delete on both FKs) written best-effort inside the existing `runIngestionAttempt()` path — reusing the match-result already computed for ADR-0058's `SocialPostIngestedEvent` publishing, so no second matching pass and no change to `matchesWatchlist()`/`matchesAst()`/`resolveWatchlistAstDispatch()` or any other matching logic. New optional `watchlistId` query parameter on `GET /v1/posts` (validation: UUID format → 400 `INVALID_WATCHLIST_ID`; existence/RLS → 404 `WATCHLIST_NOT_FOUND`; SQL JOIN on `post_watchlist_matches`; cursor pagination preserved; `SocialPostSummary` shape unchanged). Also names two downstream enablements for `social-listening-admin` (both in Story 8.9): the `selectedTopic` watchlist selector implemented for the first time (server-side path covering all `matchType` values including `boolean_query` watchlists) and the Watchlist Coverage PieChart donut widget (previously "Blocked / Not Built" per ADR-0062 Decision §8 — real per-watchlist post counts now available). Four explicit trade-offs named rather than silenced: staleness on watchlist update (accepted at v1); historical backfill gap (deferred, named as Open Question 1); storage cost proportional to `posts × active_watchlists_per_tenant` (Open Question 3); `GET /v1/watchlists` `postCount` field choice (Open Question 4, left to Story 8.9's judgment). Drafted **Proposed**, per this project's ADR-acceptance authority convention. **Accepted by Menno 2026-08-19**, verbatim: *"ADR-0063 cleanly solves the architectural prerequisite for watchlist filtering, bridges the gaps left in ADR-0062, and provides a clear path forward for both backend (Story 3.11) and frontend (Story 8.9) execution. You are fully cleared to accept ADR-0063 and unblock the associated stories."* **Story 3.11** (`docs/user-stories/epic-3-data-model-storage-and-archival.md`) moves from Blocked to **Ready** at this acceptance. **Story 8.9** (`docs/user-stories/epic-8-analytics-dashboard.md`) moves from "Blocked — pending ADR-0063 acceptance" to "Blocked — pending Story 3.11 implementation and Story 8.7 build" — ADR-level gate lifted, implementation gates remain.

³² ADR-0062 is a genuine 62nd ADR, drafted 2026-08-19 by the AI Business & Requirements Analyst persona, at Menno's direct request — a Frontend Specification (Sections 4–10) for a significantly richer Overview Tab than Stories 8.1/8.4 built: a 3-column responsive grid of nine named widget slots (stable `id` attributes for E2E/analytics targeting), an 11-dimension client-side filter model, active filter chips colour-coded by dimension, deep-link share state via URL query params, a statistical volume forecast (client-side `v(n) = lastVolume × 0.85ⁿ + 850 × (1 − 0.85ⁿ)` mean-reversion, zero new backend surface), a Crisis Alert Radar (client-side 48-hour negative sentiment momentum), a Sentiment Trajectory index (client-side `sentimentScores` mean), an inline-SVG Sentiment Gauge tri-arc ring (`strokeDashoffset` arithmetic), and an AI Spike Storyteller. Each specification recommendation was evaluated against the real codebase, the existing ADR corpus, and this project's "no fabricated data" discipline. **Adopted:** the 3-column grid (ADR-0062 Decision §2); seven of the spec's eleven filter dimensions — `selectedDateRange`, `activeSourceFilter`, `activeAuthorFilter`, `activeKeywordFilter`, `activeSentimentFilter`, `selectedTopic`, `activeDateFilter` (three excluded: `activeRegionFilter` per ADR-0054 Decision §4, `activeIntentionFilter`/`activeTagFilter` per no real `enrichment` field — Decision §3); active filter chips with colour-coded tokens and a "Clear all" control (Decision §3); deep-link share state (`window.history.replaceState` + `navigator.clipboard.writeText`, Decision §4); all four client-side-only widgets named above (Decision §6); Posts Drawer enhanced per spec §10 with real author initials avatars (Decision §8). **Partially supersedes ADR-0054** Decision §2 (AI Spike Storyteller — brought from the "speculative brainstorm content" exclusion list into accepted scope with a concrete backend endpoint) and Decision §3 (authorizes exactly one new `social-listening-core` endpoint: `POST /v1/posts/explain-spike`, calling the existing `azureOpenAiConnector.ts` Tier-2 credential, `requireTenantUser()` authorized — the sole new backend surface; all other ADR-0062 widgets remain 100% client-side). Dated "Pending supersession notes" appended to ADR-0054 Decision §2 and Decision §3, per this README's governance-table Row 5 — not edits to those sections' own Decision/Consequences text, taking effect only at acceptance. **Explicitly not adopted:** Location Insights widget (ADR-0054 Decision §4 both-disqualifying findings unchanged — Decision §7); Watchlist Coverage widget (spec itself says "static for now," same "no fabricated placeholder" discipline — Decision §7); D3 charting library (Recharts + inline SVG only, Decision §9); 8-platform social-media icon roster (only real three connectors: `gnews`/`newswire`/`tenant-owned-feed`, Decision §7); simulation stubs (MSE translation, team assignment, response compose — same category as `TOP_FANS`/`TOP_CRITICS` static fallback already rejected in ADR-0054 Context, Decision §8); Unsplash avatar CDN URLs (uncontracted external dependency, Decision §8). AI-backed predictive forecasting endpoint (`POST /api/predictive-forecast`) deferred (Decision §6) — the statistical forecast is the accepted alternative. Drafted **Proposed**, per this persona's own charter boundary — Menno (Sponsor) reviews and accepts separately. **Stories 8.7 and 8.8** (`docs/user-stories/epic-8-analytics-dashboard.md`) are drafted alongside this ADR, both **Blocked — pending ADR-0062 acceptance**, matching this series' own "no story until acceptance" precedent (ADR-0024/0026/0057). **Revised in place the same day, before acceptance, during Menno's own line-by-line review:** the AI Spike Storyteller's request/response contract simplified from an earlier client-supplied-`contextPosts` design to a server-derived one (`spikeDate` in, server pages `GET /v1/posts` internally); a custom-prompt re-fire and toggle-dismiss UX restored to Story 8.8 after being found missing against Decision §6's own original description; the canonical backend route clarified against the specification's own `/api/explain-spike` naming — `POST /v1/posts/explain-spike` in `social-listening-core`, reached only via a same-origin `POST /api/posts/explain-spike` proxy in `social-listening-admin` (ADR-0036 §2's single-choke-point rule), mirroring the already-shipped `POST /api/posts/:id/enrich` pattern; Sentiment Trajectory corrected from a single flattened mean to a genuine per-day series on a −10/+10 scale, reusing `computeSentimentHistory()`'s existing day-bucketing; Authors by Source given a concrete computation (unique-author-`Set` size per source) where it had previously only named which platforms were excluded; all eight widget `id` attributes reconciled to the ADR's own drafted `widget-` prefixed names after Story 8.7's draft had used a different, un-prefixed scheme; a citation fix (two "ADR-0062 Decision §5" references in ADR-0054's own pending-supersession notes corrected to Decision §6, the section AI Spike Storyteller actually lives in). **Accepted by Menno 2026-08-19**, verbatim: *"Excellent i can apporve ADR 0062 analytics dashboard overview"* — accepted as revised, all review-round corrections above in effect. **Stories 8.7 and 8.8** move from Blocked to **Ready** at this acceptance. The two "Pending supersession notes" already sitting in ADR-0054 Decision §2/§3 (dated 2026-08-19, self-activating) take effect.

## Proposed (not yet decided)

**Three items outstanding as of 2026-08-22 — ADR-0074 (Tenant-Facing Workspace and Matched-Posts Export), not yet reviewed by Menno.** See ADR-0074's own Status line, Context, Decision, Consequences, Alternatives Considered, and Open Questions. Drafted to satisfy the real-data constraint for implementing the Google AI Studio `TenantSettingsView.tsx` design on the real `/tenant/settings` page: it authorizes two new `social-listening-core` `GET` endpoints (`/v1/tenants/me/export/workspace` for full workspace JSON, and a CSV rendering of `GET /v1/posts` for matched posts), while leaving workspace metadata display to the already-shipped `GET /v1/tenants/me`. No workspace-card data expansion is required by this ADR. **Proposed 2026-08-22.**

**ADR-0075 (Outbound Social Post Publishing via Platform APIs), not yet reviewed by Menno.** See ADR-0075's own Status line, Context, Decision, Consequences, Alternatives Considered, and Open Questions. Drafted to authorize the real outbound post path behind ADR-0072's Polypost Composer: a new optional `SocialConnector.publish?()` method, `POST /v1/outbound/posts`, per-asset targeting (e.g. Facebook Pages), and `activity_type='post'` in the `outbound_activities` audit table introduced by ADR-0073. v1 covers immediate text/link-card posts to the caller's own connected assets; image/video upload, bulk/automated publishing, and editing/deleting published posts are explicitly deferred. **Proposed 2026-08-22.**

**ADR-0076 (Composer Deep Research Agent), not yet reviewed by Menno.** See ADR-0076's own Status line, Context, Decision, Consequences, Alternatives Considered, and Open Questions. Drafted to authorize a new on-demand research capability in the Polypost Composer: `POST /v1/composer/research` extracts key phrases from the draft, runs one-off Brave/Bing searches using the tenant's own search credentials, and returns an Azure OpenAI-synthesized context summary and comparison for the author. v1 is synchronous, bounded, and ephemeral; no background scheduler, no persistence, no image/media analysis. **Proposed 2026-08-22.**

Prior to this, this section read (as of 2026-08-19, later the same day — ADR-0062 not yet reviewed by Menno): "**One item outstanding as of 2026-08-19, later the same day — ADR-0062 (Analytics Dashboard — Overview Tab Enhancement), not yet reviewed by Menno.** See footnote 32 and the ADR's own Status line, Context, Decision, Consequences, Alternatives Considered, and Open Questions. Drafted from a Frontend Specification (§4–§10) provided by Menno, 2026-08-19, for a richer Overview Tab. Partially supersedes ADR-0054 Decision §2 (AI Spike Storyteller brought into scope) and Decision §3 (one new `POST /v1/posts/explain-spike` backend endpoint in `social-listening-core` authorized). Eight widgets built (grid layout, filter chips, deep-link share, statistical forecast, Crisis Alert Radar, Sentiment Trajectory, SVG Sentiment Gauge, AI Spike Storyteller); Watchlist Coverage and Location Insights not built (Watchlist Coverage **Blocked awaiting ADR-0063**; Location non-buildable per ADR-0054 Decision §4). `selectedTopic` (watchlist selector) is **Blocked** pending ADR-0063 — explicitly not a client-side approximation, the proper server-side path is ADR-0063. **Stories 8.7 and 8.8** (`docs/user-stories/epic-8-analytics-dashboard.md`) are drafted alongside, both **Blocked — pending ADR-0062 acceptance**. **Story 8.9** (`selectedTopic` filter + Watchlist Coverage widget) is **Blocked — pending Story 3.11 implementation and Story 8.7 build** (ADR-0063 acceptance no longer blocking — see below)."

Prior to that, this section read (as of 2026-08-19, earlier the same day — ADR-0063 accepted): "**Two items outstanding as of 2026-08-19 — ADR-0062 (Analytics Dashboard — Overview Tab Enhancement) and ADR-0063 (Post-watchlist match persistence — `post_watchlist_matches` junction table and `GET /v1/posts?watchlistId` server-side filter), both not yet reviewed by Menno.**" ADR-0063 was **accepted by Menno 2026-08-19**, verbatim: *"ADR-0063 cleanly solves the architectural prerequisite for watchlist filtering, bridges the gaps left in ADR-0062, and provides a clear path forward for both backend (Story 3.11) and frontend (Story 8.9) execution. You are fully cleared to accept ADR-0063 and unblock the associated stories."* **Story 3.11** (`docs/user-stories/epic-3-data-model-storage-and-archival.md`) moves from Blocked to **Ready**. See footnote 33 and ADR-0063's own acceptance note for full detail.

Prior to that, this section read (as of 2026-08-19, earlier still — ADR-0062 drafted, ADR-0063 not yet drafted): "**One item outstanding as of 2026-08-19 — ADR-0062 (Analytics Dashboard — Overview Tab Enhancement), not yet reviewed by Menno.** ... Stories 8.7 and 8.8 drafted alongside, both Blocked — pending ADR-0062 acceptance."

Prior to that, this section read: **None currently outstanding, as of 2026-08-18, later still the same day — ADR-0061 (Tier-3 poll scheduler, per-user enumeration) accepted.** See footnote 31 and the ADR's own Status line, Context, Decision, Consequences, Alternatives Considered, and Open Questions. Accepted as drafted — the external review Menno relayed confirmed the design rather than requesting changes, so nothing beyond the initial draft is "in effect," unlike ADR-0060's own four-point revision round. **Accepted by Menno 2026-08-18**, verbatim: *"ADR-0061 is complete, coherent, and ready for my acceptance. It cleanly bridges the gap between ADR-0060's data model and the background scheduling engine, resolving all implicit cross-user race conditions before they ever manifest in production. Excellent work and I approve ADR 0061"*. **Story 1.15** (`docs/user-stories/epic-1-repository-and-api-foundation.md`) moves from Blocked to **Ready**. The named build-order coordination point (`deriveConnectorHealth()`'s eventual four-parameter shape, `pageId?`/`userId?`) remains live, carried forward rather than resolved by this acceptance.

Prior to this, this section read (as of 2026-08-18, later still the same day, before acceptance): "**One item outstanding as of 2026-08-18, later still the same day — ADR-0061 (Tier-3 poll scheduler, per-user enumeration), not yet reviewed by Menno.**" See footnote 31 and the ADR's own Status line, Context, Decision, Consequences, Alternatives Considered, and Open Questions. Resolves ADR-0052 Decision §6/Open Question 2 now that a real Tier-3 poll connector (Facebook, ADR-0060) exists, confirming via repo-wide grep that `pollFacebook()` still has zero real call sites in the running server. Confirms, directly against the actual code, Menno's own suspected in-flight-guard/multi-user bug (real: `getMostRecentRunStatus()`/`deriveConnectorHealth()` are keyed by `(tenantId, platformId)` only, no `user_id` anywhere in `ingestion_runs`), and finds two further, previously-unnamed, adjacent bugs — a `credentialStatus` blending gap and a cross-user auto-disable false-negative already present in already-shipped `shouldAttemptIngestion()` (Story 1.11) — fixed as direct consequences of this ADR's own design. Decides a new nullable `user_id` column on `ingestion_runs`, new per-user enumeration/lookup store functions, a `deriveConnectorHealth()` fourth `userId?` parameter (a named build-order coordination point against ADR-0060/Story 6.27's own already-specified `pageId?` parameter, without editing that story's Acceptance Criteria), a new distinct `SocialConnector.pollUser?()` method retiring Facebook's throwing-placeholder `poll`, a third per-user enumeration level in the scheduler composing with (never duplicating) ADR-0060's own per-Page fan-out, and a `userId`-aware `jitterFraction()`. Explicitly confirms `RequestGate`'s own key shape is unaffected by this ADR — that decision belongs to ADR-0060/`pollFacebook()`'s own scope, not the scheduler. **Story 1.15** (`docs/user-stories/epic-1-repository-and-api-foundation.md`) is drafted alongside this ADR, **Blocked — pending this ADR's acceptance**.

Prior to this, this section read (as of 2026-08-18, later still the same day — ADR-0060 accepted): "**None currently outstanding, as of 2026-08-18, later still the same day — ADR-0060 (Facebook connector, multiple Pages per user) accepted.** See footnote 30 and the ADR's own Status line, Context, Decision, Consequences, Alternatives Considered, and Open Questions. Accepted as revised — a same-day, pre-acceptance four-point external-review pass (Amendment Log) resulted in a new `orphaned` `facebook_connected_pages` status, a structured `{ connected, errors }` partial-failure response on the plural `/select-page` endpoint, an explicitly-sequential per-Page poll loop reusing `RequestGate`'s existing serialization, and a server-computed `parentConnectionActive` field on `GET .../pages` — one recommendation (cascading a single Page's failure to flag every one of a user's Pages) was checked against the codebase and rejected as unsupported, and one specific claim (Meta's "Error Code 4") was flagged as not primary-source-verified rather than adopted as fact. **Accepted by Menno 2026-08-18**, verbatim: *"yes and i can now Approve the ADR 00060"*. **Story 6.27** (`docs/user-stories/epic-6-tenant-admin-ui.md`) is expanded from its prior light "Blocked — pending a new ADR" stub into a real, buildable story at this acceptance, moving to **Ready** — per this series' own "no story until acceptance" precedent (ADR-0024/0026/0057). The Open Questions named in the ADR (`RequestGate` re-keying, the two-users-same-Page health-blending edge case, `GET .../pages` pagination) remain live, unresolved risks, carried into Story 6.27's own out-of-scope note rather than silently dropped.

Prior to this, this section read (as of 2026-08-18, later the same day, during review, before acceptance): "**One item outstanding — ADR-0060 (Facebook connector, multiple Pages per user), revised in place following a four-point external review Menno relayed, each independently checked against the real code, not accepted on trust.** See the ADR's own Amendment Log for full dispositions: one point rejected (a single Page's credential failure should not cascade to flag every one of a user's Pages — checked against ADR-0059's own Access Token Guide finding and against this ADR's own per-Page-independent design, and found unsupported) with a related point accepted instead (a re-consent call that no longer returns a previously-connected Page now auto-transitions that row to a new `orphaned` status); a structured `{ connected, errors }` partial-failure response accepted for the plural `/select-page` endpoint; the per-Page poll loop now explicitly runs sequentially (reusing `RequestGate`'s already-confirmed existing serialization, `requestGate.ts`) as a concrete, cost-free rate-limiting mitigation, while the specific "Meta Error Code 4" claim the review cited is flagged as not primary-source-verified rather than adopted as fact, and `RequestGate`'s own re-keying stays a named, unsolved Open Question; and `GET .../pages` now returns a server-computed `parentConnectionActive` boolean (via the already-existing `isConnectorActive()`) so the frontend doesn't need to cross-reference a separate fetch. Status remains Proposed."

Prior to that, this section read (as of 2026-08-18, earlier the same day, before any revision): "**One item outstanding — ADR-0060 (Facebook connector, multiple Pages per user), not yet reviewed by Menno.** Sourced from Story 6.27's own deliberately light stub and Menno's own direct request ('could you please draft the ADR for multiple Facebook Pages') after live-testing Story 6.23's real single-select Page picker and finding it impractical at his own real Page count given the tenant's real 5-seat license ceiling. Decides: a new child table (`facebook_connected_pages`) referencing `platform_credentials` by id, with `platform_credentials` itself needing zero schema change; `pollFacebook()` becomes a per-Page fan-out, one `IngestionRun` per connected Page; `deriveConnectorHealth()` gains an optional, backward-compatible `pageId` parameter; two new REST endpoints authorized on the caller's own `userId`, not `tenant_admin` — the one place ADR-0057's own precedent doesn't transfer. Explicitly confirms ADR-0059 Decision §4's Tier-3-only credential-tier finding is unaffected. No story expanded yet."

Prior to this, this section read (as of 2026-08-18 — ADR-0059 accepted): "**None currently outstanding, as of 2026-08-18 — ADR-0059 (Facebook connector) accepted.** See footnote 29 and the ADR's own Status line, Context, Decision, Consequences, Alternatives Considered, and Open Questions. Accepted as a materially narrower connector than "Facebook" implies elsewhere in this project's roster: genuine public-content social listening verified as not buildable against Meta's current API (no keyword-search/discovery surface exists at all); the accepted scope — a tenant's own connected Page's own posts, `authMode: 'oauth'`, Tier 3 credential (revised in place during review from an initially-assumed Tier 2, per Menno's direct instruction and independently confirmed against Meta's own OAuth grant mechanics), organization-as-Author (a fourth instance of ADR-0004's generalized clause) — still requires a real, one-time, project-level Meta App Review + Business Verification gate for SocialEngage's own registered App, correcting `docs/open-decisions.md`'s prior "no-App-Review" framing. Comment/mention ingestion explicitly deferred over an unresolved third-party author-rights question. **Accepted by Menno 2026-08-18**, verbatim: *"Approved ADR 0059 Facebook Connector is approved."* **Story 2.15** (`docs/user-stories/epic-2-ingestion-connectors-and-rate-limits.md`) added at this acceptance, per this series' own "no story until acceptance" precedent (ADR-0024/0026/0042) — the Business Verification/App Review preconditions remain live, unresolved risks, carried into that story rather than dropped."

Prior to this, this section read (as of 2026-08-18, earlier the same day — ADR-0059 drafted, awaiting Menno's review): "**One item outstanding — ADR-0059 (Facebook connector), not yet reviewed by Menno.**" See the ADR's own Amendment Log for the full drafting-and-two-review-rounds record before acceptance.

Prior to that, this section read (as of 2026-08-17, later still the same day — ADR-0056, ADR-0057, and ADR-0058 all accepted):

**None outstanding, as of 2026-08-17, later still the same day — ADR-0056, ADR-0057, and ADR-0058 all accepted.** See footnotes 26/27/28 and each ADR's own Status line, Acceptance note, Context, Decision, and Open Questions. **ADR-0056** (AI-inferred origin location from Newswire dateline text) accepted as drafted, no revisions, verbatim: *"ADR 0056 is approved as well."* Its own Decision §5 recommendation ("name it, don't build it now") stands as accepted policy — **no story is drafted**, matching ADR-0054's own precedent for its Location tab. **ADR-0057** (tenant-owned-feed multi-feed administration, resolving ADR-0050's own Open Question 2) accepted as revised — all four points from its same-day external-review pass in effect (exact `DROP`/`ADD CONSTRAINT` syntax spelled out; a new Decision §1a auto-verifying a second feed on an already-verified domain, skipping redundant DNS re-proof; a `listActivations()` guard-rail note; explicit per-feed-vs-connector-wide-activation UI copy) — via a structured approval decision disambiguating it from ADR-0056, under review at the same time, rather than a single freeform verbatim message. **Story 6.20** (`docs/user-stories/epic-6-tenant-admin-ui.md`) is added at this acceptance, per this series' own "no story until acceptance" precedent (ADR-0024/0026) — Epic 6 chosen since the primary tenant-facing deliverable is the Admin UI's multi-feed list screen, the same cross-repo placement logic Story 6.16 already used.

Prior to this, this section read (as of 2026-08-17, later still the same day, before either was reviewed): "**Two items outstanding — ADR-0057 (tenant-owned-feed multi-feed administration) joins ADR-0056, neither yet reviewed by Menno.** See footnote 27 and the ADR's own Status line, Context, Decision, Consequences, Alternatives Considered, and Open Questions. Resolves ADR-0050's own Open Question 2 (multi-feed support per tenant, left open since that ADR's 2026-08-11 acceptance): three new `tenant_admin`-only REST endpoints (`GET .../activations` to list, `PATCH /:id` to edit `feedUrl` only — never `domain`, which would silently invalidate a completed DNS verification — and `DELETE /:id` as a soft-removal via a new `removed` status value, not a hard SQL delete, since the table has no `DELETE` grant and this project consistently prefers reversible-in-spirit state transitions), plus a correction to a real, pre-existing role-gating gap found while investigating (`connect`/`verify-domain` had no `tenant_admin` check at all, unlike every comparable tenant-wide connector action in this codebase). No cap on feed count per tenant — left unbounded, named as an Open Question rather than an invented ceiling. Admin UI: `TenantOwnedFeedSetup.tsx`'s single-activation state machine is replaced by a real per-tenant feed list. **Revised in place the same day** against an external review (four points, each independently checked, not accepted on trust) — most substantively, a new Decision §1a: a second feed registered under an already-`verified` domain now auto-verifies server-side rather than repeating DNS TXT verification for ownership already proven. **No story is drafted yet** — will be added at acceptance, per this series' own "no story until acceptance" precedent.

**One item outstanding — ADR-0056 (AI-inferred origin location from Newswire dateline text), not yet reviewed by Menno.** See footnote 26 and the ADR's own Status line, Context, Decision, and Open Questions. Recommends naming this design (a new, optional, Azure-OpenAI-only `AIProviderConnector.inferOriginLocation?()` method, gated by a structural dateline pre-check, invoked only from the Newswire ingest loop; confirms zero migration/`SocialPostSummary` change required, unlike ADR-0054's `post_geo_location` blocker) rather than building it now — no committed Location-tab consumer exists yet. Dated Clarification notes added to ADR-0054's Open Question 1 and ADR-0055's Amendment Log; neither ADR's own Decision/Consequences text changes. **No story is drafted**, matching ADR-0054's own precedent for its Location tab."

Prior to this, this section read (as of 2026-08-17, later the same day, before ADR-0056 was drafted): "**None outstanding, as of 2026-08-17, later the same day — ADR-0055 (Language and location enrichment feasibility) accepted.** See footnote 25 and the ADR's own Status line, Acceptance note, Context, Decision, Open Questions, and new Amendment Log entry. Accepted as drafted, no revisions — Menno approved the Language half directly ("yes please extend the language field"); Location's verdict stands unchanged. **Story 8.5** (`docs/user-stories/epic-8-analytics-dashboard.md`) moves from Blocked to **Ready**. Separately, a new, related feasibility question was raised the same reply — whether the AI provider itself (Azure OpenAI's generative completion) could infer a Newswire post's likely origin location from its own body text (a wire-service dateline) — genuinely different from anything ADR-0055's own Location research considered (structured, connector-provided geo-metadata). Verified live before answering, not assumed: PRNewswire's real feed content reliably opens with a clean dateline; GlobeNewswire's real feed, checked the same way, carries neither a dateline nor a `<content:encoded>` body at all — so this would only reliably apply to the PRNewswire-sourced half of Newswire posts. Tracked as a new candidate ADR, not yet drafted — see the "Candidate future ADRs" section below."

Prior to this, this section read (as of 2026-08-17, earlier the same day): "**One item outstanding as of 2026-08-17 — ADR-0055 (Language and location enrichment feasibility for the Analytics Dashboard), not yet reviewed by Menno.** See footnote 25 and the ADR's own Status line, Context, Decision, and Open Questions. Decides: Language is buildable now with zero `social-listening-core` change (both real `AIProviderConnector`s already compute and persist `detectedLanguage`; the only gap is `social-listening-admin`'s own `PostEnrichmentSummary` never reading it) — widens `PostEnrichmentSummary` and adds a Languages breakdown widget to Epic 8. Location remains not buildable, reaffirming ADR-0054 Decision §4 unchanged, with a new GNews-`source.country` finding named and explicitly declined as a substitute (a dated Clarification, not a supersession, added to ADR-0054's own Open Question 1). **Story 8.5** (`docs/user-stories/epic-8-analytics-dashboard.md`) is drafted, **Blocked — pending ADR-0055 acceptance**. No story is drafted for Location."

Prior to this, this section read (as of 2026-08-17, later the same day): "**None outstanding, as of 2026-08-17 — ADR-0054 (Tenant-facing Analytics Dashboard) accepted the same day it was drafted.** See footnote 24 and the ADR's own Status line, Acceptance note, Context, Decision, and Open Questions. Decides a four-tab v1 (Overview, Sentiment, Conversations, Sources — Location deferred entirely, two independent disqualifying findings on `post_geo_location`), 100% client-side aggregation from existing `GET /v1/posts`/`enrichment` (no new backend endpoint, no new stored column), and a narrow supersession of ADR-0008's "any charting UI" clause only (its "no `TopicDailyCount` table/endpoint" clause stays fully reaffirmed — see ADR-0008's own new "Supersession update" note). Also names, but does not decide: the client-side aggregation scale ceiling, per-widget export's relationship to ADR-0039's tenant-export mechanism, and cleanup of already-committed dead demo code (`src/lib/mockData.ts`/`types.ts`). **Story 8.1, Story 8.2, and Story 8.3** (`docs/user-stories/epic-8-analytics-dashboard.md`) all move from Blocked to **Ready** at this acceptance."

Prior to this, this section read (as of 2026-08-17, earlier the same day): "**One item outstanding as of 2026-08-17 — ADR-0054 (Tenant-facing Analytics Dashboard), not yet reviewed by Menno.** See footnote 24 and the ADR's own Status line, Context, Decision, and Open Questions. Decides a four-tab v1 (Overview, Sentiment, Conversations, Sources — Location deferred entirely, two independent disqualifying findings on `post_geo_location`), 100% client-side aggregation from existing `GET /v1/posts`/`enrichment` (no new backend endpoint, no new stored column), and a narrow supersession of ADR-0008's "any charting UI" clause only (its "no `TopicDailyCount` table/endpoint" clause stays fully reaffirmed). Also names, but does not decide: the client-side aggregation scale ceiling, per-widget export's relationship to ADR-0039's tenant-export mechanism, and cleanup of already-committed dead demo code (`src/lib/mockData.ts`/`types.ts`). **Epic 8** (`docs/user-stories/epic-8-analytics-dashboard.md`, Stories 8.1–8.3) is drafted alongside this ADR, all **Blocked — pending ADR-0054 acceptance**."

Prior to this, this section read (as of 2026-08-13): "**None outstanding, as of 2026-08-13 — ADR-0053 (canonical Markdown post-body normalization at ingestion) accepted the same day it was drafted, after twelve in-place revisions during live review, the deepest review pass of any ADR in this series so far.** See footnote 23 and the ADR's own Status line, Acceptance note, and Amendment Log. Decides a new `body_markdown`/`body_markdown_version` pair on `social_posts`, computed once at ingestion via a shared, version-pinned `sanitize-html`→`turndown`(+`turndown-plugin-gfm`) pipeline; both RSS/Atom connectors' parsers widened to actually capture body content (`description`/`content:encoded`/`summary`/`content`/`rawXml`); GNews's `content` field added to enrichment input; a uniform `enrichmentText` composition rule across all three connectors. Named, not designed: backfilling historical rows (deliberately declined — deleting and re-ingesting a tenant is the accepted alternative for this project's own scale), any future Markdown-to-HTML/Word/PDF renderer, and whether raw Markdown syntax measurably degrades enrichment accuracy (explicitly deferred to a possible future, separate ADR). **Story 3.10** (`docs/user-stories/epic-3-data-model-storage-and-archival.md`) is added at this acceptance, Ready.

Prior to this, this section read (as of 2026-08-13, earlier the same day): "**None outstanding, as of 2026-08-13 — ADR-0052 (live ingestion-polling scheduler) accepted the same day it was drafted, after four in-place revisions during live review.** See footnote 22 and the ADR's own Status line, Acceptance note, and Amendment Log. Decides an in-process interval-loop mechanism inside `social-listening-core` (not `pg_cron`, argued explicitly against ADR-0022's precedent generalizing to outbound-network-call jobs), a required connector-registry bootstrap at server startup (a previously-unnamed gap found while drafting — the registry was populated only by Jest contract tests, never by the real running server), a new generic `SocialConnector.poll()`/`pollCadenceMs` extension to stay compliant with ADR-0048's "no core pipeline change" policy, and a cadence derived from `ingestion_runs` history plus a deterministic ±5% per-pair jitter, rather than any new stored field. Explicitly scoped to tenant-wide poll connectors only (v1); named, not designed: Tier-3/user-bound poll scheduling (future shape now named, not built), multi-instance distributed locking (mirroring ADR-0020's own Redis-deferral precedent), and a cost/quota budget ceiling for continuous polling. **Story 1.13** (`docs/user-stories/epic-1-repository-and-api-foundation.md`) is added at this acceptance, Ready."

Prior to this, this section read (as of 2026-08-13, earlier the same day): "**One item outstanding as of 2026-08-13 — ADR-0052 (live ingestion-polling scheduler), not yet reviewed by Menno.** See footnote 22 and the ADR's own Status line, Context, Decision, and Open Questions. Drafted from a real, live gap this session's own investigation found (a DNS-verified `tenant-owned-feed` connector, Story 6.12/ADR-0050, and an active, credentialed GNews connector both producing zero ingested `SocialPost` rows in the running server — the only rows present, 66 of them, were incidental Newswire ingestion from earlier contract-test runs). Decides an in-process interval-loop mechanism (not `pg_cron`, argued explicitly against ADR-0022's precedent generalizing to outbound-network-call jobs), a required connector-registry bootstrap at server startup (a second, previously-unnamed gap found while drafting — the registry is currently populated only by Jest contract tests, never by the real running server), a new generic `SocialConnector.poll()` method to stay compliant with ADR-0048's "no core pipeline change" policy, and a cadence derived from `ingestion_runs` history rather than any new stored field. Explicitly scoped to tenant-wide poll connectors only (v1); named, not designed: Tier-3/user-bound poll scheduling, multi-instance distributed locking (mirroring ADR-0020's own Redis-deferral precedent), and a cost/quota budget ceiling for continuous polling. No user story is proposed yet, per this series' own "no story until acceptance" precedent (ADR-0024/0026) — a story would be added, likely to Epic 1, only at this ADR's acceptance."

Prior to this, this section read (as of 2026-08-12): "**None outstanding, as of 2026-08-12 — ADR-0051 (connector activation decoupled from credential storage) accepted the same day it was drafted, after seven in-place revisions during live review.** See footnote 21 and the ADR's own Status line, Acceptance note, and Amendment Log. Resolves ADR-0034's own named Open Question on whether activation needs its own table (yes — two ownership-scoped tables, `connector_activations`/`connector_user_activations`). Three deliberately unresolved Open Questions remain, named rather than decided: whether the system may ever auto-deactivate a connector, live credential validation, and platform-wide connector availability/staged rollout — all three real, all three named with their own concrete future trigger conditions, none designed here. **Story 1.11** (`docs/user-stories/epic-1-repository-and-api-foundation.md`) is added at this acceptance, Ready — backend schema and REST surface only; admin-UI wiring is separate future work."

**2026-08-12, later the same day — Story 1.11 built.** See `docs/user-stories/README.md`'s and `docs/implementation-plan.md`'s own matching dated notes and `docs/implementation-log.md` for the commit. Full `social-listening-core` suite: 54/54 suites, 391/391 tests passing.

**2026-08-13, later the same day — Story 1.13 built.** See `docs/user-stories/README.md`'s and `docs/implementation-plan.md`'s own matching dated notes and `docs/implementation-log.md` for the commit. Full `social-listening-core` suite: 58/58 suites, 425/425 tests passing.

Prior to this, this section read (as of 2026-08-12, earlier the same day): "**One item outstanding as of 2026-08-12 — ADR-0051 (connector activation decoupled from credential storage), not yet reviewed by Menno.** See footnote 21 and the ADR's own Status line, Context, Decision, and Open Questions. Resolves ADR-0034's own named Open Question on whether activation needs its own table (yes, per ADR-0051's Decision, revised in place to two ownership-scoped tables — takes effect only once ADR-0051 is actually accepted, per that ADR's own new dated note). Three deliberately unresolved Open Questions this ADR names rather than decides: whether the system may ever auto-deactivate a connector, live credential validation, and platform-wide connector availability/staged rollout — all three real, all three named with their own concrete future trigger conditions, none designed here. No user story exists yet for this ADR, per the series' own "no story until acceptance" precedent (ADR-0024/0026) — the actual schema/endpoint/UI build is future work, gated on acceptance."

Prior to this, this section read (as of 2026-08-11): "**None outstanding, as of 2026-08-11 — correcting a stale claim that persisted uncorrected since 2026-08-08.**" All 48 real ADR files in this series (0001–0044, 0047–0050 — 0045/0046 were never real entries, see footnote 19's numbering note) are now **Accepted**. The entry immediately below (and every "Prior to this" entry beneath it, back to 2026-08-08) wrongly carried ADR-0042 (Wikipedia connector) as still outstanding. Verified directly against `docs/adr/0042-wikipedia-connector-mediawiki-api-article-as-author.md`: it was **accepted by Menno on 2026-08-08**, verbatim `"ADR 0042 Approved"`, and that ADR's own Amendment Log states the README's outstanding section was updated to drop it at that time — a claim this file evidently did not actually reflect, and which then went unnoticed through every subsequent acceptance pass in this file (2026-08-10, and three separate updates on 2026-08-11) rather than being caught and fixed at any of them. Left as a plainly dated correction, not a silent edit, per this project's own "don't rewrite history" discipline. **ADR-0048, ADR-0049, and ADR-0050 accepted together 2026-08-11**, verbatim: *"ADR 0048, ADR 0049 and ADR 0050 approved."* Accepted as drafted, no revisions, all three — see footnote 20 and each ADR's own Status line and Acceptance note. Story 2.10 moves to Ready; Story 3.9 (ADR-0049) and Story 2.11 (ADR-0050) are added, both Ready.

Prior to this correction, this section (wrongly) read: "**One item outstanding as of 2026-08-11 — ADR-0042 (Wikipedia connector), not yet reviewed by Menno.**" — see above; ADR-0042 was in fact already Accepted since 2026-08-08 and should never have appeared in any of the entries below.

Prior to this, this section read (as of 2026-08-11, earlier the same day): "**Three items outstanding as of 2026-08-11 — ADR-0042 (Wikipedia connector), ADR-0049 (point-in-time author follower count on `SocialPost`), and ADR-0050 (tenant-owned-domain RSS/content-feed connector with DNS TXT verification), none yet reviewed by Menno.** **ADR-0047 (standard pattern for cross-story references and supersession language) accepted 2026-08-11**, verbatim: *"ADR-0047 is approved."* Accepted as drafted, no revisions — see footnote 19 and the ADR's own Status line and Acceptance note. No-story ADR — no story moves state as a result. This leaves the three items named above still outstanding."

Prior to this, this section read (as of 2026-08-11, earlier the same day): "**Four items outstanding as of 2026-08-11 — ADR-0042 (Wikipedia connector), ADR-0047 (standard pattern for cross-story references and supersession language), ADR-0049 (point-in-time author follower count on `SocialPost`), and ADR-0050 (tenant-owned-domain RSS/content-feed connector with DNS TXT verification), none yet reviewed by Menno.** **ADR-0044 (watchlist CRUD contract) accepted 2026-08-11**, verbatim: *"Approve the ADR 0044 Watchlist Crud contract with Personal Watchlist for Admin and Users."* Accepted as revised in place twice before acceptance (2026-08-08 terms/trigger-name corrections; 2026-08-11 §5c ownership decision + Appendices A/B) — see footnote 18 and the ADR's own Status line, Acceptance note, and Amendment Log. Story 1.5 moves to **Ready**. This leaves the four items named above still outstanding."

Prior to this, this section read (as of 2026-08-10): "**Five items outstanding as of 2026-08-10 — ADR-0042 (Wikipedia connector), ADR-0044 (watchlist CRUD contract), ADR-0047 (standard pattern for cross-story references and supersession language), ADR-0049 (point-in-time author follower count on `SocialPost`), and ADR-0050 (tenant-owned-domain RSS/content-feed connector with DNS TXT verification), none yet reviewed by Menno.** ADR-0049 closes the "Still outstanding, not yet drafted" follower-count-at-publish item this file itself flagged (see the struck bullet below) — Menno confirmed the underlying go/no-go (go) on 2026-08-10, and the ADR was drafted the same day: one optional, nullable `author_follower_count_at_publish` column on `SocialPost`, populated at ingest time only, never backfilled, `Author.followerCount` unchanged. ADR-0050 closes the "Still outstanding, not yet drafted" tenant-owned-domain connector-candidate item this file also flagged (also struck below): a new connector (`authMode: 'none'`, `deliveryMode: 'poll'`) letting a tenant monitor their own domain's RSS/content feed, gated by DNS TXT domain-ownership verification (the same mechanism ADR-0037 §4 declined for the sign-up flow, but fitting here since it gates one optional connector, not the whole product's entry path) — primary-source-verified against Google Workspace's and Microsoft 365's own domain-verification docs and the RSS 2.0/Autodiscovery specs. ADR-0050 is also the third connector needing the organization-as-Author departure from ADR-0004 (after Newswire/ADR-0024 and GNews/ADR-0026), triggering the "rule of three" ADR-0026 itself named — ADR-0050 proposes ADR-0004 gain a permanent, generalized organization-as-Author clause rather than a fourth one-off note; see ADR-0004's own new dated Pending supersession notes for both ADR-0049 and ADR-0050. Neither ADR-0049 nor ADR-0050 has a user story yet, per this series' own "no story until acceptance" convention (ADR-0024/0026's precedent)."

Prior to this, this section read (as of 2026-08-08): "**Three items outstanding as of 2026-08-08 - ADR-0042 (Wikipedia connector), ADR-0044 (watchlist CRUD contract), and ADR-0047 (standard pattern for cross-story references and supersession language), none yet reviewed by Menno.** See footnotes 16, 18, and 19 and each ADR's own Status line, Context, Decision, and Open Questions. ADR-0042 decides a connector's provider/mechanism, re-poll ingestion cadence, article-as-Author modeling, and attribution via `SocialPost.url`, and leaves open the exact CirrusSearch native-query surface, exact rate-limit ceiling, revision-materiality threshold, backfill depth, and the ADR-0004 generalization wording; no user story is proposed yet for it - following ADR-0024's/ADR-0026's own precedent, a story would be added to Epic 2 only at this ADR's acceptance, not before. ADR-0044 decides the four durable CRUD-contract questions for Story 1.5 (project-wide PATCH semantics, error-code mapping, optimistic locking, `updated_at` trigger policy) and reconciles the `watchlists` row shape with ADR-0021's boolean-query AST; Story 1.5 remains **Blocked - pending this ADR's acceptance**, the same convention every other ADR-sourced story in this series follows. ADR-0047 codifies the working writing pattern for superseded-AC markers, cross-dependency references, and known-conflict resolution notes, explicitly deferring to `docs/adr/README.md`'s seven-row governance table as the authority (not as a parallel governance definition); it is a meta-ADR that produces no new story, the same category as ADR-0027's connector-intermediary principle and ADR-0041's Platform-Admin-distinct-identity rule. Prior to this, this section read 'Two items outstanding as of 2026-08-08 - ADR-0042 and ADR-0044'; ADR-0047 was added 2026-08-08 by the same drafting pass that resolved the prior version of ADR-0047 into the redline recorded in footnote 19."

Prior to this, this section read (as of 2026-08-06, later the same day): "**Two items outstanding as of 2026-08-06 — ADR-0042 (Wikipedia connector) and ADR-0043 (self-service tenant-initiated deletion), neither yet reviewed by Menno.** See footnotes 16/17 and each ADR's own Status line, Context, Decision, and Open Questions. ADR-0042 decides a connector's provider/mechanism, re-poll ingestion cadence, article-as-Author modeling, and attribution via `SocialPost.url`, and leaves open the exact CirrusSearch native-query surface, exact rate-limit ceiling, revision-materiality threshold, backfill depth, and the ADR-0004 generalization wording; no user story is proposed yet for it — following ADR-0024's/ADR-0026's own precedent, a story would be added to Epic 2 only at this ADR's acceptance, not before. ADR-0043 decides a narrow, additive supersession of ADR-0039 Decision §1 (self-service tenant deletion, request/export/grace-period/confirm, Platform Admin audit-only visibility) and sources **Story 3.8**, already drafted and **Blocked — pending ADR-0043's acceptance** (not "not yet proposed," unlike ADR-0042 — see `docs/user-stories/epic-3-data-model-storage-and-archival.md`)." ADR-0043 was corrected in place the next day (2026-08-07 — see footnote 17's own dated correction) and accepted the same day.

Prior to this, this section read (as of 2026-08-06, earlier the same day): "**None, as of 2026-08-06.** All 41 ADRs in this series are Accepted — ADR-0041 (footnote 15) was the last outstanding item, accepted the same day it was drafted."

Prior to that, this section read (as of 2026-08-06, still earlier the same day): "**None currently outstanding as of 2026-08-06 — the three-ADR batch (0038/0039/0040) is now fully accepted, the same day two of the three already were.** **ADR-0040 accepted 2026-08-06**, verbatim: *"ADR 0040 also approved."* Story 5.18 moves to Ready. Accepted as drafted, no revisions — see the ADR's own Status line, Acceptance note, and Amendment Log. This closes the batch: ADR-0038 (Story 2.8), ADR-0039 (Story 3.7), and now ADR-0040 (Story 5.18) are all Accepted, all Ready."

Prior to that, this section read (as of 2026-08-06, still earlier the same day): "**One item outstanding as of 2026-08-06 — ADR-0040.** **ADR-0038 accepted 2026-08-06**, verbatim: *"ADR 0038 is a solid piece of work a perferct ADR that allows us to build the enrichment connectors. Approved."* Story 2.8 moves to Ready. **ADR-0039 accepted 2026-08-06**, verbatim: *"Good news ADR 0039 is approved."* Story 3.7 moves to Ready. Both accepted as drafted, no revisions — see each ADR's own Status line, Acceptance note, and Amendment Log. ADR-0040 remains **Proposed**; Story 5.18 stays **Blocked — pending ADR-0040's acceptance**."

Prior to that, this section read (as of 2026-08-05): "**Three items outstanding as of 2026-08-05 — ADR-0038, ADR-0039, and ADR-0040, all drafted the same day, none yet reviewed by Menno.** See footnote 14 and each ADR's own Status line, Context, and Open Questions. Stories 2.8 (ADR-0038), 3.7 (ADR-0039), and 5.18 (ADR-0040) are each **Blocked — pending ADR acceptance**, the same convention every other ADR-sourced story in this series follows. The eleven other stories drafted in this same batch (1.8, 1.9, 5.12, 5.13, 5.14, 5.15, 5.16, 5.17, 6.8, 6.9, 6.10) needed no new ADR and are already **Ready** — see `docs/user-stories/README.md`'s own dated note on this batch."

Prior to that, this section read (as of 2026-08-04): "**None currently outstanding — both ADR-0036 and ADR-0037 (this file's only two Proposed items) were accepted 2026-08-04, the same day both were drafted.**" **ADR-0037 accepted 2026-08-04**, verbatim: "ADR 0037 is approved as well." Accepted together with three direct instructions folded into new Decision §8 — see footnote 13 and the ADR's own Status line, Acceptance note, and Amendment Log for the full record. Story 6.7 (`docs/user-stories/epic-6-admin-ui.md`) moves to Ready. Its cross-ADR dependencies are now resolved, not just pending: ADR-0031's own "Pending supersession note" (added when ADR-0037 was drafted, naming which two of its own Open Questions ADR-0037 would resolve) and ADR-0029's own "Pending supersession note" (naming the IdP-restriction dependency) both now carry a dated "Supersession update" confirming the acceptance — see each ADR's own Open Questions section.

Prior to this, this section read (as of 2026-08-04, earlier the same day): "**ADR-0037 (self-service tenant sign-up and first-Tenant-Admin provisioning) — drafted 2026-08-04, awaiting Menno's review.** See footnote 13 and the ADR's own Status line, Context, and Open Questions. Story 6.7 (`docs/user-stories/epic-6-admin-ui.md`) is Blocked pending this ADR's acceptance. **Also carries a real, named cross-ADR dependency on ADR-0031 (Accepted) — see ADR-0031's own new 'Pending supersession note,' added at this same drafting pass, in its Open Questions section**, naming exactly which two of ADR-0031 §5's own Open Questions (the public-email-provider exclusion mechanism, and the exact domain-match rerouting UX) this ADR would resolve if accepted — not an edit to ADR-0031's own Decision/Consequences text. **Also carries a real, named tension with ADR-0029 (Accepted) §'Open Questions for decision'** — see ADR-0029's own new 'Pending supersession note,' naming that accepting ADR-0037 requires ADR-0029's own still-open 'restrict Entra self-service sign-up at the IdP level' question to stay resolved 'no' for as long as self-service sign-up remains a live feature."

Prior to that, this section read (as of 2026-08-04, earlier the same day): "**ADR-0036 (admin UI authentication/session/role-gating mechanism) — drafted 2026-08-04, awaiting Menno's review.** See footnote 12 and the ADR's own Status line, Context, and Open Questions for what it decides and what it explicitly leaves open (most notably, whether NextAuth.js/Auth.js's Entra External ID support is actually solid enough to adopt, which this ADR's own drafting could only check to a web-search-level bar, not the primary-source depth ADR-0029 held itself to). Story 6.1 (`docs/user-stories/epic-6-admin-ui.md`) is Blocked pending this ADR's acceptance, the same convention every other ADR-sourced story in this series follows." **ADR-0036 accepted 2026-08-04**, verbatim: "ADR 0036 is approved." Accepted as revised (all four in-place review-round amendments in effect — see its own Amendment Log). Story 6.1 moves to Ready — see footnote 12 and the ADR's own Status line and Acceptance note.

Prior to that, this section read (as of 2026-08-03): "**None currently outstanding — this seven-ADR batch is fully accepted as of 2026-08-03.** **ADR-0034 (connector connect/disconnect rework) accepted 2026-08-03**, its own flagged interpretive question (Tenant-Admin's offboarding revocation authority over a user-bound credential) confirmed directly by Menno rather than waved through — see its own Acceptance note. **ADR-0035 (admin UI shape) also accepted 2026-08-03**, and its own closing recommendation decided directly: yes, formalize a "no-story ADR" convention now that three real cases exist — see `docs/user-stories/README.md`'s new "No-story ADR convention" section, added at this acceptance. **ADR-0029 (authentication mechanism) was accepted 2026-08-03**, verbatim: "reviewed ADR 0029 and approved." **ADR-0030 (Admin-tier design) was also accepted 2026-08-03**, revised at review to add a narrow break-glass mechanism — Menno's own words, verbatim: *"I noted the break the glass procedure and that will be a minimum Tenant Admin User Password Reset. No other access to the clients data is permitted"* — see ADR-0030's own §3, Acceptance note, and Amendment Log. **ADR-0031 (`tenants` table shape) was also accepted 2026-08-03**, revised at review to add sign-up domain capture — verbatim: *"Store the sign up users work domain as a domain field in the tenant id to have future similar domains be rerouted"* — see ADR-0031's own §5, Acceptance note, and Amendment Log. **ADR-0032 (`users` table shape) was also accepted 2026-08-03**, after two clarifying exchanges that changed nothing, then revised at review to replace `status`'s `'suspended'` value with a nullable `access_ends_at` timestamp — Menno's own words, verbatim: *"Replace status active/suspended with a nullable end date. removing an end date makes the account according to the audit trail per that date active again"* — see ADR-0032's own §9, Acceptance note, and Amendment Log. **ADR-0033 (retire `X-Tenant-Id`) was also accepted 2026-08-03**, as drafted with no revisions — verbatim: "ADR 0033 remove X Tenant for bearer token. Is Approved." As of 2026-08-03, all 35 ADRs in this series are Accepted — this entire seven-ADR batch (0029–0035) closed the same day it was drafted.

Prior to this batch, this section read (as of 2026-08-03, earlier the same day, after ADR-0028's acceptance): "**None currently outstanding.** ADR-0028 (credential creation authority scoped by ownership tier — no system-wide credentials, tenant-wide credentials created only by Tenant-Admin, user-bound credentials self-activated by the user) was the last one in this state — drafted 2026-08-03, left Proposed since the drafting persona doesn't hold acceptance authority, then revised in place five times the same day (concrete social-platform account-type examples sharpening tiers 2/3; the AI-enrichment scope question resolved by Menno — Azure AI Language is a tenant-owned tier 2 credential, cost settled directly between tenant and vendor; a Clarification on credential-ownership-vs-data-visibility scope; a correction to its own first-drafted Reddit example, verified against Reddit's real OAuth2 documentation and found to support a tier 2 app-only path after all; a concrete tier 3 respond/reply example, parked as a requirement against the future Social Care subsystem in `docs/future-subsystems.md`) — then accepted by Menno the same day it was drafted, verbatim: *"that will allow me to approve the ADR and confirm the ADR's intentions and described concepts are approved."* See footnote 10 and the ADR's own Status line, Acceptance note, and Amendment Log. As of 2026-08-03, 28 ADRs exist in this series, all Accepted.

Prior to this acceptance, this section read (as of 2026-08-03, earlier the same day): "**ADR-0028**... is the current outstanding item — drafted 2026-08-03, left Proposed since the drafting persona doesn't hold acceptance authority, and since revised twice more the same day: concrete social-platform account-type examples added to sharpen tiers 2/3, and the AI-enrichment scope question flagged during drafting was directly resolved by Menno... As of 2026-08-03, 28 ADRs exist in this series: 27 Accepted, 1 Proposed (ADR-0028)."

Prior to ADR-0028 being drafted, this section read "None currently outstanding" as of 2026-08-01: ADR-0027 (connector architecture is a technical intermediary only, never a contracting party) was the last one in that state — drafted 2026-08-01, left Proposed since the drafting persona doesn't hold acceptance authority, revised twice the same day (a paid-tier eligibility rule added, then reversed — see its own Amendment Log), then accepted by Menno the same day: "Approved as important foundation." See footnote 9 and the ADR's own Status line, Acceptance note, and Amendment Log.

Prior to ADR-0027, this section read "None currently outstanding" as of 2026-07-31: ADR-0026 (RSS/News connector — GNews API, with publication-as-Author modeling) was the last one in that state — drafted 2026-07-31, left Proposed since the drafting persona doesn't hold acceptance authority, then accepted by Menno the same day it was reviewed (2026-07-31) — see footnote 8 and the ADR's own Status line, Acceptance note, and Amendment Log.

These originated new policy rather than documenting an existing decision — gaps identified during review (independently by this series' author and by two rounds of Copilot review) that the spec and design conversation left unaddressed. Each separated a durable *decision* from adjustable *implementation defaults*, with an Amendment Log for logging parameter changes (e.g. a deprecation window changing from 6 to 12 months) without superseding the ADR — that pattern remains in force post-acceptance; only a change to the underlying decision itself now warrants superseding. (ADR-0017, ADR-0018, ADR-0019, ADR-0020, ADR-0021, ADR-0022, and ADR-0023 have all since been accepted — see the main table above — and none remain in this section.)

Third-party review also surfaced a numeric disagreement with ADR-0017's original default, resolved and logged in that ADR's own Amendment Log (not a new ADR): ADR-0017's deprecation window shortened from 6 months to 90 days. A similar review-round disagreement briefly shortened ADR-0018's `IngestionRun` archival window from 18 months to 90 days (aligned with `rawPayload`'s window), but that alignment was reverted at acceptance — see ADR-0018's Amendment Log — back to 18 months, now configurable. Monthly range partitioning was added as the archival mechanism for both `SocialPost` and `IngestionRun` regardless of the window length. ADR-0020–0023 saw no such numeric disagreements — each was accepted with its originally proposed defaults unchanged (see footnote 5).

~~**Still outstanding, not yet drafted:** ADR-0004's point-in-time author snapshot (retaining `followerCount`-at-publish-time on `SocialPost` despite `Author` being normalized) was flagged as a genuine trade-off — not a strict improvement — during the same review round, but wasn't included in the batch above. Needs an explicit go/no-go before drafting, since it partially reintroduces the per-post duplication ADR-0004 argued against.~~

**2026-08-10 update:** Go/no-go confirmed by Menno (go). Drafted as [ADR-0049](0049-point-in-time-author-follower-count-on-social-post.md) (Status: Proposed). Pending Menno's acceptance.

**2026-08-11 update:** Accepted — see footnote 20. Story 3.9 (`docs/user-stories/epic-3-data-model-storage-and-archival.md`) added, Ready.

~~**A real, named future connector candidate, not yet scoped or drafted, flagged 2026-08-04:** a connector that lets a tenant monitor their *own* domain's RSS/content feed (e.g. their own company blog), raised by Menno while discussing ADR-0037's declined DNS-verification-at-signup idea. Distinct from both shipped connectors today — GNews (ADR-0026) and Newswire (ADR-0024) both pull from third-party sources the tenant doesn't own; nothing in the current roster involves a tenant's own domain at all. **DNS TXT-record domain-ownership verification (the same mechanism ADR-0037 §4 declined for the sign-up flow, where it would block the entire product's entry path) fits naturally here instead** — it would gate one optional, deliberately-initiated connector setup, not the ability to sign up and use the product at all, and a tenant configuring their own connector has real reason to tolerate DNS propagation delay. Not designed further here — would need its own ADR, following ADR-0024/ADR-0026's own precedent for a new connector type, if and when this becomes real scope.~~

**2026-08-10 update:** Drafted as [ADR-0050](0050-tenant-owned-domain-rss-content-feed-connector.md) (Status: Proposed). Primary-source verification of the DNS TXT verification pattern (Google Workspace, Microsoft 365 docs) and RSS 2.0/Autodiscovery specs completed this session. Pending Menno's acceptance.

**2026-08-11 update:** Accepted — see footnote 20. Story 2.11 (`docs/user-stories/epic-2-ingestion-connectors-and-rate-limits.md`) added, Ready. ADR-0004 gains its permanent, generalized "organization-as-Author" clause per this ADR's own Decision §4/Open Question 6 — see ADR-0004's own "Supersession update — 2026-08-11 (ADR-0050, organization-as-Author generalization)" section.

**A real, named future ADR candidate, not yet scoped or drafted, flagged 2026-08-10:** self-service tenant sign-up (ADR-0037/Story 6.7) collects only a single free-text "tenant name" field today — confirmed directly during this session's first successful live, manual end-to-end sign-up (`tenants.name` ended up literally `menno.drescher@gmail.com`, whatever the caller happened to type). A richer onboarding form (company name, industry, size, and similar business details) was raised by Menno as a real future improvement, deliberately not pursued now (several other ADRs already outstanding for review) — this changes `tenants`' own schema and Story 6.7's UI, not a copy tweak, so it would need its own ADR rather than folding into ADR-0037's already-Accepted Decision text. Not designed further here.

**Multi-tenant Admin/Tenant/User model (brainstormed 2026-07-30, not yet drafted):** no `tenants` table, `users` table, or Admin tier exist anywhere in this series — "tenant" today is purely a `tenant_id` UUID convention (ADR-0015 isolates it; nothing provisions it). A same-day brainstorm settled several shapes without drafting any ADR yet (paused mid-session, to be continued):
- Three tiers: Platform Admin → Tenant (with its own Tenant-Admin role) → Tenant User.
- User onboarding: invite-only, gated by a per-tenant license/seat count — no request-then-approve queue.
- ~~Connectors are tenant-owned only; personal-account connectors are a future Social Selling subsystem's concern, not this one's.~~ — **superseded 2026-08-03:** Menno (Sponsor) has since stated directly that the future authentication/credential architecture must support credentials held per-user, not only tenant-wide (the concrete driver is a future connector, Reddit's OAuth grant being the clearest example, needing an individual user's own credential rather than a tenant-wide one). This bullet's "tenant-owned only" framing no longer reflects that direction; per-user connector ownership is this subsystem's concern too, not solely a future Social Selling subsystem's. See `docs/project docs/Business-Case-v6.0.md`'s 2026-08-03 notes (Sections 2, 6, 9) for full sourcing, and ADR-0027's Negative Consequences bullet for the pre-existing architectural gap this requirement now closes. Candidate ADR #4 (`users` table shape) must be drafted against this corrected requirement, not the struck-through one.
- ~~Platform Admin's boundary leans toward provisioning-only (create/suspend a tenant, set its license count), zero tenant-data access — floated, not locked.~~ — **locked, 2026-08-03, ADR-0030 (Proposed):** Platform Admin's `platform_admin_role` is granted database access only to the `tenants` table (candidate ADR #3) and its own separate Platform Admin identity table (candidate ADR #4) — never any tenant-content table. No emergency/support-access override exists in this design either (resolving ADR-0028's own flagged Open Question on the point). Still subject to Menno's acceptance of ADR-0030 itself, but no longer floating as an undecided brainstorm item.
- ~~Two opens, unresolved: whether connector *activation* needs its own table separate from `platform_credentials`...; whether a tenant needs multiple activations of one platform...~~ — **addressed, not fully resolved, 2026-08-03, ADR-0034 (Proposed):** connector activation does not need a separate table — `platform_credentials` gains `owner_type`/`user_id` columns instead (ADR-0028's Tier 2/Tier 3 made concrete). Whether a tenant may hold multiple activations of one platform is still genuinely open — ADR-0034's schema does not structurally prevent it, but whether it *should* be allowed and how it would be surfaced is left as that ADR's own named Open Question, not decided.

**Candidate future ADRs, roughly in dependency order — all seven now drafted, 2026-08-03 (Proposed, pending Menno's acceptance):** (1) authentication mechanism — [ADR-0029](0029-authentication-mechanism-entra-external-id.md); (2) Admin-tier design — [ADR-0030](0030-admin-tier-design-platform-admin-rls-exception.md); (3) `tenants` table shape — [ADR-0031](0031-tenants-table-shape.md); (4) `users` table shape + RLS — [ADR-0032](0032-users-table-shape-and-rls.md); (5) retiring the `X-Tenant-Id` header placeholder — [ADR-0033](0033-retire-x-tenant-id-header-placeholder.md); (6) connector connect/disconnect CRUD — [ADR-0034](0034-connector-connect-disconnect-crud-ownership-tier-aware.md); (7) admin UI's own shape (one app or two) — [ADR-0035](0035-admin-ui-shape-one-app-role-gated.md). See footnote 11 for the full drafting record. This section's own historical brainstorm bullets above are left in place, struck through and dated where an ADR now supersedes them, per this file's own "don't rewrite history" convention — not deleted.

**ADR-0027 (Accepted, 2026-08-01) was a constraint on this list, not a member of it — now checked against the drafted batch, not merely anticipated.** It doesn't decide whether the connecting party ends up being the tenant or an individual user; ADR-0032 (`users` table shape) resolves that at the schema level (both tiers coexist, ADR-0028's Tier 2/Tier 3), and ADR-0034 (connect/disconnect CRUD) is the concrete point where ADR-0027's "no pooling, no contracting on anyone's behalf" principle is actually enforced in code for the first time.

**ADR-0028 (Accepted, 2026-08-03) was a second constraint on this list — now built against directly, not just anticipated.** ADR-0030 designates Tenant-Admin's mechanics (ordinary tenant-scoped RLS + an application-layer role check, no database bypass); ADR-0032 gives `users.role` a concrete `'tenant_admin'` value; ADR-0034 is where ADR-0028's three-tier credential-creation rule is actually enforced — `platform_credentials.owner_type`/`user_id`, plus the authorization checks in `connectorsRouter.ts` ADR-0034 specifies. **Resolved 2026-08-03, same day as drafting:** ADR-0028 initially flagged, without resolving, a real tension with ADR-0027's own Accepted Decision text over whether "no system-wide credentials" reaches AI-enrichment infrastructure (Azure AI Language) as currently planned. Menno resolved it directly: `AIProviderConnector` credentials are tenant-owned (ADR-0028's tier 2), cost settled directly between the tenant and the vendor, no SocialEngage-operated shared subscription — see ADR-0028's own Context/Amendment Log and footnote 10. This has a real, flagged (not yet re-derived) consequence for `Project Management Plans/Cost-Management-Plan.md`'s existing project-level enrichment cost line.

**A real, named future ADR candidate, not yet scoped or drafted, flagged 2026-08-17 (ADR-0055's own same-day Amendment Log):** whether the enrichment AI *provider itself* (Azure OpenAI's generative completion, not Azure AI Language's structured NER capability) could infer a Newswire post's likely origin location from its own body text (a wire-service "dateline"), scoped to Newswire only as a starting point, as prep work toward eventually revisiting ADR-0054/ADR-0055's own Location Open Question. Genuinely different from anything ADR-0054/ADR-0055's own Location research considered (structured, connector-provided geo-metadata) — this reads a real signal already present in already-ingested text via a generative capability, not a new data source.

**2026-08-17 update, later still the same day:** Drafted as [ADR-0056](0056-ai-inferred-origin-location-newswire-dateline-extraction.md) (Status: Proposed). Confirms the storage path is materially easier than ADR-0054's `post_geo_location` blocker (no migration, no `SocialPostSummary` widening — `enrichment` is already an unfiltered, fully-included JSONB blob), but recommends naming this design rather than building it now, for lack of a committed Location-tab consumer — see footnote 26. Pending Menno's acceptance.

Considered and explicitly **not** drafted as ADRs, per review discussion:
- **Capability-based connector composition / connector capability registry** — premature for a two-branch hierarchy (`SocialConnector`, `AIProviderConnector`); the spec doesn't describe a third, structurally different provider type that would justify it yet. Revisit if one materializes (rule of three).
- **CODEOWNERS / repo ownership** — reasonable, but a repo-governance artifact, not an architecture decision.
- **OpenAPI-first contract governance, connector certification, sandbox/test harness, DR/replay strategy** — reasonable platform-maturity investments, but ahead of where this subsystem is: pre-implementation, no downstream consumers built yet. **Revisit trigger:** before the first downstream subsystem (likely Brand Reputation & Alerts) begins integrating against `social-listening-core`'s API — not before, per a second round of Copilot review.
- **Consumer contract ownership** (who owns backward compatibility as the producer/consumer graph grows — `social-listening-core` vs. each downstream subsystem) — flagged as a likely future ADR candidate once implementation begins and a second or third real downstream consumer exists to reason about. Not drafted now, and not pinned to a specific number since 0020–0023 are now in use — noted so it isn't lost.


## Audit mechanism - emerging pattern across multiple per-table designs (added 2026-08-08)

A review of a non-series draft (`_stale-drafts/0045-audit-trail-from-other-project.md`, see below) on 2026-08-08 surfaced that this project's audit-mechanism question is being answered **incrementally per concern**, not by a single global "Audit Trail Architecture" ADR - and that the per-table pattern is the right shape, not a missed unified decision. The current per-concern design:

- **`platform_admin_audit_log`** - Platform-Admin-bypassed writes (tenant creation, suspension, license changes, break-glass). Decided by ADR-0030 §5, migration `0015`. RLS-bypassed for the acting role, granted only to `platform_admin_role` / `tenant_signup_role` (ADR-0037 §8b's INSERT-only grant). Not a tenant-content table.
- **`domain_signup_attempts`** - self-service sign-up domain-match attempts and escalations. Decided by ADR-0037 §8b, migration `0022`. Tenant-content-shaped (RLS-scoped to the calling tenant), but written by `tenant_signup_role` during a structurally-unscoped-to-tenant signup, the same chicken-and-egg pattern ADR-0037 §1 already explains.
- **`user_access_audit_log`** - `access_ends_at` writes (offboarding, reactivation). Designed by Story 5.17 (Ready), transactionally paired with the `users` update per its Acceptance Criteria. Story 5.17 itself explicitly notes "no new ADR needed" - this is an implementation-time mechanics question, not a fresh architectural one, since the underlying principle (every `access_ends_at` write is auditable) is already decided by ADR-0032 §9.
- **General tenant-content mutation audit** (e.g., who changed this watchlist's `terms`?) - **not designed, deliberately deferred.** ADR-0044 §6 (the redline on this same date) explicitly leaves this as an Open Question: "Whether a real compliance need will eventually surface for tenant-mutation audit history... if yes, a separate ADR for an `activity_logs` table naming is the right place; this ADR deliberately doesn't pre-decide the schema for a need that hasn't materialized." No story has surfaced a concrete need for a general tenant-content audit table yet - every existing audit need has been scope-specific enough to live on its own table - so this remains deferred under the rule-of-three discipline.

**This is the project's actual emerging pattern** - one audit table per distinct actor / action class, each designed at the table's own ADR or story, not a single monolithic audit schema retrofitted onto all of them. **No unifying ADR is warranted.** Anyone reviewing a future story that asks "do we have an audit table for this?" should answer by extending the per-concern pattern above, not by inventing a new global table - same as ADR-0037 §8b already did for `domain_signup_attempts` rather than reusing `platform_admin_audit_log`.

## Not captured as ADRs

The following are called out in the spec but are scope boundaries or process decisions rather than architecture decisions, so they're not recorded as ADRs — none of them involve the kind of hard-to-reverse technical trade-off this series exists to justify:

- Initial connector build order — **resolved 2026-07-29**, see spec §10 and `docs/implementation-plan.md` (RSS/News first, then Reddit; remaining platforms prioritized in Phase 4)
- Exact connector auto-disable failure threshold — governed by ADR-0010's flat placeholder through Phase 3; superseded 2026-07-30 by ADR-0023's proportional rule (Story 2.5, implemented ahead of the rest of Phase 4 — see `docs/implementation-log.md`); see spec §10. (Distinct from ADR-0020's separate per-request dead-letter threshold, Story 2.4.)
- Testing strategy and CI/CD pipeline details for the two repos — **resolved 2026-07-29**, see spec §10 (lightweight CI given this is a solo-developer project, not team-scale process). **Exception, 2026-07-30:** ADR-0025 (persistent local dev database) is dev tooling in this same general category, captured as an ADR anyway by explicit user decision after real friction was hit in practice — see its own Acceptance note and footnote 7 above. Not a reversal of this bullet's general rule, just a named, flagged exception to it.
- Geocoding of `profileLocation` — explicitly out of scope, §9, no decision made to record
