# Architecture Decision Records — Social Listening / Insights Subsystem

ADRs derived from [`2026-07-28-social-listening-ingestion-design.md`](../project%20docs/2026-07-28-social-listening-ingestion-design.md) (Status: Approved for implementation). Each record captures one architecturally significant decision from that spec, with the context and alternatives that motivated it.

Each ADR feeds a [user story](../user-stories/README.md); each story is built per [`docs/implementation-methodology.md`](../implementation-methodology.md) (contract-first, component `SKILL.md`s, permanent regression suite) — so an ADR's decision stays enforced in code, not just recorded in prose.

## Conventions for changing an existing ADR

Four situations, four different responses — don't default to editing the original Decision/Consequences text in place:

| Situation | Response |
|---|---|
| The underlying decision itself changes | New ADR, or a superseding ADR that says so explicitly |
| The decision stands, but *why* it was made needs more explanation | Add a "Note on provenance" or similar, without altering the original text (e.g. ADR-0002, ADR-0016) |
| An adjustable parameter changes (a window length, a threshold) | Log it in that ADR's "Amendment Log" (e.g. ADR-0017–0019) — doesn't need superseding |
| Implementation surfaces a constraint that was already logically required by the decision, just not stated | Add a dated "Clarification" section (e.g. ADR-0013) — not a new decision, just making an implicit requirement explicit |
| A still-Proposed ADR would change part of this (Accepted) ADR's decision, if accepted | Add a dated "Pending supersession note" pointing to it, naming exactly which part would change (e.g. ADR-0009/0010 → ADR-0023) — not an edit to the original text, and not a full supersession unless the whole decision is affected |
| A "Pending supersession note" above becomes real because the other ADR gets accepted | Add a dated "Supersession update" note confirming it, without editing the original Pending supersession note or Decision text (e.g. ADR-0009/0010, once ADR-0023 was accepted) — and don't assume already-shipped code changes automatically; it only changes when the superseding ADR's own story is actually built |
| A new ADR formalizes, as an explicit and durable principle, something a prior Accepted ADR's Decision text already assumed but never stated as project-wide policy — without changing that prior ADR's own Decision at all | Add a dated "Note on relation to ADR-XXXX" section on the prior ADR, naming what it already satisfies and why no scope change follows — not a supersession (nothing changes), not a provenance note (this isn't explaining *why* the prior decision was made), not a parameter change, not a clarification of an implicit requirement (e.g. ADR-0024/0026 → ADR-0027) |

The common thread: the original Decision and Consequences text is a historical record and stays put. Everything learned later is appended, dated, and labeled by which of the five categories it is.

| # | Title | Spec Section |
|---|-------|--------------|
| [0001](0001-two-repository-split.md) | Split into `social-listening-core` and `social-listening-admin` repositories | §2 |
| [0002](0002-unified-provider-connector-pattern.md)² | Unified `ProviderConnector` contract for social platforms and AI providers | §3.1 |
| [0003](0003-per-tenant-per-provider-rate-limiting.md) | Rate limiting enforced per `(tenantId, providerId)` via a shared `RequestGate` | §3.2 |
| [0004](0004-author-normalized-separately-from-post.md) | Normalize `Author` once per platform account, not embedded per post | §4.1 |
| [0005](0005-ingestion-run-as-audit-anchor.md) | `IngestionRun` as the immutable acquisition/audit anchor for every post | §4.3 |
| [0006](0006-watchlist-matching-connector-side-with-fallback.md) | Prefer connector-side native filtering for watchlist matching, with post-fetch fallback | §4.4 |
| [0007](0007-author-topic-signal-minimal-v1.md) | `AuthorTopicSignal` ships with raw signals only, no computed expertise score | §4.5 |
| [0008](0008-defer-topic-time-series-and-charting.md) | Defer `TopicDailyCount` aggregation and all charting to a future subsystem | §4.6, §9 |
| [0009](0009-connector-health-derived-not-stored.md) | `ConnectorHealth` is fully derived from `IngestionRun` history, not stored mutable state | §5 |
| [0010](0010-error-handling-and-auto-disable-policy.md) | Retryable-vs-non-retryable error policy with per-tenant auto-disable | §5 |
| [0011](0011-cursor-based-pagination-for-posts-api.md) | Cursor-based pagination for `GET /posts` | §6 |
| [0012](0012-thin-events-with-rest-fetch-on-demand.md) | Service Bus events carry IDs and minimal fields only; full data fetched via REST on demand | §7 |
| [0013](0013-per-tenant-event-filtering-via-subscription-rules.md) | Per-tenant event filtering via Service Bus subscription SQL filters | §7 |
| [0014](0014-credential-storage-envelope-encryption-oauth-first.md) | Envelope-encrypted credential storage via Azure Key Vault, OAuth preferred with API-key fallback | §8 |
| [0015](0015-tenant-isolation-via-postgres-row-level-security.md) | Enforce tenant isolation at the database layer with Postgres Row-Level Security | §8 |
| [0016](0016-postgres-as-database-engine.md)¹ | Postgres as the database engine | §2, §4.2, §8 |
| [0017](0017-api-versioning-and-compatibility-policy.md)³ | API versioning and compatibility policy | — (originated, see below) |
| [0018](0018-data-retention-and-archival-policy.md)⁴ | Data retention and archival policy | ADR-0005 Negative consequences |
| [0019](0019-event-schema-versioning-policy.md)³ | Event schema versioning policy | — (originated, see below) |
| [0020](0020-rate-limit-queue-bounds-and-distributed-gate-state.md)⁵ | Rate-limit queue bounds, dead-letter handling, and distributed gate state | ADR-0003 Negative consequences; third-party review |
| [0021](0021-watchlist-boolean-query-ast-and-capability-matrix.md)⁵ | Unified boolean-query AST for watchlist matching, with per-connector capability matrix | ADR-0006 Negative consequences; third-party review |
| [0022](0022-derived-data-caching-and-refresh-strategy.md)⁵ | Derived-data caching and refresh strategy (`ConnectorHealth` cache, `AuthorTopicSignal` refresh cadence) | ADR-0007, ADR-0009 Negative consequences; third-party review |
| [0023](0023-proportional-connector-failure-threshold.md)⁵ | Proportional (rate-relative) connector failure threshold for auto-disable | Spec §5/§10 placeholder; third-party review |
| [0024](0024-newswire-connector-direct-wire-rss-issuer-as-author.md)⁶ | Newswire connector — direct wire-service RSS feeds (GlobeNewswire, PR Newswire), with issuer-as-Author modeling | §10 (platform roster, order not yet decided) |
| [0025](0025-persistent-local-dev-database-separate-from-test-database.md)⁷ | Persistent local dev Postgres database, kept separate from Jest's ephemeral test database | — (originated, see below) |
| [0026](0026-rss-news-connector-gnews-api-publication-as-author.md)⁸ | RSS/News connector — GNews API (general news), with publication-as-Author modeling | §10 (RSS/News chosen as a category 2026-07-29; no concrete provider named until now) |
| [0027](0027-connector-is-technical-intermediary-not-contracting-party.md)⁹ | Connector architecture is a technical intermediary only — SocialEngage is never a party to, reseller of, or intermediary in the connecting party's relationship with a data source | — (originated, see below) |
| [0028](0028-credential-creation-authority-scoped-by-ownership-tier.md)¹⁰ | Credential creation authority is scoped by ownership tier — no system-wide credentials, tenant-wide credentials created only by Tenant-Admin, user-bound credentials self-activated by the user | — (originated, see below) |
| [0029](0029-authentication-mechanism-entra-external-id.md)¹¹ | Authentication mechanism — Microsoft Entra External ID, integrated as a thin, pluggable OIDC token issuer (single external tenant; Postgres owns tenant/role/license data) | — (originated, see below) |
| [0030](0030-admin-tier-design-platform-admin-rls-exception.md)¹¹ | Admin-tier design — Platform Admin via a narrowly-scoped, audited `BYPASSRLS` role; Tenant-Admin via ordinary tenant-scoped RLS + an application-layer role check | — (originated, see below) |
| [0031](0031-tenants-table-shape.md)¹¹ | `tenants` table shape — RLS scoped by `id`; app-layer seat/license enforcement; Platform Admin's bypass locked to this table only | — (originated, see below) |
| [0032](0032-users-table-shape-and-rls.md)¹¹ | `users` table shape + RLS — tenant-scoped like every other table; Platform Admin modeled as a separate, non-tenant table; request-time identity resolution via a fourth, narrower bypass role | — (originated, see below) |
| [0033](0033-retire-x-tenant-id-header-placeholder.md)¹¹ | Retire `X-Tenant-Id` as the tenant-identity trust mechanism, replaced by `Authorization: Bearer` + server-side resolution | — (originated, see below) |
| [0034](0034-connector-connect-disconnect-crud-ownership-tier-aware.md)¹¹ | Connector connect/disconnect CRUD reworked to be ownership-tier-aware (ADR-0028), closing a real gap in Story 1.6's already-shipped, placeholder-auth code | — (originated, see below) |
| [0035](0035-admin-ui-shape-one-app-role-gated.md)¹¹ | Admin UI shape — one role-gated Next.js app, not two separate deployables; a second "Platform Console" deployable named and deliberately deferred | — (originated, see below) |
| [0036](0036-admin-ui-authentication-session-and-role-gating-mechanism.md)¹² | Admin UI's own authentication/session mechanism — server-side (BFF) session, no bearer token in browser JS, no third-party auth framework adopted at v1, role-gating layered and UX-only | — (originated, see below) |

¹ Unlike 0001–0015, the spec states this decision as a given rather than arguing it — the spec doesn't carry the reasoning. The reasoning is instead sourced from the chat conversation that produced the spec (linked in the ADR), not from the spec document itself. See its "Note on provenance."

² Mostly spec-sourced (§3.1/§3.3), with one exception: the `deliveryMode` rationale in its Consequences section is sourced from the same chat conversation as ADR-0016, not from the spec. See its "Note on provenance."

³ Originated as Proposed ADRs (gaps neither the spec nor the design conversation addressed), then accepted on 2026-07-29 — deliberately ahead of the implementation phase that would otherwise touch them (Phase 0 of `docs/implementation-plan.md`), because both are cheap to build in from day one and expensive to retrofit later. See each ADR's "Acceptance note." Unlike ADR-0016, these don't have spec/chat-sourced reasoning to cite — the reasoning is the ADR's own, same as when they were Proposed; only the Status changed.

⁴ Also originated as a Proposed ADR, accepted on 2026-07-29 — unlike ADR-0017/0019, this one wasn't accepted early for a schedule reason; it's a straightforward acceptance of the tiered-retention policy with `rawPayload` at 90 days and `IngestionRun` at 18 months, both configurable. See its "Acceptance note" and Amendment Log for the full numeric history.

⁵ ADR-0020–0023, all also originated as Proposed ADRs and all accepted on 2026-07-29, closing out this series' last four open decisions. Each ADR's own "Acceptance note" and Amendment Log carry the specifics, but the shared thread: numeric implementation defaults were kept as originally proposed in every case (no real traffic data existed to justify changing them), and two decisions were deliberately sequenced rather than built immediately — ADR-0020's Redis-backed distributed `RequestGate` state waits for an actual second concurrent instance to be deployed, and ADR-0023's rate-relative failure threshold doesn't retroactively change Stories 2.3/4.3's already-shipped flat-rule code until Story 2.5 is actually built (see ADR-0009's and ADR-0010's "Supersession update" notes). ADR-0023's `deliveryMode`-based variation question was deferred, not resolved — logged as a known gap, revisit once a push-mode connector exists.

⁶ ADR-0024 is a genuine 24th ADR, outside the original "23 ADRs, one per story" scope this series started with (see `CLAUDE.md`) — it originates a connector-specific provider selection and a scoped `Author`-modeling exception, not a decision the original design spec or its 23-ADR derivation anticipated. Drafted ahead of implementation like ADR-0017/0019, revised twice during review (RTPR's real pricing, then dropped entirely for direct wire-service RSS — see its own Amendment Log), then accepted 2026-07-30 the same day, with Story 2.6 added to Epic 2. Its own Acceptance note records that two follow-up deep-research passes (~25 additional candidates) found nothing that beat the accepted GlobeNewswire+PR Newswire scope.

⁷ ADR-0025 is a genuine 25th ADR, and — unlike every other ADR in this series — its own subject matter is dev tooling, not production architecture: `docs/implementation-plan.md`'s Phase 0 explicitly pre-classifies "local dev environment" as "also build, not storied," the same category this README's own "Not captured as ADRs" section uses for testing-strategy/CI details generally. It's here anyway as an explicit, user-directed exception — see its own Acceptance note — because the friction it fixes (a shared ephemeral test container getting torn down out from under a running dev server) was hit and demonstrated in practice, and fixing it surfaced a genuine `execSync`-vs-`spawn` correctness bug worth a durable record. Story 1.4 added to Epic 1, accepted the same day it was built and verified.

⁸ ADR-0026 is a genuine 26th ADR, drafted 2026-07-31 by the AI Business & Requirements Analyst persona to close Phase 1's own longest-standing gap — the RSS/News connector's concrete provider was never named, only the category ("poll-only, API-key auth, no paid tier," decided 2026-07-29). Like ADR-0024, it originates both a provider selection (GNews API, verified directly against `gnews.io`/`docs.gnews.io`) and a scoped `Author`-modeling exception (publication-as-Author — the second connector to need ADR-0024's exact pattern, not a new one). **Unlike ADR-0024/0025, it was not self-accepted the same day it was drafted** — the persona that drafted it does not hold ADR-acceptance authority — but it was reviewed and accepted by Menno later the same day (2026-07-31), see its own Acceptance note. Story 2.7 is now Ready. See its own Amendment Log for the full research trail, including two candidates (NewsAPI.org, Currents API) whose free tiers were verified to explicitly forbid the real/production use this connector needs, and one (NewsData.io) deferred after its own canonical terms pages proved unreachable for direct verification.

⁹ ADR-0027 is a genuine 27th ADR, drafted 2026-08-01 by the AI Business & Requirements Analyst persona from a principle Menno stated directly this session (in the context of Story 2.7/ADR-0026's GNews API key signup), not from the design spec. Unlike every prior ADR, it originates no interface, stored field, endpoint, or provider selection — it formalizes, as an explicit and durable project-wide rule, a practice already present but only stated as a connector-local default in ADR-0014, ADR-0024, and ADR-0026: SocialEngage's connector architecture is a technical connection mechanism only, never a party to, reseller of, or intermediary in the connecting party's own commercial/legal relationship with a data source. Deliberately framed as orthogonal to, not blocked by, this file's own 2026-07-30 multi-tenant governance note — it constrains whoever the future Authentication/Users ADRs (candidate ADRs #1–#4, below) determine "the connecting party" to be, rather than depending on that determination. Revised twice before acceptance the same day: a paid-tier-disqualifies-the-source eligibility rule was added, then reversed after Menno clarified the actual rule is narrower — paid tiers are fine, the connecting party pays the source directly, SocialEngage just never intermediates billing or pricing (see the ADR's own Amendment Log for the full, honestly-labeled reversal). **Accepted by Menno the same day** ("Approved as important foundation") — unlike ADR-0026, which took until later the same day it was drafted to be reviewed, this one was drafted, revised twice, and accepted all in one day. **Has no user story of its own** — the first ADR in this series without one; left as a documented, named exception rather than prompting a new "non-story ADR" convention (one instance isn't enough to generalize from). ADR-0024 and ADR-0026 each received a short, dated "Note on relation to ADR-0027" pointing to it, per this series' own governance-table convention (now a fifth row, added at this ADR's acceptance) for a new ADR formalizing an existing-but-unstated practice — neither ADR's own Decision/Consequences text was edited.

¹⁰ ADR-0028 is a genuine 28th ADR, drafted 2026-08-03 by the AI Business & Requirements Analyst persona from a principle Menno (Sponsor) stated directly this session, independent of (though related to) the connecting-party question ADR-0027 already governs: credential-creation authority is scoped by ownership tier — no system-wide credential is ever created, a tenant-wide credential may only be created by Tenant-Admin, and a user-bound credential is made available by the system but created only by the user's own act of activation. Its first tier substantially formalizes something already implicit in ADR-0027's Accepted "No pooling or sharing" bullet; its second and third tiers are new content with no prior shipped code or ADR language behind them, sourced only from Menno's statement this session — named as such in the ADR's own Context and Consequences rather than presented as already-practiced. Sharpened the same day with concrete social-platform account-type examples from a Menno follow-up (a Company Page/multi-admin Group for tier 2; an individually-issued account and personal, private profile for tier 3), grounding the tier 2/tier 3 distinction in each platform's own account/asset model rather than an abstract policy label. **Deliberately scoped, following ADR-0027's own technique, to state a governing principle without deciding candidate ADR #2's (Admin-tier design) or #4's (`users` table shape) exact mechanics** — Tenant-Admin's identity, authentication, and RLS shape remain those ADRs' job. **Raised, then resolved the same day, a real tension with ADR-0027's own already-Accepted Decision text**, which already lists "an AI enrichment provider" among the data sources its no-pooling rule covers: Menno confirmed directly that `AIProviderConnector` credentials (Azure AI Language) are tenant-owned tier 2 credentials, cost settled directly between the tenant and the vendor, no SocialEngage-operated shared subscription — a real, previously-uncosted consequence for `Project Management Plans/Cost-Management-Plan.md`'s existing project-level enrichment cost line, flagged there with a dated note rather than re-derived; see the ADR's own Context and Amendment Log. **Also corrected the same day: its own first-drafted Reddit worked example.** Verified directly against Reddit's own OAuth2 developer documentation (`github.com/reddit-archive/reddit/wiki/OAuth2`), Reddit's API supports both an app-only/"userless" grant (tenant-wide, tier 2 — public-content monitoring) and a personal user-context grant (tier 3 — needed only for capabilities that act as a specific account, e.g. replying to a post); the ADR's initial claim that "any Reddit connection is squarely tier 3, never tier 2" was wrong and corrected in place with a dated note, not silently rewritten. The tier 3 respond/reply case was named as a concrete example and parked as a requirement against the future Social Care subsystem in `docs/future-subsystems.md`, explicitly not new scope for this subsystem. **Has no user story at drafting time, but not for ADR-0027's reason** — tier 1 is already satisfied by shipped code the same way ADR-0027's principle is, but tiers 2/3 cannot be built against anything yet (no `tenants`/`users` table, no authentication, no Admin-tier role exist for a contract test to target); recommended as deferred to whichever of candidate ADR #4's or #6's own stories eventually implements the authorization/activation mechanics this ADR constrains, rather than treated as a second instance of ADR-0027's specific "permanently no story" exception. See the ADR's own "A note on this ADR's own place in the series' conventions" section. **Accepted by Menno the same day it was drafted** ("that will allow me to approve the ADR and confirm the ADR's intentions and described concepts are approved") — the fastest drafted-to-accepted turnaround in this series alongside ADR-0027, and unlike ADR-0027, revised in place five separate times before that acceptance, all in the same session. See the ADR's own Status line, Acceptance note, and Amendment Log.

¹¹ ADR-0029 through ADR-0035 are a genuine seven-ADR batch, drafted 2026-08-03 by the AI Business & Requirements Analyst persona, closing out this file's own 2026-07-30 "Candidate future ADRs, roughly in dependency order, none drafted" list in full — see that section, now updated below, for the running record of what each one decided and what it deliberately left open. Drafted strictly in dependency order (ADR-0029 first, since every later one assumes its shape; ADR-0035 last), per this batch's own task framing, not in parallel. **Menno's one fixed input this session — authentication mechanism is Microsoft Entra External ID, Azure-native, chosen for consistency with ADR-0014's own "stay Azure-native, avoid bespoke" precedent and its free tier — is treated as a given throughout, not re-litigated by ADR-0029.** Two mid-drafting course corrections from Menno were folded into ADR-0029 before this batch moved on to ADR-0030: an explicit "Entra as a thin, pluggable OIDC issuer only" Decision-level requirement (bounding a future provider-migration's blast radius), and a "single source of truth per field" data-ownership principle (Postgres owns profile data by default; two named, narrow exceptions). All seven ADRs were left **Proposed** — this persona does not hold ADR-acceptance authority (the same rule ADR-0026/0027/0028 followed) — Menno reviews and accepts each separately. **ADR-0029 accepted 2026-08-03**, verbatim: "reviewed ADR 0029 and approved." **ADR-0030 accepted 2026-08-03**, revised at review to add a narrow Tenant-Admin-credential-reset break-glass mechanism per Menno's own direct instruction — see its own Status line, Acceptance note, and Amendment Log. **ADR-0031 accepted 2026-08-03**, revised at review to add sign-up domain capture — see its own Status line, Acceptance note, and Amendment Log. **ADR-0032 accepted 2026-08-03**, revised at review to add `access_ends_at` after two clarifying exchanges that changed nothing — see its own Acceptance note and Amendment Log. **ADR-0033 accepted 2026-08-03**, as drafted, no revisions — see its own Status line and Acceptance note. **ADR-0034 accepted 2026-08-03**, its own flagged Tenant-Admin-revocation reading confirmed as drafted. **ADR-0035 accepted 2026-08-03**, its own rule-of-three recommendation decided: yes, formalize the no-story convention now (`docs/user-stories/README.md`). All 35 ADRs are now Accepted. Six of the seven generate a user story (ADR-0029–0034); the seventh (ADR-0035, admin UI shape) does not, following ADR-0027's/ADR-0028's own precedent for a reasoned "no story" case rather than forcing one for symmetry — see ADR-0035's own "A note on this ADR's own place in the series' conventions," which also flags that this is now the **third** such case and recommends Menno decide, at acceptance, whether that finally warrants a formal convention rather than a third one-off exception. ADR-0034 additionally surfaced a real, standing gap unrelated to any of the seven decisions themselves: `docs/implementation-log.md` has no entry for the already-shipped "Story 1.6" (connector connect/disconnect) despite its code and an 8-assertion contract test genuinely existing on disk, confirmed directly this session — flagged for correction, not fixed by this batch (see ADR-0034's own Context and Consequences).

¹² ADR-0036 is a genuine 36th ADR, drafted 2026-08-04 by the AI Business & Requirements Analyst persona while drafting the admin UI's required user stories (Menno's own request), after confirming directly that `social-listening-admin` remains an empty shell (no Next.js scaffold; only `src/lib/core-client.ts` and Story 1.1's own contract exist). Unlike Stories 1.5/1.6/1.7's ordinary CRUD precedent (no ADR needed), this ADR decides the admin UI's own authentication/session mechanism — where a bearer token physically lives (server-side session cookie only, never browser-reachable storage), whether to adopt a third-party auth framework (NextAuth.js/Auth.js, rejected for v1 pending verification of its Entra External ID/CIAM support), and how role-gating is enforced in Next.js routing — reasoned to be architecturally significant by the same bar ADR-0027/0028/0035 already established (hard-to-reverse, real security consequence, not just a new field/endpoint shape). It also surfaces a real, previously unnamed cross-repo dependency: `social-listening-core` has no endpoint exposing `resolveIdentity()`'s result over HTTP, confirmed directly against `src/identity/identityResolution.ts` and every `versions/v1/*Router.ts` file — the admin UI cannot determine a caller's role without one, since ADR-0029 §2 already forbids inferring role from any Entra-side claim. Left **Proposed** — this persona does not hold ADR-acceptance authority. See `docs/user-stories/epic-6-admin-ui.md` (Story 6.1) for the story this ADR sources, and its own "A note on this ADR's own place in the series' conventions"-equivalent reasoning inline in its Context section for why it departs from the "ordinary UI/CRUD surface doesn't need an ADR" default.

## Proposed (not yet decided)

**ADR-0036 (admin UI authentication/session/role-gating mechanism) — drafted 2026-08-04, awaiting Menno's review.** See footnote 12 and the ADR's own Status line, Context, and Open Questions for what it decides and what it explicitly leaves open (most notably, whether NextAuth.js/Auth.js's Entra External ID support is actually solid enough to adopt, which this ADR's own drafting could only check to a web-search-level bar, not the primary-source depth ADR-0029 held itself to). Story 6.1 (`docs/user-stories/epic-6-admin-ui.md`) is Blocked pending this ADR's acceptance, the same convention every other ADR-sourced story in this series follows.

Prior to this, this section read (as of 2026-08-03): "**None currently outstanding — this seven-ADR batch is fully accepted as of 2026-08-03.** **ADR-0034 (connector connect/disconnect rework) accepted 2026-08-03**, its own flagged interpretive question (Tenant-Admin's offboarding revocation authority over a user-bound credential) confirmed directly by Menno rather than waved through — see its own Acceptance note. **ADR-0035 (admin UI shape) also accepted 2026-08-03**, and its own closing recommendation decided directly: yes, formalize a "no-story ADR" convention now that three real cases exist — see `docs/user-stories/README.md`'s new "No-story ADR convention" section, added at this acceptance. **ADR-0029 (authentication mechanism) was accepted 2026-08-03**, verbatim: "reviewed ADR 0029 and approved." **ADR-0030 (Admin-tier design) was also accepted 2026-08-03**, revised at review to add a narrow break-glass mechanism — Menno's own words, verbatim: *"I noted the break the glass procedure and that will be a minimum Tenant Admin User Password Reset. No other access to the clients data is permitted"* — see ADR-0030's own §3, Acceptance note, and Amendment Log. **ADR-0031 (`tenants` table shape) was also accepted 2026-08-03**, revised at review to add sign-up domain capture — verbatim: *"Store the sign up users work domain as a domain field in the tenant id to have future similar domains be rerouted"* — see ADR-0031's own §5, Acceptance note, and Amendment Log. **ADR-0032 (`users` table shape) was also accepted 2026-08-03**, after two clarifying exchanges that changed nothing, then revised at review to replace `status`'s `'suspended'` value with a nullable `access_ends_at` timestamp — Menno's own words, verbatim: *"Replace status active/suspended with a nullable end date. removing an end date makes the account according to the audit trail per that date active again"* — see ADR-0032's own §9, Acceptance note, and Amendment Log. **ADR-0033 (retire `X-Tenant-Id`) was also accepted 2026-08-03**, as drafted with no revisions — verbatim: "ADR 0033 remove X Tenant for bearer token. Is Approved." As of 2026-08-03, all 35 ADRs in this series are Accepted — this entire seven-ADR batch (0029–0035) closed the same day it was drafted.

Prior to this batch, this section read (as of 2026-08-03, earlier the same day, after ADR-0028's acceptance): "**None currently outstanding.** ADR-0028 (credential creation authority scoped by ownership tier — no system-wide credentials, tenant-wide credentials created only by Tenant-Admin, user-bound credentials self-activated by the user) was the last one in this state — drafted 2026-08-03, left Proposed since the drafting persona doesn't hold acceptance authority, then revised in place five times the same day (concrete social-platform account-type examples sharpening tiers 2/3; the AI-enrichment scope question resolved by Menno — Azure AI Language is a tenant-owned tier 2 credential, cost settled directly between tenant and vendor; a Clarification on credential-ownership-vs-data-visibility scope; a correction to its own first-drafted Reddit example, verified against Reddit's real OAuth2 documentation and found to support a tier 2 app-only path after all; a concrete tier 3 respond/reply example, parked as a requirement against the future Social Care subsystem in `docs/future-subsystems.md`) — then accepted by Menno the same day it was drafted, verbatim: *"that will allow me to approve the ADR and confirm the ADR's intentions and described concepts are approved."* See footnote 10 and the ADR's own Status line, Acceptance note, and Amendment Log. As of 2026-08-03, 28 ADRs exist in this series, all Accepted.

Prior to this acceptance, this section read (as of 2026-08-03, earlier the same day): "**ADR-0028**... is the current outstanding item — drafted 2026-08-03, left Proposed since the drafting persona doesn't hold acceptance authority, and since revised twice more the same day: concrete social-platform account-type examples added to sharpen tiers 2/3, and the AI-enrichment scope question flagged during drafting was directly resolved by Menno... As of 2026-08-03, 28 ADRs exist in this series: 27 Accepted, 1 Proposed (ADR-0028)."

Prior to ADR-0028 being drafted, this section read "None currently outstanding" as of 2026-08-01: ADR-0027 (connector architecture is a technical intermediary only, never a contracting party) was the last one in that state — drafted 2026-08-01, left Proposed since the drafting persona doesn't hold acceptance authority, revised twice the same day (a paid-tier eligibility rule added, then reversed — see its own Amendment Log), then accepted by Menno the same day: "Approved as important foundation." See footnote 9 and the ADR's own Status line, Acceptance note, and Amendment Log.

Prior to ADR-0027, this section read "None currently outstanding" as of 2026-07-31: ADR-0026 (RSS/News connector — GNews API, with publication-as-Author modeling) was the last one in that state — drafted 2026-07-31, left Proposed since the drafting persona doesn't hold acceptance authority, then accepted by Menno the same day it was reviewed (2026-07-31) — see footnote 8 and the ADR's own Status line, Acceptance note, and Amendment Log.

These originated new policy rather than documenting an existing decision — gaps identified during review (independently by this series' author and by two rounds of Copilot review) that the spec and design conversation left unaddressed. Each separated a durable *decision* from adjustable *implementation defaults*, with an Amendment Log for logging parameter changes (e.g. a deprecation window changing from 6 to 12 months) without superseding the ADR — that pattern remains in force post-acceptance; only a change to the underlying decision itself now warrants superseding. (ADR-0017, ADR-0018, ADR-0019, ADR-0020, ADR-0021, ADR-0022, and ADR-0023 have all since been accepted — see the main table above — and none remain in this section.)

Third-party review also surfaced a numeric disagreement with ADR-0017's original default, resolved and logged in that ADR's own Amendment Log (not a new ADR): ADR-0017's deprecation window shortened from 6 months to 90 days. A similar review-round disagreement briefly shortened ADR-0018's `IngestionRun` archival window from 18 months to 90 days (aligned with `rawPayload`'s window), but that alignment was reverted at acceptance — see ADR-0018's Amendment Log — back to 18 months, now configurable. Monthly range partitioning was added as the archival mechanism for both `SocialPost` and `IngestionRun` regardless of the window length. ADR-0020–0023 saw no such numeric disagreements — each was accepted with its originally proposed defaults unchanged (see footnote 5).

**Still outstanding, not yet drafted:** ADR-0004's point-in-time author snapshot (retaining `followerCount`-at-publish-time on `SocialPost` despite `Author` being normalized) was flagged as a genuine trade-off — not a strict improvement — during the same review round, but wasn't included in the batch above. Needs an explicit go/no-go before drafting, since it partially reintroduces the per-post duplication ADR-0004 argued against.

**Multi-tenant Admin/Tenant/User model (brainstormed 2026-07-30, not yet drafted):** no `tenants` table, `users` table, or Admin tier exist anywhere in this series — "tenant" today is purely a `tenant_id` UUID convention (ADR-0015 isolates it; nothing provisions it). A same-day brainstorm settled several shapes without drafting any ADR yet (paused mid-session, to be continued):
- Three tiers: Platform Admin → Tenant (with its own Tenant-Admin role) → Tenant User.
- User onboarding: invite-only, gated by a per-tenant license/seat count — no request-then-approve queue.
- ~~Connectors are tenant-owned only; personal-account connectors are a future Social Selling subsystem's concern, not this one's.~~ — **superseded 2026-08-03:** Menno (Sponsor) has since stated directly that the future authentication/credential architecture must support credentials held per-user, not only tenant-wide (the concrete driver is a future connector, Reddit's OAuth grant being the clearest example, needing an individual user's own credential rather than a tenant-wide one). This bullet's "tenant-owned only" framing no longer reflects that direction; per-user connector ownership is this subsystem's concern too, not solely a future Social Selling subsystem's. See `docs/project docs/Business-Case-v6.0.md`'s 2026-08-03 notes (Sections 2, 6, 9) for full sourcing, and ADR-0027's Negative Consequences bullet for the pre-existing architectural gap this requirement now closes. Candidate ADR #4 (`users` table shape) must be drafted against this corrected requirement, not the struck-through one.
- ~~Platform Admin's boundary leans toward provisioning-only (create/suspend a tenant, set its license count), zero tenant-data access — floated, not locked.~~ — **locked, 2026-08-03, ADR-0030 (Proposed):** Platform Admin's `platform_admin_role` is granted database access only to the `tenants` table (candidate ADR #3) and its own separate Platform Admin identity table (candidate ADR #4) — never any tenant-content table. No emergency/support-access override exists in this design either (resolving ADR-0028's own flagged Open Question on the point). Still subject to Menno's acceptance of ADR-0030 itself, but no longer floating as an undecided brainstorm item.
- ~~Two opens, unresolved: whether connector *activation* needs its own table separate from `platform_credentials`...; whether a tenant needs multiple activations of one platform...~~ — **addressed, not fully resolved, 2026-08-03, ADR-0034 (Proposed):** connector activation does not need a separate table — `platform_credentials` gains `owner_type`/`user_id` columns instead (ADR-0028's Tier 2/Tier 3 made concrete). Whether a tenant may hold multiple activations of one platform is still genuinely open — ADR-0034's schema does not structurally prevent it, but whether it *should* be allowed and how it would be surfaced is left as that ADR's own named Open Question, not decided.

**Candidate future ADRs, roughly in dependency order — all seven now drafted, 2026-08-03 (Proposed, pending Menno's acceptance):** (1) authentication mechanism — [ADR-0029](0029-authentication-mechanism-entra-external-id.md); (2) Admin-tier design — [ADR-0030](0030-admin-tier-design-platform-admin-rls-exception.md); (3) `tenants` table shape — [ADR-0031](0031-tenants-table-shape.md); (4) `users` table shape + RLS — [ADR-0032](0032-users-table-shape-and-rls.md); (5) retiring the `X-Tenant-Id` header placeholder — [ADR-0033](0033-retire-x-tenant-id-header-placeholder.md); (6) connector connect/disconnect CRUD — [ADR-0034](0034-connector-connect-disconnect-crud-ownership-tier-aware.md); (7) admin UI's own shape (one app or two) — [ADR-0035](0035-admin-ui-shape-one-app-role-gated.md). See footnote 11 for the full drafting record. This section's own historical brainstorm bullets above are left in place, struck through and dated where an ADR now supersedes them, per this file's own "don't rewrite history" convention — not deleted.

**ADR-0027 (Accepted, 2026-08-01) was a constraint on this list, not a member of it — now checked against the drafted batch, not merely anticipated.** It doesn't decide whether the connecting party ends up being the tenant or an individual user; ADR-0032 (`users` table shape) resolves that at the schema level (both tiers coexist, ADR-0028's Tier 2/Tier 3), and ADR-0034 (connect/disconnect CRUD) is the concrete point where ADR-0027's "no pooling, no contracting on anyone's behalf" principle is actually enforced in code for the first time.

**ADR-0028 (Accepted, 2026-08-03) was a second constraint on this list — now built against directly, not just anticipated.** ADR-0030 designates Tenant-Admin's mechanics (ordinary tenant-scoped RLS + an application-layer role check, no database bypass); ADR-0032 gives `users.role` a concrete `'tenant_admin'` value; ADR-0034 is where ADR-0028's three-tier credential-creation rule is actually enforced — `platform_credentials.owner_type`/`user_id`, plus the authorization checks in `connectorsRouter.ts` ADR-0034 specifies. **Resolved 2026-08-03, same day as drafting:** ADR-0028 initially flagged, without resolving, a real tension with ADR-0027's own Accepted Decision text over whether "no system-wide credentials" reaches AI-enrichment infrastructure (Azure AI Language) as currently planned. Menno resolved it directly: `AIProviderConnector` credentials are tenant-owned (ADR-0028's tier 2), cost settled directly between the tenant and the vendor, no SocialEngage-operated shared subscription — see ADR-0028's own Context/Amendment Log and footnote 10. This has a real, flagged (not yet re-derived) consequence for `Project Management Plans/Cost-Management-Plan.md`'s existing project-level enrichment cost line.

Considered and explicitly **not** drafted as ADRs, per review discussion:
- **Capability-based connector composition / connector capability registry** — premature for a two-branch hierarchy (`SocialConnector`, `AIProviderConnector`); the spec doesn't describe a third, structurally different provider type that would justify it yet. Revisit if one materializes (rule of three).
- **CODEOWNERS / repo ownership** — reasonable, but a repo-governance artifact, not an architecture decision.
- **OpenAPI-first contract governance, connector certification, sandbox/test harness, DR/replay strategy** — reasonable platform-maturity investments, but ahead of where this subsystem is: pre-implementation, no downstream consumers built yet. **Revisit trigger:** before the first downstream subsystem (likely Brand Reputation & Alerts) begins integrating against `social-listening-core`'s API — not before, per a second round of Copilot review.
- **Consumer contract ownership** (who owns backward compatibility as the producer/consumer graph grows — `social-listening-core` vs. each downstream subsystem) — flagged as a likely future ADR candidate once implementation begins and a second or third real downstream consumer exists to reason about. Not drafted now, and not pinned to a specific number since 0020–0023 are now in use — noted so it isn't lost.

## Not captured as ADRs

The following are called out in the spec but are scope boundaries or process decisions rather than architecture decisions, so they're not recorded as ADRs — none of them involve the kind of hard-to-reverse technical trade-off this series exists to justify:

- Initial connector build order — **resolved 2026-07-29**, see spec §10 and `docs/implementation-plan.md` (RSS/News first, then Reddit; remaining platforms prioritized in Phase 4)
- Exact connector auto-disable failure threshold — governed by ADR-0010's flat placeholder through Phase 3; superseded 2026-07-30 by ADR-0023's proportional rule (Story 2.5, implemented ahead of the rest of Phase 4 — see `docs/implementation-log.md`); see spec §10. (Distinct from ADR-0020's separate per-request dead-letter threshold, Story 2.4.)
- Testing strategy and CI/CD pipeline details for the two repos — **resolved 2026-07-29**, see spec §10 (lightweight CI given this is a solo-developer project, not team-scale process). **Exception, 2026-07-30:** ADR-0025 (persistent local dev database) is dev tooling in this same general category, captured as an ADR anyway by explicit user decision after real friction was hit in practice — see its own Acceptance note and footnote 7 above. Not a reversal of this bullet's general rule, just a named, flagged exception to it.
- Geocoding of `profileLocation` — explicitly out of scope, §9, no decision made to record
