# ADR-0139: RAG search and ask endpoint — namespace resolution

**Status:** Proposed (2026-08-28)

**Authorizes:** refinements to ADR-0084: resolves tenant namespace at query dispatch time and enforces zero cross-namespace vector traversal.