# Implementation & Delivery Plan — Social Listening / Insights Subsystem

Sequences the [user stories](user-stories/README.md) — grounded in the [ADRs](adr/README.md) — into build phases. Originally 23 stories/ADRs (2026-07-29); 25 after Story 2.6/ADR-0024 and Story 1.4/ADR-0025 were each added and accepted the same day they were drafted (2026-07-30); 26 after Story 2.7/ADR-0026 (RSS/News connector — GNews API) was drafted 2026-07-31 and accepted by Menno later the same day — see `docs/adr/README.md`'s footnotes 6/7/8. Unlike the first two additions, ADR-0026 wasn't self-accepted by the same session that drafted it (the drafting persona doesn't hold acceptance authority), so Story 2.7 was briefly Blocked before Menno's separate review — it is now Ready, same as every other story that predates this paragraph's next update. **Now 34 stories total**, after a seven-ADR batch (ADR-0029–ADR-0035, `docs/adr/README.md` footnote 11) drafted 2026-08-03 added Story 1.6 (retroactive, formalizing already-shipped connect/disconnect work), Story 1.7 (ADR-0034), and Stories 5.6–5.10 (ADR-0029–ADR-0033) — sequenced below into a **new Phase 4.5**, not folded into Phase 0–4 (which predate the multi-tenant model entirely) or Phase 5 (cross-cutting hardening work with no stories of its own) — see Phase 4.5's own goal statement for the reasoning. This is a dependency-ordered plan, not a calendar: no durations or sprint lengths are estimated. This is a solo-developer personal project (confirmed 2026-07-29), so "no team-scale process" is a deliberate choice throughout this plan (see Phase 0's testing/CI recommendation and Phase 4's note on multi-instance work), not just an absence of data to estimate against.

## How to read this

- **Ready** stories (34 of 34, as of 2026-08-03 — ADR-0034's acceptance moved Story 1.7 from Blocked to Ready, the last one; every story in this series is now Ready) are sourced from Accepted ADRs (or, for Stories 1.5/1.6, from unstoried Phase 1 work already built) and can be built as written. "Ready" is not the same as "built yet," though: several Ready stories (e.g. 2.4, 2.5, 3.5, 3.6, 4.4) are still deliberately scheduled for a later phase — see each one's own scheduling note. Story 2.7 was Ready-but-unbuilt as of 2026-07-31; it's been built since, 2026-08-01 — see Phase 1's own note below.
- **Blocked** stories: **none, as of 2026-08-03.** ADR-0034 (Story 1.7) and ADR-0035 (admin UI shape, no story) were both accepted 2026-08-03, closing out this seven-ADR batch entirely (ADR-0030 revised at review to add a narrow break-glass mechanism; ADR-0031 revised at review to add sign-up domain capture; ADR-0032 revised at review to add `access_ends_at`; ADR-0033 accepted as drafted; ADR-0034's flagged Tenant-Admin-revocation reading confirmed; ADR-0035's rule-of-three recommendation decided — see `docs/user-stories/README.md`'s new "No-story ADR convention" section). **Phase 4.5 is now fully unblocked.**
- **This plan is not the whole scope.** The original 23 stories cover architecturally significant decisions — that's what the ADR series was for. Ordinary CRUD surface (watchlist create/read/update/delete, connector connect/disconnect endpoints, the admin UI's screens) was never architecturally interesting enough to warrant its own ADR, but it's real, necessary work that has to happen alongside the storied work. It's called out per phase below so it isn't invisible.
- **Not covered at all:** Brand Reputation & Alerts, Social Care, Social Selling — explicitly deferred to be designed separately once this subsystem ships (spec §1, §9). Nothing in this plan builds toward them beyond leaving the REST API and Service Bus events they'll eventually consume. See [`docs/future-subsystems.md`](future-subsystems.md) for what's parked against each one, rather than just "out of scope."
- **How each story actually gets built** (scope discipline, contract-first testing, component `SKILL.md`s, permanent regression suite) is a separate concern from *when* — see [`docs/implementation-methodology.md`](implementation-methodology.md), operationalized as the `implement-story` Claude Code skill. This plan says what and when; that document and skill say how, uniformly, for every story in every phase below.

## Decisions resolved 2026-07-29

Three things needed an answer that nothing in the ADR series or spec resolved on its own — two were spec's own long-standing open questions (§10), one was a judgment call surfaced by phasing the work. All three are now decided (see spec §10 for the authoritative record):

1. **Which platform ships first:** RSS/News first (poll-only, API-key auth, no paid tier — cheapest way to validate the full pipeline without also debugging OAuth and rate-limit-tier economics in the same pass), then Reddit second (poll, OAuth, real rate limits, real boolean-query needs) specifically to prove the `ProviderConnector` abstraction (ADR-0002) generalizes rather than having been shaped around only the first connector.
2. **Testing strategy and CI/CD:** deliberately lightweight, matching a solo-developer project rather than team-scale process — GitHub Actions on push/PR running lint, type-check, and unit tests for both repos, plus one integration-test tier against an ephemeral/dockerized Postgres (RLS and JSONB behavior need real Postgres, not a mock). No CODEOWNERS, no OpenAPI-first contract-testing pipeline yet — consistent with those already being deferred in `docs/adr/README.md`'s "explicitly not drafted" list, for the same underlying reason: that's process built for contributors and consumers that don't exist yet.
3. **All 23 ADRs in this series are now Accepted (2026-07-29).** ADR-0017 (API versioning) and ADR-0019 (event schema versioning) were accepted ahead of the phases that would otherwise touch them — both were cheap to decide now and expensive to retrofit after real endpoints/consumers exist. ADR-0018 (data retention and archival) — `rawPayload` at 90 days, `IngestionRun` at 18 months, both configurable — and ADR-0020–0023 were accepted on their normal schedule, not early for a scheduling reason; each simply resolved its own open implementation-default questions at acceptance (see each ADR's "Acceptance note"). Being Accepted doesn't pull a story's *build* forward, though: Stories 3.5, 2.4, 2.5, 3.6, and 4.4 are all Ready now but stay scheduled for Phase 4 below, same as before acceptance — only the "needs an ADR decision before this phase can close" gate is gone, not the phase sequencing itself. One acceptance has a real code consequence worth flagging here: ADR-0023's rate-relative failure threshold partially supersedes the flat rule Stories 2.3 and 4.3 already shipped with (see ADR-0009's and ADR-0010's "Supersession update" notes) — their code stays as shipped until Story 2.5 is actually built, which is when that healing happens, not now.

---

## Phase 0 — Foundations (decisions + scaffolding, no ingestion yet)

**Goal:** two deployable-but-empty repos, a tenant-isolated database, and credential storage — nothing that ingests a post yet, but everything a connector will need to plug into.

**Stories:** 1.1 (repo split), 1.2 (Postgres + JSONB), 5.4 (RLS on every tenant table), 5.3 (Key Vault envelope-encrypted credential storage), 1.3 (API versioning — decided ahead of schedule, see above), 1.4 (persistent local dev database, ADR-0025, accepted 2026-07-30 — the Postgres half of the "local dev environment" line below, promoted out of unstoried tooling after real friction was hit in practice; see ADR-0025's own Acceptance note for why this is a deliberate exception).
**Also build, not storied:** `/v1/` route scaffold (empty, per the now-accepted ADR-0017); lightweight GitHub Actions CI per the decision above; the Key Vault half of a local dev environment (an emulator or dev tenant — the Postgres half is now Story 1.4 above).

**Deliverable:** `social-listening-core` and `social-listening-admin` both deploy successfully to a dev environment; a smoke-test tenant can be created with RLS-isolated tables and a stored, encrypted dummy credential — with no connector, watchlist, or post yet.

---

## Phase 1 — MVP: one connector, end to end

**Goal:** the smallest real slice — one platform, one tenant, one watchlist, posts flowing from that platform into a queryable API, with visible health status. This is the phase that proves the architecture, not just the first platform.

**Stories:** 2.1 (connector framework), 3.1 (Author), 3.2 (IngestionRun), 2.2 (per-tenant rate limiting), 2.3 (error handling + flat auto-disable threshold), 3.3 (connector-side/fallback watchlist matching), 3.4 (cursor pagination on `GET /posts`), 4.3 (derived `ConnectorHealth`), 2.7 (RSS/News connector: GNews API, publication-as-Author — ADR-0026, drafted and accepted by Menno 2026-07-31, **built 2026-08-01**; see below).
**Also build, not storied:** `POST/GET/PATCH/DELETE /watchlists` CRUD; `POST /connectors/:platformId/connect` + `DELETE .../disconnect`; the admin UI's connect flow, watchlist management screen, and connector status view (the "minimal admin UI" scope named in the original design conversation).

**2026-07-31 update — the "first connector implementation itself" line above is now Story 2.7, not unstoried scope.** RSS/News was chosen as a *category* 2026-07-29 (poll-only, API-key auth, no paid tier) but never given a concrete provider — the gap ADR-0024's own Context flagged explicitly, and the single oldest unbuilt piece of this plan even after Story 2.6 (a later, Phase-4 connector) shipped ahead of it. ADR-0026 (drafted 2026-07-31 by the AI Business & Requirements Analyst persona) selects **GNews API** as the concrete provider, verified directly against `gnews.io`/`docs.gnews.io`, with a publication-as-Author modeling departure mirroring ADR-0024's issuer-as-Author exception. Accepted by Menno later the same day. See ADR-0026 and Story 2.7 for the full decision and its Amendment Log (including two candidates, NewsAPI.org and Currents API, whose free tiers were verified to explicitly forbid the real/production use this connector needs).

**2026-08-01 update — Story 2.7 built, closing Phase 1's own longest-standing gap.** `src/connectors/gnews/gnewsConnector.ts`/`pollGNewsSearch.ts` poll GNews's real Search endpoint using a per-tenant, envelope-encrypted API key (ADR-0014/ADR-0027 — never a SocialEngage-held credential), normalizing articles into `SocialPost` with `Author` resolving to the source publication. Contract proven against genuinely live GNews results and a real Azure Key Vault, same bar as Story 2.6.

**2026-08-01 update — Story 1.5 (watchlist CRUD) complete.** `POST/GET/PATCH/DELETE /v1/watchlists` REST surface implemented with full tenant isolation via RLS (`tenant_isolation` policy). Includes `src/watchlists/watchlistStore.ts` (CRUD operations), `src/http/versions/v1/watchlistsRouter.ts` (HTTP layer), `migrations/0014_create_watchlists.sql` (schema), and `contracts/epic-1/story-1.5.watchlist-crud.contract.test.ts` (15 acceptance criteria). All contracts pass, including cross-tenant isolation (AC7), X-Tenant-Id header validation (AC8-11), and default value handling (AC14). This closes the watchlist CRUD gap in Phase 1's "also build, not storied" work.

**2026-08-01 update — Story 2.6 (Newswire connector) complete.** GlobeNewswire and PR Newswire RSS feeds now ingesting via `src/connectors/newswire/newswireConnector.ts` with `pollNewswireFeeds()`. All contracts pass, including AC1 (real items from both wires) and AC3 (fallback behavior for unsupported query features). Phase 1 now has **two working connectors** (GNews API + Newswire RSS).

**2026-08-01 update — Story 1.6 (Connector connect/disconnect) complete.** `POST /v1/connectors/:platformId/connect` and `DELETE /v1/connectors/:platformId/disconnect` endpoints implemented in `src/http/versions/v1/connectorsRouter.ts`, with `deleteCredential()` added to `src/credentials/credentialStore.ts`. All 8 contracts pass, including tenant isolation via RLS (AC6). See `contracts/epic-1/story-1.6.connector-connect-disconnect.contract.test.ts` and `.claude/skills/credential-envelope-encryption/SKILL.md`. **This story's own formal entry in `docs/user-stories/epic-1-repository-and-api-foundation.md` was added retroactively, 2026-08-03** — it existed only informally, in this file and `docs/open-items-and-deferred-work.md`, until then.

**2026-08-03 forward-pointing note — Story 1.6's placeholder-auth shape is scheduled for supersession in Phase 4.5, not fixed here.** Story 1.6 trusts `X-Tenant-Id` as its entire tenant boundary and has no ownership-tier or role concept, exactly as `Business-Case-v6.0.md` §6's dependency matrix anticipated before this story was built ("could theoretically be built against the placeholder... would need rework once real auth lands"). That rework is **Story 1.7** (ADR-0034), scheduled in the new **Phase 4.5** below, once real authentication (ADR-0029/ADR-0033) and the ownership-tier model (ADR-0028, ADR-0032) exist. Story 1.6's code is not touched by this note — it stays exactly as shipped until Story 1.7 is actually built, the same "don't assume already-shipped code changes automatically" discipline this plan already applies to ADR-0023's supersession of Stories 2.3/4.3 (§ "Decisions resolved 2026-07-29," and `docs/adr/README.md` footnote 5). **Resolved, 2026-08-04:** Story 1.7 is now built — see Phase 4.5's own dated note below for what changed.

Phase 1 now closes on its own original terms ("one platform, one tenant, one watchlist... end to end") for the storied half of that deliverable. The "also build, not storied" half has **watchlist CRUD complete** and **connector connect/disconnect endpoints complete (Story 1.6, placeholder-auth shape — see the forward-pointing note above)** — only admin UI (connect flow, watchlist management screen, connector status view) remains open, see below.

**2026-08-04, Story 6.1 now built** — `social-listening-admin` is a real Next.js scaffold with a working, real (not mocked) Entra External ID sign-in and a server-side BFF session, contract-verified (`social-listening-admin/contracts/epic-6/story-6.1.nextjs-scaffold-and-entra-signin.contract.test.ts`, 18/18, `docs/implementation-log.md`). Stories 6.2/6.3/6.4's own practical "after Story 6.1" sequencing note (`docs/user-stories/epic-6-admin-ui.md`) can now actually start. Still blocked on the same named cross-repo gap ADR-0036 §5 always named: `GET /v1/me` doesn't exist in `social-listening-core` yet, so role-gating (Story 6.2) has nothing real to key off until that endpoint is built.

**2026-08-04 update — the admin UI's own required user stories are now drafted, not just named as an unstoried line item.** `docs/user-stories/epic-6-admin-ui.md` (Stories 6.1–6.6) covers this row's own three named screens (connect flow → Story 6.3; watchlist management → Story 6.4; connector status view → Story 6.5) plus a prerequisite auth/session story (6.1), a role-gated routing shell (6.2, citing ADR-0035 directly per that ADR's own recommendation), and Phase 3's "beyond the Phase 1 minimum" Platform Admin console (6.6, below). **This drafting pass found the "admin UI" line item understates its own real scope in two concrete ways, not previously named here:** (1) the admin UI's own authentication/session mechanism is not ordinary CRUD/UI surface — it is a real, hard-to-reverse security decision (where a bearer token lives; whether a third-party auth framework's Entra External ID support is verified) warranting its own ADR (**ADR-0036**, Proposed) by this project's own established bar, unlike Stories 1.5/1.6/1.7; (2) the admin UI cannot determine a signed-in caller's role without a new `social-listening-core` endpoint exposing `resolveIdentity()`'s result over HTTP (confirmed not to exist today) — a real, previously unnamed cross-repo dependency, not designed or built by this drafting pass. See ADR-0036 and Story 6.1's own Acceptance Criteria.

**2026-08-04 update, same day — a third gap found in the "admin UI" line item: self-service tenant sign-up itself.** Every screen named above assumes a tenant, and at least one Tenant-Admin inside it, already exist — none of Stories 6.1–6.6 covers how the very first tenant and its first Tenant-Admin come into being outside of Platform Admin manually provisioning one (Story 6.6, itself blocked on its own backend gap). Menno's direct request for "a user sign up screen for tenant admins" surfaced this as a real, previously undesigned gap: ADR-0031 §5 names the event but explicitly declines to design it; ADR-0032 §6 designs only the invite flow, not first-tenant creation; `tenantStore.ts`'s `createTenant()` is reachable only through `platform_admin_role` today. **ADR-0037** (Proposed) and **Story 6.7** (`docs/user-stories/epic-6-admin-ui.md`) now cover this — Story 6.7 is Blocked pending ADR-0037's acceptance, and depends on a new `POST /v1/tenants/self-service-signup`-shaped core endpoint that does not exist yet, named as a required prerequisite, not built by this drafting pass, the same treatment Story 6.1 gave `GET /v1/me`.

**A real scoping note, not a decision, worth flagging here explicitly:** for this project's own current scale (solo-operated, Menno as both Sponsor and, in practice, Platform Admin), self-service sign-up is not strictly required to operate — Platform Admin can already provision a tenant directly via Story 6.6's own console (once its own backend gap closes) or `tenantStore.createTenant()` directly. Menno explicitly asked for this feature anyway, so it is real, requested scope, not something this drafting pass deprioritizes unilaterally — named here only so the "is this on the critical path to a usable product, or a later-launch feature" question is visible for Menno's own sequencing call, not pre-answered.

**Deliverable:** a tenant can connect one real platform through the admin UI, create a watchlist, and see matching posts appear via `GET /posts` within one polling cycle — with `GET /connectors` correctly showing `healthy`, and correctly flipping to `degraded`/`failing` when the connection is deliberately broken in a test.

---

## Phase 2 — Enrichment and AI-provider swappability

**Goal:** posts get sentiment/entities/key phrases, and the AI provider is proven swappable the same way the social connector was in Phase 1 — not just implemented once and assumed generic.

**Stories:** 4.1 (`AuthorTopicSignal` raw signals), 4.2 (confirm `TopicDailyCount`-supporting fields are captured — this one is largely a verification, not new build).
**Also build, not storied:** the first `AIProviderConnector` implementation (Azure AI Language, per spec §2); `POST /ai-providers/:id/connect`, `GET /ai-providers/:id/models`, `POST /ai-providers/:id/select-model`; wiring enrichment into the ingestion pipeline after normalization.
**Recommended validation, mirroring Phase 1's two-connector proof:** stand up a second `AIProviderConnector` (even a trivial/mock one) specifically to confirm swapping AI providers really does need zero core pipeline changes, the same way Reddit validated the social side in Phase 1.

**Deliverable:** ingested posts carry `sentiment`, `keyPhrases`, `entities`, `detectedLanguage`, and `modelUsed`; `GET /topics/:topic/authors?sortBy=mentionCount` returns real, non-empty results for a topic with enough history.

---

## Phase 3 — Eventing and admin UI completion

**Goal:** the subsystem starts actually behaving like a platform other subsystems can build on, even though none exist yet — publish real events, and finish the admin UI beyond the Phase 1 minimum.

**Stories:** 5.1 (thin `SocialPostIngestedEvent`/`ConnectorHealthChangedEvent`), 5.2 (per-tenant Service Bus subscription filtering), 5.5 (`schemaVersion` message property — decision already made in Phase 0, built here).
**Also build, not storied:** a throwaway or internal test subscriber, since there's no real downstream subsystem yet to validate against — without one, ADR-0012/0013's design is unverified in practice.

**2026-08-04 update — "finish the admin UI beyond the Phase 1 minimum" is now concretely Story 6.6** (`docs/user-stories/epic-6-admin-ui.md`): the Platform Admin console (tenant provisioning/suspension, license-seat management, the two-phase break-glass credential-reset flow, and the Platform-Admin audit log), governed by ADR-0030/ADR-0031/ADR-0035, all already Accepted. **A real, substantial gap this drafting pass surfaced, not previously named in this plan:** no HTTP/REST surface exists for any of it yet — confirmed directly against `social-listening-core/.claude/skills/tenants/SKILL.md` ("No HTTP/REST surface exists for `tenants` yet") and `platform-admin-access/SKILL.md` (break-glass request/execute and the audit log are store/mechanism-level only, Stories 5.7/5.8). Everything Phase 4.5 built (`tenantStore.ts`, `breakGlassCredentialReset.ts`, `platform_admin_audit_log`) is real and contract-verified at the store/database layer, but none of it is reachable over `/v1/` yet — Story 6.6 cannot be built end-to-end until that REST surface exists, real `social-listening-core` work this plan did not previously account for.

**Deliverable:** every ingested post publishes a correctly-filtered, correctly-versioned event within a defined latency of being written; a test subscriber scoped to one tenant provably never receives another tenant's events.

---

## Phase 4 — Multi-connector scale-out and hardening

**Goal:** everything that only matters once there's more than one connector, more than one instance, or real data volume — deliberately sequenced after Phase 1–3 prove the architecture on a small footprint, not before.

**Stories:** 3.6 (watchlist AST + capability matrix — becomes valuable once a 3rd+ connector makes native/fallback drift a real risk), 2.4 (bounded queues, DLQ, distributed `RequestGate` state), 2.5 (proportional failure threshold, replacing 2.3's flat rule — best tuned once real differently-paced connectors exist to tune it against), 4.4 (derived-data caching — needed once read volume justifies it), 3.5 (tiered retention/archival — needed once storage volume, not correctness, is the driver), 2.6 (Newswire connector — GlobeNewswire + PR Newswire direct RSS, ADR-0024, accepted 2026-07-30, outside the original 23-ADR scope).
**Also build, not storied:** third and fourth connectors (whichever platforms are prioritized next) — Story 2.6/ADR-0024 is the first of these to get its own ADR, because of the issuer-as-Author modeling decision it needed, not because connector selection itself became storied work generally.

**Solo-project note on Story 2.4 specifically:** its whole premise (ADR-0020) is correctness under more than one `social-listening-core` instance running concurrently. For a solo-operated deployment, that condition may not arise for a long time, if ever — there's no team driving a scaling need, and a single instance may simply be enough. Don't build the Redis-backed distributed gate speculatively; a single-instance in-process `RequestGate` already satisfies ADR-0003 correctly on its own. Treat the distributed-gate half as "build when you actually deploy a second concurrent instance," not "build in Phase 4 on schedule" — it may end up the last piece of this entire plan to actually get built, or never, and that's fine. **Update 2026-07-30:** the other half — bounded queue TTL/depth ceiling and per-request dead-lettering, "relevant even on a single instance" per ADR-0020's own framing — has shipped (see `docs/implementation-log.md`); only the distributed-gate half remains deferred by this note.

**Deliverable:** `social-listening-core` runs correctly as more than one instance under load; a genuinely-broken low-frequency connector and a flaky high-frequency one are both judged fairly by auto-disable; storage growth is bounded without losing analytically-relevant history.

---

## Phase 4.5 — Multi-tenant identity & access foundation

**Goal:** close the authentication and tenant/user-identity gap this project has flagged as release-blocking since before any tenant existed — `Business-Case-v6.0.md` Risk R-04 ("No authentication mechanism exists... release-blocking... once external users are contemplated") and `docs/adr/README.md`'s own 2026-07-30 candidate-ADR framing ("(1) authentication mechanism — blocks everything else"). Six stories, sourced from a seven-ADR batch (ADR-0029–ADR-0035, see `docs/adr/README.md` footnote 11), built in strict dependency order. **All seven ADRs (0029–0035) accepted 2026-08-03** — every story in this phase (5.6–5.10, 1.7) is Ready. Phase 4.5 is fully unblocked, the same day it was drafted. **A seventh story, 5.11, was added 2026-08-05** — see the dependency-chain list and its own dated note below; the "six stories... built in strict dependency order" framing in this paragraph describes this phase's original scope, not its current one.

**Why this is its own phase, not folded into Phase 0–4 or Phase 5 — stated explicitly, as this plan's own task required:**
- **Not Phase 0–4:** every one of those phases predates the multi-tenant Admin/Tenant/User model entirely — Phase 0's own deliverable already shipped a tenant-isolated database built around a bare `tenant_id` UUID convention with no real caller identity behind it, and Phases 1–4 were built and, in most cases, already shipped against that same placeholder. Retrofitting this batch into any of those phases would misrepresent when it actually became buildable (only once ADR-0028's ownership-tier principle and this session's Entra decision existed) and would require reopening already-closed phases.
- **Not Phase 5:** Phase 5 ("Production readiness," below) is explicitly "not storied, not phase-specific to any ADR" — cross-cutting hardening and go-live verification against properties this project's ADR series *already* argued for. This batch is the opposite: six real, ADR-sourced, contract-testable stories that *themselves* need to exist before Phase 5's own "load/chaos testing against the isolation guarantees this whole series argued for" claim can even be evaluated against a real caller identity rather than a self-declared header. Burying six foundational stories inside a phase whose own goal statement says "not storied" would hide them, not schedule them.
- **A new phase, not a renumbering of Phase 5 onward:** "Phase 5" is referenced by name in seven other project documents (`Business-Case-v6.0.md`'s Milestone M7, several `Project Management Plans/*.md` files, `docs/open-items-and-deferred-work.md`, ADR-0027) — renumbering it to "Phase 6" to make room would touch every one of those files for no decision-relevant reason. Inserting a new phase between 4 and 5 avoids that blast radius entirely, consistent with this plan's own existing practice of sequencing insertions without renumbering (Story 2.6/ADR-0024 shipped as Phase 4 work ahead of Phase 1's own connector without renumbering anything).

**Stories, in strict dependency order (each one's own Status line named this chain while their source ADRs were still Proposed; all six are now Ready, all seven ADRs Accepted):**

1. **5.6** (ADR-0029 — Entra External ID authentication) — the root; nothing else in this phase can be built first.
2. **5.7** (ADR-0030 — Platform Admin's audited `BYPASSRLS` role) — depends on 5.6 for the caller identity a Platform Admin action authenticates.
3. **5.8** (ADR-0031 — `tenants` table + RLS) — depends on 5.7's bypass role to be provisioned/administered at all.
4. **5.9** (ADR-0032 — `users` table, RLS, and request-time identity resolution) — depends on 5.8 (`tenants.id` is `users.tenant_id`'s foreign-key target) and reuses 5.7's bypass-role pattern for its own narrower identity-resolution role.
5. **5.10** (ADR-0033 — retire `X-Tenant-Id`) — depends on 5.6/5.9: cannot resolve a real tenant/user identity to replace the header with until both exist.
6. **1.7** (ADR-0034 — ownership-tier-aware connector connect/disconnect, superseding Story 1.6) — depends on 5.9 (the `users.role`/`users.id` this story's authorization checks read) and 5.10 (the `Authorization: Bearer` resolution this story's endpoints rely on instead of `X-Tenant-Id`). **Numbered in Epic 1, not Epic 5, but scheduled in this phase** — see Phase 1's own forward-pointing note above for why its epic and its build phase differ.
7. **5.11** (ADR-0036 §5 — `GET /v1/me`, exposing `resolveIdentity()`'s result over HTTP) — a genuine, late addition to this phase, drafted 2026-08-05, not part of the original six-story chain above. Depends on 5.9 (`resolveIdentity()` itself) and 5.10 (`createTenantAuthMiddleware()`'s "one seam," the auth this new route mounts behind) — both already built. Surfaced by Epic 6's own drafting pass (Story 6.1/ADR-0036, 2026-08-04): the admin UI cannot role-gate anything without it. See `docs/user-stories/epic-5-security-isolation-and-messaging.md`'s own Story 5.11 entry for why this reopens, rather than renumbers, an otherwise-closed phase — the same "append a genuine late addition to an existing phase without renumbering" practice Story 2.6/ADR-0024 already established for Phase 4.

**Also decided, not storied, in this phase:** ADR-0035 (admin UI shape — one role-gated app, not two separate deployables) generates no story of its own (see `docs/user-stories/README.md`'s own note), but is the governing structural constraint for whichever future story first builds a Platform-Admin-facing screen — named here so it isn't lost between this phase and Phase 3's still-open "admin UI... remains open" item (Phase 1, above).

**Also build, not storied, in this phase:** a replacement mechanism for the large share of this project's existing ~130 contract tests that currently set `X-Tenant-Id` directly to establish tenant context under test (ADR-0033's own named Open Question) — real, substantial rework flagged there as a build-time concern, not designed by any of the six ADRs above, and not to be discovered as a surprise mid-phase.

**Deliverable:** every `/v1` endpoint derives tenant and user identity exclusively from a validated Entra-issued bearer token, resolved against real `tenants`/`users` tables under RLS; `X-Tenant-Id` is retired as a trust mechanism everywhere; connector connect/disconnect enforces ADR-0028's ownership tiers for the first time in real code; `Business-Case-v6.0.md` Risk R-04 is closed, not merely mitigated.

**2026-08-05 correction — Phase 4.5 is not fully closed after all.** The paragraph immediately below (2026-08-04) called Story 1.7 "the sixth and final story in Phase 4.5" and this phase's traceability row previously read "no remaining stories" — both true at the time, overtaken the next day when Story 5.11 (ADR-0036 §5's `GET /v1/me` endpoint) was drafted into this same phase, surfaced by Epic 6's own drafting pass. Not fixed by editing the paragraph below in place, per this project's own "don't rewrite history" convention — see the dependency-chain list above (item 7) and this phase's own Traceability-table row for the current, accurate state.

**2026-08-04 update — Story 1.7 built, the sixth and final story in Phase 4.5.** `platform_credentials` gains `owner_type`/`user_id` via an additive migration — every pre-existing row (Stories 1.6/2.6/2.7) becomes `owner_type = 'tenant'`, unchanged in meaning. `POST /v1/connectors/:platformId/connect`'s `ownerType: 'tenant'` path now requires the caller's resolved role to be `tenant_admin`; `ownerType: 'user'` always sets `user_id` to the caller's own resolved identity, never a client-supplied one — proven directly, a spoofed body value has zero effect. Disconnect is the one asymmetric case: tenant-wide requires `tenant_admin`; user-bound succeeds for the owning user or a `tenant_admin` of the same tenant (an offboarding safety valve, confirmed at ADR-0034's own acceptance), no one else. `deleteCredential()`/`getLatestCredentialId()` are reworked to require `(tenantId, platformId, ownerType, userId?)` explicitly — no longer an indiscriminate delete, closing the real, latent bug ADR-0034 named as its own "single most load-bearing item": an ordinary tenant-wide disconnect could otherwise silently destroy a coexisting user's personal credential for the same platform. `X-Tenant-Id` is fully retired from these endpoints too, extending Story 5.10's own cutover. Story 1.6's own contract was updated in place (dated supersession note, per this project's convention) since its fixtures implicitly assumed tenant-wide operations with no role concept — now explicitly `tenant_admin`, matching what those tests always meant. See `.claude/skills/connector-connect-disconnect/SKILL.md`. Phase 4.5 has no remaining stories.

**2026-08-03 update — Story 5.10 built, the fifth and last dependency-chain story in this phase.** `X-Tenant-Id` no longer has any trust role anywhere in this codebase — closes `Business-Case-v6.0.md` Risk R-04 for real, not just architecturally. `createTenantAuthMiddleware()` composes Story 5.6's token verification and Story 5.9's `resolveIdentity()` into ADR-0033 §1's "one seam," threaded by `app.ts` into `createV1Router()` (`versions/v1/router.ts`), which mounts it ahead of each substantive `/v1` sub-router — `/v1/health` stays deliberately public. Every route handler that used to read `req.header('X-Tenant-Id')` now reads `req.identity` via a shared `requireTenantUser()` helper (403 for a non-tenant-user identity, e.g. Platform Admin, on these tenant-content routes); no downstream store function's signature changed (ADR-0033 §2). A stray `X-Tenant-Id` header is fully inert — never read anywhere (§3).

Per Menno's own explicit sign-off (ADR-0033's own flagged Open Question — the ADR itself asked for this before implementation): the ~six existing router-level contract tests that established tenant context via `X-Tenant-Id` (Stories 1.5, 1.6, 3.4, 4.1, 4.4, 5.1 — a real but far smaller set than the ADR's own conservative "~130+" estimate, since most of the suite calls store functions directly rather than through HTTP) were reworked onto a test-only auth bypass (`testAuthBypassMiddleware`, gated strictly to `NODE_ENV === 'test'`) reading a JSON-encoded identity from a new `X-Test-Identity` header, rather than minting real per-test Entra tokens — decided this way because real interactive user tokens with the claims `resolveIdentity()` needs aren't available in this project's test infrastructure yet.

**A real regression was caught and fixed via `heal-contract-failure` during this story's own full-suite validation, not silently absorbed:** the first implementation mounted the new auth middleware once, at the very top of the whole `/v1` router (`app.use('/v1', authMiddleware, v1Router)`), which broke Story 1.3's own contract requiring `/v1/health` to stay reachable with no credentials at all (it predates any auth mechanism). Fixed by converting the static `v1Router` export into a `createV1Router(authMiddleware)` factory that mounts the middleware per-protected-sub-router instead, leaving `/health` exempt — no change to Story 1.3's own files. See `.claude/skills/tenant-auth-middleware/SKILL.md` and `.claude/skills/http-api-versioning/SKILL.md` for the full account.

**2026-08-03 update — Story 5.9 built, the fourth story in this phase.** `users` (migration `0018`) is tenant-scoped with RLS identical in shape to every other table; `platform_admins` is deliberately *not* a `users` row (ADR-0032 §3) — RLS enabled and forced with zero policies, so it fails closed to everyone without `BYPASSRLS` while `app_user` still gets a real `SELECT` grant that always returns zero rows. `identity_resolver_role` is a fourth Postgres role, narrower than `platform_admin_role`'s own: `BYPASSRLS`, column-scoped `SELECT`-only on both tables, no write grant at all (ADR-0032 §5). Implementation surfaced and resolved a real tension the ADR itself left implicit: §5 says the resolver role has "never any write grant," but §6 requires linking `external_subject`/`status` at first sign-in — a write. Resolved the way AC3 itself points to: the resolver role performs only the initial *read* (by `sub`, then by `email` for an unlinked `invited` row); once that read reveals `tenant_id`, the actual link write runs through the *ordinary* `withTenant(tenantId, ...)` `app_user` path, the same one every other tenant-scoped write in this codebase already uses. `access_ends_at` (§9) drives "currently active" resolution directly (`status = 'active' AND (access_ends_at IS NULL OR access_ends_at > now())`), proven for past/future/cleared transitions. Not yet mounted on any real route — Story 5.10's job. See `.claude/skills/identity-resolution/SKILL.md`.

**2026-08-03 update — Story 5.8 built, the third story in this phase.** `tenants` (migration `0017`) is the first table in this project whose own primary key IS the tenant identity — no separate `tenant_id` foreign key, RLS policy scoped by `id` directly (ADR-0031 §2). It's also `platform_admin_role`'s first real grant since Story 5.7 (`SELECT, INSERT`, plus a **column-scoped** `UPDATE (status, license_seat_count)`) — narrower than platform-admin-access/SKILL.md's original sketch, because Story 5.8's own AC3 required `platform_admin_role` to be DB-level denied from writing `active_seat_count`, not just trusted not to; Postgres's column-level GRANT is what makes that a real, provable boundary rather than an application convention. The seat-count race condition ADR-0031 §3 named as its own open gap is resolved using the ADR's own suggested fix: a single atomic `UPDATE ... WHERE active_seat_count < license_seat_count RETURNING *`, not a separate `SELECT` then `UPDATE`. `domain` (§5, added at review) is nullable with a partial unique index enforcing uniqueness only when set; its public-email-provider exclusion and sign-up "rerouting" UX remain explicitly out of scope, deferred to whoever builds Story 5.9's own invite/sign-up flow. See `.claude/skills/tenants/SKILL.md`.

**2026-08-03 update — Story 5.7 built, the second story in this phase.** `platform_admin_role` (Postgres, `BYPASSRLS`) is granted nothing beyond `platform_admin_audit_log` and (added this story) `platform_admin_break_glass_requests` — proven to have zero access to every tenant-content table that exists today. The break-glass mechanism (ADR-0030 §3) was extended twice during implementation, both via dated Clarifications on the already-Accepted ADR, not Decision rewrites: first, into an explicit two-phase request-then-execute workflow (never a single automated action), then to also issue a real Temporary Access Pass alongside the password reset, covering a lost-MFA-device lockout a password reset alone cannot fix. A real, empirically-discovered constraint shaped the design: Microsoft Graph rejects a principal removing its own directory role assignment, so the JIT elevation needs two separate identities (an "elevator" that only grants/revokes roles, a "resetter" that only performs the reset/TAP actions) — see `.claude/skills/platform-admin-access/SKILL.md` for the full account, including real, observed Entra directory replication lag this story's own retry logic accounts for.

**2026-08-03 update — Story 5.6 built, the first story in this phase.** `src/http/auth/entraAuthMiddleware.ts` validates a bearer token's signature and issuer via standard OIDC/JWT verification (`jose`) against a **real** Microsoft Entra External ID tenant (`getsocialengage.onmicrosoft.com`, provisioned this same session specifically for this story) — not a mock. Proven with a real, live-issued token (client-credentials grant against a test-only app registration), including real signature-tamper and foreign-issuer rejection. **Not yet mounted on any real route** — per this story's own scope, wiring it into the `/v1` router and retiring `X-Tenant-Id` is Story 5.10's job. See `.claude/skills/entra-authentication/SKILL.md` for what's deferred and one real, partial finding on ADR-0029's own `oid`-vs-`sub` Open Question (present for client-credentials tokens; still unverified for interactive user sign-in tokens). A pre-existing, unrelated timeout flakiness in Story 1.6's own contract (real Key Vault calls, no `jest.setTimeout` override) was found and fixed via `heal-contract-failure` during this story's full-suite validation — see `docs/implementation-log.md`'s matching Healing entry.

---

## Phase 5 — Production readiness

**Goal:** the cross-cutting work that doesn't map to any single ADR — security review, load/chaos testing against the isolation guarantees this whole series argued for (RLS, per-tenant rate limits, per-tenant event filtering), operational runbooks for the failure modes ADR-0009/0010/0023 describe, and final go-live checks.
**Not storied, not phase-specific to any ADR** — this phase exists because "ADR-driven" and "production-ready" aren't the same bar, and nothing above claims otherwise.
**Authentication itself is no longer this phase's job** — Phase 4.5, above, is where it's built. What remains here is verifying, under load and adversarial conditions, that the isolation properties this whole series argued for actually hold once a real authenticated caller (not a placeholder) is exercising them.

**2026-08-06 — this phase is a precondition for Go-Live, not the same decision.** `docs/project docs/Project Management Plans/Go-Live-Readiness-Definition.md` §5.3 names this phase's completion as a required gate criterion, but Go-Live itself is a separate, explicit, dated Sponsor decision recorded there — completing Phase 5 makes Go-Live possible, not automatic. That document also defines the environment stages (ephemeral test infrastructure → persistent development environment → pilot → Go-Live) this phase's own "final go-live checks" line was previously the only place naming.

**Deliverable:** a go-live decision, backed by evidence that the isolation and resilience properties this whole ADR series was built around actually hold under test, not just on paper.

---

## Traceability

| Phase | Ready stories | Blocked stories (ADR must be accepted to close the phase) |
|---|---|---|
| 0 | 1.1, 1.2, 1.3, 1.4, 5.3, 5.4 | — |
| 1 | 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 3.4, 4.3, 2.7, 1.5, 1.6, 5.15, 5.16, 5.18 | — |
| 2 | 4.1, 4.2, 2.8 | — |
| 3 | 5.1, 5.2, 5.5, 5.12, 5.13, 5.14, 6.8, 6.9, 6.10 | — |
| 4 | 2.4 (may never trigger — see Phase 4's solo-project note), 2.5, 3.5, 3.6, 4.4, 2.6, 3.7 | — |
| 4.5 | 5.6, 5.7, 5.8, 5.9, 5.10, 1.7, 5.11, 1.8, 1.9, 5.17 — **all Ready, ADR-0029–ADR-0034 all Accepted 2026-08-03, ADR-0036 (§5) Accepted 2026-08-04; 5.11 built 2026-08-05, same day it was drafted; 1.8/1.9/5.17 added 2026-08-05, a further late continuation — see the dated note below** | — |
| 5 | — | — |

Every story from the original 23-ADR series appears exactly once, plus Story 2.6 (ADR-0024), Story 1.4 (ADR-0025), and Story 2.7 (ADR-0026) — three genuine additions from 2026-07-30/31. The first two were both accepted 2026-07-30, the day each was drafted (see `docs/adr/README.md`'s footnotes 6/7); **Story 2.7/ADR-0026 (drafted 2026-07-31) briefly broke that pattern** — it was drafted by the AI Business & Requirements Analyst persona, which does not hold ADR-acceptance authority, and was left Proposed for Menno's own review rather than self-accepted the same day (see ADR-0026's Status line and footnote 8) — **Menno accepted it later the same day.** **Story 2.7 is now built (2026-08-01)** — Phase 1 closes on its own original terms ("one platform... end to end") for the storied half of that deliverable; the "also build, not storied" CRUD/admin-UI half remains open (see Phase 1's own "Also build, not storied" line above). Phase 4's six stories remain scheduled there on their own merits (multi-connector/multi-instance/volume triggers), not because any ADR is still pending. Unlike every other addition here, Story 1.4/ADR-0025 is dev tooling, not production architecture — see its own Acceptance note for why it's an ADR anyway.

**2026-08-03 addition — the seven-ADR batch (ADR-0029–ADR-0035) and its six stories.** Story 1.6 is added to Phase 1's Ready column, retroactively — it was already built (2026-08-01) and is unaffected by this batch's own Proposed ADRs; only its formal `docs/user-stories/` entry and this table's own row were missing until now. All six of this batch's story-generating ADRs (0029–0034) are now Accepted, and ADR-0035 (no story) is Accepted too — **every story in Phase 4.5, including Story 1.7, is Ready.** ADR-0030 was revised at review to add a narrow break-glass mechanism; ADR-0031 to add sign-up domain capture; ADR-0032 to add `access_ends_at`; ADR-0033 accepted as drafted; ADR-0034's own flagged Tenant-Admin-revocation reading was confirmed; ADR-0035's own rule-of-three recommendation was decided (yes, formalize — see `docs/user-stories/README.md`'s new "No-story ADR convention" section). **34 of 34 stories are Ready — none Blocked**, the first time this has been true since this batch was drafted.

**2026-08-05 addition — Story 5.11 (ADR-0036 §5, `GET /v1/me`).** Not part of the 34-story count above or the later Epic 6 additions (`docs/user-stories/README.md`'s own running counts) — a further, separately-dated addition, drafted after Epic 6's own drafting pass (Story 6.1/ADR-0036) surfaced that the endpoint ADR-0036 §5 named as a required prerequisite was never itself given a story. Sourced from ADR-0036 §5 directly (no new ADR), Ready (ADR-0036 Accepted 2026-08-04), added to Phase 4.5 above and Epic 5 (`docs/user-stories/epic-5-security-isolation-and-messaging.md`), not Epic 6 — see that story's own entry for why.

**2026-08-05, later the same day — Story 5.11 built.** `GET /v1/me` is mounted, contract-verified (`contracts/epic-5/story-5.11.get-v1-me.contract.test.ts`, 10/10), and the full `social-listening-core` suite passes (37/37 suites, 205/205 tests) — see `docs/implementation-log.md`. Phase 4.5 is genuinely closed again, this time for real: all seven stories Ready and built.

**2026-08-05, later the same day still — a 14-story batch drafted from Menno's own cross-referencing pass over all 6 epics and all (then-37) ADRs.** See `docs/user-stories/README.md`'s own dated note for the full batch account (which items needed a new ADR and why; the numbering correction on Epic 6's "6.11" → 6.10; the discrepancy between Menno's own "16 stories" framing and the 14 actually specified) and `docs/adr/README.md`'s own footnote 14 for the three new ADRs (0038, 0039, 0040, all Proposed). Phase placement for each, reasoned per this plan's own existing "which named gap does this close" practice rather than assigned by epic number alone:

- **1.8, 1.9, 5.17 → Phase 4.5 (continuation, not a renumbering)** — the same "reopen an otherwise-closed phase for a genuine late addition" treatment Story 5.11 already established for this same phase. Story 1.8 (tenant self-view) and Story 1.9 (user invite/offboard REST) close gaps this phase's own already-built `tenants`/`users` tables (Stories 5.8/5.9) left unexposed over HTTP; Story 5.17 (access-history audit trail) is only concrete once Story 1.9 exists to write the field it audits.
- **2.8 → Phase 2**, closing that phase's own already-named goal directly: "the first `AIProviderConnector` implementation." **Blocked** — sourced from ADR-0038, still Proposed.
- **3.7 → Phase 4**, alongside Story 3.5's own tiered-retention/archival work — tenant-offboarding deletion is a direct, natural extension of the same storage-lifecycle machinery Story 3.5 already built (partition-based archival), not a fresh, unrelated capability. **Blocked** — sourced from ADR-0039, still Proposed. **Not placed in Phase 5** despite its compliance-adjacent framing: Phase 5's own goal statement explicitly says "not storied," and this is real, storied, buildable work with concrete Acceptance Criteria, not cross-cutting verification work.
- **5.12, 5.13, 5.14 → Phase 3**, directly closing the backend REST-surface gap this same phase's own 2026-08-04 dated update already flagged for Story 6.6 ("no HTTP/REST surface exists for any of it yet... real `social-listening-core` work this plan did not previously account for"). No new ADR needed — see each story's own Source line.
- **5.15, 5.16 → Phase 1**, closing the gap this phase's own 2026-08-04 dated update already flagged for Story 6.7 ("depends on a new `POST /v1/tenants/self-service-signup`-shaped core endpoint that does not exist yet... not built by this drafting pass"). No new ADR needed for either — both build directly against ADR-0037's own already-exhaustive design.
- **5.18 → Phase 1**, alongside 5.15/5.16 (the endpoint it rate-limits). **Blocked** — sourced from ADR-0040, still Proposed; ADR-0037 §7 itself already named this as "a precondition for exposing this endpoint publicly, not an optional hardening pass," so Story 5.15 should not be considered safe for real, untrusted traffic until this story is also built, even though 5.15 itself is independently Ready.
- **6.8, 6.9, 6.10 → Phase 3**, alongside 6.6 — all three continue "finish the admin UI beyond the Phase 1 minimum," this phase's own already-established framing for Epic 6's later screens. No new ADR needed for any of the three.

**2026-08-06 addition — ADR-0038 and ADR-0039 both accepted, same day.** Story 2.8 (Phase 2) and Story 3.7 (Phase 4) both move from Blocked to Ready — the table above reflects this directly. Only Story 5.18 (ADR-0040, Phase 1, still Proposed) remains Blocked in this batch. See `docs/adr/README.md`'s own dated note and `docs/user-stories/README.md`'s own dated note for the full acceptance record.

**2026-08-06, later the same day — ADR-0040 accepted too.** Story 5.18 (Phase 1) moves from Blocked to Ready — the table above reflects this directly. All three ADRs from this batch (0038, 0039, 0040) are now Accepted; all fourteen of the batch's stories are Ready, none Blocked.

**2026-08-06, later still the same day — Story 5.15 built.** `POST /v1/tenants/self-service-signup` is real: a fifth Postgres role (`tenant_signup_role`), a new `domain_signup_attempts` table, and the one route accepting a caller `resolveIdentity()` cannot match — see `docs/implementation-log.md`. Closes Story 6.7's own first named cross-repo prerequisite. This paragraph's own §189 caution stands unchanged: not safe for real, untrusted traffic until Story 5.18 (rate-limiting) is also built.

**2026-08-06 addition — Story 6.6's own named backend REST-surface gap (5.12, 5.13, 5.14) is now fully closed.** `GET/POST/PATCH /v1/admin/tenants` (Story 5.12), the break-glass request/execute surface (Story 5.13), and the audit-log query surface (Story 5.14) are all built and contract-verified in `social-listening-core` — see `docs/implementation-log.md` for the real commit hashes. Phase 3's own row above (5.12, 5.13, 5.14) reflects this without a status-column change, since this table tracks phase placement/ADR-readiness, not build status — see `docs/user-stories/README.md`'s own dated running notes for the fuller account.

**A real, named cross-story caution, not previously stated:** Story 5.15's own `POST /v1/tenants/self-service-signup` is Ready and independently buildable, but per ADR-0037 §7's and this batch's own ADR-0040's shared framing, it should not be exposed to real, untrusted (non-test) traffic until Story 5.18 is also built — the same "Ready but practically sequenced" pattern this plan already applies elsewhere (Stories 3.5, 2.4, 4.4), stated here explicitly because the practical risk (unbounded tenant creation) is more concrete than most of those other cases.
