# Future subsystems — scope boundary & parking lot

**Why this file exists:** "Brand Reputation & Alerts, Social Care, Social Selling — out of scope, designed later" is repeated near-verbatim in at least seven places (design spec §9, `implementation-plan.md` line 10, `open-items-and-deferred-work.md` §C, `Project-Charter.md`, `Business-Case-v6.0.md`, `Ideation-Document-v7.2.md`, `Ideation-Enhanced.md`, `Concept-Validation.md`) — but none of them say what each subsystem actually covers, or where a concept discovered while building *this* subsystem should land once it's judged out of scope here. That repetition-without-substance is the gap this file closes: one place that (a) minimally defines each downstream subsystem and (b) tracks concepts parked against a specific one, so "out of scope" always resolves to "tracked, over there" rather than a dead end. Other docs should link here instead of re-stating the exclusion.

**Status of all three, as of 2026-07-31:** no charter, no ADR series, not started — each is "a separate future project with its own charter" per `Project-Charter.md` line 37. Nothing below commits to building any of them; this is a landing place for forward-looking notes only.

---

## Brand Reputation & Alerts

**What it covers** (per `Concept-Validation.md`'s MSE feature-area mapping): brand/reputation monitoring and alerting — the "notice something is happening and tell someone" layer that this subsystem's own spec explicitly excludes (§1: "does not include alerting/crisis detection... deferred to future subsystems"). More concretely, this kind of capability typically bundles:

- **Brand mentions** — monitoring mentions of the company, products, or key people across websites, news, blogs, forums, and social media (this subsystem's own ingestion pipeline is the natural upstream feed for this).
- **Sentiment analysis** — whether mentions are positive, neutral, or negative.
- **Review tracking** — customer reviews from platforms such as Google Reviews, Trustpilot, or app stores.
- **Reputation scorecards** — dashboards showing trends in public perception over time (related to, but narrower than, the "Topic Center" concept tracked separately below — that one turned out not to be Brand-Reputation-shaped once its research/discovery angle was added).
- **Alerts and notifications** — real-time or scheduled, e.g. a negative review appears, mentions spike unexpectedly, a competitor is mentioned alongside the brand, a potential PR crisis emerges, or an influential outlet covers the organization.
- **Competitor monitoring** — comparing brand visibility and sentiment against competitors.

**Example alert rules** this capability would support: notify marketing when a news article mentions the company; alert if social sentiment drops below a threshold; open an incident ticket when a high-profile negative review is detected.

**Business value:** protect brand image, respond quickly to customer concerns, detect emerging issues before they become crises, measure PR/marketing effectiveness, and improve customer trust.

**Parked concepts:**

- **Brand/topic drift detection with alerting — Tenant Brand Reputation Manager (BRM-01, "Detect Brand Drift"), Drift Sentinel (DS-01, "Detect Entity Drift"), Sentiment Drift Operator (SDO-01, "Track Sentiment Drift" — see the correction note directly below), Alerts Subsystem Engineer (ASE-01, "Consume Thin Events"), and Drift Response Agent (DRA-01, "Respond to Drift Events" — a second, unrelated use of the "DRA-01" abbreviation; see the naming-collision note below).** Surfaced 2026-08-05, from Menno's "Master User Story Backlog." Collectively: flag sentiment/topic/influencer/author-behavior drift against a baseline, trigger alerts on severe drift, consume this subsystem's existing thin `SocialPostIngestedEvent`/`ConnectorHealthChangedEvent` events (ADR-0012) with per-tenant Service Bus subscription filtering and full-post REST fetch on demand, and classify a response once an alert fires. This is a close, direct match to this subsystem's own "What it covers" list above (sentiment analysis, reputation scorecards, alerts and notifications) — not a stretch fit. **ASE-01's own ADR citations were checked directly and are accurate:** "tenant filtering applied per ADR-0013" matches ADR-0013's actual Decision text (Service Bus subscription SQL filters on `tenantId`); "event schema versioning respected per ADR-0019" matches ADR-0019's actual Decision text (`schemaVersion` as a Service Bus message property). **Underlying data it would build on:** `SocialPost.enrichment.sentiment`/`entities`/`keyPhrases` (populated only once Story 2.8/ADR-0038 ships — currently Proposed, not built), `Author` (ADR-0004), `AuthorTopicSignal` (ADR-0007, shipped), and the existing thin-event stream (ADR-0012/ADR-0013/ADR-0019, shipped). **Real conflict, not a coincidental overlap: every one of these stories' "drift signal"/"drift visible" ACs implies exactly the topic-volume/sentiment-over-time aggregation ADR-0008 explicitly forbids building in this subsystem** — its Decision text states plainly, "Do not build a `TopicDailyCount` aggregation table, endpoint, or any charting UI in this subsystem." That is precisely why this whole group is parked rather than treated as buildable scope now.
- **Naming collision in the source material, not among the problems Menno had already caught:** the pasted backlog uses the abbreviation "DRA-01" for two entirely different, unrelated personas — Data Retention & Archival Operator ("Archive RawPayload") and Drift Response Agent ("Respond to Drift Events"). The first is already-shipped work (Story 3.5 — see "Already covered," below); the second is parked here. Flagging this so the abbreviation doesn't get silently conflated later.
- **Correction to the first-pass categorization that accompanied this backlog: Sentiment Drift Operator (SDO-01) belongs here, not under Topic Center.** Its own AC set includes "alerts triggered for severe drift," which matches this subsystem's already-stated scope directly (line 17, above: "alert if social sentiment drops below a threshold") — nothing in Topic Center's own current definition (below) mentions alerting at all. Stated explicitly rather than deferred to the original split.

## Social Care

**What it covers:** case routing / customer-service workflows off of ingested social mentions — one of MSE's four original feature areas (`Concept-Validation.md`), explicitly named as CRM-adjacent workflow this business case declined to fund (`Business-Case-v6.0.md` line 29). Concretely, this is the process of responding to customer questions, complaints, and support requests through social media channels (X, Facebook, Instagram, LinkedIn, online communities). Social care teams monitor brand mentions and alerts, engage with customers, resolve issues, and protect the organization's reputation this way.

**Example:** a customer posts on X, "My internet has been down for two days." The social care team sees the mention, responds publicly, moves the conversation to direct messages if needed, and works to resolve the problem.

**Parked concepts:**

- **Requirement: any "respond to social media content" capability inherits a fixed credential rule from this subsystem's ADR-0028, not something Social Care would decide on its own.** Surfaced 2026-08-03 while drafting `docs/adr/0028-credential-creation-authority-scoped-by-ownership-tier.md` (this subsystem's credential-ownership-tier ADR) and confirmed directly by Menno. The rule: any capability that acts *as* a specific platform account — replying to a post, sending a direct message, voting/reacting, submitting a comment — requires that platform's own user-context authentication (e.g. Reddit's `authorization_code` OAuth grant, distinct from the app-only `client_credentials`/`installed_client` grant this subsystem's own public-content monitoring uses — verified against `github.com/reddit-archive/reddit/wiki/OAuth2`), because an app-only, tenant-wide credential has no account identity to act as. Under ADR-0028's Tier 3 rule, that credential can only be created by the individual user's own act of activation — never by Tenant-Admin or Platform Admin on that user's behalf, even for a support/case-routing workflow. Whenever Social Care is actually chartered, its own connect-flow design needs to account for this: responding through a given platform requires that specific tenant user to personally hold and activate their own credential for it, not a shared/organizational one. See ADR-0028's Decision section (Tier 3 worked example) for the fuller reasoning, sourced there rather than duplicated here.
- **Requirement: "respond to negative sentiment" needs the drift/alerting mechanism this doc parks under Brand Reputation & Alerts, not something Social Care would build itself.** Surfaced 2026-08-05, Tenant Social Care Agent (TSCA-01, "Respond to Negative Sentiment") from Menno's "Master User Story Backlog." Its own ACs: thin events carry sentiment (depends on Story 2.8/ADR-0038 populating `enrichment.sentiment` — currently Proposed, not built), alerts trigger on negative drift (the same ADR-0008-bounded drift/alerting capability parked under Brand Reputation & Alerts above — not something this section should duplicate), the agent fetches the full post via REST (already this subsystem's design, ADR-0012), and `ConnectorHealth` shows ingestion status (ADR-0009, shipped). The "respond" half of this persona is exactly the Tier-3 credential case the bullet directly above already covers — not a new instance, just this backlog's own worked example of it.

## Social Selling

**What it covers:** lead-generation recommendations off of ingested social activity — the fourth MSE feature area, same explicit exclusion as Social Care. In the context of social media, social selling is the practice of using social networks to build relationships, establish trust, identify prospects, and ultimately influence purchasing decisions — engagement and relationship-building first, rather than pitching products directly.

**How it typically works:**
- **Share valuable content** — industry insights, thought leadership, customer success stories, educational videos.
- **Engage with prospects** — comment on posts, answer questions, participate in discussions, congratulate connections on achievements.
- **Build credibility** — demonstrate expertise, showcase experience, publish case studies and insights.
- **Develop relationships** — connect with potential customers, understand their challenges, advise before pitching.
- **Convert opportunities** — move conversations to DMs/meetings/calls, present solutions once a business need is identified.

**Example:** a managed-services consultant shares Microsoft 365 governance best practices on LinkedIn, comments on posts from IT leaders discussing security challenges, publishes insights on Copilot adoption/compliance, and connects with decision-makers interested in those topics. Over time, prospects come to recognize the consultant as a trusted expert, making later sales conversations easier.

**Social selling vs. traditional selling:**

| Traditional selling | Social selling |
|---|---|
| Cold calls | Warm introductions through networks |
| Sales pitch first | Relationship first |
| One-way communication | Two-way engagement |
| Focus on product | Focus on customer problems |
| Short-term interaction | Long-term trust building |

**Common platforms:** LinkedIn (most common for B2B), X, Facebook, Instagram, TikTok, YouTube, industry communities/forums.

**Typical KPIs:** new connections/followers, engagement rate, conversations started, qualified leads generated, pipeline influenced, revenue attributed to social activity.

**Relationship to the other three functions in this doc** (and to this project's own in-build subsystem):
- **Brand Reputation & Alerts** → monitoring what people say about the brand.
- **Social Care** → responding to customer questions and complaints.
- **Social Listening / Insights** (this project's current, in-build subsystem) → analyzing conversations and trends.
- **Social Selling** → building relationships that generate business opportunities and sales.

In short: social selling uses social media to build trust and relationships that lead to business opportunities and revenue, rather than relying solely on direct sales outreach.

**Parked concepts:**

- **Social Selling Strategist (SSS-01) — Identify Experts.** Surfaced 2026-08-05, from Menno's "Master User Story Backlog." Maps closely onto `AuthorTopicSignal` (ADR-0007, shipped, Story 4.1) — "AuthorTopicSignal sortable by mentionCount" and multi-year retention supporting "credibility scoring" are both already-shipped capabilities (ADR-0007/Story 4.1; ADR-0018/Story 3.5), consistent with ADR-0007's own stated rationale ("downstream consumers, including future subsystems like Social Selling... can apply their own weighting"). "Engagement metrics visible" is modeled (`AuthorTopicSignal.avgEngagement`) but not yet populated end to end — see the "Already covered" section below for the live gap. "Topic drift signals update expertise" is the one AC nothing shipped or storied covers — the same ADR-0008-bounded drift capability named under Brand Reputation & Alerts above, not new scope specific to this persona. **Underlying data:** `AuthorTopicSignal`, `Author` (ADR-0004), the retention tiering in ADR-0018.
- **Author-authorship / redistribution-consent policy — a content-authorship, attribution, and liability-disclaimer requirement for user-generated content redistributed by followers/connections, not yet chartered as product scope.** Surfaced 2026-08-06, from Menno's own direct working-through of a personal LinkedIn post's requirements (verbatim, typos preserved): *"Im working through the requiremetns for how different roles interact with public content, permissions and trust -- an important part of building friendship and relationships that are useful to professional attritbutes. One of the requirements is to ensure Author remains ownership and the platform is not liable for reposts or forwards to others is to enable the content to remain within legal boundaries of what can be writtten en to provide authors consent to repost my content and ensure linkedin is not held responsible for the redistributioon of my content without my prior written approval. So to ensure these remain within reaonsable boundaries of the intent i grant my followers and connected work related peers to rights to distribute my content."* Distilled to four requirements: (1) an author retains ownership/attribution of their own content regardless of how far it's reshared; (2) a hosting/intermediary platform should not be held liable for redistribution occurring under the author's own granted permission; (3) any such grant needs to stay within reasonable, legally sound bounds rather than being unlimited; (4) an author may proactively grant a defined audience (followers, professional connections) a standing right to reshare, rather than requiring case-by-case permission.
  **Checked directly against this subsystem's actual shape and found not to fit today.** SocialEngage's Social Listening / Insights subsystem, as built through Phase 6, only *ingests* third-party public content (GNews, Newswire, later Reddit) about brands — `Author` (`docs/adr/0004-author-normalized-separately-from-post.md`) represents the *ingested content's* issuing organization/publication, not a SocialEngage tenant user, and no ADR or story in this series gives a tenant user the ability to author original content inside SocialEngage that then gets reshared to a public audience the way this requirement describes. `docs/adr/0027-connector-is-technical-intermediary-not-contracting-party.md` (connector architecture is a technical intermediary only, never a contracting party) is the closest existing platform-liability decision this project has made, but it governs a structurally different relationship — SocialEngage's own liability posture toward the third-party *data sources* it connects to, not a tenant user's liability posture toward *their own followers* resharing *their own* authored content. Neither ADR gives this requirement a real hook.
  **Closest conceptual fit is Social Selling's own "share valuable content" / "engage with prospects" framing, above** — the requirement is squarely a content-authorship/consent/liability policy question for whatever product surface eventually lets a user author and distribute their own content — but Social Selling itself has "no charter, no ADR series, not started" (this file's own status line, top), so there is no in-scope product decision to make yet. This subsystem (Social Listening / Insights) has no analogous feature either: nothing shipped, Ready, or Blocked in `docs/user-stories/` lets a tenant user author content inside SocialEngage or export/forward a derived insight to an external audience.
  **Deliberately not drafted as an ADR.** Creating one now, against either subsystem, would invent product scope that neither the design spec, any Accepted ADR, nor any current story names — the same discipline this file's own "Convention for adding to this file" (below) and its treatment of every other not-yet-chartered persona above already applies: name the gap, park it against the subsystem it would belong to, don't force a decision that has nothing real to attach to yet.
  **If/when Social Selling is ever chartered, or if this subsystem (or a later phase of it) ships a real tenant-facing content-resharing/export feature** — e.g., forwarding a watchlist finding or a derived insight to a colleague or external audience — **this exact authorship/attribution/consent/liability-disclaimer policy needs its own ADR at that time**, reasoned the same way `docs/adr/0027-...md` pre-decided connector-vetting criteria before every connector existed, rather than after the fact.
  **A boundary any such future ADR would need to respect, not invent:** whatever consent-grant or liability-disclaimer copy that feature ships would state *product/design intent* only — whether that copy is actually legally effective (author consent surviving redistribution, a hosting platform's own liability shield) is a legal question no ADR can certify. This is the identical discipline `docs/adr/0039-tenant-offboarding-data-lifecycle-export-and-deletion.md`'s own Consequences section already applied to a different legal question (GDPR Article 17): "This ADR does not itself resolve whether this project's design satisfies GDPR Article 17... as a matter of law — that is a legal question beyond what an architecture-decision record can certify."

---

## Personas already covered by this subsystem's own shipped or storied work (not parked — 2026-08-05, from Menno's "Master User Story Backlog")

Several personas in that backlog describe capabilities this subsystem already ships or has a Ready story for, not a downstream-subsystem gap. Listed here with citations, rather than silently dropped or parked as if they were new scope — the same discipline Topic Center's own "leading authors" entry (below) already established for this file.

- **Ingestion Pipeline Operator (IPO-01, IPO-02) — Monitor Ingestion Runs / Manage Rate-Limit Queues.** Its ADR citations were checked directly against the cited ADRs' own Decision text and are accurate — not the citation problem Menno suspected might be present here: IPO-01's "run history per ADR-0005" matches ADR-0005 (`IngestionRun` as the immutable per-run acquisition/audit anchor); IPO-02's "queue TTL/depth visible per ADR-0020" matches ADR-0020's actual accepted defaults (6-hour queue TTL, 1,000-request depth ceiling, 3-failure dead-letter threshold) and its "no silent drops" AC echoes ADR-0020's own language ("never dropped silently"). The underlying data layer is real and shipped (`ConnectorHealth`/ADR-0009, `IngestionRun`/ADR-0005, `RequestGate`/ADR-0003/ADR-0020). One real caveat: the admin-UI surface that would make any of this visible to an actual operator (Story 6.5, connector status view) is drafted/Ready, not yet built — so "visible" in these ACs depends on a story that exists on paper, not shipped code, per `docs/user-stories/epic-6-admin-ui.md` and `docs/open-items-and-deferred-work.md` §A.
- **Connector Developer (CD-01) — Implement New Connector.** Its ADR citations were also checked directly and are accurate: "implements ProviderConnector per ADR-0002" matches ADR-0002's Decision text; "native filtering supported where possible per ADR-0006" matches ADR-0006's Decision text ("prefer connector-side native filtering... wherever the platform supports it") closely, not just approximately — this was not the loose citation Menno's own note flagged as needing verification. This is exactly the mechanism Story 2.1 (shipped, `social-listening-core@7604c25`) built, and Stories 2.6/2.7 (Newswire, GNews — both shipped) already used to add two real connectors with zero core-pipeline changes, per ADR-0002's own stated design goal. Not a gap.
- **Data Retention & Archival Operator (DRA-01 — the first, unrelated use of that abbreviation; see the naming-collision note under Brand Reputation & Alerts above) — Archive RawPayload.** Every AC (monthly partition detachment, `rawPayload` replaced with a pointer, blob storage holding the archived payload, audit anchors intact) matches ADR-0018's Decision text and is shipped: Story 3.5, `social-listening-core@f4bd93c`, 2026-07-30. Not a gap. One already-known, live caveat (not newly surfaced by this backlog): nothing schedules the archival functions periodically yet (`pg_cron` wiring is manual) — tracked in `docs/open-items-and-deferred-work.md` §B.
- **Tenant Analyst (TA-01) — Analyze Topic-Author Relationships.** Partially covered, stated precisely rather than rounded up to "done." `AuthorTopicSignal` sortable by `mentionCount`/`activeMonths` and multi-year retention are shipped (Story 4.1/ADR-0007, Story 3.5/ADR-0018). Two ACs are not covered: "filter by sentiment" has no query parameter anywhere in Story 4.1's actual contract (`GET /topics/:topic/authors` accepts only `sortBy=activeMonths|mentionCount`) — a real, small, in-subsystem API gap, not future-subsystem scope, since the underlying `sentimentBreakdown` field already exists on the model; and "drift signals visible" is the same not-yet-built, ADR-0008-bounded capability parked under Brand Reputation & Alerts and Topic Center. Also worth naming directly: `AuthorTopicSignal.avgEngagement`/`sentimentBreakdown` are modeled but not populated end to end by any shipped code yet (`docs/open-items-and-deferred-work.md` §B) — blocked on the same unbuilt enrichment pipeline (Story 2.8/ADR-0038, Proposed) as several items parked above.
- **Tenant Knowledge Manager (TKM-01) — Curate Topic Definitions.** Partially covered. Watchlist CRUD is shipped (Story 1.5, `social-listening-core@1fb3e55`, 2026-08-01 — see the dated correction under Topic Center below) and "manager approves changes"/"watchlists reflect new definitions" map onto it directly. "Topic drift signals update" is not covered — the same ADR-0008-bounded capability parked above, not a gap specific to this persona.

---

## Topic Center (parked concept — doesn't fit any subsystem above, assignment unresolved)

**Not one of the four MSE-derived categories** (social insights, brand reputation, social care, social selling — confirmed directly against Microsoft's own archived MSE docs). Introduced 2026-07-31 as a project-original concept: research-oriented, built around a *subject/topic* rather than a brand, aimed at (1) everything MSE's "Search Topic" tracked, (2) finding the leading authors on that subject, and (3) researching novelty/IP — is this subject new/emerging, who spoke first, what's the whitespace.

**Includes everything Search Topic covered** (see the corrected terminology note below): volume trend, sources, languages, location, word cloud, sentiment — all scoped to one tracked subject at a time.

**Extends it with two more capabilities:**

1. **Leading authors on a subject** — given a topic, who are the most authoritative/active voices talking about it. **This is not a gap** — it's already built, today, in this subsystem: `AuthorTopicSignal` (ADR-0007) plus `GET /topics/:topic/authors?sortBy=mentionCount` (Phase 2, shipped) does exactly this, ranking authors by `mentionCount`/`avgEngagement`/`activeMonthsCount`. Nothing new needs building for this half.
2. **Novelty / IP research** — is this subject genuinely new or already well-covered; tracking earliest mentions; whitespace/prior-art-style discovery ("who said this first," "how crowded is this space"). **This is a real gap** — nothing in this project's 25 ADRs, the design spec, or either other future subsystem covers assessing novelty or first-mover timing on a topic. It doesn't fit the three subsystems above either: no alerting/crisis angle (not Brand Reputation & Alerts), no case routing (not Social Care), no lead-gen (not Social Selling).

**Correction (verified 2026-07-31, against Microsoft's archived MSE documentation):** an earlier note in this file assumed "Topic Center" was MSE's own feature name — it isn't. MSE's real terminology was **"Search Topic"**: a tenant-defined saved keyword/hashtag/handle query, with two per-topic analytics views — an **Overview** page (volume, sources, languages, location, word cloud) and a **Sentiment** page (only populated once a specific Search Topic is selected, not "All Search Topics"). No feature literally named "Topic Center" appears in Microsoft's own docs. As now (re)defined by this project, "Topic Center" is Search Topic's scope *plus* leading-authors-discovery *plus* novelty/IP research — broader than anything MSE shipped.

**Mapping to this project's own vocabulary:**
- Search Topic-equivalent tracking ≈ this subsystem's own `Watchlist` (ADR-0006/ADR-0021 — saved keyword/boolean query), which exists conceptually but has no persisted table or CRUD yet (`docs/open-items-and-deferred-work.md` §A).
- Leading-authors discovery ≈ `AuthorTopicSignal` (ADR-0007) — already built.
- Volume/sentiment-over-time charting ≈ `TopicDailyCount`, deferred by ADR-0008.
- Novelty/IP research ≈ **nothing existing** — a new capability with no current owner.

**Correction, 2026-08-05 (superseding the "no persisted table or CRUD yet" bullet directly above, per this file's own don't-rewrite-history convention — left in place rather than edited out):** that bullet was accurate when this section was written (2026-07-31), but is now stale. Story 1.5 (Watchlist CRUD REST surface) shipped 2026-08-01, `social-listening-core@1fb3e55` (`docs/implementation-log.md`) — a persisted `Watchlist` table and CRUD endpoints now exist. The Search-Topic-equivalent-tracking half of Topic Center's own mapping is therefore also further along than the original bullet states, independent of anything in this backlog.

**Example:** researching "quantum-resistant encryption" as a subject (not a brand) — Topic Center would show mention volume and sentiment (Search Topic-equivalent), the authors most actively and knowledgeably discussing it (`AuthorTopicSignal`, already available today), and — the new piece — when the conversation started, whether it's accelerating, and whether a given angle on it is already saturated or still whitespace.

**Open question, not yet resolved:** is the "leading authors + novelty/IP research" combination (1) a natural extension of *this* subsystem's own topic model — meaning a new ADR/story here, through the normal `implement-story` workflow, not a future-subsystem parking-lot note — or (2) a genuinely new, fifth capability area outside the original four-subsystem MSE-derived vision, deserving its own future charter the way the other three do? Recorded here as open rather than guessed at silently, per this file's own convention below.

**2026-08-05 additions, from Menno's "Master User Story Backlog":**

- **Trend Cartographer (TC-01) — Map Emerging Trends.** AC: clusters semantic neighborhoods, a topic-evolution graph, multi-year retention supporting trend mapping, drift signals integrated. Fits Topic Center's existing subject-research framing (not brand-alerting, not case-routing, not lead-gen) — extends, rather than duplicates, the already-named novelty/IP gap above. **Real conflict, named explicitly rather than glossed over:** "topic evolution graph" is exactly the charting UI ADR-0008 forbids this subsystem from building ("Do not build a `TopicDailyCount` aggregation table, endpoint, or any charting UI in this subsystem"); the underlying data (`enrichment.entities`/`keyPhrases`, `publishedAt`) already exists and is retained per ADR-0018's design, but the aggregation/visualization itself is out of scope here by explicit decision, not by oversight.
- **Topic Drift Analyst (TDA-01) — Detect Topic Semantic Drift.** AC: baseline-vs-current semantic comparison, new-subtopic surfacing, reclassification, downstream drift-signal updates. Same ADR-0008 boundary as above. No alerting AC here (unlike Sentiment Drift Operator, filed under Brand Reputation & Alerts above) — that absence is the basis for filing this one here rather than there.
- **Trend Forecaster (TF-01) — Forecast Trend Trajectories.** AC: extrapolates trend lines from drift signals, multi-year retention supports modeling, forecasts surfaced to downstream subsystems. Genuinely new relative to Topic Center's own current definition above (which claims only volume/sentiment/leading-authors/novelty, not forecasting) — filed here as an extension because it depends on the same subject-scoped trend data, not because it was already implied by Topic Center's stated scope. Flagged as an addition, not silently absorbed into the existing definition.
- **IP Discovery Specialist (IPD-01) — Detect Emerging IP Signals** and **Novelty Researcher (NR-01) — Identify Novel Concepts.** Not new parked concepts — this is the same gap Topic Center's own definition already names above ("Novelty / IP research... nothing in this project's ADRs, the design spec, or either other future subsystem covers assessing novelty or first-mover timing on a topic"), described independently by this backlog in near-identical terms (novel terminology/key phrases, entity lineage, a novelty score/timeline, whitespace/first-mover discovery). This independent arrival at the same gap is worth recording as confirmation that the gap is real, not a document artifact. **One residual complication IPD-01 introduces that wasn't here before:** its own AC includes "Legal Advisor notified" — a hand-off to a persona whose own subsystem home is itself unresolved (see below). Not resolved here; named so it isn't lost.
- **Entity extraction's scope, refined 2026-08-05 per a direct follow-up from Menno after this section was first drafted:** the "entity lineage" language above, and the Knowledge-Graph-framed personas tracked in the "Unresolved" section below, should be understood as covering not just entity *nodes* (people, companies, topics) but the **relationship or action verb connecting two entities as a labeled edge** — e.g. "Company A acquired Company B," "Person X hired at Company Y," "Company A partnered with Company B," "Company A launched Product Z." The parked concept is entity extraction **and** relationship/action-verb extraction together — modeling what's happening *between* entities, not only which entities exist. **Priority domain, per the same follow-up:** professional/business-context topics specifically — hiring, product launches, M&A/acquisitions, partnerships, funding rounds — not general social/personal topics, consistent with this being a B2B social listening tool where that's the signal a tenant would actually want extracted. **Not new buildable scope** — this is a refinement of the already-parked entity-lineage/novelty angle and the "Unresolved" Knowledge-Graph-framed material below, recorded as an additional detail on those parked concepts, not a new capability to design or build now. Nothing in this project's shipped `enrichment` shape (`entities: { text, category, confidenceScore }[]`, ADR-0038's own Context) captures relationship/edge data between two entities at all — `entities` is a flat list, not a graph or relation set — so this refinement doesn't change any "already covered" finding elsewhere in this file. **Revisit trigger, per the same 2026-08-05 confirmation from Menno:** the Knowledge-Graph-shaped storage question (semantic/graph storage of a post vs. the relational model this subsystem actually uses) is a genuine future expansion, not to be built now — and Menno's own suggestion is that it be reconsidered specifically when/if Topic Center is actually chartered and built, since a real implementation here is the most likely place to discover whether relational `Author` rows are actually sufficient. See the "Unresolved" section below for the full note; not duplicated in full here.

---

## Unresolved — deliberately not assigned to any subsystem above (2026-08-05, from Menno's "Master User Story Backlog")

Per this file's own convention (below): named explicitly as unclear rather than force-fit.

- **Knowledge Graph Maintainer (KGM-01, KGM-02), Knowledge Graph Steward (KGS-01), Entity Drift Maintainer (EDM-01).** **Terminology problem, confirmed by direct search of this project's docs, not just carried forward from Menno's own flag:** "Knowledge Graph" appears nowhere in this project's 40 ADRs, the design spec, or any other project doc as an actual architectural concept — this subsystem's entity model is relational Postgres (`Author`, keyed by `(tenantId, platformId, externalAuthorId)`, ADR-0004; `SocialPost`; `AuthorTopicSignal`, ADR-0007), not a graph database or graph-shaped storage of any kind. Adopting "Knowledge Graph" language for these personas uncritically would misdescribe this project's own architecture. **Underneath the mismatched label is a real and currently unowned capability, confirmed by directly re-reading ADR-0004's Decision text:** `Author` is upserted by an *exact* key match — there is no merge/dedupe logic anywhere in this project for the case these three personas actually describe (the same real person or organization appearing as separate `Author` rows across platforms, or a profile change that should update one row rather than create a duplicate). Nothing in ADR-0004, ADR-0007, or any shipped story addresses this. **Refined 2026-08-05, per a direct follow-up from Menno after this section was first drafted:** the parked concept here is broader than entity dedup alone — it also covers extracting the **relationship or action verb connecting two entities as a labeled edge** (e.g. "Company A acquired Company B," "Person X hired at Company Y," "Company A partnered with Company B," "Company A launched Product Z"), not just which entities exist. **Priority domain:** professional/business-context topics specifically — hiring, product launches, M&A/acquisitions, partnerships, funding rounds — not general social/personal topics, matching this project's B2B positioning. See the matching note under Topic Center above (same refinement, cross-referenced rather than duplicated in full). This is still a parked concept only, not new buildable scope. **Not assigned to any of the three named subsystems or Topic Center** — none of their own "What it covers" definitions mention entity deduplication or relationship/edge extraction. This may not even be future-subsystem-shaped scope at all — it could equally be a gap in *this* subsystem's own `Author`/`enrichment` model (an eventual story here, the same open-question shape Topic Center's own "leading authors + novelty" question already has above), not a downstream product feature. Left open rather than guessed at, per this file's own convention. **Confirmed 2026-08-05, directly by Menno:** storing a post's data in a semantic/knowledge-graph shape (rather than the relational model above) would itself be a genuine expansion of this subsystem's current storage architecture, not incremental work — deliberately a future expansion, not something to build now, consistent with everything else in this file. **Revisit trigger named:** rather than leave this fully untethered, Menno's suggestion is that this gap be reconsidered specifically when/if Topic Center is actually chartered and built — Topic Center's own "entity lineage" need (see above) is the closest natural point where this question would come up again, since a real implementation of leading-authors-plus-novelty-discovery may itself surface whether relational `Author` rows are sufficient or whether entity/relationship modeling of this kind becomes load-bearing. Not a commitment to build a knowledge graph as part of Topic Center — a trigger condition for *reconsidering* this parked item, nothing more.
- **Legal Advisor (LA-01) — Assess Legal Risk in Drift.** Does not fit Brand Reputation & Alerts, Social Care, Social Selling, or Topic Center — none of their stated scopes include legal-risk classification or compliance-decision logging, and its "highlights impersonation/misinformation drift" AC is adjacent to, but narrower than, Brand Reputation & Alerts' own "potential PR crisis" bullet above, not a clean match for it. Left unresolved rather than force-assigned. Also depends on the drift mechanism parked (not built) under Brand Reputation & Alerts above, so it inherits that same ADR-0008-bounded unbuilt dependency even once/if a subsystem home is found for it.
- **Entity Extraction Engineer (EEE-01, EEE-02) — a residual gap after Story 2.8/ADR-0038, not fully "already covered" as first suspected.** The suggestion that this might be "a partial overlap" with Story 2.8/ADR-0038 was worth checking directly, and was checked against ADR-0038's actual text: ADR-0038 (Proposed, not yet accepted) decides *which* AI enrichment provider to build first (Azure AI Language) and names a second-provider candidate (an Azure-hosted LLM); it does not decide anything about per-model rate-limit *display*, extraction-accuracy comparison over time, or drift monitoring on enrichment output itself. Two of the backlog's own ACs ("model selection per provider configurable," "displays per-model rate limits") match capability already named — but not yet built — as ordinary Phase 2, in-subsystem work in `docs/open-items-and-deferred-work.md` §A (`POST /ai-providers/:id/select-model`, `GET /ai-providers/:id/models`), not future-subsystem scope and not newly discovered by this backlog. "Switch models without pipeline changes" (EEE-02) is already a shipped architectural property, not a gap at all — ADR-0002's Decision text states this as the whole point of the unified `ProviderConnector` contract, and Story 2.1 (shipped) already proves it works for two connector kinds. "Errors surface through ConnectorHealth" (EEE-02) is also already covered by design — ADR-0009/ADR-0010 apply uniformly to any registered connector, `AIProviderConnector` included, per ADR-0002's unified contract. **What's left is a real, unassigned gap:** ongoing extraction-accuracy evaluation, anomaly surfacing, and baseline-vs-current drift monitoring on the enrichment pipeline's own output (EEE-01's ACs, plus EEE-02's "enrichment drift monitored"). This doesn't fit Brand Reputation & Alerts, Social Care, Social Selling, or Topic Center's own stated scopes either — it's an internal engineering/observability concern about *this subsystem's own* AI-provider connectors, not a downstream tenant-facing product feature. Left unresolved rather than filed under Brand Reputation & Alerts by default just because its ACs use the word "drift" — the actual subject (enrichment-pipeline accuracy, not tenant-facing brand/topic drift) doesn't match that subsystem's own definition.

---

## Open naming gap (unresolved, not just undocumented)

ADR-0008 and spec §4.6/§9 defer `TopicDailyCount` aggregation and all charting UI to **"a future insights/dashboard subsystem"** — that exact phrase, never tied to Brand Reputation & Alerts, Social Care, or Social Selling by name anywhere in the doc corpus. Worth noticing: this subsystem is itself named "Social Listening / **Insights**," so "a future insights subsystem" reads ambiguously — it could mean Brand Reputation & Alerts, a fourth/fifth thing never named in any charter (see Topic Center above), or a later phase of *this* subsystem that just hasn't been scoped. Nothing in this file resolves that ambiguity — Topic Center is deliberately left unassigned above rather than force-fit into one of the three named subsystems, the same way `docs/open-items-and-deferred-work.md` §D tracks other not-yet-decided items.

## Convention for adding to this file

When work on the in-build subsystem surfaces a concept that's out of scope here but belongs to a specific future subsystem, add it under that subsystem's "Parked concepts" above — name, one-line description, source (which conversation/ADR/doc surfaced it), and the underlying data it'd build on if that's already known. If it's unclear which subsystem owns it, say so explicitly (as above) rather than guessing silently.
