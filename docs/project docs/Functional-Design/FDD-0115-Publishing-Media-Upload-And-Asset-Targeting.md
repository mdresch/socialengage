# Functional Design Document

## 1. Document Control

| Field | Value |
|---|---|
| Document Title | SocialEngage — Publishing: Media Upload and Asset Targeting |
| Version | 0.1 |
| Date | 2026-08-23 |
| Author(s) | FDD Writer — Batch Agent |
| Reviewer(s) | Product Owner / Technical Lead |
| Status | Draft (ADR status: Proposed (2026-08-23); BRD status: Proposed — draft for review; may change before acceptance) |
| Related Documents | ADR-0115, BRD-0115, related feature designs, user stories |

---

## 2. Purpose and Scope

### 2.1 Purpose
**Note:** The source ADR is currently **Proposed**. This FDD is a draft for review and may change.

SocialEngage's publishing surface currently only supports text and link-card outbound posts. Real social media management requires images, videos, and per-asset targeting so a single composed post can be dispatched to the right Facebook Page, LinkedIn profile, or other social asset. Without these capabilities, users must leave the product and upload content directly on each platform, fragmenting the publishing workflow.

This FDD translates the accepted architecture and business requirements from ADR-0115 and BRD-0115 into a coherent functional design for implementation.

### 2.2 Scope

- **In scope:** - `POST /v1/outbound/media` multipart/form-data upload for images and videos
- Tenant-scoped Azure Blob storage and a new `media_assets` table
- Presigned 24-hour download URLs returned to the client and connectors
- `assets` array inside `OutboundPost` with `image`, `video`, and `link-card` types
- `assetTargets` map of `platformId` to page/account target for multi-asset publishing
- `GET /v1/connectors/:platformId/targets` to list available pages/accounts
- Image validation: JPEG, PNG, GIF, WebP; max 8 MB
- Video validation: MP4, MOV; max 512 MB (v1 support limited to images and link cards for most connectors)
- Link cards using a supplied URL and optional `mediaId` image; OpenGraph metadata may be used for preview
- Connector-specific translation of generic assets into platform-native uploads
- Error codes `400 MISSING_ASSET_TARGET` and `422 PLATFORM_ASSET_REJECTED`
- **Out of scope:** - Full video publishing for all connectors in v1 (support is connector-specific; most connectors start with images and link cards)
- AI copy assistance, alt-text generation, and image suitability review
- Advanced approval workflows beyond the simple state machine already in `outbound_activities`
- Smart scheduling and optimal send-time prediction
- Bulk CSV publishing and recurring posts
- Media thumbnail/previews generated by the backend for the UI
- Automatic cleanup and expiry of unreferenced media assets
- **Assumptions and constraints:** - Azure Blob Storage is already provisioned and used for other features
- `outbound_activities` and `SocialConnector.publish?()` are already in place or will land before this feature
- A tenant has at least one connected platform asset (Facebook Page, LinkedIn page, etc.) that supports publishing
- The Polypost Composer will provide the drag/drop media UX and platform preview tabs

### 2.3 Target Audience
Engineers, QA, product owners, UX, and platform operations.

---

## 3. Context and Background

### 1. Text-only publishing is not enough
`docs/product-research/feature-designs/07-publishing-and-scheduling.md` and `ADR-0098` authorize basic publishing. Most real posts include images or videos. This ADR adds media upload, asset targeting, and the per-asset dispatch rules.

### 2. Platform asset rules vary
Facebook, Instagram, LinkedIn, and X have different image/video size, format, and caption rules. The connector must translate the platform-agnostic asset request into platform-specific uploads.

### 3. Asset targeting is required for multi-page connectors
A single tenant may have multiple Facebook Pages or LinkedIn pages. The composer must let the user pick which asset receives which post.

---

---

## 4. Goals and Objectives

| # | Objective | Success Measure |
|---|---|---|
| 1 | Enable rich outbound posts (images, videos, and link cards) so users do not switch to Hootsuite or Buffer | % of published posts that include media increases; support tickets for missing media decrease |
| 2 | Support multi-page / multi-account publishing from one composed message | A single post can target different pages on the same or different platforms in one request |
| 3 | Reduce failed publishes due to platform-specific asset rules | Platform asset rejections produce clear 422 `PLATFORM_ASSET_REJECTED` messages before dispatch |
| 4 | Keep the product competitive with Sprout Social, Hootsuite, and Sprinklr | Core composer supports media upload and per-platform preview by v1 |
| 5 | Maintain tenant isolation and cost control for media storage | All media is tenant-scoped in Blob and tracked in `media_assets` with owner, size, and MIME type |

---

---

## 5. Functional Requirements

| ID | Requirement | Priority | Acceptance Criteria | Owner |
|---|---|---|---|---|
| FR-001 | The system shall provide `POST /v1/outbound/media` accepting `multipart/form-data` file uploads | Must | Endpoint accepts a file, tenant context, and returns `mediaId`, `url`, `mimeType`, and `sizeBytes` | Backend Lead |
| FR-002 | The system shall store uploaded media in a tenant-scoped Azure Blob container and persist metadata in `media_assets` | Must | `media_assets` contains `id`, `tenant_id`, `owner_id`, `blob_path`, `mime_type`, `size_bytes`, and `created_at` | Backend Lead |
| FR-003 | The system shall return a presigned Blob URL valid for 24 hours on media upload | Must | Connectors and UI can download the file for 24 hours; after expiry a new URL is required | Backend Lead |
| FR-004 | `OutboundPost` shall accept an `assets` array with `image`, `video`, and `link-card` entries | Must | `POST /v1/outbound/posts` accepts `assets` and `assetTargets` fields | Backend Lead |
| FR-005 | The system shall support `assetTargets` mapping `platformId` to a target page/account | Must | A post can specify different targets for different platforms; missing required target returns `400 MISSING_ASSET_TARGET` | Backend Lead |
| FR-006 | The system shall expose `GET /v1/connectors/:platformId/targets` to list available pages/accounts | Must | Returns pages, boards, or accounts the user may publish to for that platform | Backend Lead |
| FR-007 | The system shall validate uploaded image and video formats and sizes | Must | Images: JPEG, PNG, GIF, WebP; max 8 MB. Videos: MP4, MOV; max 512 MB. Invalid files are rejected before storage | Backend Lead |
| FR-008 | The system shall support `link-card` assets using a provided URL and optional `mediaId` image | Should | Composer can attach a link with optional preview image; connector may use OpenGraph metadata or attach the URL directly | Backend Lead |
| FR-009 | Each `SocialConnector` with `publish?()` shall translate generic assets to platform-specific uploads | Must | Connector downloads from presigned URL and re-uploads to platform; rejects with `422 PLATFORM_ASSET_REJECTED` and reason | Backend Lead |
| FR-010 | The composer shall provide a media upload drop zone and per-target preview | Should | Tenant-User can drag/drop media, see per-platform preview, and pick `assetTargets` | Frontend Lead |
| FR-011 | The system shall include clear error responses for missing asset targets and rejected assets | Must | `400 MISSING_ASSET_TARGET` and `422 PLATFORM_ASSET_REJECTED` are returned with human-readable messages | Backend Lead |

---

## 6. User Interaction and Workflows

### 6.1 Primary Actors

| Stakeholder | Role / Interest | Impact | Key Needs |
|---|---|---|---|
| Tenant-User | Primary composer; creates and publishes posts | High | Upload images/videos, pick target pages, preview per platform, publish or schedule |
| Tenant-Social-Care-Agent | Uses publishing for rapid public replies or DMs from the inbox | High | Quick media attach and confident dispatch to the right channel |
| Tenant-Admin | Enables publishing features; manages tenant assets | Medium | Visibility into media storage, queue, and failed publishes |
| Tenant-Brand-Reputation-Manager | Reviews crisis-response content before publish | Medium | Clear preview and validation before a post with media is sent |
| Platform-Admin | Operates the multi-tenant platform | Medium | Cost and storage visibility without seeing tenant content |
| Backend Engineer | Implements the upload, storage, and connector translation | High | Stable contracts, clear validation rules, and error codes |
| Frontend Engineer | Builds composer media drop zone and preview | High | API endpoints and payloads that map cleanly to the UI |

---

### 6.2 User Stories / Use Cases

| ID | As a ... | I want to ... | So that ... | Acceptance Criteria |
|---|---|---|---|---|
| 13.9 | backend engineer | `POST /v1/outbound/media`, `media_assets`, and per-asset targeting, | the composer can publish images, videos, and link cards. | `POST /v1/outbound/media` uploads files to tenant-scoped Blob and returns `mediaId`.; `media_assets` table tracks `tenant_id`, `owner_id`, `blob_path`, `mime_type`, `size_bytes`.; `GET /v1/connectors/:platformId/targets` lists pages/accounts. |
| 13.10 | `Tenant-User` | to upload images and videos in the composer and pick which page/account to target, | I can publish rich posts to the right place. | Media upload drop zone in `PolypostComposer`.; Preview of image/link card per target platform.; `assetTargets` selector for multi-page platforms. |

### 6.3 Workflow Diagrams / Steps

### 1. Media upload endpoint
```
POST /v1/outbound/media
Content-Type: multipart/form-data

Response:
{
  mediaId: string;
  url: string;
  mimeType: string;
  sizeBytes: number;
}
```

- The file is uploaded to Azure Blob Storage under a tenant-scoped container.
- A `media_assets` table tracks `id`, `tenant_id`, `owner_id`, `blob_path`, `mime_type`, `size_bytes`, and `created_at`.
- The response `url` is a presigned Blob URL valid for 24 hours, used by connectors to download and re-upload.

### 2. Asset model in `OutboundPost`
```ts
{
  text: string;
  assets?: Array<{
    type: 'image' | 'video' | 'link-card';
    mediaId?: string;              // references media_assets
    url?: string;                  // for link cards
    alt?: string;
    target?: string;               // platform-specific asset/page id
  }>;
  assetTargets: Record<string, string>; // platformId -> target asset/page id
}
```

### 3. Per-asset targeting
- `assetTargets` maps `platformId` to the target page/account (e.g. Facebook Page ID, LinkedIn Page URN).
- `GET /v1/connectors/:platformId/targets` lists available targets for the user (pages, boards, accounts).
- If a platform requires a target and none is selected, the request returns `400 MISSING_ASSET_TARGET`.
- A single post can target different pages on different platforms.

### 4. Connector asset translation
- Each `SocialConnector` with `publish?()` implements `uploadAssets()` or receives `mediaId` URLs and handles the platform-specific upload in `publish()`.
- `OutboundPost` assets are generic; the connector decides how many images, videos, or link cards the platform allows.
- If the platform rejects an asset (format, size), `publish()` returns a `422 PLATFORM_ASSET_REJECTED` with the reason.

### 5. Media validation
- Supported image types: JPEG, PNG, GIF, WebP.
- Supported video types: MP4, MOV (where the platform supports it).
- Max image size: 8 MB.
- Max video size: 512 MB (v1 supports only images and link cards for most connectors).
- Images are validated for width/height where the platform publishes limits.

### 6. Link cards
- `type='link-card'` uses OpenGraph metadata from the provided `url`.
- The connector may generate a link card preview or attach the URL directly.
- `image` for a link card is optional and can be provided as a `mediaId`.

---

---

## 7. Data Requirements

| Data Element | Description | Source | Owner | Sensitivity |
|---|---|---|---|---|
| `media_assets.id` | Unique identifier for an uploaded media file | Generated by system | Backend | Internal |
| `media_assets.tenant_id` | Tenant that owns the media | Entra / tenant context | Backend | Tenant boundary |
| `media_assets.owner_id` | User who uploaded the media | Authenticated user | Backend | Personal data (user reference) |
| `media_assets.blob_path` | Path to the object in Azure Blob Storage | Generated on upload | Backend | Internal |
| `media_assets.mime_type` | MIME type of the uploaded file | File header / extension | Backend | Internal |
| `media_assets.size_bytes` | Size of the uploaded file in bytes | File metadata | Backend | Internal |
| `media_assets.created_at` | Upload timestamp | System clock | Backend | Internal |
| `OutboundPost.assets` | Array of image, video, and link-card references attached to a post | Composer | Backend | User content |
| `OutboundPost.assetTargets` | Map of platform IDs to target page/account IDs | Composer | Backend | User content |
| `GET /v1/connectors/:platformId/targets` response | List of pages/accounts available for publishing | Connector/platform APIs | Backend | Tenant content |

---

---

## 8. Business Rules and Logic

| ID | Rule |
|---|---|
| BRU-001 | Every uploaded media file is stored in an Azure Blob path scoped to the uploading tenant |
| BRU-002 | `media_assets` records the tenant, owner, MIME type, size in bytes, and creation timestamp for every file |
| BRU-003 | The download URL returned for a media asset is a presigned Blob URL valid for 24 hours |
| BRU-004 | Only supported image formats (JPEG, PNG, GIF, WebP) and video formats (MP4, MOV) may be uploaded |
| BRU-005 | Image uploads may not exceed 8 MB; video uploads may not exceed 512 MB |
| BRU-006 | A `link-card` must contain a URL and may optionally reference an uploaded image by `mediaId` |
| BRU-007 | If a platform requires a target page/account and none is selected, the publish request returns `400 MISSING_ASSET_TARGET` |
| BRU-008 | A single composed post may target different pages or accounts on different platforms |
| BRU-009 | Each connector decides how many images, videos, or link cards the platform allows and how to upload them |
| BRU-010 | When a platform rejects an asset for size, format, or policy reasons, the connector returns `422 PLATFORM_ASSET_REJECTED` with a reason |
| BRU-011 | Video upload is supported only where the connector explicitly enables it; most connectors start with images and link cards |

---

---

## 9. Interfaces and Integrations

### 1. Media upload endpoint
```
POST /v1/outbound/media
Content-Type: multipart/form-data

Response:
{
  mediaId: string;
  url: string;
  mimeType: string;
  sizeBytes: number;
}
```

- The file is uploaded to Azure Blob Storage under a tenant-scoped container.
- A `media_assets` table tracks `id`, `tenant_id`, `owner_id`, `blob_path`, `mime_type`, `size_bytes`, and `created_at`.
- The response `url` is a presigned Blob URL valid for 24 hours, used by connectors to download and re-upload.

### 2. Asset model in `OutboundPost`
```ts
{
  text: string;
  assets?: Array<{
    type: 'image' | 'video' | 'link-card';
    mediaId?: string;              // references media_assets
    url?: string;                  // for link cards
    alt?: string;
    target?: string;               // platform-specific asset/page id
  }>;
  assetTargets: Record<string, string>; // platformId -> target asset/page id
}
```

### 3. Per-asset targeting
- `assetTargets` maps `platformId` to the target page/account (e.g. Facebook Page ID, LinkedIn Page URN).
- `GET /v1/connectors/:platformId/targets` lists available targets for the user (pages, boards, accounts).
- If a platform requires a target and none is selected, the request returns `400 MISSING_ASSET_TARGET`.
- A single post can target different pages on different platforms.

### 4. Connector asset translation
- Each `SocialConnector` with `publish?()` implements `uploadAssets()` or receives `mediaId` URLs and handles the platform-specific upload in `publish()`.
- `OutboundPost` assets are generic; the connector decides how many images, videos, or link cards the platform allows.
- If the platform rejects an asset (format, size), `publish()` returns a `422 PLATFORM_ASSET_REJECTED` with the reason.

### 5. Media validation
- Supported image types: JPEG, PNG, GIF, WebP.
- Supported video types: MP4, MOV (where the platform supports it).
- Max image size: 8 MB.
- Max video size: 512 MB (v1 supports only images and link cards for most connectors).
- Images are validated for width/height where the platform publishes limits.

### 6. Link cards
- `type='link-card'` uses OpenGraph metadata from the provided `url`.
- The connector may generate a link card preview or attach the URL directly.
- `image` for a link card is optional and can be provided as a `mediaId`.

---

---

## 10. Non-Functional Considerations

| ID | Requirement | Category | Priority | Acceptance Criteria |
|---|---|---|---|---|
| NFR-001 | Uploaded media must be isolated to the uploading tenant and accessible only via presigned URLs | Security | Must | No cross-tenant Blob access; `media_assets` is RLS-scoped by `tenant_id` |
| NFR-002 | An 8 MB image upload should complete end-to-end within 5 seconds under normal conditions | Performance | Should | Measured in contract and UI tests |
| NFR-003 | Connector `publish()` must fail gracefully and record the reason without crashing the scheduler | Reliability | Must | Failed publishes update `outbound_activities.status` to `failed` with `error_message` |
| NFR-004 | Media upload and storage must support 512 MB video files when enabled by a connector | Scalability | Should | Contract tests demonstrate accepted video size for the relevant connector |
| NFR-005 | Media access and publishing actions must be auditable per tenant | Compliance | Should | `media_assets` records `owner_id` and `created_at`; `outbound_activities` records publish attempts |
| NFR-006 | The composer must provide accessible, keyboard-navigable media upload and preview controls | Usability | Should | WCAG 2.1 AA keyboard and screen-reader guidance applied to media drop zone and target selector |

---

---

## 11. Error Handling and Exceptions

| ID | Risk | Likelihood | Impact | Mitigation | Owner |
|---|---|---|---|---|---|
| R-001 | Azure Blob storage and egress costs increase with media uploads | Medium | Medium | Tenant-scoped containers, size limits, and future lifecycle/cleanup policy; monitor usage | Platform-Admin |
| R-002 | Video support increases connector complexity and testing burden | Medium | High | Defer full video support to connectors that explicitly opt in; most v1 connectors support images and link cards only | Backend Lead |
| R-003 | OAuth scope creep for uploading to Facebook/LinkedIn/X | Medium | High | Verify exact scopes per platform before release; separate media upload scope from publish scope in connector documentation | Backend Lead |
| R-004 | Platform-specific asset rules are fragmented and may change | Medium | Medium | Build a per-platform capability matrix and let connectors own validation; keep platform rules in connector configuration, not hardcoded core rules | Backend Lead |
| R-005 | Missing asset targets or invalid media cause a poor composer experience | Medium | Medium | Provide real-time validation feedback and clear, actionable error messages in the UI | Frontend Lead |

---

---

## 12. Assumptions and Dependencies

- Azure Blob Storage is already provisioned and used for other features
- `outbound_activities` and `SocialConnector.publish?()` are already in place or will land before this feature
- A tenant has at least one connected platform asset (Facebook Page, LinkedIn page, etc.) that supports publishing
- The Polypost Composer will provide the drag/drop media UX and platform preview tabs

| ID | Dependency | Type | Owner | Expected Resolution |
|---|---|---|---|---|
| D-001 | ADR-0098 (publishing and scheduling) | Predecessor ADR | Backend Lead | Before media upload integration |
| D-002 | ADR-0101 (multi-source connector capability matrix) | Predecessor ADR | Backend Lead | Before connector asset translation |
| D-003 | ADR-0075 (outbound social post publishing) | Foundation ADR | Backend Lead | Before `POST /v1/outbound/posts` accepts `assets` |
| D-004 | `07-publishing-and-scheduling` feature design | Feature context | Product Owner | Reference material |
| D-005 | `07-publishing-and-scheduling-deep-research.md` | Research brief | Product Owner | Reference material |
| D-006 | `feature-adr-scoping.md` | Scoping plan | Product Owner | Reference material |
| D-007 | Story 13.9 (publishing media upload and asset targeting, backend) | Story | Backend Lead | Before frontend story |
| D-008 | Story 13.10 (media upload and asset targeting UI, frontend) | Story | Frontend Lead | After backend story |
| D-009 | Azure Blob Storage and presigned URL support | Infrastructure | Platform-Admin | Already provisioned |
| D-010 | Facebook connector with `publish?()` | Existing connector | Backend Lead | In place or landing with ADR-0098 |

---

---

## 13. Open Questions

- Should video upload be supported in v1, or deferred to v2?
- How is asset expiry handled? Should unreferenced media be cleaned up after 30 days?
- Should `media_assets` include a thumbnail or preview for the UI?
- How does the composer preview a multi-image post across different platforms?

---

---

## 14. Appendix

### Reference Documents

- ADR-0115: `docs/adr/0115-publishing-media-upload-and-asset-targeting.md`
- BRD-0115: `docs/project docs/Business-Requirements/BRD-0115-Publishing-Media-Upload-And-Asset-Targeting.md`
- Feature design: `docs/product-research/feature-designs/07-publishing-and-scheduling.md`
- Deep-research report: `docs/product-research/reports/07-publishing-and-scheduling-deep-research.md`
- User stories: `docs/user-stories/epic-13-adr-0109-to-0117.md`

### Missing Sources Noted

- _All expected related sources were found._

### Revision History

| Version | Date | Author | Description |
|---|---|---|---|
| 0.1 | 2026-08-23 | FDD Writer — Batch Agent | Initial synthesis from ADR-0115 and BRD-0115. |