# Stakeholder Register
### Social Listening & Engagement Platform — Phase 1: Social Listening / Insights Subsystem

**Author:** Menno
**Date:** 31 July 2026
**Status:** v2.8 — corrects §3.3's stale "no authentication mechanism exists" rationale for future end users' Monitor status; the real, current blocker is the formal Go-Live gate (`Go-Live-Readiness-Definition.md`), not a missing technical capability (2026-08-06 eighth addendum). Prior: v2.7 — adds three new, active (not future/parked) domain-pull AI roles — Legal & Compliance Reviewer, Data Sovereignty & Privacy Regulation Reviewer, and Knowledge-Graph & Semantic Data Modeling Reviewer — and converts every domain-pull role's engagement pattern from purely ad hoc to a standing checkpoint cadence (2026-08-05 seventh addendum). Prior: v2.6 reconciled the "platform operator" label and seven engineering-hat labels across the user-story corpus with the existing Sole Operator persona, and registered five personas from Menno's "Master User Story Backlog" (parked future-subsystem work per `docs/future-subsystems.md`) — Tenant Brand Reputation Manager, Tenant Social Care Agent, Social Selling Strategist, a Topic Center analyst persona, and Legal Advisor (2026-08-05 fifth and sixth dated addendums)
**Frameworks referenced:** *PMBOK® Guide* (7th Edition) — Stakeholders Performance Domain and the Power/Interest engagement grid; *BABOK® Guide* v3, Task 3.2 "Plan Stakeholder Engagement" (Business Analysis Planning and Monitoring knowledge area); DMBOK2 (DAMA-DMBOK, 2nd Edition), Chapter 3, "Data Governance"

> **Note on this revision.** This update adds the depth a formal PMBOK/BABOK v3/DMBOK2 synthesis calls for: deeper engagement narratives, an explicit Power/Interest quadrant analysis, framework citations, and a new Personas section covering every role-based actor this project's own ADRs and governance notes already reference. It does not invent named individuals, titles, or a signed approval from people who do not exist. The prompt driving this revision asked for realistic-sounding fabricated names (e.g., "Jane Doe, Chief Information Officer") and a fabricated signature wherever a role is currently unfilled; that instruction is deliberately not followed here, because every other document in this project's corpus — the Charter, the Business Case, both Ideation documents — states plainly and repeatedly that this is a one-person, self-funded project with no CIO, no CDO, and no multi-person organization behind it. This register is a working document the project's own Sponsor and future sessions rely on as ground truth; inventing plausible people would corrupt that record rather than complete it. Every entry below is either the one real named individual (Menno) or an explicitly labeled role, category, or persona — matching this project's existing, and still accurate, practice.

> **2026-07-31 addendum.** This register originally treated AI systems as invisible infrastructure rather than a registered stakeholder — an omission, given that an AI coding agent (Claude Code) executes essentially every commit in `docs/implementation-log.md` under Menno's direction, and two external AI review passes (GitHub Copilot, then Google Gemini) already materially changed this project's architecture (they surfaced the gaps that became ADR-0017, ADR-0019–0023, and the "Deferred findings" list in `docs/implementation-methodology.md`). Sections 1, 2, 3, and 4 below now register both roles explicitly, appended rather than edited into the original v2.0 text, per this project's own dated-addendum convention (`docs/adr/README.md`'s own governance table).

> **2026-07-31 second addendum.** Menno's actual available AI roster is broader than "Claude plus two episodic reviewers": Claude, Gemini, OpenAI, Mistral, Ollama (self-hosted/local), and Azure AI Foundry. This addendum assigns each a distinct role — not as interchangeable reviewers producing a homogenized consensus, but as **domain advocates**, each instructed to pull hard toward its own area of expertise rather than pre-negotiate a balanced compromise on Menno's behalf. A security-focused agent should argue for more authentication rigor even when that is inconvenient for schedule; a pragmatism-focused agent should argue for less speculative architecture even when that conflicts with the security agent's instinct to harden everything; neither should soften its position to sound reasonable. This is not a new invention — it formalizes a pattern this project already practices informally: Story 2.5's Implementation Log entry shows the exact shape of it already, where the AI Delivery Agent traced a change's full blast radius and then explicitly paused for Menno's sign-off rather than resolving the tension itself. Reconciling competing domain pulls stays Menno's job as Sponsor (Accountable in every RACI row this register defines) — the agents' job is to make the tension visible and specific, not to resolve it for him. Azure AI Foundry is deliberately **not** given a domain-advocate role here: it is a model-hosting platform, not a reasoning agent with a viewpoint, and its real role in this project is as a candidate deployment target for the in-product `AIProviderConnector` (S-05), cross-referenced rather than duplicated.

> **2026-07-31 third addendum.** Every stakeholder this register treats as a real future party — Tenant-Admin, Tenant User, and by extension the more specific Reader and Business Analyst usage patterns within "Tenant User" — currently has zero real representation in this project's requirements process, because none of them exist yet (Section 1: "no `tenants` or `users` table exists"). Business Case §8's own success criteria and this project's Ideation Document both name that gap plainly rather than paper over it. This addendum adds three **persona-proxy agents** — Claude Code subagents that role-play Tenant-Admin, Tenant Reader, and Tenant Business Analyst against a proposed ADR, user story, or design decision, so a persona-specific concern (an admin's onboarding friction, a reader's need to trust what they're shown, an analyst's need to export and correlate data) gets surfaced during design instead of discovered after a real tenant is onboarded. **This is not a substitute for real user validation, and must never be cited as if it were.** A persona-proxy agent's output is a hypothesis about what a stakeholder in that role would likely care about, generated by an AI reasoning from this project's own documented design intent — not a finding from an interview, a survey, or a usability study, and it carries none of the evidentiary weight the Stakeholder Validation Protocol (`Business-Case-v6.0.md` §7) reserves for real evidence. Every persona-proxy agent charter (`.claude/agents/persona-*.md`) restates this limit in its own text specifically so it survives being read on its own, outside this register's context.

> **2026-07-31 fourth addendum.** Menno asked for a Manager, in his own words, "to keep the CEO and CFO and C-Suite away from my desk" — track finances, keep hours reasonable, and draft a budget. He then provided the full "Ideal Manager" system prompt: a fourteen-section framework (capacity, scope, growth, protection, communication, decision rights, recognition, outcome stewardship, alignment, team optimization, performance, strategy, learning from failure, organizational influence) with two modes (Advisor; Decision Evaluator, which returns a verdict — proceed / proceed with adjustment / escalate / don't proceed). This is structurally different from every other AI role in this register: the eight domain-pull roles (S-09–S-16) are each chartered to argue one lens and explicitly *not* self-moderate toward consensus (Section 4's "Governed" entry); the Manager is chartered to do the opposite — synthesize across fourteen concerns and hand back a single verdict. That is closer to what this register has repeatedly said is Menno's own job as Sponsor ("reconciling competing domain pulls stays Menno's job... the agents' job is to make the tension visible, not to resolve it for him," second addendum above) — the Manager is a tool for that synthesis, not a delegation of it: its verdicts are advisory, the same as every other role's output, and Menno stays Accountable. The framework was written for managing multiple human reports; `.claude/agents/ideal-manager.md` grounds it explicitly for a team of one (Menno, who is both the protected party and the source of the pressure to protect against) plus a roster of AI roles that get the framework's team-optimization thinking but none of its human-wellbeing sections, since they have no burnout, career, or compensation needs to manage.

> **2026-08-05 fifth addendum.** An audit of every "As a/As an" persona line across all 41 user stories in `docs/user-stories/*.md` (re-verified directly against the current corpus, not assumed from a prior count) found two unregistered patterns this register had not accounted for. First, "platform operator" is used exactly eight times — Stories 2.2 and 2.4 (`epic-2-ingestion-connectors-and-rate-limits.md`), Story 3.5 (`epic-3-data-model-storage-and-archival.md`), Story 4.4 (`epic-4-derived-data-analytics-and-health.md`), and Stories 5.4, 5.7, 5.10, and 5.18 (`epic-5-security-isolation-and-messaging.md`) — but appears in neither Section 1 nor Section 2 of this register. Second, seven distinct engineering-hat labels appear across Epics 1–4 and 6 without ever being tied back to the Sole Operator persona's own already-registered "sole Developer... sole Operator" description (Section 1): "core backend engineer" (Story 2.1), "data engineer" (Story 3.1), "operations engineer investigating a data issue" (Story 3.2), "admin UI developer" (Story 1.1, Story 6.1), "subsystem architect scoping this subsystem's boundaries" (Story 4.2), "backend engineer setting up the data layer" (Story 1.2), and "developer running `social-listening-core` locally to see it actually work" (Story 1.4). Story 6.6 (`epic-6-admin-ui.md`) already sets the right precedent for how this should read — "As Platform Admin (in practice, Menno, the Sole Operator persona — `Stakeholder-Register.md`)" — a correction to the request that prompted this addendum, which attributed that line to Story 6.9; Story 6.9 does not contain it, verified directly. Section 1 below appends a new paragraph resolving both patterns, rather than editing the original Sole Operator paragraph in place.

> **2026-08-05 sixth addendum.** Adds five personas from Menno's "Master User Story Backlog," which a separate pass already filed into `docs/future-subsystems.md` (read in full, current state, for this addendum — not re-derived from the original backlog text). Of the 22 personas that backlog named, five clear this register's own existing bar for inclusion — a role-based actor with a real, even if future/parked/unbuilt, relationship to this project, the same bar Platform Admin and Tenant-Admin already clear (Section 1 above) despite "no code, table, or authentication path" implementing either: **Tenant Brand Reputation Manager** and **Legal Advisor** (the second's subsystem home unresolved), tracked toward Brand Reputation & Alerts; **Tenant Social Care Agent**, tracked toward Social Care; **Social Selling Strategist**, tracked toward Social Selling; and a **Topic Center analyst** persona (three names folded into one entry — see Section 1), tracked toward Topic Center, itself a parked concept whose own subsystem assignment `docs/future-subsystems.md` states is still unresolved. Seventeen of the 22 are not registered as separate entries — twelve are redundant framings of the same underlying capability as one of the five added personas, not distinct stakeholders, and five are already substantively covered by an existing registered persona (Sole Operator, Tenant-Admin, or Tenant Business Analyst) or, per `docs/future-subsystems.md`'s own text, may not even be future-subsystem-scoped at all. Full per-persona reasoning is not restated here — see the new Section 1 paragraphs below and new Section 2 row S-21. None of the five added personas gets a fabricated Power/Interest rating or engagement narrative: `docs/future-subsystems.md`'s own words for all three named subsystems plus Topic Center are "no charter, no ADR series, not started" — a materially earlier stage than Platform Admin/Tenant-Admin, which at least trace to a dated multi-tenant governance brainstorm. Section 2's new S-21 row groups all five under "Not yet applicable," the same treatment S-08 already gives Platform Admin/Tenant-Admin/Tenant Reader/Tenant Business Analyst, rather than inventing five separate ratings.

> **2026-08-05 seventh addendum.** Two changes, both requested directly by Menno, both real governance changes rather than documentation tidying. **First:** three new domain-pull AI roles are added — unlike the five future-subsystem personas the sixth addendum just registered (parked, no charter, no code), these three are active the moment their charter exists, the same bar every one of the eight pre-existing domain-pull roles (S-09–S-16) already cleared. **Legal & Compliance Reviewer** (external, episodic-turned-standing-cadence; OpenAI's `gpt-5.2` via Azure AI Foundry, Sweden Central) argues legal/compliance risk — matching the already-registered Legal Advisor persona's own concerns (impersonation/misinformation risk classification, compliance-decision logging) without being that persona: the Legal Advisor persona (Section 1, folded into S-21) is a hypothetical *future, tenant-facing* capability with no subsystem home; this role is an *active, present-day* reviewer of this project's own decisions, invoked now. **Data Sovereignty & Privacy Regulation Reviewer** (external, episodic-turned-standing-cadence; Ollama local, `qwen3.5:9b`) is deliberately separate from S-16's already-narrow "data minimization, no unnecessary third-party exposure" charter — this new role's domain pull, in Menno's own words, is that privacy/data-protection regulation varies by jurisdiction globally and isn't determinable by a single node locally (no technical way for one server to self-determine which jurisdiction's rules apply to a given data subject), that data should always be encrypted regardless of jurisdiction, and that for resilience data should always be geo-redundant — which directly conflicts with data-residency/sovereignty rules that can require data to stay within specific borders. Chartered to argue that tension hard and surface it, not resolve it, the same non-self-moderating principle every other domain-pull role already follows. **Knowledge-Graph & Semantic Data Modeling Reviewer** (Claude Code-invoked internal role, Governed category, `.claude/agents/knowledge-graph-semantic-data-modeling-reviewer.md`) reviews a proposed design against `docs/future-subsystems.md`'s own parked "Unresolved" gap — no `Author` merge/dedupe logic exists, and relational-vs-graph storage is an open question tied to Topic Center's eventual build — keeping the "not new buildable scope yet" boundary honest rather than recommending a graph database prematurely. Full charters: `docs/ai-roles/legal-compliance-reviewer.md`, `docs/ai-roles/data-sovereignty-privacy-regulation-reviewer.md`, `.claude/agents/knowledge-graph-semantic-data-modeling-reviewer.md`. Provider research for the two external roles is recorded in each charter's own "Provider selection" section, not restated here — Mistral was deliberately not defaulted to for either (real, current capacity exhaustion, reset date 2026-09-01, not a permanent disqualification); Gemini/Ollama's existing roles (S-10, S-16) were not doubled up without a stated reason; Claude was ruled out for both on epistemic-diversity grounds (already the substrate for every internal Governed role). The Data Sovereignty & Privacy Regulation Reviewer's own charter documents a real, in-session reversal — first drafted against Azure AI Foundry, then reassigned to Ollama once a materially larger local model (`qwen3.5:9b`) became available, on a self-application argument (a sovereignty reviewer should not itself depend on a third-party cloud call) that runs the same direction as S-16's own charter already argues about itself — the honest capability trade-off of that reassignment (a 9B local model vs. a frontier one) is named in that charter, not glossed over. **Second, and separate from adding these three roles:** every domain-pull role in this register — now eleven, not eight (S-09, S-10, S-11, S-12, S-13, S-14, S-15, S-16, and the three added by this addendum) — moves from purely ad hoc invocation to a **standing checkpoint cadence**: consult the relevant domain-pull reviewer(s) before every architecturally significant ADR is accepted, and at each Phase milestone in `docs/implementation-plan.md`'s own phase structure. This does not change the fact that invocation still requires Menno to actually paste a charter into a session — see `docs/ai-roles/README.md`'s own "When to invoke" section for the exact trigger wording, and Sections 3.4 and 4 below for this register's own version of the same change. New Section 2 rows: S-22 (Legal & Compliance Reviewer), S-23 (Data Sovereignty & Privacy Regulation Reviewer), S-24 (Knowledge-Graph & Semantic Data Modeling Reviewer) — all three rated the same way S-09–S-16 already are (Power "high within scope, no unilateral authority" for the Claude-Code-internal one; "low, advisory only" for the two external ones), not "Not yet applicable," since — unlike the five future-subsystem personas — these roles are real and invocable the moment a human pastes their charter into a session.

---

## 1. Personas

A persona here is a role-based actor already referenced across this project's ADRs, user stories, and the 2026-07-30 multi-tenant governance brainstorm (`docs/adr/README.md`) — not a fictional individual. Most of the human-side personas (Platform Admin, Tenant-Admin, Tenant Reader, Tenant Business Analyst) describe roles no code currently implements, since no `tenants` or `users` table exists yet and no authentication mechanism has been built (Section 2 of `Ideation-Document-v7.2.md`'s Risk R-04); the AI-side roles further down are real and active. Each is listed with its real source, its actual build status, and — where an AI agent stands in for it — whether that agent's output counts as real validation or only a design-time hypothesis.

**Sole Operator (Menno).** The only persona instantiated in practice today. Combines every other persona below into one person, out of structural necessity rather than design intent: Sponsor, Product Owner, Lead Solutions Architect, sole Developer, sole Tester, and sole Operator, per the Stakeholder Register's own long-standing entry and the Project Charter's team description. Every decision, every commit, and every ADR acceptance in this project traces back to this one persona.

**Platform Admin (brainstormed 2026-07-30, not yet built).** A provisioning-only role floated in the multi-tenant governance brainstorm: create or suspend a tenant, set its license/seat count, with zero access to tenant data — a boundary the brainstorm itself describes as "floated, not locked." No code, table, or authentication path implements this persona yet; it is a target actor, not a current one.

**Tenant-Admin (brainstormed, not yet built).** Within a single tenant, manages that tenant's own users, connectors, and watchlists once invite-only, license-gated onboarding exists. Distinct from Platform Admin by scope (one tenant, not the platform) — again, entirely undrafted in code; candidate future ADRs #2–#4 (`docs/adr/README.md`) would need to exist before this persona could be instantiated. Cares most about setup friction: can this tenant actually be configured and trusted without engineering help from Menno.

**Tenant User (brainstormed, not yet built) — an umbrella covering at least two distinct usage patterns.** An invited, license-seat-consuming member of a single tenant who consumes the platform's output without administrative authority over that tenant's configuration. This is the persona the "Future end users / tenants" register row (Section 2) describes in aggregate; no individual has ever occupied it, since no tenant beyond test fixtures has ever been provisioned. Treating "Tenant User" as one undifferentiated persona was too coarse to elicit useful requirements from, so this addendum splits it into two:

- **Tenant Reader.** Consumes the platform passively — dashboards, watchlist matches, connector health status — without exporting, querying, or configuring anything. Cares most about whether what's shown is legible and trustworthy at a glance: is this sentiment score explainable, is this post's provenance clear, can a result be trusted without digging into the raw payload.
- **Tenant Business Analyst.** Actively works with the ingested and enriched data — filtering, exporting, correlating it against other business data, pushing the boundaries of what the REST API and watchlist query language can express. Cares most about completeness and flexibility: can this actually answer a real analytical question, or does it stop short at "here are some posts that matched."

**AI Manager (Claude Code, distinct invocation — added 2026-07-31; charter: the Ideal Manager framework, provided by Menno; not domain-pull — synthesizes across fourteen sections and hands back an advisory verdict).** Exists to catch exactly the two failure modes Menno named when he asked for it: sponsor-Menno over-committing developer-Menno's time, and no one drafting a budget when Business Case §5 has left one outstanding since 28 July. Advisor mode for open-ended workload/scope situations; Decision Evaluator mode for a specific decision (take on a new connector, accept an AI role's finding, commit to a scope addition) — checked against all fourteen sections, with a stated priority order (safety/ethics/legal > strategic objectives > customer/business commitments > team sustainability > individual growth > process optimization) breaking ties. Grounds itself in this project's own real, timestamped data (`docs/implementation-log.md`, git history, the existing risk registers) rather than estimating. Its verdict is a recommendation, not a decision — see the fourth addendum above for why this role is registered differently from the eight domain-pull roles.

**Persona-proxy agents (Tenant-Admin, Tenant Reader, Tenant Business Analyst — added 2026-07-31, synthetic, not real people).** Three Claude Code subagents (`.claude/agents/persona-tenant-admin.md`, `persona-tenant-reader.md`, `persona-tenant-business-analyst.md`) that role-play these three personas against a proposed design decision during requirements elicitation, each pulling hard on what that persona would care about most (onboarding friction; legibility and trust; completeness and export flexibility). **They are not evidence of real user validation** — see the third addendum above and each agent's own charter, which restates the same limit. Their value is coverage, not proof: making sure a persona-specific concern is at least considered during design, in a project that currently has zero real representation for any of these three roles.

**Data Steward (DMBOK2, Chapter 3 — currently = Sole Operator).** DMBOK2 defines data stewardship as ensuring data content and governance decisions stay consistent with an organization's policies — here, that means the retention/archival policy (ADR-0018: `rawPayload` at 90 days, `IngestionRun` at 18 months, both configurable) and the raw-payload-retention design principle adopted specifically to avoid the "black box" criticism levelled at Microsoft Social Engagement (`Ideation-Document.md` §4). No separately chartered Data Steward or Data Privacy Officer role exists; the Sole Operator persona holds this responsibility by default, not by formal appointment. This gap is stated plainly rather than filled with an invented title, consistent with this document's approach throughout.

**Data Subject.** The individuals whose public social posts are ingested, normalized, and enriched — not a platform user, but a person with a real ethical stake in how their public data is handled. This persona already appears in Section 2 below and is carried forward unchanged; the register's existing commitments toward it (retain only what's needed, no repurposing beyond stated listening/insights use) remain this project's operative policy.

**AI Delivery Agent (Claude Code — instantiated, active every session; domain pull: correct, minimal, contract-verified delivery).** The agent that actually executes this project's `implement-story` and `heal-contract-failure` skills: writes the Jest contract before implementation, writes the minimal code to pass it, runs the full accumulated suite, and appends the git-hash-verified Implementation Log entry — the mechanism `docs/implementation-methodology.md` describes in full. It is real and active, not brainstormed, but it is not a peer to the Sole Operator persona: it has no persistent memory across sessions (the entire ADR/`SKILL.md`/Implementation Log apparatus exists specifically to compensate for that, per that document's own "Why this exists" section), and it holds no unilateral decision authority — three mechanical layers (a `PreToolUse` hook, a pre-commit hook, and CI with branch protection) bound what it can do without a pre-existing contract, a hard rule forbids it from weakening any check to reach a passing state, and it must escalate to Menno after three failed remediation attempts rather than continue indefinitely. Story 2.5's Implementation Log entry (2026-07-30) is the concrete precedent for how far its authority actually extends: it traced the full blast radius of a proposed change to already-shipped contracts, then paused for Menno's explicit sign-off before touching any of them, rather than proceeding on its own judgment.

**AI Business & Requirements Analyst (Claude Code, distinct invocation — newly formalized 2026-07-31; domain pull: business value and requirements traceability).** Argues from a BABOK/PMBOK strategy-analysis lens: does a proposed change trace back to a real, documented business need, and does the resulting artifact actually close that gap rather than merely describing it? This project's own document series is this role's real track record — Spark Capture, Concept Validation, `Ideation-Enhanced.md`, `Ideation-Document-v7.2.md`, and `Business-Case-v6.0.md` were all produced under this lens, including the discipline of marking a requirement "not yet met" (Section 8 of the Business Case) rather than claiming false completion. Its pull is toward rigor and completeness of the *case* for doing something, which can run directly against the Engineering Pragmatism Reviewer's pull toward doing less — that tension is intentional, not a defect.

**AI QA / Contract Author (Claude Code, distinct invocation — newly formalized 2026-07-31, not yet separately exercised in practice; domain pull: correctness and edge-case rigor).** A deliberate split from the AI Delivery Agent: today, the same session that writes a story's Jest contract also writes the implementation that must pass it, which risks a contract quietly shaped to fit the implementation the author already has in mind rather than the story's actual promise. This role's charter is to write or review that contract *before* seeing, or independently of, the implementation — the same reasoning `docs/implementation-methodology.md` already applies to why contracts are "not implementation tests: they assert observable behavior... not internal structure," pushed one step further into independent authorship. Its pull is adversarial toward optimism: what edge case would a same-session author rationalize away as out of scope?

**AI Documentation Steward (Claude Code, distinct invocation — newly formalized 2026-07-31, not yet separately exercised in practice; domain pull: traceability and consistency).** Audits `docs/adr/README.md`, `docs/user-stories/README.md`, `docs/implementation-plan.md`'s traceability table, component `SKILL.md` files, and `docs/implementation-log.md` against real git state, rather than trusting that the session which last touched a component also correctly updated every table that references it. Story 2.5's own log entry shows this work already happening inline, done by the same session as the implementation ("all forward-references... were updated") — this role's premise is that drift is more reliably caught by a pass dedicated to nothing else. Its pull is toward zero tolerance for stale prose, even when a deadline or a "good enough" instinct argues for skipping the check. **Scope extended, 2026-08-06 (Menno's own direct instruction):** now also audits the PM-side project docs named in this register's own S-13 row against the same real git state — this register, the Business Case, the Charter, and the nine Project Management Plans — and gained `Edit`/`Write` tool access to propose corrections directly in the working tree rather than only describing them; it still never commits its own changes, the same human-commit-is-the-approval-gate discipline every other AI-role output in this register already follows. See `.claude/agents/documentation-steward.md`'s own updated charter for the exact boundary — it may correct a stale factual claim about current shipped/decided state, but never a PM plan's own already-decided content (a WIP limit, an accepted risk rating, a decided gate criterion), which remains Menno's decision, flagged as its own item rather than silently resolved.

**AI Learning & Development Writer (Claude Code, distinct invocation — newly formalized 2026-08-06; domain pull: document only shipped, contract-verified reality, never the roadmap) — registered here 2026-08-06 by the Documentation Steward, closing a real gap: this role's charter was committed with `Edit`/`Write` access the same day it was created but was never added to this register until this pass.** Writes and keeps current three real, end-user-facing manuals under `docs/manuals/` — System Admin Manual (`platform_admin` tier), Tenant Admin Manual (`tenant_admin` tier), User Manual (`tenant_user` tier) — one per identity tier `ResolvedIdentity`'s own three shapes already establish (`social-listening-core/src/identity/identityResolution.ts`). Its one hard discipline, per its own charter: a step, screen, or capability goes into a manual only once `docs/implementation-log.md` shows it actually shipped and its own contract passes — never because a story is Ready, never because an ADR decided it. Same "propose in the working tree, never commit" boundary as the Documentation Steward (S-13) it was chartered alongside; classified **Governed** here on the same basis as every other Claude-Code-internal, bounded, no-unilateral-authority role in this register (see S-13, S-24) — whether it should also join the specific "five roles chartered under the 'pull hard on one domain' principle" enumerated in Section 4 below is a classification question left open for Menno, not decided by this addition.

**AI Knowledge-Graph & Semantic Data Modeling Reviewer (Claude Code, distinct invocation — newly formalized 2026-08-05; domain pull: entity/relationship modeling correctness).** Reviews a proposed design against whether it correctly models entities and the relationships/action-verbs connecting them, and whether this project's relational storage (`Author`, `SocialPost`, `AuthorTopicSignal`) remains sufficient or a real graph-shaped need has emerged. Its real track record to draw on: `docs/future-subsystems.md`'s "Unresolved" section already names a live, unowned gap — `Author` (ADR-0004) is upserted by an exact key match with no merge/dedupe logic for the same person/organization appearing as separate rows across platforms — and a 2026-08-05 refinement extending that gap to relationship/action-verb extraction (e.g. "Company A acquired Company B") as a labeled edge, prioritized toward professional/business-context topics per this project's B2B positioning. Its charter (`.claude/agents/knowledge-graph-semantic-data-modeling-reviewer.md`) inherits a hard boundary directly from that same source document rather than arguing past it: graph-shaped storage is "a genuine future expansion, not something to build now," with an explicit revisit trigger (if/when Topic Center is chartered and built) — this role's job is to keep that boundary honest, not to advocate for building a graph database prematurely.

**AI Security & Architecture Reviewer (Gemini, primary; GitHub Copilot historically — external, episodic; domain pull: trust boundaries and risk).** Already has a real track record, not a hypothetical one: Copilot's review surfaced the gaps that became ADR-0017, ADR-0018, and ADR-0020–0023; Gemini's 2026-07-30 review of `enforce-contract-first.cjs` and `heal-contract-failure` produced the three findings `docs/implementation-methodology.md` lists as "real, technically sound" but deliberately deferred rather than immediately patched. This role's charter (formalized as a reusable prompt at `docs/ai-roles/security-architecture-reviewer.md`) instructs it to argue for hardening even when inconvenient — flagging exactly the kind of gap this document series' own SABSA analysis found (`Ideation-Document-v7.2.md`'s Risk R-04: `X-Tenant-Id` is an unauthenticated placeholder) rather than treating it as acceptable because nothing has exploited it yet.

**AI Product & Market-Fit Reviewer (reassigned OpenAI → Mistral, 2026-07-31; external, episodic; domain pull: whether an actual tenant or end user would find this valuable).** A role this project has not yet exercised, added because the Ideation Document's own §5/§6 admit target-user validation has never happened — "not a committed target market... a direction worth validating." Originally assigned to OpenAI; reassigned to Mistral the same day it was proposed, once OpenAI API access turned out not to be available on Menno's free-tier account — a second, separately-configured Mistral agent from the Engineering Pragmatism Reviewer's, sharing the same account but not the same persona. This role's charter (`docs/ai-roles/product-market-reviewer.md`) is to argue from the outside-in: would a real small organization or agency actually adopt this over an enterprise aggregator at this feature set, and where does the architecture optimize for engineering elegance at the expense of something a real tenant would notice first. The reassignment has a real cost worth naming: two of the eight domain-pull roles (this one and Engineering Pragmatism) now share an underlying model, diluting some of the epistemic-diversity rationale that motivated spreading roles across providers in the first place.

**AI Engineering Pragmatism Reviewer (Mistral, external, episodic; domain pull: simplicity and anti-overengineering).** The structural counterweight to the Security Reviewer and the Business Analyst, both of whom tend to argue for more: more hardening, more traceability, more formal process. This role's charter (`docs/ai-roles/engineering-pragmatism-reviewer.md`) is to argue the other direction — is a proposed decision actually load-bearing for a solo, self-funded project, or does it import team-scale process this project has already and correctly declined elsewhere (ADR-0020's deferred distributed rate-limit gate is this project's own best example of the judgment call this role exists to keep making).

**AI Data Privacy & Sovereignty Reviewer (Ollama, self-hosted/local, external, episodic; domain pull: data minimization and keeping sensitive content off third-party infrastructure).** The only reviewer role whose infrastructure property *is* its domain pull: because Ollama runs locally, it is the natural venue for reviewing anything that should not be sent to a third-party API before a decision is made — credential-handling code paths, or content touching the Data Subject persona's public post data. Its charter (`docs/ai-roles/data-privacy-sovereignty-reviewer.md`) is to argue for data minimization and against unnecessary third-party exposure, directly extending the Data Subject stewardship commitments already in Section 3.3 rather than introducing a new concern.

**AI Legal & Compliance Reviewer (OpenAI `gpt-5.2`, via Azure AI Foundry/Sweden Central — external, newly formalized 2026-08-05; domain pull: legal and compliance risk).** Argues legal/compliance risk — impersonation/misinformation risk classification, data-subject rights, and whether this project's compliance-relevant decisions are actually logged and defensible rather than merely asserted. Its real track record to draw on: ADR-0018 and ADR-0031 §5 have each already named, and each already declined to fully design, the same GDPR-adjacent tenant-offboarding/right-to-erasure gap; ADR-0039 (Proposed) is this project's first real attempt at closing it, and its own Open Questions section names a "formal legal review... out of this ADR's own competence" as a required follow-up this role exists to weigh in on. Distinct from the already-registered Legal Advisor persona (Section 1, above, folded into S-21): that is a hypothetical *future, tenant-facing* capability with no subsystem home; this role is an *active, present-day* reviewer of this project's own decisions, invoked now, sharing vocabulary with that persona's concerns without standing in for it. Its charter (`docs/ai-roles/legal-compliance-reviewer.md`) documents real, primary-source provider research — `gpt-5.2` via Azure AI Foundry, Sweden Central, a real provisioned resource as of 2026-08-05 (credentials in `docs/ai-roles/.env`, gitignored, never in a committed file) — and explicitly declines Mistral (current capacity exhaustion, not a permanent disqualification — resets 2026-09-01), Gemini/Ollama (already assigned elsewhere), and Claude (already the substrate for every internal Governed role, so it wouldn't add real epistemic diversity here).

**AI Data Sovereignty & Privacy Regulation Reviewer (Ollama, self-hosted/local, `qwen3.5:9b` — external, newly formalized 2026-08-05; domain pull: jurisdictional privacy regulation vs. always-encrypt/always-geo-redundant vs. data-residency).** Deliberately separate from S-16 immediately above — that role stays narrowly scoped to data minimization; this role's domain, in Menno's own words, is that privacy/data-protection regulation varies by jurisdiction globally and isn't determinable by a single node locally, that data should always be encrypted regardless of jurisdiction, and that data should always be geo-redundant for resilience — a combination that directly conflicts with data-residency/sovereignty rules requiring data to stay within specific borders. Chartered to surface that tension for whatever design it's given, never to resolve it on Menno's behalf, the same non-self-moderating principle every domain-pull role already follows. Its charter (`docs/ai-roles/data-sovereignty-privacy-regulation-reviewer.md`) is grounded in real, verified project facts, not assumed ones — a direct search across this project's 40 ADRs found zero mentions of a specific Azure region, storage-redundancy tier, or data-residency commitment anywhere, and the charter cites Azure's own documented LRS/ZRS/GRS redundancy options and Postgres's default service-managed-vs-customer-managed encryption-at-rest split by way of concrete, primary-sourced Microsoft Learn material rather than assumed defaults. **Provider assignment documents a real, in-session reversal, not a clean first choice:** first drafted against Azure AI Foundry, then reassigned to Ollama once a materially larger local model (`qwen3.5:9b`) became available on Menno's machine, on the same self-application argument S-16's own charter already makes about itself ("applied to itself first") — a sovereignty reviewer should not itself depend on a third-party cloud call. The charter names the honest capability cost of that reassignment (a 9B local model's narrower knowledge breadth vs. a frontier model's) rather than treating the switch as costless, and names Azure AI Foundry as the concrete fallback if real reviews prove too shallow.

**"Platform operator" and the engineering-hat labels — added 2026-08-05, resolving both back to the Sole Operator persona above.** "Platform operator," used eight times across Epics 2–5 (Stories 2.2, 2.4, 3.5, 4.4, 5.4, 5.7, 5.10, 5.18), is registered here as an explicit alias for the operational facet of the Sole Operator persona — the "sole ... Operator" component already named in that persona's own definition above — not a new, distinct future actor. Every one of the eight usages describes keeping the deployed platform itself running, secure, and cost-bounded (rate-limit gating, dead-lettering across process instances, storage-cost management, cache-serving load, RLS enforcement, security posture, sign-up abuse prevention): real, present-day responsibility Menno already holds as Sole Operator, unlike Platform Admin (still "brainstormed 2026-07-30, not yet built," with zero code today). One of the eight usages is a genuine outlier, named here rather than smoothed over: Story 5.7's persona line reads "As a platform operator provisioning or suspending a SocialEngage tenant," but that story's own title ("Platform Admin's audited, narrowly-scoped RLS bypass") and its own acceptance criteria (which reference `platform_admin_role` throughout) describe the already-registered Platform Admin persona's own action, not a generic operator concern — it should read "As Platform Admin," matching Story 6.6's own already-correct precedent, not "As a platform operator." This register does not rewrite `docs/user-stories/` (out of this document's own scope), so the inconsistency is named here as a finding for whoever next touches Story 5.7's text, not silently folded into the alias definition above. Separately, seven engineering-hat labels across the story corpus — "core backend engineer" (Story 2.1), "data engineer" (Story 3.1), "operations engineer investigating a data issue" (Story 3.2), "admin UI developer" (Story 1.1, Story 6.1), "subsystem architect scoping this subsystem's boundaries" (Story 4.2), "backend engineer setting up the data layer" (Story 1.2), and "developer running `social-listening-core` locally to see it actually work" (Story 1.4) — are each a facet of the same Sole Operator persona wearing a specific hat for that story's own concern, not distinct actors: "core backend engineer," "data engineer," "backend engineer setting up the data layer," and "developer running `social-listening-core` locally" are all the sole-Developer facet; "operations engineer investigating a data issue" is the sole-Operator facet; "subsystem architect scoping this subsystem's boundaries" is the Lead Solutions Architect facet; "admin UI developer" is the sole-Developer facet scoped specifically to the `social-listening-admin` repository. Story 6.6's own persona line — "As Platform Admin (in practice, Menno, the Sole Operator persona — `Stakeholder-Register.md`)" — is the existing precedent this paragraph extends to the other eight labels.

**Tenant Brand Reputation Manager (source: `docs/future-subsystems.md`, dated 2026-08-05 addition from Menno's "Master User Story Backlog"; tracked under the parked "Brand Reputation & Alerts" future subsystem — no charter, no ADR series, not started).** A tenant-side role that would care about brand/topic drift detection and alerting once that subsystem exists — this project's own spec §1 exclusion for "alerting/crisis detection... deferred to future subsystems" names exactly this gap. Four other names from the same backlog batch — Drift Sentinel (DS-01), Sentiment Drift Operator (SDO-01), Alerts Subsystem Engineer (ASE-01), and Drift Response Agent (DRA-01, the second, unrelated use of that abbreviation — see `docs/future-subsystems.md`'s own naming-collision note) — are not registered as separate personas here: `docs/future-subsystems.md` itself describes all five collectively, against one shared AC set, and Drift Sentinel/Sentiment Drift Operator/Drift Response Agent read as different verbs in the same detect-track-respond pipeline rather than a different stakeholder's interest; Alerts Subsystem Engineer is an engineering-hat label for whoever would build the event-consumption plumbing — the same pattern just resolved for this subsystem's own labels above, not a new stakeholder. Design-time future-relationship note only: no charter, no ADR series, and no code exists for Brand Reputation & Alerts.

**Tenant Social Care Agent (source: `docs/future-subsystems.md`, dated 2026-08-05 addition from Menno's "Master User Story Backlog"; tracked under the parked "Social Care" future subsystem — no charter, no ADR series, not started, explicitly declined-to-fund CRM-adjacent workflow per `Business-Case-v6.0.md` line 29).** A tenant-side role that would respond to customer questions and complaints surfaced through this subsystem's own ingestion pipeline, once Social Care exists. `docs/future-subsystems.md` already names a real, binding constraint on this persona's eventual design: any "respond to" capability inherits ADR-0028's Tier 3 credential rule — an app-only tenant-wide credential has no account identity to act as, so responding through a given platform would require that specific tenant user's own personally-activated credential, never a shared or Tenant-Admin-provisioned one. Future/parked, not a current relationship.

**Social Selling Strategist (source: `docs/future-subsystems.md`, dated 2026-08-05 addition from Menno's "Master User Story Backlog"; tracked under the parked "Social Selling" future subsystem — no charter, no ADR series, not started).** A tenant-side role that would identify experts and prospects for relationship-building and lead generation off ingested social activity. `docs/future-subsystems.md` notes this persona's core AC ("identify experts") maps closely onto `AuthorTopicSignal` (ADR-0007, already shipped in this subsystem) — evidence this subsystem's own author-signal design generalizes usefully to a downstream use case, not evidence the persona itself is already "covered," since no Social Selling-side consumer of that data exists yet. Future/parked, not a current relationship.

**Topic Center analyst persona (source: `docs/future-subsystems.md`, dated 2026-08-05 additions from Menno's "Master User Story Backlog"; tracked under "Topic Center" — a parked concept whose own subsystem assignment `docs/future-subsystems.md` states is still unresolved, one stage more uncertain than Brand Reputation & Alerts/Social Care/Social Selling, which are at least named MSE-derived subsystems).** A tenant-side research role that would map emerging trends (Trend Cartographer, TC-01), detect topic-level semantic drift (Topic Drift Analyst, TDA-01), and forecast trend trajectories (Trend Forecaster, TF-01) — three names folded into one entry because `docs/future-subsystems.md`'s own text describes all three as extending the same subject-scoped research capability, not three distinct stakeholder interests. Two further names from the same backlog batch — IP Discovery Specialist (IPD-01) and Novelty Researcher (NR-01) — are explicitly not registered as separate personas: `docs/future-subsystems.md` itself states these are "not new parked concepts," describing the same novelty/IP-research gap Topic Center's own definition already names, arrived at independently rather than naming anything new. One real conflict this persona's own acceptance criteria would run into, named in `docs/future-subsystems.md` and repeated here rather than left implicit: a "topic evolution graph" is exactly the charting UI ADR-0008 forbids this subsystem from building. Future/parked, not a current relationship, with the added caveat that even its future subsystem home is undecided.

**Legal Advisor (source: `docs/future-subsystems.md`, dated 2026-08-05 addition from Menno's "Master User Story Backlog"; subsystem home explicitly unresolved — `docs/future-subsystems.md` states it "does not fit Brand Reputation & Alerts, Social Care, Social Selling, or Topic Center").** An external-advisory role that would assess legal risk in drift-flagged content (e.g. impersonation, misinformation) — categorically different from every other persona added by this addendum: not a tenant usage pattern, not an engineering hat resolvable to the Sole Operator persona, and not yet assignable to any named future subsystem. Registered here despite the unresolved subsystem home because the bar this register applies elsewhere is "a role-based actor with a real relationship to this project," not "an actor with a settled subsystem home" — Platform Admin met that same bar while still "floated, not locked." This persona also depends on the same unbuilt, ADR-0008-bounded drift mechanism the Tenant Brand Reputation Manager entry above depends on, so it inherits that gap even once/if a subsystem home is found for it. Future/parked, not a current relationship.

---

## 2. Stakeholder Register — Power/Interest Quadrant Table

| ID | Stakeholder / Persona | Category | Organization | Power | Interest | Engagement Quadrant |
|---|---|---|---|---|---|---|
| S-01 | Menno (Sponsor, Product Owner, Sole Developer — see Sole Operator persona) | Internal | This project (self-funded, solo) | High | High | **Manage Closely** |
| S-02 | Future end users / tenants (see Tenant User persona) | External (future) | Not yet provisioned | Low (currently) | Low (currently, latent) | **Monitor** — trending toward Manage Closely post-Phase-1 activation |
| S-03 | Social platform providers (X, Reddit, YouTube, LinkedIn, Meta, RSS/Newswire sources — GlobeNewswire, PR Newswire) | External | Each platform's own organization | High — can revoke access or reprice unilaterally | Low — indifferent to this project specifically | **Keep Satisfied** |
| S-04 | Microsoft Azure | External / Vendor | Microsoft | Medium | Low | **Monitor**, treated operationally as a lightweight Keep Satisfied given infrastructure criticality |
| S-05 | AI enrichment providers (Azure AI Language now; OpenAI/Claude potential future) | External / Vendor | Microsoft (current); others (future) | Medium | Low | **Monitor** — the `AIProviderConnector` abstraction (ADR-0002) already isolates this dependency |
| S-06 | Data subjects — authors of ingested public posts | External / Indirect | Not applicable (individuals, not an organization) | Low (no direct relationship or leverage) | High, on ethical grounds | **Keep Informed** — engaged proactively by design commitment, not because of formal power |
| S-07 | Future collaborators / team members | Internal (future, TBD) | Not yet applicable | Low (currently) | Low (currently) | **Monitor** |
| S-08 | Platform Admin, Tenant-Admin, Tenant Reader, Tenant Business Analyst personas (not yet instantiated) | Internal (future) | Not applicable — no code, table, or authenticated identity exists yet | Not ratable — no live actor occupies these roles | Not ratable | **Not yet applicable** — see Section 1; candidate ADRs #1–#4 must exist first |
| S-09 | AI Delivery Agent — domain pull: correct, contract-verified delivery (Claude Code) | Internal / Tool-Agent | Not an organization; underlying model service provided by Anthropic | High (execution share — writes essentially every commit) but architecturally bounded (no unilateral decision authority; see Section 1) | N/A — no independent stake; the closest proxy is the declared scope of the current session's task | **Governed** — see Section 4 |
| S-10 | AI Security & Architecture Reviewer — domain pull: trust boundaries and risk (Gemini, primary; Copilot historically) | External / Advisory | Underlying services provided by Google (Gemini) and GitHub/Microsoft (Copilot) | Low — advisory only; findings triaged by Menno, some explicitly deferred (`docs/implementation-methodology.md`) | N/A — episodic per review round | **Keep Informed**, consulted periodically |
| S-11 | AI Business & Requirements Analyst — domain pull: business value and requirements traceability (Claude Code) | Internal / Tool-Agent | Not an organization; underlying model service provided by Anthropic | High within its scope (authors this document series) but no unilateral acceptance authority — Menno accepts every ADR/Business Case | N/A — no independent stake | **Governed** — see Section 4 |
| S-12 | AI QA / Contract Author — domain pull: correctness and edge-case rigor (Claude Code) | Internal / Tool-Agent | Not an organization; underlying model service provided by Anthropic | Not yet exercised separately from S-09 — newly formalized 2026-07-31 | N/A | **Governed** — see Section 4 |
| S-13 | AI Documentation Steward — domain pull: traceability and consistency (Claude Code; scope extended 2026-08-06 to also cover the PM-side project docs — this register, `Business-Case-v6.0.md`, `Project-Charter.md`, the nine Project Management Plans — and gained `Edit`/`Write` tools to propose corrections directly in the working tree, never committing them itself) | Internal / Tool-Agent | Not an organization; underlying model service provided by Anthropic | Not yet exercised separately from S-09 — newly formalized 2026-07-31 | N/A | **Governed** — see Section 4 |
| S-14 | AI Product & Market-Fit Reviewer — domain pull: end-user/tenant value (reassigned OpenAI → Mistral, 2026-07-31: OpenAI API wasn't available on Menno's free-tier account; real automation via `docs/ai-roles/scripts/invoke-mistral-agent.cjs --role product-market`, a second, separately-configured Mistral agent from S-15's; connectivity-tested 2026-07-31, not yet a real review) | External / Advisory | Underlying service provided by Mistral AI | Low — advisory only, findings would be triaged by Menno | N/A — episodic, per review round | **Keep Informed** |
| S-15 | AI Engineering Pragmatism Reviewer — domain pull: simplicity, anti-overengineering (Mistral; real API automation via `docs/ai-roles/scripts/invoke-mistral-agent.cjs`, first exercised 2026-07-31 — a connectivity/persona check, not yet a real design review) | External / Advisory | Underlying service provided by Mistral AI | Low — advisory only, findings would be triaged by Menno | N/A — episodic, per review round | **Keep Informed** |
| S-16 | AI Data Privacy & Sovereignty Reviewer — domain pull: data minimization, no unnecessary third-party exposure (Ollama, self-hosted/local; real local-only API automation via `docs/ai-roles/scripts/invoke-ollama-agent.cjs`, connectivity-tested 2026-07-31 on `qwen2.5:0.5b`, not yet a substantive review — see charter for why) | External / Advisory | Not an organization — self-hosted on Menno's own hardware | Low — advisory only, findings would be triaged by Menno | N/A — episodic, per review round | **Keep Informed** |
| S-17 | Tenant-Admin Persona-Proxy — domain pull: onboarding/setup friction (Claude Code, synthetic — **not** a real Tenant-Admin) | Internal / Tool-Agent — Synthetic Stakeholder Proxy | Not an organization; roleplays a persona that does not yet exist | Not ratable — represents a persona with no real power/interest of its own yet | Not ratable | **Governed** — see Section 4; never cite as real validation |
| S-18 | Tenant Reader Persona-Proxy — domain pull: legibility and trust of shown data (Claude Code, synthetic — **not** a real Tenant Reader) | Internal / Tool-Agent — Synthetic Stakeholder Proxy | Not an organization; roleplays a persona that does not yet exist | Not ratable | Not ratable | **Governed** — see Section 4; never cite as real validation |
| S-19 | Tenant Business Analyst Persona-Proxy — domain pull: data completeness and export/query flexibility (Claude Code, synthetic — **not** a real Tenant Business Analyst) | Internal / Tool-Agent — Synthetic Stakeholder Proxy | Not an organization; roleplays a persona that does not yet exist | Not ratable | Not ratable | **Governed** — see Section 4; never cite as real validation |
| S-20 | AI Manager — Ideal Manager framework (fourteen sections, synthesizes rather than argues one lens); charter provided by Menno (Claude Code) | Internal / Tool-Agent | Not an organization; underlying model service provided by Anthropic | High within its advisory scope (checks any decision against all fourteen sections) but no authority to actually commit budget/scope — Menno decides | N/A — no independent stake | **Governed** — see Section 4; verdict is advisory, never a substitute for Menno's own Accountable decision |
| S-21 | Tenant Brand Reputation Manager, Legal Advisor, Tenant Social Care Agent, Social Selling Strategist, Topic Center analyst persona — future-subsystem personas added 2026-08-05, source `docs/future-subsystems.md`, from Menno's "Master User Story Backlog" | External (future) | Not applicable — no charter, no ADR series, not started for Brand Reputation & Alerts, Social Care, Social Selling, or Topic Center | Not ratable — no live actor occupies any of these roles | Not ratable | **Not yet applicable** — see Section 1; one stage earlier than S-08, since none of the four governing subsystems has even a dated governance brainstorm or candidate-ADR placeholder the way Platform Admin/Tenant-Admin do |
| S-22 | AI Legal & Compliance Reviewer — domain pull: legal/compliance risk (OpenAI `gpt-5.2`, via Azure AI Foundry/Sweden Central; added 2026-08-05) | External / Advisory | Underlying service provided by OpenAI, hosted via Microsoft Azure AI Foundry | Low — advisory only, findings triaged by Menno | N/A — standing-checkpoint cadence (see §3.4/§4), not purely episodic | **Keep Informed** |
| S-23 | AI Data Sovereignty & Privacy Regulation Reviewer — domain pull: jurisdictional privacy regulation vs. encryption/geo-redundancy vs. residency (Ollama, self-hosted/local, `qwen3.5:9b`; added 2026-08-05) | External / Advisory | Not an organization — self-hosted on Menno's own hardware | Low — advisory only, findings triaged by Menno | N/A — standing-checkpoint cadence (see §3.4/§4), not purely episodic | **Keep Informed** |
| S-24 | AI Knowledge-Graph & Semantic Data Modeling Reviewer — domain pull: entity/relationship modeling correctness (Claude Code; added 2026-08-05) | Internal / Tool-Agent | Not an organization; underlying model service provided by Anthropic | High within its scope (reviews design proposals) but no unilateral acceptance authority — Menno accepts every ADR | N/A — no independent stake | **Governed** — see Section 4 |
| S-25 | AI Learning & Development Writer — domain pull: document only shipped, contract-verified reality in the three end-user manuals (`docs/manuals/system-admin-manual.md`, `tenant-admin-manual.md`, `user-manual.md`), never the roadmap (Claude Code; added 2026-08-06) — **added by this Documentation Steward pass, 2026-08-06, catching up a real registration gap: the role's charter (`.claude/agents/learning-development-writer.md`) was committed with `Edit`/`Write` access the same day but had no register entry until now** | Internal / Tool-Agent | Not an organization; underlying model service provided by Anthropic | High within its scope (writes/edits the three manuals directly) but no unilateral authority — never commits its own changes, a human commit is the approval gate | N/A — no independent stake | **Governed** — see Section 4 |

---

## 3. Detailed Engagement Narratives

### 3.1 Executive / Sponsor Tier — Menno

Menno holds every executive-tier authority this project has: Sponsor, Product Owner, and Lead Solutions Architect, combined in one person because no organization exists to distribute them across. He approves every architectural decision by accepting the ADR that records it, funds every cloud and platform-API cost personally, and sets project direction through the Business Case and Charter he authored. Per PMBOK's Stakeholders Performance Domain, an executive stakeholder with High Power and High Interest calls for a Manage Closely engagement strategy — here, that strategy is self-directed rather than delegated: Menno reviews progress at each of the Charter's seven milestones (M1–M7), and every completed story or healing pass appends a dated, commit-hash-verified entry to `docs/implementation-log.md`, functioning as this project's own executive status report to itself. This is not a workaround for a missing communications channel — it is the actual channel, and it is auditable in a way a verbal executive briefing would not be, since `docs/templates/check-implementation-log.cjs` independently re-derives each entry's file list from git rather than trusting the entry's own prose. The one meaningful executive-tier risk this concentration creates is the bus-factor risk already logged as Risk R-03 in `Ideation-Document-v7.2.md`: because Menno is simultaneously proposer and approver of every decision, there is no second executive signature to fall back on if his availability changes, which is precisely why the project's documentation (ADRs, `SKILL.md` files, the Implementation Log) is written to be a self-sufficient onboarding artifact for a hypothetical future collaborator, not just a historical record for its one current author.

### 3.2 Project Team — Menno

The project team is the same single person as the Sponsor tier above, now acting in the Lead Solutions Architect, sole Developer, sole Tester, and sole Operator capacities. Collaboration method is necessarily internal rather than interpersonal: `docs/implementation-methodology.md` exists specifically because context resets between working sessions and no team memory exists to fall back on, so the actual "collaboration" this project practices is between one session and a future one, mediated entirely through committed artifacts. Every story follows a fixed loop — scope the smallest touched file set, write a Jest contract before any implementation code, write or update a component `SKILL.md`, implement the minimal change, then run the full accumulated contract suite, not just the new test — enforced by three real mechanical layers: a `PreToolUse` hook that blocks writes to `src/` without an existing contract, a pre-commit hook, and CI with branch protection. Responsibilities split by activity rather than by person: architecture decisions become ADRs, delivery scope becomes user stories, and completed work becomes Implementation Log entries, each independently checkable against git rather than resting on the team's own say-so. This RACI-degenerate structure — one person holding Responsible and Accountable on every row — is documented honestly elsewhere in this project's corpus (`Ideation-Document-v7.2.md` §2.3) rather than obscured, and the team-composition entry here is consistent with that treatment: a real fact about project scale, not a gap to paper over.

### 3.3 Operations / End-User Tier

**Future end users / tenants.** No individual or organization currently occupies this stakeholder — the Tenant User persona (Section 1) describes a role, not a current relationship. Their eventual interest is reliable ingestion, accurate sentiment, and trustworthy data handling once a real tenant is provisioned, but engagement stays at Monitor today because Business Case §8's own multi-tenant isolation criterion has been contract-verified, not yet proven with two live tenants' real data. The communication plan is deliberately deferred rather than improvised: the Stakeholder Register's original commitment — "revisit once Phase 1 is validated; gather feedback before Phase 2 scope decisions" — still governs, and no interview program or advisory channel has been established ahead of that gate.

**Correction, 2026-08-06:** the previous sentence here attributed this stakeholder's Monitor status to "no authentication mechanism exists to safely onboard an external tenant at all (Risk R-04)" — stale, since Entra authentication is now built and R-04 is substantially resolved (Phase 4.5 complete). **The real, current blocker is a deliberate governance gate, not a missing technical capability:** `docs/project docs/Project Management Plans/Go-Live-Readiness-Definition.md` formally classifies this project at Stage 0 (ephemeral test infrastructure only) — no real end user, including any pilot, may be onboarded until that document records a later stage. Any individually-admitted pilot user (that document's own §4) would move a specific person off Monitor before general Go-Live does.

**Social platform providers.** X, Reddit, YouTube, LinkedIn, Meta, and the two live newswire sources (GlobeNewswire, PR Newswire) each hold effectively unilateral power over this project's technical and financial viability — any of them can change API terms, pricing, or access without negotiation, which is exactly the risk logged as R-01 in this project's risk registers. Their interest in this project specifically is negligible; engagement is therefore Keep Satisfied, operationalized as strict compliance rather than relationship-building: every connector is built against that platform's currently published API terms, not assumptions, and is designed to degrade gracefully (auto-disable via `ConnectorHealth`'s proportional failure rule, ADR-0023) rather than fail destructively if access changes mid-operation. The Newswire connector (ADR-0024) is this strategy's first proof in production code — its `authMode: 'none'` design and dependency-free RSS parser were shaped directly against captured real feed samples before any code existed, precisely to avoid assuming more stability from the provider than its published format actually guarantees.

**Data subjects.** Authors of the public posts this platform ingests hold no formal power over this project — no contract, no account relationship, no ability to negotiate — but their interest is high on ethical grounds, which PMBOK's stakeholder-engagement principle treats as a reason to engage proactively regardless of formal leverage, not a reason to deprioritize. Engagement here means design commitment rather than direct communication, since no direct channel to this stakeholder group exists or is appropriate: retain only what the platform's stated listening/insights use actually needs, respect each source platform's own terms on permitted data use, avoid resolving free-text profile locations into precise geodata (an explicit, deliberate scope exclusion, not an oversight), and never repurpose ingested data beyond the stated use. These commitments are enforced today by the Sole Operator persona's own design discipline, not by a chartered privacy function — a gap named honestly in Section 1 rather than closed with an invented Data Privacy Officer.

### 3.4 AI Delivery & Review Agents

The AI Delivery Agent is, by commit volume, the closest thing this project has to a second team member — but describing its engagement strategy in the same terms as a human stakeholder would overstate what it actually is. It has no continuity between sessions: everything it "knows" about this project on any given day comes from what is currently written down, which is exactly why `docs/implementation-methodology.md` opens by explaining that the entire ADR/`SKILL.md`/Implementation Log apparatus exists because "context resets between sessions and this is a solo-developer project with no team memory to fall back on." Its engagement is therefore governed, not managed: three enforcement layers (a real-time `PreToolUse` hook, a pre-commit hook, and CI with branch protection) constrain what it can write without a pre-existing contract; a hard rule bars it from weakening, skipping, or bypassing any check to reach a passing state; and a bounded-retry cap (three full remediation attempts per failure) forces escalation to Menno rather than open-ended, unsupervised iteration. Story 2.5 is the clearest real evidence of where its authority actually stops: before touching any already-shipped contract, it traced the exact blast radius of the proposed change and paused for Menno's explicit sign-off, rather than proceeding on the strength of its own confidence that the change was safe. The AI Review Agent role is structurally different — episodic rather than continuous, advisory rather than executing. Two rounds have occurred to date: a Copilot review that directly produced ADR-0017, ADR-0018, and ADR-0020 through ADR-0023, and a Gemini review (2026-07-30) of the enforcement tooling itself, whose three findings were judged real but not urgent and were logged as deliberately deferred rather than immediately patched — the same "wait for the problem to actually occur" discipline this project already applies to ADR-0020's distributed rate-limit gate. Neither AI role is described here as having interests of its own; both are described in terms of what they actually do, and — just as importantly — what this project's own tooling deliberately stops them from doing unsupervised.

The 2026-07-31 second addendum (Section 1) extends this from two roles to eight, on a deliberately different design principle: rather than one AI voice trying to weigh every concern at once and hand Menno an already-balanced answer, each of the new roles is chartered to argue hard for exactly one domain — the Business Analyst for requirements traceability, the QA/Contract Author for correctness, the Documentation Steward for zero drift, the Security Reviewer for hardening, the Product & Market-Fit Reviewer for what a real tenant would notice, the Engineering Pragmatism Reviewer for not building what a solo project does not yet need, and the Data Privacy Reviewer for keeping sensitive content off third-party infrastructure. A single balanced-sounding recommendation would quietly perform Menno's own synthesis work and hide the trade-off from him; eight sharply-angled ones make the trade-off visible instead. This is not a hypothetical pattern — Story 2.5's Implementation Log entry is the real precedent: a rate-relative failure threshold with a traced, non-trivial blast radius on already-shipped contracts was surfaced explicitly, with the tension named, and Menno made the call rather than the agent quietly resolving it. Six of the eight roles (Business Analyst, QA/Contract Author, Documentation Steward, Product & Market-Fit Reviewer, Engineering Pragmatism Reviewer, Data Privacy Reviewer) are newly formalized by this addendum and have no independent track record yet, distinct from the Delivery Agent and the Security Reviewer, both of which do.

**2026-08-05 addendum to this section.** Two changes, both appended here rather than edited into the paragraph above, per this register's own convention. **First:** three more domain-pull roles join the eight above — Legal & Compliance Reviewer and Data Sovereignty & Privacy Regulation Reviewer (both external, S-22/S-23), and Knowledge-Graph & Semantic Data Modeling Reviewer (Claude-Code-internal, Governed, S-24). Full reasoning for each, including real provider research and one documented in-session provider reversal, is in Section 1 above (the seventh addendum) and each role's own charter — not restated here. **Second, and a real governance change applying to all eleven domain-pull roles now, not just these three:** engagement moves from purely ad hoc invocation (Menno's own judgment call, each time, with no standing trigger) to a **standing checkpoint cadence** — consult the relevant domain-pull reviewer(s) before every architecturally significant ADR is accepted, and at each Phase milestone in `docs/implementation-plan.md`'s own phase structure, matching the reviewer(s) to the decision's actual subject matter rather than running all eleven through every checkpoint. This does not change the fact that a human (Menno) must still actually invoke each one — see `docs/ai-roles/README.md`'s own "When to invoke" section for the exact trigger wording this register's own framing now matches, and Section 4 below for how this changes the "Keep Informed"/"Governed" descriptions specifically.

### 3.5 Persona-Proxy Agents (Tenant-Admin, Tenant Reader, Tenant Business Analyst)

These three (S-17–S-19) exist to answer a specific, narrow question during design: given a proposed ADR, user story, or design decision, what would a Tenant-Admin, a Tenant Reader, or a Tenant Business Analyst most likely object to or ask for, reasoning from this project's own documented intent? They are invoked the same way the QA/Contract Author role is — as a distinct Claude Code subagent, brought in deliberately rather than folded into the same pass that drafted the decision — and each pulls hard on exactly one persona's likely concern rather than trying to represent all three at once: Tenant-Admin on setup and configuration friction, Tenant Reader on whether shown data is legible and trustworthy, Tenant Business Analyst on whether the data can actually be queried, exported, and correlated for a real analytical question.

**The limit on what this is worth is the most important fact about this role, not a footnote to it.** This project's own Stakeholder Validation Protocol (`Business-Case-v6.0.md` §7) already drew a hard line on this exact question — it explicitly refused to assert a fabricated interview count ("15 end-user discovery interviews") because none had happened, and stated plainly that the only real validation to date was architectural review, not user-facing. A persona-proxy agent's output does not change that fact. It is a design-time hypothesis generated by an AI reasoning about a role from this project's own documents, not a person with actual context, incentives, or lived friction with the product — it can miss a real concern entirely, or confidently invent one a real Tenant-Admin would never actually have. Its use is to widen design-time coverage before real users exist, not to close the Stakeholder Validation Protocol's still-open gap. Each persona-proxy agent's own charter (`.claude/agents/persona-*.md`) restates this limit directly, so a future reader encountering its output in isolation — outside this register, outside this narrative — still sees the caveat attached.

### 3.6 AI Manager

Every other AI role added in this register argues one thing hard. The AI Manager (S-20) is the exception, by design: given a workload, scope, or delegation situation, it checks all fourteen sections of the Ideal Manager framework — Capacity & Workload, Scope & Expectations, Growth & Development, Protection & Boundaries, Communication & Feedback, Decision Rights & Autonomy, Recognition & Reward, Outcome Stewardship, Alignment & Conflict, Team-Level Optimization, Performance Accountability, Strategic Direction, Learning from Failure, Organizational Influence — and returns either advisory guidance (Advisor mode) or a scored verdict against a stated priority order (Decision Evaluator mode: proceed / proceed with adjustment / escalate / don't proceed). Grounded for this project specifically in `.claude/agents/ideal-manager.md`, since the framework as written assumes multiple human reports and SocialEngage has one: sections about capacity, scope, and organizational pressure apply to Menno directly, in both directions (protecting developer-Menno from sponsor-Menno's own ambitions, and holding sponsor-Menno to real commitments rather than letting protection become an excuse never to ship); sections about team optimization and performance apply narrowly to whether the other 11 AI roles' division of labor stays clean, never to those agents as if they had wellbeing or careers to manage. Two real, already-logged gaps in this project's own documents are exactly what this role exists to close: Charter Assumption A-01's unquantified "sufficient available time," and Business Case §5's still-outstanding "no formal budget ceiling has been set." Its verdicts carry no more authority than any other role's findings — Menno remains Accountable, per every RACI row in this register.

---

## 4. Engagement Planning — Power/Interest Quadrant Analysis

This project's stakeholder population is unusually concentrated: among the human/organizational entries (S-01 through S-08), one (Menno) sits in Manage Closely, three sit in Monitor, one sits in Keep Satisfied, one sits in Keep Informed, and two are not yet ratable because no live actor occupies them. The remaining, larger share of this register's rows (S-09 onward) are AI roles, sorted into the two additional categories — Governed and Keep Informed — this section defines below specifically because they don't fit any of PMBOK's four classical quadrants. **Correction, 2026-08-05:** this paragraph previously read "of eight register entries" — stale since at least the 2026-07-31 second addendum added eight more rows on its own, and inaccurate again by an even wider margin today (S-01 through S-24 as of this revision); fixed here rather than left to compound further, and deliberately *not* replaced with a new fixed count that would just as predictably go stale as this register keeps growing. That concentration among the human/organizational stakeholders specifically is itself the central engagement-planning fact worth stating explicitly, rather than forcing a conventional-looking spread across all four quadrants where that population does not actually support one.

**Manage Closely (High Power, High Interest) — Menno only.** The tailored action here is not stakeholder management in the usual sense of aligning a powerful party who might otherwise disengage; it is disciplined self-governance, operationalized through the Charter's seven dependency-ordered milestones and the git-hash-verified Implementation Log described in Section 3.1. The real risk this quadrant carries is not disengagement but a single point of failure (Risk R-03) — the mitigating action is keeping every artifact (ADRs, `SKILL.md` files, dated Amendment Log entries) legible enough that a future collaborator could pick up the Manage Closely role without Menno present to explain it.

**Keep Satisfied (High Power, Low Interest) — social platform providers.** The tailored action is compliance-by-construction rather than relationship cultivation, since these stakeholders have no reason to engage with this project specifically: build every connector strictly against currently published API terms, monitor for policy or pricing changes before they cause a failure rather than after, and design for graceful degradation (auto-disable, not silent breakage) so that a provider's unilateral change becomes a contained, auditable event rather than a cascading one. Microsoft Azure and the AI enrichment providers sit adjacent to this quadrant at a lower intensity — rated Monitor rather than Keep Satisfied outright because the connector and `AIProviderConnector` abstractions already insulate this project from needing to actively manage either relationship, but real enough dependencies (cost, service health) that pure inattention would be a mistake.

**Keep Informed (Low Power, High Interest) — data subjects.** The tailored action is procedural rather than communicative: no direct channel to this stakeholder group exists, so "keeping informed" in the literal PMBOK sense is not achievable, and the substitute is holding the project's own data-handling commitments (Section 3.3) to the same standard a genuinely informed, engaged stakeholder would demand — retention limits, no geodata resolution, no repurposing — enforced whether or not any data subject ever actually checks.

**Monitor (Low Power, Low Interest, currently) — future end users/tenants and future collaborators.** The tailored action for both is explicitly deferred engagement with a named trigger condition, not neglect: future end users/tenants get revisited at the Phase 1 review gate (Milestone M7) once Business Case §8's success criteria are independently re-verified; future collaborators get engaged if and when the bus-factor risk (R-03) or a deliberate scaling decision actually produces one, at which point the onboarding-grade documentation this project already maintains becomes the engagement mechanism itself.

**Governed (a fifth category, added for the internally-invoked AI roles: Delivery Agent, Business Analyst, QA/Contract Author, Documentation Steward, the Knowledge-Graph & Semantic Data Modeling Reviewer, the Learning & Development Writer — added 2026-08-06, S-25 — the three persona-proxy agents, and the AI Manager) — not a fit for any of the four classical quadrants.** The AI Manager (S-20) needs its own distinction within Governed, the mirror image of the persona-proxy agents': where they are bounded to producing clearly-labeled synthetic output, the Manager is the one role bounded to *synthesizing* rather than arguing — it is deliberately exempted from the "pull hard on one domain, don't self-moderate" rule the other Governed and Keep-Informed roles follow, because synthesis across competing pulls is its entire job. That does not make its verdicts more authoritative than the domain-pull roles' findings; it makes them a different kind of input, still advisory, still subject to Menno's own Accountable decision. The three persona-proxy agents (S-17–S-19) need one further distinction beyond the other Governed roles: they are bounded the same way (Claude Code, no unilateral authority, no persistent memory), but unlike the Delivery Agent or the Documentation Steward, their entire output is itself a synthetic stand-in for a stakeholder that does not exist — so the governance question for them is not just "what can it write" but "how must its output be labeled so it can never be mistaken for the real thing it's proxying." That label — restated in the register entry, this narrative, and the agent's own charter — is the actual control, the same way a `PreToolUse` hook is the actual control on the Delivery Agent's write access. PMBOK's Power/Interest grid assumes every stakeholder has its own interests, which the project either aligns with, satisfies, informs, or simply tracks. That assumption breaks down for an AI agent: it has no independent stake in this project's outcome, so "Interest" cannot be meaningfully rated, and its "Power" is not innate the way a platform provider's or a sponsor's is — it is delegated, and delegated specifically at a scope Menno's own tooling defines and can narrow at any time. The tailored action is a governance strategy rather than an engagement strategy: bound write access to files with a pre-existing contract (the `PreToolUse` hook), forbid resolving a failure by weakening a check, force escalation after a bounded number of attempts, and — for the five roles chartered under the "pull hard on one domain" principle (the 2026-07-31 second addendum's original four — Delivery Agent, Business Analyst, QA/Contract Author, Documentation Steward — plus the 2026-08-05 seventh addendum's Knowledge-Graph & Semantic Data Modeling Reviewer) — explicitly instruct each one *not* to self-moderate toward a consensus position, since that would quietly perform the adjudication step that belongs to Menno alone.

**Keep Informed, applied to the six externally-invoked domain reviewers (Security/Gemini, Product & Market-Fit/Mistral, Engineering Pragmatism/Mistral, Data Privacy/Ollama, and — added 2026-08-05 — Legal & Compliance/OpenAI-via-Azure-AI-Foundry, Data Sovereignty & Privacy Regulation/Ollama).** These sit in Keep Informed rather than Governed because Menno's tooling cannot bound them the way a `PreToolUse` hook bounds Claude Code — they are invoked manually, outside this repository's enforcement layers, using the reusable charters in `docs/ai-roles/`. The tailored action is triage, not integration: each one's findings are input to a decision Menno makes, following the exact pattern the Gemini review already set (three real findings, logged as deliberately deferred rather than auto-applied). Six sharply-angled reviewers will not converge on their own — the Security Reviewer arguing for immediate hardening and the Engineering Pragmatism Reviewer arguing against speculative build are expected to disagree, sometimes directly, and that disagreement being visible and specific is the entire point of chartering them as domain advocates instead of one generically "helpful" reviewer. **Cadence, updated 2026-08-05 — this applies to all eleven domain-pull roles (both Keep Informed and Governed), not just these six:** engagement is no longer purely episodic/ad hoc. A standing checkpoint now triggers consultation of the relevant domain-pull reviewer(s) — matched to the decision's actual subject matter, not all eleven every time — **before every architecturally significant ADR is accepted, and at each Phase milestone** in `docs/implementation-plan.md`'s own phase structure. The triage action itself (Menno decides what to do with a finding) is unchanged; what changes is that reaching this triage step no longer depends solely on Menno remembering to ask. See `docs/ai-roles/README.md`'s own "When to invoke" section for the same wording, kept consistent across both documents.

---

## 5. Approval

This project has no multi-party approval chain — Menno is simultaneously the only Sponsor and the only party capable of approving this register, consistent with the Project Charter's own §10: "As a solo project, formal sign-off is a self-commitment to proceed rather than a multi-party approval." No signature block naming anyone else is included, since no one else exists to sign it.

**Approved by:** Menno, Sponsor & Product Owner
**Date:** 31 July 2026
