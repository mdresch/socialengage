---
name: connector-health-and-error-handling
description: Retryable/non-retryable error classification, the ingestion-attempt orchestrator, and ConnectorHealth derivation for social-listening-core. Read this before adding a new error kind, before wiring a real connector's HTTP calls, or before touching how health/auto-disable is computed.
---

# Connector health and error handling

## What this is

The judgment layer sitting on top of `IngestionRun` history: `src/ingestion/errorClassification.ts` classifies a failure as retryable or not; `src/ingestion/runIngestionAttempt.ts` is the generic orchestrator every real connector's poll/webhook handler will call through — it opens an `IngestionRun`, retries retryable errors with backoff, attempts an OAuth refresh once before surfacing a credential failure, and closes the run either way; `src/connectors/connectorHealth.ts` derives `ConnectorHealth` (and drives auto-disable) purely by querying that same run history. Two ADRs, two stories, one shared derivation — see ADR-0009/ADR-0010's own text and this project's epic docs, which flag this exact coupling directly.

## Governing ADRs and Stories

| ADR | Decision | Story |
|---|---|---|
| ADR-0010 | Retryable errors get backoff+retry; non-retryable fail immediately; OAuth refreshes once before surfacing a credential failure; auto-disable after the failure threshold, per tenant | 2.3 |
| ADR-0009 | `ConnectorHealth` (`healthy`/`degraded`/`failing`/`disconnected`, `credentialStatus`) is fully derived from `IngestionRun` history at read time — never separately stored mutable state | 4.3 |
|| ADR-0059 Decision §4 | A fifth status, `reconnect_required` — a credential-class (401/403) failure on the single most recent run, checked first in the health derivation (before `disabled`/`failing`/`stalled`). Updated by ADR-0109: it is now also a blocked state in `shouldAttemptIngestion()` and triggers the same `ConnectorIngestionAlertEvent` as `disabled`. Backed by `ingestion_runs.is_credential_failure` (migration `0032`), computed at `runIngestionAttempt()`'s `completeIngestionRun({status:'failed'})` call sites via `isCredentialError()`. | 2.15 |
| ADR-0023 | `failing` was rate-relative (≥50% of ≥5 attempts in the trailing hour) with an absolute 20-consecutive-failure ceiling, superseding ADR-0009's/ADR-0010's flat "≥10/hour" rule. **Superseded in part by ADR-0109 (2026-08-28):** the rate rule and 20 ceiling are replaced by 5 consecutive failed `ingestion_runs`; the `deliveryMode` variation remains deferred. | 2.5 |
| ADR-0024 | `runIngestionAttempt()`'s `attempt` callback gains a `runId` parameter (additive) — the wiring a real connector needs to satisfy `insertSocialPost()`'s required `acquisitionId`; no change to retry/backoff/OAuth-refresh/health-derivation behavior itself | 2.6 |
| ADR-0010/ADR-0023 §Clarification (2026-08-12) | Originally excluded retryable failures from `recentFailures`/`consecutiveFailures`. **Superseded by ADR-0109 (2026-08-28):** every failed `ingestion_runs` row now counts toward the 5-consecutive `failing` threshold. The latest non-retryable run still immediately yields `disabled`; the latest credential-class run still yields `reconnect_required`. | 2.12 |
| ADR-0109/Story 13.1 (2026-08-28) | Supersedes the 2026-08-17 half-open probe: `failing`, `disabled`, and `reconnect_required` are all blocked states in `shouldAttemptIngestion()`. Recovery is via `POST /v1/connectors/:platformId/enable`, which runs a `health_check` poll and resets the consecutive streak. | 13.1 |
| ADR-0060 Decision §4 | `deriveConnectorHealth()` gains a 3rd param, `pageId?: string` — reserved/unused until Story 6.27 (multi-Page Facebook) actually builds it; structurally present now so ADR-0061's own 4th param lands in a stable position | 6.27 (Ready, not yet built) |
| ADR-0061 Decision §2/§3 | `deriveConnectorHealth()` gains a 4th param, `userId?: string`, filtering both the `ingestion_runs` and `platform_credentials` sub-queries to that user's own rows; `shouldAttemptIngestion()`'s own internal call is fixed to actually forward `ownerType`/`userId` through instead of silently dropping them (a pre-existing Story 1.11 bug, found and fixed as part of this story) | 1.15 |
| ADR-0060 Decision §4 | The reserved 3rd `pageId?: string` param actually filters now: omitted, byte-for-byte unchanged; supplied, the `ingestion_runs` query additionally filters to that Page's own rows (`platform_credentials` has no `page_id` column, so `pageId` never filters `credentialStatus`) — Facebook's own per-Page health, `deriveConnectorHealth(tenantId, 'facebook', pageId)` | 6.27 |
| ADR-0070 | `ConnectorHealthStatus` widened to include `'stalled'`. Strict evaluation precedence updated by ADR-0109: `disconnected` -> `reconnect_required` -> `disabled` -> `failing` -> `stalled` -> `degraded` -> `healthy`. Derives `stalled` when active with valid credentials and elapsed time since `lastAttemptAt >= 3 * effectiveCadenceMs` (min 45m) OR silence since `lastSuccessfulFetchAt >= 24h` with prior runs. Lock-safe watchdog stale run reconciliation and Service Bus `ConnectorIngestionAlertEvent` publishing. | 1.16 |
| ADR-0109 | Connector health auto-disable and recovery. Supersedes ADR-0023's rate-relative rule with a 5-consecutive-failure `failing` threshold. Adds `disabled` status for a non-retryable, non-credential latest run. Adds `reconnect_required` and `disabled` as blocked states. Adds `consecutiveSuccesses` and auto-recovery to `healthy` after 3 consecutive successes. Adds `health_check` `trigger_type` and `POST /v1/connectors/:platformId/enable` manual re-enable. Adds `connector_disabled` to `IngestionAlertType`. | 13.1 |

|| ADR-0073 | `errorClassification.ts` gains reply-specific `ErrorKind` values (`missing_permission`, `post_not_found`, `reconnect_required`, `rate_limited`, `reply_not_supported`) without changing existing retryable/credential classification for ingestion errors | 2.26 |

## Contracts that constrain this component

- `contracts/epic-1/story-1.16.ingestion-watchdog-and-stalled-alerts.contract.test.ts` — `idx_ingestion_runs_stale_watchdog` partial index; lock-safe `reconcileStaleIngestionRuns()` timeout reconciliation; strict health derivation precedence and `stalled` derivation; `ConnectorIngestionAlertEvent` construction & publishing; idempotent `POST /v1/connectors/:id/retry` endpoint.
- `contracts/epic-2/story-2.3.error-handling-auto-disable.contract.test.ts` — retryable errors retry with backoff; non-retryable errors fail immediately with no further attempts; a failed OAuth refresh (not a successful one) is what surfaces; `shouldAttemptIngestion()` flips to `false` once the connector-level failure threshold is crossed (Story 2.5 owns the exact rule), with `getAutoDisableReason()` returning the most recent failure's message; a second tenant is provably unaffected.
- `contracts/epic-4/story-4.3.derived-connector-health.contract.test.ts` — no `connector_health` (or similarly named) table exists at all; all four `ConnectorHealth` states derive correctly from a manually inserted `IngestionRun` sequence; `credentialStatus` reads from `platform_credentials.status` directly, independent of run-derived `status`.
- `contracts/epic-4/story-4.3.health-derivation-index.contract.test.ts` (healing pass, 2026-07-29) — `ingestion_runs` has an index covering `(tenant_id, platform_id, started_at)`, the exact columns `deriveConnectorHealth()`'s query uses, confirmed via live `pg_indexes` introspection.
- `contracts/epic-2/story-2.4.bounded-queues-and-dead-lettering.contract.test.ts` — a gate wait abandoned on TTL, reclassified by a synthetic `attempt()` into a `ClassifiableError`, records a non-retryable failed `IngestionRun`; a request that exhausts retries for a genuinely retryable error gets a distinctly-worded "dead-lettered" `errorSummary`, and dead-lettering one request alone does not cross `shouldAttemptIngestion()`'s threshold — the two mechanisms proven independent.
- `contracts/epic-2/story-2.5.proportional-failure-threshold.contract.test.ts` — **superseded by ADR-0109 (2026-08-28):** the rate-relative rule is replaced by a 5-consecutive-failure `failing` threshold. The contract now encodes 5 consecutive failed runs -> `failing`; fewer than 5 -> not `failing`; the threshold is window-independent; and `shouldAttemptIngestion()` is `false` with no half-open probe. It also proves a non-retryable latest run immediately yields `disabled`, taking precedence over `failing`.
- `contracts/epic-2/story-2.6.newswire-connector.contract.test.ts` — exercises `runIngestionAttempt()`'s `attempt(runId)` parameter and the `acquireForProvider()` → `ClassifiableError` reclassification pattern via a real connector for the first time (see `.claude/skills/newswire-connector/SKILL.md`); doesn't itself constrain the retry/health-derivation rules above.
- `contracts/epic-2/story-2.12.retryable-failures-excluded-from-auto-disable.contract.test.ts` — **superseded by ADR-0109 (2026-08-28):** every failed run now counts toward the 5-consecutive `failing` threshold. The contract encodes that 5 retryable failures -> `failing`; the latest non-retryable run -> `disabled`; mixed runs extend the streak; a connector recovers to `healthy` after 3 consecutive successes; and a `NULL` `retryable` value does not immediately disable but still counts toward `failing`.
- `contracts/epic-2/story-2.15.facebook-connector.contract.test.ts` — a credential-class (401/403) failed run derives `ConnectorHealth.status === 'reconnect_required'`, distinct from an ordinary non-credential failed run (still `failing`/`degraded` as before) — see `.claude/skills/facebook-connector/SKILL.md` for the connector-specific half of this story.
- `contracts/epic-1/story-1.15.tier3-poll-scheduling.contract.test.ts` — `deriveConnectorHealth(tenantId, platformId, pageId?, userId?)`'s new 4th param scopes both `ingestion_runs` and `platform_credentials` reads to one user's own rows, so one tenant-scoped connector and one user-scoped connector on the same `(tenantId, platformId)` never blend health/failure counts; `shouldAttemptIngestion(tenantId, platformId, ownerType, userId)` genuinely forwards `userId` into its own `deriveConnectorHealth()` call for `ownerType: 'user'` (previously silently dropped it — a real Story 1.11 bug this story found and fixed) so a `userId`'s own activation/health gate is scoped, not tenant-blended.
- `contracts/epic-2/story-6.27.facebook-multi-page-support.contract.test.ts` — `deriveConnectorHealth(tenantId, 'facebook', pageId)`'s 3rd param, now actually wired: two connected Pages under one user (one with only successful runs, one with 20 consecutive non-retryable failures) return independently correct, different statuses; the 2-arg call (`pageId` omitted) is byte-for-byte unchanged.
- `contracts/epic-13/story-13.1.connector-health-auto-disable-and-recovery.contract.test.ts` — the new ADR-0109 contract: 5 consecutive failures -> `failing`; a non-retryable or credential latest run -> `disabled`/`reconnect_required`; `degraded` auto-recovers to `healthy` after 3 consecutive successes; `shouldAttemptIngestion()` is `false` for blocked states; `POST /v1/connectors/:platformId/enable` re-enables a blocked connector with a `health_check` poll; `connector_disabled` and `reconnect_required` alerts are emitted on the failing->blocked transition; and the `health_check` `trigger_type` resets consecutive streaks.

- `contracts/epic-2/story-2.26.connector-reply-framework.contract.test.ts` (ADR-0073) — `errorClassification.ts` supports reply-specific `ErrorKind` values (`missing_permission`, `post_not_found`, `reconnect_required`, `rate_limited`, `reply_not_supported`) without changing ingestion classification; `ClassifiableError` thrown from `SocialConnector.reply?()` maps to a `failed` `outbound_activities`-shaped row.

## How to extend this safely

- **Wiring a real connector's HTTP calls:** call `runIngestionAttempt({ tenantId, connectorInfo, attempt, refreshOAuthToken? })`, where `attempt` makes the real platform call and throws a `ClassifiableError` (not a raw `Error`) for anything that should be retried, refreshed, or failed — a raw thrown error propagates as a genuine programming bug, not an ingestion outcome, by design (see Load-bearing constraints). If `attempt` calls `acquireForProvider()`/`acquireForAiModel()` (Story 2.2/2.4) and it throws `QueueTtlExceededError`/`QueueDepthExceededError`, catch and reclassify into `ClassifiableError('queue_ttl_exceeded' | 'queue_depth_exceeded', ...)` — `RequestGate` itself has no ingestion-domain knowledge, so this reclassification is the connector's job, not the gate's.
- **Adding a new error kind:** add it to `ErrorKind` in `errorClassification.ts` and place it in `RETRYABLE_KINDS`/`CREDENTIAL_KINDS` (or neither, defaulting to non-retryable) — never add ad hoc classification logic inside `runIngestionAttempt()` itself. Reply-specific kinds (`missing_permission`, `post_not_found`, `reconnect_required`, `rate_limited`, `reply_not_supported`) go in `ErrorKind` but do not change the existing `RETRYABLE_KINDS`/`CREDENTIAL_KINDS` sets used for ingestion.
- **Checking whether to schedule the next poll:** call `shouldAttemptIngestion(tenantId, platformId)` before running one — never introduce a separate "disabled" column/flag anywhere; that would reintroduce exactly the drift risk ADR-0009 exists to prevent.
- **Reading connector status for the admin UI:** don't call `deriveConnectorHealth()` directly from a router — go through Story 4.4's cache (`getCachedConnectorHealth()`, `.claude/skills/derived-data-caching-and-refresh/SKILL.md`), which itself only ever calls this function to populate an entry.
- **Tuning the `failing`/`disabled` rules (Story 13.1, ADR-0109):** `CONSECUTIVE_FAILURE_THRESHOLD` (5) and `CONSECUTIVE_SUCCESS_RECOVERY` (3) live at the top of `connectorHealth.ts`. The half-open probe (`PROBE_COOLDOWN_MS`) is removed. `disabled` follows from the latest run being `failed` with `retryable = false` and `is_credential_failure !== true`; `reconnect_required` follows from `is_credential_failure = true` on the latest run.

## Load-bearing constraints — do not change casually

- **`runIngestionAttempt()` only catches `ClassifiableError` — anything else rethrows.** This is deliberate: an unclassified exception is a bug in the connector's own code, not a legitimate retryable/non-retryable ingestion outcome, and swallowing it into a "failed" `IngestionRun` would hide real programming errors behind ADR-0010's error-handling policy.
- **The `failing`/`disabled`/`reconnect_required` rules (ADR-0109, Story 13.1) live in exactly one place** (`connectorHealth.ts`) and are what both `deriveConnectorHealth()`'s status and (transitively, via `shouldAttemptIngestion()`) Story 2.3's auto-disable both consult. `failing` follows from 5 consecutive failed `ingestion_runs` of any kind; `disabled` follows from the latest run being a non-retryable, non-credential failure; `reconnect_required` follows from the latest run being a credential-class failure. These rules supersede the rate-relative rule and 20-consecutive ceiling of ADR-0023 and the original "≥10/hour" placeholder of ADR-0009/ADR-0010.
- **Auto-disable has no stored flag anywhere — it is entirely `shouldAttemptIngestion()`'s live read of `deriveConnectorHealth()`.** Don't "optimize" this into a cached boolean column on some connector-config table; that reintroduces the exact drift risk ADR-0009's Alternatives Considered section explicitly rejected.
|- **`shouldAttemptIngestion()` blocks `failing`, `disabled`, and `reconnect_required` (ADR-0109).** The 2026-08-17 half-open probe (`PROBE_COOLDOWN_MS`) is removed. The only recovery path is the manual `POST /v1/connectors/:platformId/enable` health check, which records a `health_check` run and resets the consecutive streak.
- **`credentialStatus` and run-derived `status` are independent axes, not merged into one value.** A connector can be `healthy` by run history while its credential is `expired` (the credential just hasn't been *used* yet to fail a run) — this is correct, not a bug, per ADR-0009's explicit separation.
- **Dead-lettering (Story 2.4, ADR-0020) is a distinctly-worded `errorSummary` on retry exhaustion, not a stored flag, a new `ingestion_runs.status` value, or a separate DLQ table.** It only applies when retries were genuinely exhausted for a retryable error (`isRetryable(err.kind) && attemptsMade > maxRetries`) — an immediate non-retryable failure is a different case and is never worded as dead-lettered. It is deliberately independent of the `failing` threshold/auto-disable (individual-request granularity vs. aggregate connector-wide pattern, per ADR-0020's own Decision text) — don't wire one to trigger the other.
- **Stories 2.3, 2.5, 2.12, 4.3, and 1.16's contracts carry ADR-0109 (2026-08-28) supersession notes, not left to silently rot or rewritten wholesale.** Story 13.1's own contract owns the new 5-consecutive `failing` threshold, immediate `disabled`/`reconnect_required`, degraded auto-recovery, manual re-enable, and `connector_disabled` alert behavior. The earlier contracts were edited to keep passing under the new rules; their original ADR-0023 and 2026-08-17 dated notes are preserved for traceability.
- **`deriveConnectorHealth()`'s `userId?` (4th param, Story 1.15) filters both sub-queries it runs — `ingestion_runs` and `platform_credentials` — not just one.** Filtering only the runs query while leaving the credential-status query tenant-wide would let a Tier-3 user's `credentialStatus` silently read a different user's (or the tenant-wide) credential row — the same class of cross-user blending bug this story's own contract exists to close. `pageId?` (3rd param, ADR-0060) stays reserved/structurally present but unused until Story 6.27 actually builds Facebook multi-Page health.
- **`shouldAttemptIngestion()` must forward its own `ownerType`/`userId` into `deriveConnectorHealth()` — never call it bare.** This was a real, silent Story 1.11 bug (found during Story 1.15): the internal call read `deriveConnectorHealth(tenantId, platformId)` regardless of the caller's own `ownerType`/`userId` arguments, so a Tier-3 user's eligibility check was actually reading tenant-wide (or another user's) health. Fixed to `deriveConnectorHealth(tenantId, platformId, undefined, ownerType === 'user' ? userId : undefined)`.
- **`reconnect_required` (Story 2.15) is checked on `runs[0]` alone (the single most recent run, already ordered DESC) — before the rate/ceiling derivation, and returned immediately if true, never blended with `failing`/`degraded`.** A later successful run naturally becomes `runs[0]` and clears the status on its own — don't add a "sticky until manually acknowledged" mechanic; that would misrepresent a tenant who already fixed the credential as still needing to.
|- **Every failed run counts toward the 5-consecutive `failing` threshold, regardless of `retryable` (ADR-0109, Story 13.1).** A `NULL` `retryable` value counts as a failed run but does not immediately disable; only `retryable = false` on the latest run yields `disabled`. A `retryable = true` failed run can still be retried inside `runIngestionAttempt()`, but once the run record is closed as `failed`, it extends `consecutiveFailures` just like any other failure.

## Known gaps / deferred work

- **The Newswire connector (Story 2.6, ADR-0024) is now the first real connector calling `runIngestionAttempt()`**, including the `acquireForProvider()` → reclassify → `ClassifiableError` wiring this file's own "How to extend this safely" section prescribes — see `.claude/skills/newswire-connector/SKILL.md`. `runIngestionAttempt()`'s `attempt` callback also gained a `runId: string` parameter as part of that story (additive only — every existing zero-arg `attempt` callback in this component's own contracts above remains valid). RSS/News's real connector (Phase 1's "also build, not storied" scope) still doesn't exist.
- `acquireForAiModel()`'s equivalent reclassification (the AI-provider half) still isn't exercised by any real caller — no real `AIProviderConnector` exists yet (Phase 2 work).
- Story 4.4's read-through cache (ADR-0022, accepted 2026-07-29, implemented 2026-07-30) builds directly on this component — see `.claude/skills/derived-data-caching-and-refresh/SKILL.md`.
|| ADR-0109 (Story 13.1) keeps the `deliveryMode`-based threshold variation (push vs. poll) as an open, deliberately-deferred question — not this story's scope, and not built.
- **Distributed (Redis-backed) `RequestGate` state across more than one process instance (the other half of Story 2.4, ADR-0020) is deliberately not built** — see `provider-connector-framework`'s SKILL.md for why (solo-deployment, single-instance is the real shape right now).
- ADR-0014's Negative consequences names Key Vault throttling/outage as "almost certainly retryable" for connector error handling, but `errorClassification.ts`'s `ErrorKind` has no dedicated kind for it — flagged by the same 2026-07-29 consistency audit that caught the two gaps below, deliberately left open (generic `network`/`http_5xx` may already cover it adequately; revisit if a real Key Vault-dependent connector call shows otherwise).

## Corrections

- **2026-08-20 (healing pass):** `deriveConnectorHealth()` previously invoked `isConnectorActive()` inside its outer `withTenant()` callback, triggering a nested connection checkout from the PostgreSQL connection pool. When multiple connectors were queried concurrently on page load, this caused connection pool exhaustion and deadlocks. Fixed by querying `connector_user_activations` and `connector_activations` directly on the active transaction `client`.
- **2026-08-28 (Story 13.1, ADR-0109):** the rate-relative `failing` rule, the 20-consecutive ceiling, and the 2026-08-17 half-open probe are all replaced. `failing` follows from 5 consecutive failed `ingestion_runs`; `disabled` from a non-retryable latest run; `reconnect_required` from a credential-class latest run. `shouldAttemptIngestion()` blocks all three. Recovery is the manual `POST /v1/connectors/:platformId/enable` health check.
- **2026-07-29 (healing pass):** a full ADR/story consistency audit found two gaps this component had shipped without: `ingestion_runs.retryable` (ADR-0005 names it as a field; Story 3.2's own migration deferred it to "Story 2.3," which then shipped without adding it) and the `(tenant_id, platform_id, started_at)` index ADR-0009's own Negative consequences names as needed. Both fixed in this pass — see the two new contracts above and `runIngestionAttempt.ts`/the migration for the fix itself.
