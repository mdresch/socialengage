export default function AlreadyHaveAccountPage() {
  return (
    <main>
      <h1>You already have an account</h1>
      <p>This Microsoft account already belongs to a SocialEngage tenant. Sign in instead of signing up again.</p>
      <a href="/sign-in">Sign in</a>
    </main>
  );
}
